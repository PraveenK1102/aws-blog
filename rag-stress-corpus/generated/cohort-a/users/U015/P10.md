## The problem

Running it end to end put accuracy at 19%. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Splitting the pipeline around the embedding cache made the failures much easier to localise. Because the harness came before the optimisation, I caught a regression that looked like a win. Accuracy landed at 32% once I measured it end to end. Splitting the pipeline around the retrieval step made the failures much easier to localise. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Two configurations differed by one parameter and by far more than one parameter's worth of output. Instrumenting the ingestion path cost a day and saved considerably more than that later. I now write the evaluation before the change, which has saved me from at least two false wins. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap.
## What I built

The tempting change was the model; the effective change was how documents were split. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Measured properly, latency came out at 8%.

That gave retrieval of 35%, which I wrote down at the time. The result held up in staging and quietly regressed the moment traffic patterns changed. Writing the failure cases down first made the design conversation much shorter. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Comparing against a fixed baseline made the June numbers finally interpretable. I do not trust an improvement that only appears on examples I picked myself. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The fallback path had never been exercised, so nobody knew it returned an empty result.
## What I measured

I rebuilt the reranking pass so the before-and-after comparison would actually mean something. On the instrumented run, accuracy showed 59%. Keeping a log of every configuration felt tedious until I needed to explain a regression. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the LangGraph inputs? Mostly the inputs. Tuning Redis helped marginally; fixing how documents were split helped substantially. I rebuilt the fallback branch so the before-and-after comparison would actually mean something.
## What I would change

Most of the week went into the fallback branch rather than the change I had actually planned to make. When I finally measured it, retrieval was 59%. Instrumenting the scoring function cost a day and saved considerably more than that later. I keep a short list of results I do not believe yet, and revisit it weekly. The interesting failures were the ones where the output read well but cited the wrong source. Instrumenting the retrieval step cost a day and saved considerably more than that later. Every improvement I could not reproduce twice turned out not to be an improvement. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Cleaning the Python inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real LangGraph inputs arrived.
