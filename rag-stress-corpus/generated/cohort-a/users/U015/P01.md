## The problem

Instrumenting the fallback branch cost a day and saved considerably more than that later. The tempting change was the model; the effective change was how documents were split. Accuracy landed at 40% once I measured it end to end.

Cleaning the Python inputs produced a larger gain than any change to the model did. The interesting failures were the ones where the output read well but cited the wrong source. Was the gain from the model or from cleaning the Kubernetes inputs? Mostly the inputs. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Most of the week went into the chunking logic rather than the change I had actually planned to make. The result held up in staging and quietly regressed the moment traffic patterns changed. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Keeping a log of every configuration felt tedious until I needed to explain a regression. Instrumenting the embedding cache cost a day and saved considerably more than that later. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Instrumenting the reranking pass cost a day and saved considerably more than that later. Cleaning the LangGraph inputs produced a larger gain than any change to the model did. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Tuning Python helped marginally; fixing how documents were split helped substantially. Comparing against a fixed baseline made the March numbers finally interpretable. Every improvement I could not reproduce twice turned out not to be an improvement. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Because the harness came before the optimisation, I caught a regression that looked like a win.
## What I built

The fallback path had never been exercised, so nobody knew it returned an empty result. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Caching LangGraph lookups helped until invalidation became the more interesting problem.

The number I got for accuracy was 37%. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Comparing against a fixed baseline made the March numbers finally interpretable. I keep a short list of results I do not believe yet, and revisit it weekly. Was the gain from the model or from cleaning the Spring inputs? Mostly the inputs. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Cleaning the Spring inputs produced a larger gain than any change to the model did. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Cleaning the Kubernetes inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. Instrumenting the retrieval step cost a day and saved considerably more than that later. Cleaning the Redis inputs produced a larger gain than any change to the model did. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Two configurations differed by one parameter and by far more than one parameter's worth of output. Writing the failure cases down first made the design conversation much shorter. The result held up in staging and quietly regressed the moment traffic patterns changed. Keeping a log of every configuration felt tedious until I needed to explain a regression.
## What I measured

Batching improved throughput and made the tail latency considerably harder to reason about. Measured properly, retrieval came out at 58%. I now write the evaluation before the change, which has saved me from at least two false wins. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I do not trust an improvement that only appears on examples I picked myself. The first version worked on the sample set and fell apart the moment real LangGraph inputs arrived. Instrumenting the chunking logic cost a day and saved considerably more than that later. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. Caching Python lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real Kubernetes inputs arrived. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Caching Spring lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the reranking pass made the failures much easier to localise. Splitting the pipeline around the embedding cache made the failures much easier to localise. I now write the evaluation before the change, which has saved me from at least two false wins. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Batching improved throughput and made the tail latency considerably harder to reason about. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first.
## What I would change

Writing the failure cases down first made the design conversation much shorter. Two configurations differed by one parameter and by far more than one parameter's worth of output. Most of the week went into the fallback branch rather than the change I had actually planned to make. Every improvement I could not reproduce twice turned out not to be an improvement. Because the harness came before the optimisation, I caught a regression that looked like a win. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Tuning Redis helped marginally; fixing how documents were split helped substantially. In March I assumed the bottleneck was compute; it was almost entirely I/O. Tuning LangGraph helped marginally; fixing how documents were split helped substantially. Splitting the pipeline around the retrieval step made the failures much easier to localise. Tuning Spring helped marginally; fixing how documents were split helped substantially. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Most of the week went into the embedding cache rather than the change I had actually planned to make. Caching Redis lookups helped until invalidation became the more interesting problem. Caching Kubernetes lookups helped until invalidation became the more interesting problem. The fallback path had never been exercised, so nobody knew it returned an empty result. Caching Python lookups helped until invalidation became the more interesting problem. The tempting change was the model; the effective change was how documents were split. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.
