Spent most of this week on the retrieval side of the project.

## The problem

I rebuilt the slowest stage in the pipeline and that alone explained most of the gap. The experiment started with the assumptions baked into the defaults which took longer than the fix itself. Measured properly, accuracy came out at 40%. The experiment started with the fallback path nobody exercised and I kept the harness afterwards. The experiment started with a small evaluation harness which took longer than the fix itself. I rebuilt the retrieval step on its own which took longer than the fix itself. Most of the effort went into a small evaluation harness which made the regression obvious. I began by isolating the slowest stage in the pipeline which made the regression obvious. Most of the effort went into a small evaluation harness so the comparison would actually mean something. I rebuilt the slowest stage in the pipeline and I kept the harness afterwards. Before changing anything I measured the assumptions baked into the defaults which took longer than the fix itself. Before changing anything I measured the retrieval step on its own because the earlier result was not reproducible. The experiment started with the chunking behaviour so the comparison would actually mean something. The interesting part turned out to be the chunking behaviour and the numbers were not what I expected. I began by isolating the fallback path nobody exercised and that alone explained most of the gap. I began by isolating the assumptions baked into the defaults and the numbers were not what I expected. Before changing anything I measured the slowest stage in the pipeline because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap.
## What I built

The experiment started with the way inputs were normalised which took longer than the fix itself. The experiment started with the slowest stage in the pipeline so the comparison would actually mean something. Accuracy landed at 37% once I measured it end to end. I began by isolating the assumptions baked into the defaults which made the regression obvious. Most of the effort went into a small evaluation harness because the earlier result was not reproducible. The interesting part turned out to be the chunking behaviour and I kept the harness afterwards. I began by isolating the ranking of candidate passages and the numbers were not what I expected. Most of the effort went into the fallback path nobody exercised and I kept the harness afterwards. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression. Before changing anything I measured the retrieval step on its own which made the regression obvious. The experiment started with a small evaluation harness so the comparison would actually mean something. The interesting part turned out to be a small evaluation harness and I kept the harness afterwards. Before changing anything I measured the slowest stage in the pipeline and that alone explained most of the gap. The experiment started with the fallback path nobody exercised before touching anything else. Before changing anything I measured the ranking of candidate passages and the numbers were not what I expected. Most of the effort went into a small evaluation harness and I kept the harness afterwards. The experiment started with the fallback path nobody exercised which took longer than the fix itself. Before changing anything I measured a small evaluation harness and that alone explained most of the gap.
## What I measured

Measured properly, retrieval came out at 58%. I rebuilt a small evaluation harness and the numbers were not what I expected. The interesting part turned out to be the slowest stage in the pipeline and that alone explained most of the gap. Before changing anything I measured a small evaluation harness which made the regression obvious. I began by isolating the way inputs were normalised and that alone explained most of the gap. I rebuilt the fallback path nobody exercised and I kept the harness afterwards. I began by isolating the retrieval step on its own which took longer than the fix itself. The experiment started with the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the slowest stage in the pipeline which made the regression obvious. I rebuilt the slowest stage in the pipeline because the earlier result was not reproducible. Most of the effort went into the slowest stage in the pipeline which took longer than the fix itself. The interesting part turned out to be the way inputs were normalised and the numbers were not what I expected. The interesting part turned out to be a small evaluation harness and the numbers were not what I expected. I rebuilt the ranking of candidate passages and I kept the harness afterwards. The experiment started with a small evaluation harness because the earlier result was not reproducible. The experiment started with the way inputs were normalised which made the regression obvious. Most of the effort went into the retrieval step on its own before touching anything else.
## What I would change

Most of the effort went into the ranking of candidate passages and that alone explained most of the gap. I rebuilt the assumptions baked into the defaults which took longer than the fix itself. I rebuilt the way inputs were normalised which took longer than the fix itself. I rebuilt the chunking behaviour so the comparison would actually mean something. I rebuilt the chunking behaviour and I kept the harness afterwards. The experiment started with the way inputs were normalised and the numbers were not what I expected. Most of the effort went into the chunking behaviour and that alone explained most of the gap. The interesting part turned out to be the slowest stage in the pipeline because the earlier result was not reproducible. I began by isolating the retrieval step on its own and that alone explained most of the gap. I rebuilt the way inputs were normalised which made the regression obvious. The experiment started with the assumptions baked into the defaults because the earlier result was not reproducible. Most of the effort went into a small evaluation harness before touching anything else. The interesting part turned out to be the fallback path nobody exercised and the numbers were not what I expected. I began by isolating a small evaluation harness which made the regression obvious. The interesting part turned out to be the ranking of candidate passages which took longer than the fix itself. Most of the time went into building a way to measure the change rather than making the change.
