## The problem

My recorded figure for accuracy is 50%. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The result held up in staging and quietly regressed the moment traffic patterns changed.

Every improvement I could not reproduce twice turned out not to be an improvement. I do not trust an improvement that only appears on examples I picked myself. Two configurations differed by one parameter and by far more than one parameter's worth of output. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. The interesting failures were the ones where the output read well but cited the wrong source. Keeping a log of every configuration felt tedious until I needed to explain a regression. Cleaning the Redis inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the Spring inputs? Mostly the inputs. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. Splitting the pipeline around the reranking pass made the failures much easier to localise. Instrumenting the retrieval step cost a day and saved considerably more than that later. Instrumenting the embedding cache cost a day and saved considerably more than that later. Instrumenting the reranking pass cost a day and saved considerably more than that later. Writing the failure cases down first made the design conversation much shorter. In March I assumed the bottleneck was compute; it was almost entirely I/O. The fallback path had never been exercised, so nobody knew it returned an empty result. Batching improved throughput and made the tail latency considerably harder to reason about.
## What I built

The fallback path had never been exercised, so nobody knew it returned an empty result. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The number I got for latency was 17%. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Most of the week went into the reranking pass rather than the change I had actually planned to make. I keep a short list of results I do not believe yet, and revisit it weekly. In March I assumed the bottleneck was compute; it was almost entirely I/O. Most of the week went into the chunking logic rather than the change I had actually planned to make. Batching improved throughput and made the tail latency considerably harder to reason about. The first version worked on the sample set and fell apart the moment real Kubernetes inputs arrived. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Most of the week went into the fallback branch rather than the change I had actually planned to make. Cleaning the Kubernetes inputs produced a larger gain than any change to the model did. Cleaning the LangGraph inputs produced a larger gain than any change to the model did. The tempting change was the model; the effective change was how documents were split. I keep a short list of results I do not believe yet, and revisit it weekly. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March.
## What I measured

Measured properly, accuracy came out at 58%. Because the harness came before the optimisation, I caught a regression that looked like a win. Was the gain from the model or from cleaning the LangGraph inputs? Mostly the inputs. Writing the failure cases down first made the design conversation much shorter. Splitting the pipeline around the embedding cache made the failures much easier to localise. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Instrumenting the scoring function cost a day and saved considerably more than that later. Caching Spring lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the scoring function made the failures much easier to localise. Caching Redis lookups helped until invalidation became the more interesting problem. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Caching LangGraph lookups helped until invalidation became the more interesting problem. Caching Python lookups helped until invalidation became the more interesting problem. Caching Kubernetes lookups helped until invalidation became the more interesting problem. The result held up in staging and quietly regressed the moment traffic patterns changed. I do not trust an improvement that only appears on examples I picked myself. Because the harness came before the optimisation, I caught a regression that looked like a win.
## What I would change

The tempting change was the model; the effective change was how documents were split. I now write the evaluation before the change, which has saved me from at least two false wins. Accuracy landed at 54% once I measured it end to end. Tuning Redis helped marginally; fixing how documents were split helped substantially. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Comparing against a fixed baseline made the March numbers finally interpretable. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Tuning LangGraph helped marginally; fixing how documents were split helped substantially. Cleaning the Python inputs produced a larger gain than any change to the model did. Cleaning the Spring inputs produced a larger gain than any change to the model did. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Tuning Python helped marginally; fixing how documents were split helped substantially. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Tuning Spring helped marginally; fixing how documents were split helped substantially. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The interesting failures were the ones where the output read well but cited the wrong source.
