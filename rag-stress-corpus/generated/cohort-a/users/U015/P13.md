Notes from a few days of experiments, written while they are still fresh.

## The problem

Latency landed at 59% once I measured it end to end. The experiment started with the assumptions baked into the defaults and the numbers were not what I expected. The experiment started with the ranking of candidate passages so the comparison would actually mean something. The experiment started with the chunking behaviour because the earlier result was not reproducible. The experiment started with a small evaluation harness and that alone explained most of the gap. Before changing anything I measured the assumptions baked into the defaults which made the regression obvious. Most of the effort went into the fallback path nobody exercised and the numbers were not what I expected. The experiment started with the way inputs were normalised because the earlier result was not reproducible. Most of the effort went into the fallback path nobody exercised so the comparison would actually mean something. Before changing anything I measured the chunking behaviour before touching anything else. The experiment started with the slowest stage in the pipeline and that alone explained most of the gap. I rebuilt the slowest stage in the pipeline because the earlier result was not reproducible. I rebuilt the slowest stage in the pipeline before touching anything else. The interesting part turned out to be a small evaluation harness and that alone explained most of the gap. The experiment started with a small evaluation harness because the earlier result was not reproducible. The interesting part turned out to be the slowest stage in the pipeline which made the regression obvious.
## What I built

The interesting part turned out to be the chunking behaviour and that alone explained most of the gap. I began by isolating the way inputs were normalised before touching anything else. The number I got for latency was 51%. Most of the effort went into the fallback path nobody exercised before touching anything else. I rebuilt the chunking behaviour which made the regression obvious. The interesting part turned out to be the retrieval step on its own which took longer than the fix itself. I began by isolating the slowest stage in the pipeline and the numbers were not what I expected. Most of the time went into building a way to measure the change rather than making the change. Most of the effort went into the way inputs were normalised and that alone explained most of the gap. I rebuilt the slowest stage in the pipeline and that alone explained most of the gap. The interesting part turned out to be the assumptions baked into the defaults which made the regression obvious. The experiment started with the slowest stage in the pipeline and the numbers were not what I expected. The interesting part turned out to be the ranking of candidate passages because the earlier result was not reproducible. Most of the effort went into a small evaluation harness and the numbers were not what I expected. I began by isolating the assumptions baked into the defaults because the earlier result was not reproducible.
## What I measured

I rebuilt the fallback path nobody exercised which took longer than the fix itself. The interesting part turned out to be a small evaluation harness which took longer than the fix itself. Accuracy landed at 13% once I measured it end to end. Before changing anything I measured the slowest stage in the pipeline before touching anything else. Most of the effort went into the chunking behaviour and I kept the harness afterwards. Most of the effort went into the assumptions baked into the defaults so the comparison would actually mean something. The experiment started with the retrieval step on its own and the numbers were not what I expected. The interesting part turned out to be the fallback path nobody exercised and the numbers were not what I expected. I rebuilt the ranking of candidate passages before touching anything else. The interesting part turned out to be the fallback path nobody exercised and that alone explained most of the gap. I rebuilt the fallback path nobody exercised and the numbers were not what I expected. The interesting part turned out to be the fallback path nobody exercised before touching anything else. I began by isolating the retrieval step on its own which made the regression obvious. I rebuilt the chunking behaviour and the numbers were not what I expected. I began by isolating the slowest stage in the pipeline so the comparison would actually mean something.
## What I would change

I began by isolating the fallback path nobody exercised and I kept the harness afterwards. I began by isolating the chunking behaviour before touching anything else. The experiment started with the assumptions baked into the defaults so the comparison would actually mean something. Most of the effort went into a small evaluation harness because the earlier result was not reproducible. The experiment started with a small evaluation harness which made the regression obvious. Before changing anything I measured the way inputs were normalised and that alone explained most of the gap. Before changing anything I measured the slowest stage in the pipeline and that alone explained most of the gap. Before changing anything I measured the ranking of candidate passages so the comparison would actually mean something. Most of the effort went into the assumptions baked into the defaults which took longer than the fix itself. The interesting part turned out to be the fallback path nobody exercised which took longer than the fix itself. I rebuilt a small evaluation harness so the comparison would actually mean something. Before changing anything I measured the assumptions baked into the defaults and I kept the harness afterwards. The experiment started with the assumptions baked into the defaults and that alone explained most of the gap. Most of the effort went into a small evaluation harness which made the regression obvious.
