## The problem

On the instrumented run, latency showed 59%. Cleaning the Kubernetes inputs produced a larger gain than any change to the model did. Tuning LangGraph helped marginally; fixing how documents were split helped substantially.

The tempting change was the model; the effective change was how documents were split. Because the harness came before the optimisation, I caught a regression that looked like a win. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Most of the week went into the chunking logic rather than the change I had actually planned to make. Comparing against a fixed baseline made the July numbers finally interpretable. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Caching LangGraph lookups helped until invalidation became the more interesting problem. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Cleaning the Redis inputs produced a larger gain than any change to the model did. Caching Kubernetes lookups helped until invalidation became the more interesting problem. Instrumenting the scoring function cost a day and saved considerably more than that later. Two configurations differed by one parameter and by far more than one parameter's worth of output. Because the harness came before the optimisation, I caught a regression that looked like a win.
## What I built

Running it end to end put latency at 51%. I keep a short list of results I do not believe yet, and revisit it weekly. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Instrumenting the embedding cache cost a day and saved considerably more than that later. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I do not trust an improvement that only appears on examples I picked myself. Cleaning the Python inputs produced a larger gain than any change to the model did. In July I assumed the bottleneck was compute; it was almost entirely I/O. Cleaning the LangGraph inputs produced a larger gain than any change to the model did. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Keeping a log of every configuration felt tedious until I needed to explain a regression. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. Tuning Spring helped marginally; fixing how documents were split helped substantially. Instrumenting the fallback branch cost a day and saved considerably more than that later. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Caching Python lookups helped until invalidation became the more interesting problem. Every improvement I could not reproduce twice turned out not to be an improvement.
## What I measured

Caching Spring lookups helped until invalidation became the more interesting problem. The fallback path had never been exercised, so nobody knew it returned an empty result. That gave accuracy of 13%, which I wrote down at the time.

The result held up in staging and quietly regressed the moment traffic patterns changed. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Writing the failure cases down first made the design conversation much shorter. Batching improved throughput and made the tail latency considerably harder to reason about. Was the gain from the model or from cleaning the Kubernetes inputs? Mostly the inputs. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Every improvement I could not reproduce twice turned out not to be an improvement. The first version worked on the sample set and fell apart the moment real Python inputs arrived. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Splitting the pipeline around the scoring function made the failures much easier to localise. Comparing against a fixed baseline made the July numbers finally interpretable. Writing the failure cases down first made the design conversation much shorter.
## What I would change

Splitting the pipeline around the evaluation harness made the failures much easier to localise. Most of the week went into the reranking pass rather than the change I had actually planned to make. I now write the evaluation before the change, which has saved me from at least two false wins. Splitting the pipeline around the fallback branch made the failures much easier to localise. The interesting failures were the ones where the output read well but cited the wrong source. Instrumenting the chunking logic cost a day and saved considerably more than that later. Tuning Redis helped marginally; fixing how documents were split helped substantially. Caching Redis lookups helped until invalidation became the more interesting problem. Instrumenting the ingestion path cost a day and saved considerably more than that later. Two configurations differed by one parameter and by far more than one parameter's worth of output. Cleaning the Spring inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real Kubernetes inputs arrived. Tuning Python helped marginally; fixing how documents were split helped substantially. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The fallback path had never been exercised, so nobody knew it returned an empty result.
