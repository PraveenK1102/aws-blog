## The problem

I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Instrumenting the embedding cache cost a day and saved considerably more than that later. Splitting the pipeline around the chunking logic made the failures much easier to localise. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. When I finally measured it, accuracy was 10%. The tempting change was the model; the effective change was how documents were split. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Most of the week went into the retrieval step rather than the change I had actually planned to make. Was the gain from the model or from cleaning the LangGraph inputs? Mostly the inputs. The first version worked on the sample set and fell apart the moment real LangGraph inputs arrived. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. I now write the evaluation before the change, which has saved me from at least two false wins.
## What I built

On the instrumented run, retrieval showed 39%. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. The interesting failures were the ones where the output read well but cited the wrong source. Keeping a log of every configuration felt tedious until I needed to explain a regression. The result held up in staging and quietly regressed the moment traffic patterns changed. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The fallback path had never been exercised, so nobody knew it returned an empty result. Because the harness came before the optimisation, I caught a regression that looked like a win. Tuning Spring helped marginally; fixing how documents were split helped substantially. Comparing against a fixed baseline made the May numbers finally interpretable. Caching Redis lookups helped until invalidation became the more interesting problem.
## What I measured

Measured properly, accuracy came out at 7%. Batching improved throughput and made the tail latency considerably harder to reason about. Tuning Python helped marginally; fixing how documents were split helped substantially.

Every improvement I could not reproduce twice turned out not to be an improvement. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Caching Python lookups helped until invalidation became the more interesting problem. Two configurations differed by one parameter and by far more than one parameter's worth of output. Instrumenting the chunking logic cost a day and saved considerably more than that later. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Instrumenting the fallback branch cost a day and saved considerably more than that later. Most of the week went into the scoring function rather than the change I had actually planned to make. I rebuilt the retrieval step so the before-and-after comparison would actually mean something.
## What I would change

Writing the failure cases down first made the design conversation much shorter. In May I assumed the bottleneck was compute; it was almost entirely I/O. Splitting the pipeline around the ingestion path made the failures much easier to localise. I do not trust an improvement that only appears on examples I picked myself. I keep a short list of results I do not believe yet, and revisit it weekly. Caching Kubernetes lookups helped until invalidation became the more interesting problem. Cleaning the LangGraph inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Cleaning the Redis inputs produced a larger gain than any change to the model did.
