## The problem

Measured properly, retrieval came out at 6%. Two configurations differed by one parameter and by far more than one parameter's worth of output. The result held up in staging and quietly regressed the moment traffic patterns changed. Batching improved throughput and made the tail latency considerably harder to reason about.

Comparing against a fixed baseline made the April numbers finally interpretable. Was the gain from the model or from cleaning the Spring inputs? Mostly the inputs. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Writing the failure cases down first made the design conversation much shorter. Every improvement I could not reproduce twice turned out not to be an improvement. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap.
## What I built

The interesting failures were the ones where the output read well but cited the wrong source. The number I got for latency was 26%. I keep a short list of results I do not believe yet, and revisit it weekly.

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. Because the harness came before the optimisation, I caught a regression that looked like a win. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Was the gain from the model or from cleaning the LangGraph inputs? Mostly the inputs. In April I assumed the bottleneck was compute; it was almost entirely I/O. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Caching Spring lookups helped until invalidation became the more interesting problem.
## What I measured

The fallback path had never been exercised, so nobody knew it returned an empty result. Instrumenting the evaluation harness cost a day and saved considerably more than that later. The tempting change was the model; the effective change was how documents were split. When I finally measured it, latency was 27%. Most of the week went into the fallback branch rather than the change I had actually planned to make. Keeping a log of every configuration felt tedious until I needed to explain a regression. I do not trust an improvement that only appears on examples I picked myself. I now write the evaluation before the change, which has saved me from at least two false wins. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Instrumenting the chunking logic cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real Spring inputs arrived.
## What I would change

Splitting the pipeline around the scoring function made the failures much easier to localise. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I spent an afternoon convinced the model was wrong before finding a bug in the loader. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Caching Kubernetes lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Cleaning the Python inputs produced a larger gain than any change to the model did. Splitting the pipeline around the fallback branch made the failures much easier to localise. Most of the week went into the scoring function rather than the change I had actually planned to make. I rebuilt the fallback branch so the before-and-after comparison would actually mean something.
