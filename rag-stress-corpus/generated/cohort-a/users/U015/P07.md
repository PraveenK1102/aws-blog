Spent most of this week on the retrieval side of the project.

## The problem

The interesting part turned out to be the fallback path nobody exercised which took longer than the fix itself. The number I got for latency was 40%. I rebuilt a small evaluation harness because the earlier result was not reproducible. Measured properly, latency came out at 18%. I rebuilt the fallback path nobody exercised because the earlier result was not reproducible. I rebuilt the chunking behaviour and that alone explained most of the gap. I rebuilt the fallback path nobody exercised which took longer than the fix itself. Most of the effort went into the ranking of candidate passages so the comparison would actually mean something. Most of the effort went into the retrieval step on its own and the numbers were not what I expected. I rebuilt a small evaluation harness and that alone explained most of the gap. Most of the effort went into the ranking of candidate passages which took longer than the fix itself. Most of the effort went into the retrieval step on its own because the earlier result was not reproducible. I rebuilt a small evaluation harness before touching anything else. I rebuilt the assumptions baked into the defaults which made the regression obvious. I rebuilt the retrieval step on its own before touching anything else. I rebuilt the assumptions baked into the defaults and that alone explained most of the gap. The experiment started with the assumptions baked into the defaults before touching anything else.
## What I built

The interesting part turned out to be the ranking of candidate passages and I kept the harness afterwards. I began by isolating the assumptions baked into the defaults so the comparison would actually mean something. Measured properly, accuracy came out at 9%. Accuracy landed at 52% once I measured it end to end. Before changing anything I measured the retrieval step on its own and the numbers were not what I expected. The experiment started with the assumptions baked into the defaults and the numbers were not what I expected. Most of the time went into building a way to measure the change rather than making the change. I rebuilt the ranking of candidate passages which took longer than the fix itself. The experiment started with a small evaluation harness and the numbers were not what I expected. The interesting part turned out to be the slowest stage in the pipeline and that alone explained most of the gap. I began by isolating a small evaluation harness and that alone explained most of the gap. I rebuilt the ranking of candidate passages and the numbers were not what I expected. I rebuilt the chunking behaviour because the earlier result was not reproducible. I rebuilt the way inputs were normalised because the earlier result was not reproducible. Most of the effort went into a small evaluation harness because the earlier result was not reproducible. The experiment started with the chunking behaviour and I kept the harness afterwards.
## What I measured

The interesting part turned out to be the retrieval step on its own before touching anything else. The number I got for accuracy was 16%. I began by isolating the chunking behaviour and the numbers were not what I expected. Before changing anything I measured the chunking behaviour so the comparison would actually mean something. Most of the effort went into the chunking behaviour and the numbers were not what I expected. The experiment started with the retrieval step on its own because the earlier result was not reproducible. Before changing anything I measured the chunking behaviour which made the regression obvious. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression. I began by isolating the ranking of candidate passages and I kept the harness afterwards. Most of the effort went into the assumptions baked into the defaults which took longer than the fix itself. Before changing anything I measured the fallback path nobody exercised and the numbers were not what I expected. The interesting part turned out to be the way inputs were normalised which made the regression obvious. Writing the evaluation harness before the optimisation saved me from at least two false wins. The interesting part turned out to be the chunking behaviour before touching anything else. I rebuilt the fallback path nobody exercised so the comparison would actually mean something.
## What I would change

The number I got for accuracy was 38%. The first version worked on the sample data and fell apart the moment the inputs got messy. I began by isolating the assumptions baked into the defaults and the numbers were not what I expected. Before changing anything I measured the way inputs were normalised and the numbers were not what I expected. Most of the effort went into the ranking of candidate passages and the numbers were not what I expected. I rebuilt the ranking of candidate passages so the comparison would actually mean something. I rebuilt a small evaluation harness which made the regression obvious. The interesting part turned out to be the way inputs were normalised before touching anything else. Before changing anything I measured the chunking behaviour because the earlier result was not reproducible. I began by isolating a small evaluation harness and I kept the harness afterwards. I began by isolating a small evaluation harness which took longer than the fix itself. Before changing anything I measured the way inputs were normalised so the comparison would actually mean something. I began by isolating the fallback path nobody exercised because the earlier result was not reproducible. I began by isolating the chunking behaviour which made the regression obvious. I began by isolating the way inputs were normalised so the comparison would actually mean something.
