## The problem

The interesting failures were the ones where the output read well but cited the wrong source. The number I got for latency was 18%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Keeping a log of every configuration felt tedious until I needed to explain a regression. When I finally measured it, latency was 40%.

In May I assumed the bottleneck was compute; it was almost entirely I/O. Two configurations differed by one parameter and by far more than one parameter's worth of output. Caching Python lookups helped until invalidation became the more interesting problem. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Splitting the pipeline around the scoring function made the failures much easier to localise. Instrumenting the chunking logic cost a day and saved considerably more than that later. Caching Kubernetes lookups helped until invalidation became the more interesting problem. Instrumenting the ingestion path cost a day and saved considerably more than that later. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Caching LangGraph lookups helped until invalidation became the more interesting problem. Caching Redis lookups helped until invalidation became the more interesting problem. Cleaning the LangGraph inputs produced a larger gain than any change to the model did.
## What I built

The tempting change was the model; the effective change was how documents were split. Most of the week went into the chunking logic rather than the change I had actually planned to make. Measured properly, accuracy came out at 52%. Accuracy landed at 9% once I measured it end to end. The fallback path had never been exercised, so nobody knew it returned an empty result.

The result held up in staging and quietly regressed the moment traffic patterns changed. The first version worked on the sample set and fell apart the moment real LangGraph inputs arrived. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Writing the failure cases down first made the design conversation much shorter. Caching Spring lookups helped until invalidation became the more interesting problem. I do not trust an improvement that only appears on examples I picked myself. Cleaning the Python inputs produced a larger gain than any change to the model did. Splitting the pipeline around the ingestion path made the failures much easier to localise. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Cleaning the LangGraph inputs produced a larger gain than any change to the model did. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. I spent an afternoon convinced the model was wrong before finding a bug in the loader.
## What I measured

My recorded figure for accuracy is 16%. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Every improvement I could not reproduce twice turned out not to be an improvement.

Cleaning the Redis inputs produced a larger gain than any change to the model did. I keep a short list of results I do not believe yet, and revisit it weekly. Comparing against a fixed baseline made the May numbers finally interpretable. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the evaluation harness cost a day and saved considerably more than that later. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Most of the week went into the retrieval step rather than the change I had actually planned to make. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Instrumenting the fallback branch cost a day and saved considerably more than that later. Cleaning the Kubernetes inputs produced a larger gain than any change to the model did. Tuning Python helped marginally; fixing how documents were split helped substantially. Writing the failure cases down first made the design conversation much shorter.
## What I would change

Running it end to end put accuracy at 38%. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Because the harness came before the optimisation, I caught a regression that looked like a win. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Batching improved throughput and made the tail latency considerably harder to reason about. Tuning Spring helped marginally; fixing how documents were split helped substantially. Cleaning the Spring inputs produced a larger gain than any change to the model did. Tuning Redis helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Was the gain from the model or from cleaning the LangGraph inputs? Mostly the inputs. Most of the week went into the scoring function rather than the change I had actually planned to make. Instrumenting the scoring function cost a day and saved considerably more than that later. Batching improved throughput and made the tail latency considerably harder to reason about. Every improvement I could not reproduce twice turned out not to be an improvement.
