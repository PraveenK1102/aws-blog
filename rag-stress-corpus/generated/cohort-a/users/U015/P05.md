## The problem

Was the gain from the model or from cleaning the Kubernetes inputs? Mostly the inputs. On the instrumented run, latency showed 58%. In April I assumed the bottleneck was compute; it was almost entirely I/O.

Caching Redis lookups helped until invalidation became the more interesting problem. Comparing against a fixed baseline made the April numbers finally interpretable. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. The fallback path had never been exercised, so nobody knew it returned an empty result. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Cleaning the Python inputs produced a larger gain than any change to the model did. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Two configurations differed by one parameter and by far more than one parameter's worth of output.
## What I built

Measured properly, accuracy came out at 60%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Writing the failure cases down first made the design conversation much shorter. I keep a short list of results I do not believe yet, and revisit it weekly. Cleaning the Redis inputs produced a larger gain than any change to the model did. Splitting the pipeline around the fallback branch made the failures much easier to localise. Instrumenting the embedding cache cost a day and saved considerably more than that later. The interesting failures were the ones where the output read well but cited the wrong source. Every improvement I could not reproduce twice turned out not to be an improvement.
## What I measured

Instrumenting the fallback branch cost a day and saved considerably more than that later. The result held up in staging and quietly regressed the moment traffic patterns changed. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Cleaning the Kubernetes inputs produced a larger gain than any change to the model did. That gave accuracy of 41%, which I wrote down at the time. I do not trust an improvement that only appears on examples I picked myself. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Because the harness came before the optimisation, I caught a regression that looked like a win. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Splitting the pipeline around the embedding cache made the failures much easier to localise. Instrumenting the evaluation harness cost a day and saved considerably more than that later.
## What I would change

Keeping a log of every configuration felt tedious until I needed to explain a regression. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Tuning Python helped marginally; fixing how documents were split helped substantially. When I finally measured it, retrieval was 43%. I now write the evaluation before the change, which has saved me from at least two false wins. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. The tempting change was the model; the effective change was how documents were split. Batching improved throughput and made the tail latency considerably harder to reason about. Latency turned out to be dominated by the scoring function, which I had assumed was cheap.
