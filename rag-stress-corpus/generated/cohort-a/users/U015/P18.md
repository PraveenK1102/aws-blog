## The problem

The number I got for latency was 38%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The fallback path had never been exercised, so nobody knew it returned an empty result. Comparing against a fixed baseline made the August numbers finally interpretable. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Instrumenting the ingestion path cost a day and saved considerably more than that later. Every improvement I could not reproduce twice turned out not to be an improvement. Splitting the pipeline around the fallback branch made the failures much easier to localise. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap.
## What I built

Instrumenting the chunking logic cost a day and saved considerably more than that later. In August I assumed the bottleneck was compute; it was almost entirely I/O. Accuracy landed at 50% once I measured it end to end.

I keep a short list of results I do not believe yet, and revisit it weekly. The tempting change was the model; the effective change was how documents were split. Most of the week went into the retrieval step rather than the change I had actually planned to make. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Keeping a log of every configuration felt tedious until I needed to explain a regression. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. I now write the evaluation before the change, which has saved me from at least two false wins. The first version worked on the sample set and fell apart the moment real Spring inputs arrived.
## What I measured

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Caching Kubernetes lookups helped until invalidation became the more interesting problem. The interesting failures were the ones where the output read well but cited the wrong source.

My recorded figure for accuracy is 25%. Splitting the pipeline around the scoring function made the failures much easier to localise. Tuning LangGraph helped marginally; fixing how documents were split helped substantially. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Cleaning the Spring inputs produced a larger gain than any change to the model did. Most of the week went into the scoring function rather than the change I had actually planned to make. Because the harness came before the optimisation, I caught a regression that looked like a win. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I rebuilt the retrieval step so the before-and-after comparison would actually mean something.
## What I would change

Writing the failure cases down first made the design conversation much shorter. Batching improved throughput and made the tail latency considerably harder to reason about. Measured properly, latency came out at 35%. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. Caching Spring lookups helped until invalidation became the more interesting problem. I do not trust an improvement that only appears on examples I picked myself. The result held up in staging and quietly regressed the moment traffic patterns changed. Two configurations differed by one parameter and by far more than one parameter's worth of output. Caching Python lookups helped until invalidation became the more interesting problem. Tuning Redis helped marginally; fixing how documents were split helped substantially.
