Spent most of this week on the retrieval side of the project.

## The problem

Before changing anything I measured the retrieval step on its own which took longer than the fix itself. The interesting part turned out to be the way inputs were normalised and that alone explained most of the gap. Latency landed at 57% once I measured it end to end. Most of the effort went into the chunking behaviour and the numbers were not what I expected. Before changing anything I measured the retrieval step on its own and I kept the harness afterwards. The experiment started with the slowest stage in the pipeline which made the regression obvious. I began by isolating a small evaluation harness and I kept the harness afterwards. The experiment started with a small evaluation harness which took longer than the fix itself. I began by isolating the slowest stage in the pipeline which made the regression obvious. Before changing anything I measured the slowest stage in the pipeline and I kept the harness afterwards. I began by isolating the fallback path nobody exercised because the earlier result was not reproducible.
## What I built

The experiment started with a small evaluation harness and that alone explained most of the gap. Latency landed at 27% once I measured it end to end. The interesting part turned out to be the fallback path nobody exercised before touching anything else. Most of the effort went into the retrieval step on its own which made the regression obvious. I rebuilt the fallback path nobody exercised before touching anything else. The interesting part turned out to be the ranking of candidate passages because the earlier result was not reproducible. Before changing anything I measured the way inputs were normalised before touching anything else. I rebuilt the chunking behaviour which took longer than the fix itself. Most of the time went into building a way to measure the change rather than making the change. Most of the effort went into a small evaluation harness and the numbers were not what I expected.
## What I measured

Latency landed at 32% once I measured it end to end. Latency turned out to be dominated by a step I had assumed was cheap. The interesting part turned out to be the slowest stage in the pipeline before touching anything else. Before changing anything I measured the fallback path nobody exercised which made the regression obvious. The interesting part turned out to be the chunking behaviour which made the regression obvious. Before changing anything I measured the ranking of candidate passages because the earlier result was not reproducible. The experiment started with the slowest stage in the pipeline and the numbers were not what I expected. Most of the effort went into the fallback path nobody exercised and the numbers were not what I expected. The experiment started with the chunking behaviour before touching anything else. Before changing anything I measured the ranking of candidate passages before touching anything else.
## What I would change

The experiment started with the fallback path nobody exercised which took longer than the fix itself. I rebuilt the retrieval step on its own and that alone explained most of the gap. Retrieval landed at 53% once I measured it end to end. I rebuilt a small evaluation harness before touching anything else. I rebuilt the assumptions baked into the defaults because the earlier result was not reproducible. Before changing anything I measured the assumptions baked into the defaults and the numbers were not what I expected. The interesting part turned out to be the slowest stage in the pipeline and I kept the harness afterwards. I rebuilt the fallback path nobody exercised which took longer than the fix itself. Before changing anything I measured the chunking behaviour which made the regression obvious. The interesting part turned out to be the retrieval step on its own because the earlier result was not reproducible.
