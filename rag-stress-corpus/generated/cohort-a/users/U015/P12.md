## The problem

Two configurations differed by one parameter and by far more than one parameter's worth of output. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Keeping a log of every configuration felt tedious until I needed to explain a regression. Measured properly, latency came out at 57%. Every improvement I could not reproduce twice turned out not to be an improvement. The tempting change was the model; the effective change was how documents were split. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. I keep a short list of results I do not believe yet, and revisit it weekly. Most of the week went into the fallback branch rather than the change I had actually planned to make. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. The fallback path had never been exercised, so nobody knew it returned an empty result. Splitting the pipeline around the ingestion path made the failures much easier to localise.
## What I built

I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Running it end to end put latency at 27%. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Comparing against a fixed baseline made the June numbers finally interpretable. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. I spent an afternoon convinced the model was wrong before finding a bug in the loader. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Caching Redis lookups helped until invalidation became the more interesting problem. Most of the week went into the retrieval step rather than the change I had actually planned to make.
## What I measured

Batching improved throughput and made the tail latency considerably harder to reason about. My recorded figure for latency is 32%. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The result held up in staging and quietly regressed the moment traffic patterns changed. Because the harness came before the optimisation, I caught a regression that looked like a win. Tuning Spring helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the Spring inputs? Mostly the inputs. The interesting failures were the ones where the output read well but cited the wrong source. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real Kubernetes inputs arrived.
## What I would change

Writing the failure cases down first made the design conversation much shorter. Instrumenting the scoring function cost a day and saved considerably more than that later. Splitting the pipeline around the retrieval step made the failures much easier to localise.

On the instrumented run, retrieval showed 53%. I do not trust an improvement that only appears on examples I picked myself. In June I assumed the bottleneck was compute; it was almost entirely I/O. Instrumenting the chunking logic cost a day and saved considerably more than that later. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Tuning Redis helped marginally; fixing how documents were split helped substantially. Cleaning the Redis inputs produced a larger gain than any change to the model did. Caching Spring lookups helped until invalidation became the more interesting problem.
