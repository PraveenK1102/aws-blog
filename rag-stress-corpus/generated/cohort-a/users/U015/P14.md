Spent most of this week on the retrieval side of the project.

## The problem

Latency landed at 33% once I measured it end to end. Latency landed at 37% once I measured it end to end. The experiment started with the retrieval step on its own and I kept the harness afterwards. The experiment started with the way inputs were normalised so the comparison would actually mean something. I rebuilt the fallback path nobody exercised before touching anything else. A surprising amount of the gain came from cleaning the inputs rather than changing the model. I rebuilt the assumptions baked into the defaults because the earlier result was not reproducible. The experiment started with a small evaluation harness which made the regression obvious. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression. The experiment started with the slowest stage in the pipeline and that alone explained most of the gap. The experiment started with the retrieval step on its own so the comparison would actually mean something.
## What I built

I rebuilt the fallback path nobody exercised and the numbers were not what I expected. I began by isolating the chunking behaviour which took longer than the fix itself. The number I got for accuracy was 34%. I began by isolating the way inputs were normalised so the comparison would actually mean something. I rebuilt the chunking behaviour which took longer than the fix itself. The interesting part turned out to be the ranking of candidate passages which took longer than the fix itself. Most of the effort went into a small evaluation harness and I kept the harness afterwards. I began by isolating the ranking of candidate passages and the numbers were not what I expected. Most of the effort went into a small evaluation harness because the earlier result was not reproducible. Before changing anything I measured the ranking of candidate passages and the numbers were not what I expected.
## What I measured

Measured properly, retrieval came out at 53%. The interesting part turned out to be the way inputs were normalised which made the regression obvious. I began by isolating the fallback path nobody exercised so the comparison would actually mean something. I rebuilt the retrieval step on its own and that alone explained most of the gap. I began by isolating the retrieval step on its own and the numbers were not what I expected. Most of the effort went into a small evaluation harness so the comparison would actually mean something. I began by isolating the ranking of candidate passages because the earlier result was not reproducible. Before changing anything I measured the chunking behaviour which took longer than the fix itself. Most of the effort went into a small evaluation harness which took longer than the fix itself.
## What I would change

Before changing anything I measured the fallback path nobody exercised before touching anything else. Most of the effort went into the ranking of candidate passages and the numbers were not what I expected. The number I got for retrieval was 39%. The interesting part turned out to be the way inputs were normalised and I kept the harness afterwards. The interesting part turned out to be a small evaluation harness because the earlier result was not reproducible. Before changing anything I measured the assumptions baked into the defaults and I kept the harness afterwards. The experiment started with the way inputs were normalised and I kept the harness afterwards. The interesting part turned out to be the fallback path nobody exercised and I kept the harness afterwards. The interesting part turned out to be the ranking of candidate passages and I kept the harness afterwards.
