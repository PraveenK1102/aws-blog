## The problem

The interesting failures were the ones where the output read well but cited the wrong source. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Most of the week went into the fallback branch rather than the change I had actually planned to make. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.

My recorded figure for latency is 33%. Latency landed at 37% once I measured it end to end. Instrumenting the embedding cache cost a day and saved considerably more than that later. Two configurations differed by one parameter and by far more than one parameter's worth of output. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Batching improved throughput and made the tail latency considerably harder to reason about. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Spring inputs? Mostly the inputs.
## What I built

Was the gain from the model or from cleaning the Kubernetes inputs? Mostly the inputs. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Splitting the pipeline around the chunking logic made the failures much easier to localise. The number I got for accuracy was 34%. Cleaning the Kubernetes inputs produced a larger gain than any change to the model did. I now write the evaluation before the change, which has saved me from at least two false wins. Cleaning the Redis inputs produced a larger gain than any change to the model did. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I keep a short list of results I do not believe yet, and revisit it weekly. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. In July I assumed the bottleneck was compute; it was almost entirely I/O. Comparing against a fixed baseline made the July numbers finally interpretable.
## What I measured

Running it end to end put retrieval at 53%. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Keeping a log of every configuration felt tedious until I needed to explain a regression. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Every improvement I could not reproduce twice turned out not to be an improvement. Caching Kubernetes lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the fallback branch made the failures much easier to localise. Writing the failure cases down first made the design conversation much shorter. Instrumenting the ingestion path cost a day and saved considerably more than that later. The result held up in staging and quietly regressed the moment traffic patterns changed.
## What I would change

The tempting change was the model; the effective change was how documents were split. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Measured properly, retrieval came out at 39%. I do not trust an improvement that only appears on examples I picked myself. Because the harness came before the optimisation, I caught a regression that looked like a win. Instrumenting the scoring function cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. Most of the week went into the retrieval step rather than the change I had actually planned to make. The fallback path had never been exercised, so nobody knew it returned an empty result.
