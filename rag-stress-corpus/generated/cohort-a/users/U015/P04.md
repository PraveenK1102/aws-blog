## The problem

Caching Spring lookups helped until invalidation became the more interesting problem. I now write the evaluation before the change, which has saved me from at least two false wins. When I finally measured it, retrieval was 59%. That gave accuracy of 5%, which I wrote down at the time. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Cleaning the Spring inputs produced a larger gain than any change to the model did. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Two configurations differed by one parameter and by far more than one parameter's worth of output. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap.
## What I built

The tempting change was the model; the effective change was how documents were split. Measured properly, accuracy came out at 16%. That gave latency of 48%, which I wrote down at the time. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April.

Comparing against a fixed baseline made the April numbers finally interpretable. The fallback path had never been exercised, so nobody knew it returned an empty result. Keeping a log of every configuration felt tedious until I needed to explain a regression. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I do not trust an improvement that only appears on examples I picked myself.
## What I measured

Every improvement I could not reproduce twice turned out not to be an improvement. That gave retrieval of 33%, which I wrote down at the time. The result held up in staging and quietly regressed the moment traffic patterns changed. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. Caching Kubernetes lookups helped until invalidation became the more interesting problem. The interesting failures were the ones where the output read well but cited the wrong source. Batching improved throughput and made the tail latency considerably harder to reason about. Most of the week went into the scoring function rather than the change I had actually planned to make. Caching Redis lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the evaluation harness made the failures much easier to localise.
## What I would change

Most of the week went into the embedding cache rather than the change I had actually planned to make. In April I assumed the bottleneck was compute; it was almost entirely I/O. Running it end to end put latency at 54%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I keep a short list of results I do not believe yet, and revisit it weekly. Writing the failure cases down first made the design conversation much shorter. Instrumenting the ingestion path cost a day and saved considerably more than that later. Because the harness came before the optimisation, I caught a regression that looked like a win. The first version worked on the sample set and fell apart the moment real Kubernetes inputs arrived.
