## The problem

I spent an afternoon convinced the model was wrong before finding a bug in the loader. Tuning Spring helped marginally; fixing how documents were split helped substantially. My recorded figure for accuracy is 27%.

The interesting failures were the ones where the output read well but cited the wrong source. Measured properly, latency came out at 47%. The result held up in staging and quietly regressed the moment traffic patterns changed. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Keeping a log of every configuration felt tedious until I needed to explain a regression. Instrumenting the embedding cache cost a day and saved considerably more than that later. The tempting change was the model; the effective change was how documents were split. Cleaning the Redis inputs produced a larger gain than any change to the model did. Two configurations differed by one parameter and by far more than one parameter's worth of output. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Tuning LangGraph helped marginally; fixing how documents were split helped substantially. Cleaning the LangGraph inputs produced a larger gain than any change to the model did. I do not trust an improvement that only appears on examples I picked myself. Comparing against a fixed baseline made the August numbers finally interpretable. Batching improved throughput and made the tail latency considerably harder to reason about. I keep a short list of results I do not believe yet, and revisit it weekly.
## What I built

Running it end to end put accuracy at 50%. Writing the failure cases down first made the design conversation much shorter. Comparing against a fixed baseline made the August numbers finally interpretable. The fallback path had never been exercised, so nobody knew it returned an empty result. In August I assumed the bottleneck was compute; it was almost entirely I/O. I keep a short list of results I do not believe yet, and revisit it weekly. Tuning Python helped marginally; fixing how documents were split helped substantially. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Most of the week went into the reranking pass rather than the change I had actually planned to make. Instrumenting the ingestion path cost a day and saved considerably more than that later. Splitting the pipeline around the fallback branch made the failures much easier to localise. Cleaning the Kubernetes inputs produced a larger gain than any change to the model did. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Caching Python lookups helped until invalidation became the more interesting problem. Caching Spring lookups helped until invalidation became the more interesting problem. Instrumenting the reranking pass cost a day and saved considerably more than that later. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Because the harness came before the optimisation, I caught a regression that looked like a win. Cleaning the Redis inputs produced a larger gain than any change to the model did.
## What I measured

The number I got for latency was 34%. I do not trust an improvement that only appears on examples I picked myself. Most of the week went into the retrieval step rather than the change I had actually planned to make. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Was the gain from the model or from cleaning the Spring inputs? Mostly the inputs. Because the harness came before the optimisation, I caught a regression that looked like a win. Batching improved throughput and made the tail latency considerably harder to reason about. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the LangGraph inputs? Mostly the inputs. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. The first version worked on the sample set and fell apart the moment real Kubernetes inputs arrived. Instrumenting the chunking logic cost a day and saved considerably more than that later. Instrumenting the reranking pass cost a day and saved considerably more than that later. Instrumenting the evaluation harness cost a day and saved considerably more than that later. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Keeping a log of every configuration felt tedious until I needed to explain a regression. Writing the failure cases down first made the design conversation much shorter. The interesting failures were the ones where the output read well but cited the wrong source.
## What I would change

Cleaning the Python inputs produced a larger gain than any change to the model did. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The first version worked on the sample set and fell apart the moment real LangGraph inputs arrived.

When I finally measured it, latency was 40%. I now write the evaluation before the change, which has saved me from at least two false wins. Every improvement I could not reproduce twice turned out not to be an improvement. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Splitting the pipeline around the ingestion path made the failures much easier to localise. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Caching Kubernetes lookups helped until invalidation became the more interesting problem. Tuning Redis helped marginally; fixing how documents were split helped substantially. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Caching LangGraph lookups helped until invalidation became the more interesting problem. Cleaning the Spring inputs produced a larger gain than any change to the model did. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Two configurations differed by one parameter and by far more than one parameter's worth of output. The tempting change was the model; the effective change was how documents were split.
