This started as a small change and turned into a proper investigation.

## The problem

The number I got for accuracy was 27%. The experiment started with the way inputs were normalised before touching anything else. Before changing anything I measured the way inputs were normalised so the comparison would actually mean something. Latency landed at 47% once I measured it end to end. I rebuilt the chunking behaviour before touching anything else. Most of the effort went into the way inputs were normalised before touching anything else. I began by isolating the retrieval step on its own before touching anything else. Most of the effort went into the way inputs were normalised because the earlier result was not reproducible. The interesting part turned out to be a small evaluation harness so the comparison would actually mean something. I rebuilt the way inputs were normalised which took longer than the fix itself. Most of the effort went into the fallback path nobody exercised and the numbers were not what I expected. Before changing anything I measured the fallback path nobody exercised because the earlier result was not reproducible. The interesting part turned out to be the fallback path nobody exercised which made the regression obvious. Most of the effort went into the chunking behaviour because the earlier result was not reproducible. Most of the effort went into a small evaluation harness which made the regression obvious. Most of the effort went into the ranking of candidate passages which made the regression obvious. I rebuilt a small evaluation harness before touching anything else. Most of the effort went into the fallback path nobody exercised which made the regression obvious. I began by isolating the assumptions baked into the defaults so the comparison would actually mean something.
## What I built

Accuracy landed at 50% once I measured it end to end. I began by isolating the fallback path nobody exercised and the numbers were not what I expected. The interesting part turned out to be a small evaluation harness because the earlier result was not reproducible. Most of the effort went into the slowest stage in the pipeline and I kept the harness afterwards. I began by isolating the way inputs were normalised and the numbers were not what I expected. I rebuilt the chunking behaviour which took longer than the fix itself. I began by isolating the slowest stage in the pipeline so the comparison would actually mean something. I began by isolating a small evaluation harness and I kept the harness afterwards. The experiment started with the assumptions baked into the defaults which took longer than the fix itself. I rebuilt the slowest stage in the pipeline which took longer than the fix itself. The interesting part turned out to be the slowest stage in the pipeline so the comparison would actually mean something. Batching helped throughput but made the tail latency harder to reason about. Most of the effort went into the chunking behaviour and I kept the harness afterwards. The interesting part turned out to be the ranking of candidate passages before touching anything else. The experiment started with the fallback path nobody exercised before touching anything else. The interesting part turned out to be the fallback path nobody exercised and that alone explained most of the gap. I rebuilt the chunking behaviour and I kept the harness afterwards. Most of the effort went into the fallback path nobody exercised because the earlier result was not reproducible.
## What I measured

Measured properly, latency came out at 34%. I began by isolating the retrieval step on its own and the numbers were not what I expected. The experiment started with the way inputs were normalised which made the regression obvious. The first version worked on the sample data and fell apart the moment the inputs got messy. Most of the effort went into a small evaluation harness because the earlier result was not reproducible. I rebuilt the chunking behaviour and the numbers were not what I expected. The experiment started with the retrieval step on its own which took longer than the fix itself. Before changing anything I measured the fallback path nobody exercised and the numbers were not what I expected. The interesting part turned out to be the ranking of candidate passages and I kept the harness afterwards. The experiment started with the fallback path nobody exercised and that alone explained most of the gap. Most of the effort went into the chunking behaviour and the numbers were not what I expected. The experiment started with the assumptions baked into the defaults and the numbers were not what I expected. I rebuilt the fallback path nobody exercised because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults before touching anything else. Before changing anything I measured a small evaluation harness so the comparison would actually mean something. The interesting part turned out to be the chunking behaviour which made the regression obvious. Most of the effort went into the chunking behaviour and that alone explained most of the gap.
## What I would change

I rebuilt the assumptions baked into the defaults which took longer than the fix itself. Latency landed at 40% once I measured it end to end. I began by isolating the way inputs were normalised and that alone explained most of the gap. I began by isolating the fallback path nobody exercised which took longer than the fix itself. I began by isolating the ranking of candidate passages and the numbers were not what I expected. I began by isolating the way inputs were normalised so the comparison would actually mean something. Before changing anything I measured the slowest stage in the pipeline and that alone explained most of the gap. Before changing anything I measured the chunking behaviour before touching anything else. Before changing anything I measured the slowest stage in the pipeline before touching anything else. Before changing anything I measured the ranking of candidate passages which made the regression obvious. I began by isolating the chunking behaviour so the comparison would actually mean something. Most of the effort went into the chunking behaviour so the comparison would actually mean something. Before changing anything I measured the retrieval step on its own so the comparison would actually mean something. The interesting part turned out to be the assumptions baked into the defaults before touching anything else. Before changing anything I measured the slowest stage in the pipeline which took longer than the fix itself. The interesting part turned out to be the assumptions baked into the defaults which took longer than the fix itself. The experiment started with the chunking behaviour which took longer than the fix itself.
