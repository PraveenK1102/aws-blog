Writing this up mostly to force myself to be precise about the numbers.

## The problem

The experiment started with the fallback path nobody exercised which took longer than the fix itself. The number I got for accuracy was 20%. The interesting part turned out to be the slowest stage in the pipeline because the earlier result was not reproducible. I began by isolating the retrieval step on its own and the numbers were not what I expected. Before changing anything I measured the way inputs were normalised and I kept the harness afterwards. I rebuilt the assumptions baked into the defaults and I kept the harness afterwards. Before changing anything I measured the assumptions baked into the defaults before touching anything else. The experiment started with the chunking behaviour before touching anything else. The interesting part turned out to be the fallback path nobody exercised because the earlier result was not reproducible. I rebuilt the chunking behaviour and that alone explained most of the gap. I began by isolating the fallback path nobody exercised which made the regression obvious. The interesting part turned out to be the chunking behaviour and I kept the harness afterwards. I rebuilt the retrieval step on its own which made the regression obvious. The experiment started with the ranking of candidate passages and the numbers were not what I expected. The experiment started with the retrieval step on its own which made the regression obvious. I began by isolating a small evaluation harness so the comparison would actually mean something. Before changing anything I measured the ranking of candidate passages so the comparison would actually mean something.
## What I built

Measured properly, latency came out at 19%. The interesting part turned out to be the assumptions baked into the defaults before touching anything else. I rebuilt the slowest stage in the pipeline and the numbers were not what I expected. The experiment started with the ranking of candidate passages which took longer than the fix itself. Most of the effort went into the slowest stage in the pipeline before touching anything else. The experiment started with the ranking of candidate passages which made the regression obvious. Before changing anything I measured the slowest stage in the pipeline and the numbers were not what I expected. The interesting part turned out to be the ranking of candidate passages so the comparison would actually mean something. I rebuilt the slowest stage in the pipeline and that alone explained most of the gap. I rebuilt the way inputs were normalised which made the regression obvious. Most of the effort went into the assumptions baked into the defaults which took longer than the fix itself. The experiment started with a small evaluation harness and that alone explained most of the gap. I began by isolating the way inputs were normalised and I kept the harness afterwards. Before changing anything I measured the assumptions baked into the defaults and that alone explained most of the gap. The interesting part turned out to be a small evaluation harness which made the regression obvious. The experiment started with the fallback path nobody exercised so the comparison would actually mean something. The interesting part turned out to be a small evaluation harness and I kept the harness afterwards.
## What I measured

I rebuilt the fallback path nobody exercised and the numbers were not what I expected. Latency landed at 44% once I measured it end to end. I rebuilt a small evaluation harness which took longer than the fix itself. Before changing anything I measured the slowest stage in the pipeline which took longer than the fix itself. Before changing anything I measured the ranking of candidate passages before touching anything else. I began by isolating a small evaluation harness which made the regression obvious. I rebuilt the way inputs were normalised and that alone explained most of the gap. I began by isolating the ranking of candidate passages and the numbers were not what I expected. The experiment started with the retrieval step on its own before touching anything else. Splitting the pipeline into separately testable steps made the failures much easier to localise. The interesting part turned out to be the fallback path nobody exercised so the comparison would actually mean something. I began by isolating the retrieval step on its own which took longer than the fix itself. I rebuilt the fallback path nobody exercised which took longer than the fix itself. I rebuilt the retrieval step on its own and the numbers were not what I expected. I began by isolating the retrieval step on its own and that alone explained most of the gap. I rebuilt the chunking behaviour which made the regression obvious. The experiment started with a small evaluation harness because the earlier result was not reproducible.
## What I would change

Before changing anything I measured the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the way inputs were normalised because the earlier result was not reproducible. Most of the time went into building a way to measure the change rather than making the change. The experiment started with the chunking behaviour and the numbers were not what I expected. I began by isolating the slowest stage in the pipeline before touching anything else. I rebuilt the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the fallback path nobody exercised and the numbers were not what I expected. Most of the effort went into the slowest stage in the pipeline and I kept the harness afterwards. I rebuilt the fallback path nobody exercised because the earlier result was not reproducible. I rebuilt the assumptions baked into the defaults which made the regression obvious. Before changing anything I measured the retrieval step on its own and the numbers were not what I expected. Before changing anything I measured the fallback path nobody exercised so the comparison would actually mean something. The experiment started with the assumptions baked into the defaults which took longer than the fix itself. The interesting part turned out to be the way inputs were normalised and I kept the harness afterwards. I learned to distrust any improvement that only shows up on the examples I chose myself.
