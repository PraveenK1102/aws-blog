## The problem

My recorded figure for accuracy is 20%. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I keep a short list of results I do not believe yet, and revisit it weekly.

Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Two configurations differed by one parameter and by far more than one parameter's worth of output. Was the gain from the model or from cleaning the Spring inputs? Mostly the inputs. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. Every improvement I could not reproduce twice turned out not to be an improvement. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially. Cleaning the Python inputs produced a larger gain than any change to the model did. Tuning Python helped marginally; fixing how documents were split helped substantially. Most of the week went into the scoring function rather than the change I had actually planned to make. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning Spring helped marginally; fixing how documents were split helped substantially. I keep a short list of results I do not believe yet, and revisit it weekly. The fallback path had never been exercised, so nobody knew it returned an empty result.
## What I built

I do not trust an improvement that only appears on examples I picked myself. Writing the failure cases down first made the design conversation much shorter. Splitting the pipeline around the scoring function made the failures much easier to localise. The tempting change was the model; the effective change was how documents were split. The number I got for latency was 19%. Caching Spring lookups helped until invalidation became the more interesting problem. In May I assumed the bottleneck was compute; it was almost entirely I/O. Tuning Spring helped marginally; fixing how documents were split helped substantially. The result held up in staging and quietly regressed the moment traffic patterns changed. Cleaning the Redis inputs produced a larger gain than any change to the model did. The interesting failures were the ones where the output read well but cited the wrong source. Keeping a log of every configuration felt tedious until I needed to explain a regression. Splitting the pipeline around the embedding cache made the failures much easier to localise. Instrumenting the fallback branch cost a day and saved considerably more than that later. Tuning Redis helped marginally; fixing how documents were split helped substantially. Cleaning the Spring inputs produced a larger gain than any change to the model did. Cleaning the Python inputs produced a larger gain than any change to the model did. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Batching improved throughput and made the tail latency considerably harder to reason about.
## What I measured

I rebuilt the embedding cache so the before-and-after comparison would actually mean something. On the instrumented run, latency showed 44%. Because the harness came before the optimisation, I caught a regression that looked like a win. Batching improved throughput and made the tail latency considerably harder to reason about. Caching LangGraph lookups helped until invalidation became the more interesting problem. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Kubernetes inputs arrived. I now write the evaluation before the change, which has saved me from at least two false wins. Most of the week went into the retrieval step rather than the change I had actually planned to make. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Instrumenting the retrieval step cost a day and saved considerably more than that later. Instrumenting the embedding cache cost a day and saved considerably more than that later. The result held up in staging and quietly regressed the moment traffic patterns changed. Cleaning the Kubernetes inputs produced a larger gain than any change to the model did.
## What I would change

Comparing against a fixed baseline made the May numbers finally interpretable. Most of the week went into the embedding cache rather than the change I had actually planned to make. Instrumenting the evaluation harness cost a day and saved considerably more than that later.

Tuning LangGraph helped marginally; fixing how documents were split helped substantially. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Instrumenting the scoring function cost a day and saved considerably more than that later. The fallback path had never been exercised, so nobody knew it returned an empty result. Splitting the pipeline around the ingestion path made the failures much easier to localise. Caching Redis lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the LangGraph inputs? Mostly the inputs. Instrumenting the chunking logic cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Cleaning the LangGraph inputs produced a larger gain than any change to the model did. Caching Python lookups helped until invalidation became the more interesting problem. Caching Kubernetes lookups helped until invalidation became the more interesting problem. Caching LangGraph lookups helped until invalidation became the more interesting problem. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Instrumenting the scoring function cost a day and saved considerably more than that later.
