## The problem

When I finally measured it, accuracy was 19%. Caching LangGraph lookups helped until invalidation became the more interesting problem. Batching improved throughput and made the tail latency considerably harder to reason about.

The tempting change was the model; the effective change was how documents were split. The number I got for latency was 38%. The result held up in staging and quietly regressed the moment traffic patterns changed. Tuning LangGraph helped marginally; fixing how documents were split helped substantially. Cleaning the LangGraph inputs produced a larger gain than any change to the model did. Most of the week went into the reranking pass rather than the change I had actually planned to make. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Caching Python lookups helped until invalidation became the more interesting problem. Tuning Redis helped marginally; fixing how documents were split helped substantially. Caching Kubernetes lookups helped until invalidation became the more interesting problem.
## What I built

I now write the evaluation before the change, which has saved me from at least two false wins. Every improvement I could not reproduce twice turned out not to be an improvement. Running it end to end put retrieval at 11%. I spent an afternoon convinced the model was wrong before finding a bug in the loader.

Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Was the gain from the model or from cleaning the Kubernetes inputs? Mostly the inputs. Instrumenting the reranking pass cost a day and saved considerably more than that later. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Because the harness came before the optimisation, I caught a regression that looked like a win. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap.
## What I measured

Writing the failure cases down first made the design conversation much shorter. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Latency landed at 17% once I measured it end to end. Comparing against a fixed baseline made the August numbers finally interpretable. Two configurations differed by one parameter and by far more than one parameter's worth of output. I do not trust an improvement that only appears on examples I picked myself. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning Python helped marginally; fixing how documents were split helped substantially. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Most of the week went into the fallback branch rather than the change I had actually planned to make. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Splitting the pipeline around the chunking logic made the failures much easier to localise.
## What I would change

Once I logged every configuration, explaining the regression to myself took minutes rather than a day. In August I assumed the bottleneck was compute; it was almost entirely I/O. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap.

On the instrumented run, accuracy showed 6%. The interesting failures were the ones where the output read well but cited the wrong source. I keep a short list of results I do not believe yet, and revisit it weekly. The fallback path had never been exercised, so nobody knew it returned an empty result. Splitting the pipeline around the scoring function made the failures much easier to localise. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Cleaning the Spring inputs produced a larger gain than any change to the model did. Instrumenting the retrieval step cost a day and saved considerably more than that later.
