## The problem

My recorded figure for accuracy is 13%. When I finally measured it, retrieval was 14%. Writing the failure cases down first made the design conversation much shorter. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Caching Python lookups helped until invalidation became the more interesting problem. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I keep a short list of results I do not believe yet, and revisit it weekly. Splitting the pipeline around the embedding cache made the failures much easier to localise. Was the gain from the model or from cleaning the Spring inputs? Mostly the inputs. Cleaning the Kubernetes inputs produced a larger gain than any change to the model did. Most of the week went into the chunking logic rather than the change I had actually planned to make.
## What I built

I spent an afternoon convinced the model was wrong before finding a bug in the loader. Batching improved throughput and made the tail latency considerably harder to reason about. Cleaning the Spring inputs produced a larger gain than any change to the model did. Measured properly, accuracy came out at 26%. Two configurations differed by one parameter and by far more than one parameter's worth of output. Keeping a log of every configuration felt tedious until I needed to explain a regression. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Comparing against a fixed baseline made the March numbers finally interpretable. I now write the evaluation before the change, which has saved me from at least two false wins. Tuning Redis helped marginally; fixing how documents were split helped substantially. Instrumenting the evaluation harness cost a day and saved considerably more than that later.
## What I measured

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Measured properly, retrieval came out at 42%. The first version worked on the sample set and fell apart the moment real LangGraph inputs arrived. The fallback path had never been exercised, so nobody knew it returned an empty result.

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Because the harness came before the optimisation, I caught a regression that looked like a win. Cleaning the Redis inputs produced a larger gain than any change to the model did. Most of the week went into the ingestion path rather than the change I had actually planned to make. Caching LangGraph lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. The interesting failures were the ones where the output read well but cited the wrong source.
## What I would change

Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Running it end to end put latency at 47%. Instrumenting the scoring function cost a day and saved considerably more than that later. The tempting change was the model; the effective change was how documents were split. Tuning Python helped marginally; fixing how documents were split helped substantially. Instrumenting the embedding cache cost a day and saved considerably more than that later. In March I assumed the bottleneck was compute; it was almost entirely I/O. Every improvement I could not reproduce twice turned out not to be an improvement. I rebuilt the reranking pass so the before-and-after comparison would actually mean something.
