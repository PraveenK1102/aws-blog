## The problem

Running it end to end put accuracy at 30%. Writing the failure cases down first made the design conversation much shorter. I rebuilt the scoring function so the before-and-after comparison would actually mean something. I keep a short list of results I do not believe yet, and revisit it weekly. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Two configurations differed by one parameter and by far more than one parameter's worth of output. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Cleaning the Redis inputs produced a larger gain than any change to the model did. In July I assumed the bottleneck was compute; it was almost entirely I/O. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Tuning Kubernetes helped marginally; fixing how documents were split helped substantially.
## What I built

When I finally measured it, latency was 11%. Tuning Python helped marginally; fixing how documents were split helped substantially. Instrumenting the retrieval step cost a day and saved considerably more than that later. The result held up in staging and quietly regressed the moment traffic patterns changed. The tempting change was the model; the effective change was how documents were split. Cleaning the Spring inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Keeping a log of every configuration felt tedious until I needed to explain a regression. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Tuning Spring helped marginally; fixing how documents were split helped substantially. Instrumenting the chunking logic cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real Spring inputs arrived. Cleaning the Python inputs produced a larger gain than any change to the model did.
## What I measured

I spent an afternoon convinced the model was wrong before finding a bug in the loader. I now write the evaluation before the change, which has saved me from at least two false wins. The number I got for retrieval was 39%. The interesting failures were the ones where the output read well but cited the wrong source. The fallback path had never been exercised, so nobody knew it returned an empty result. Most of the week went into the embedding cache rather than the change I had actually planned to make. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Batching improved throughput and made the tail latency considerably harder to reason about. Caching Kubernetes lookups helped until invalidation became the more interesting problem. Comparing against a fixed baseline made the July numbers finally interpretable. Splitting the pipeline around the evaluation harness made the failures much easier to localise.
## What I would change

I rebuilt the chunking logic so the before-and-after comparison would actually mean something. I do not trust an improvement that only appears on examples I picked myself. Splitting the pipeline around the ingestion path made the failures much easier to localise. Because the harness came before the optimisation, I caught a regression that looked like a win. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Every improvement I could not reproduce twice turned out not to be an improvement.
