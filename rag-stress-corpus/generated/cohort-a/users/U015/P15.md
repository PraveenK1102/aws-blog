Writing this up mostly to force myself to be precise about the numbers.

## The problem

I began by isolating the slowest stage in the pipeline before touching anything else. A surprising amount of the gain came from cleaning the inputs rather than changing the model. Accuracy landed at 30% once I measured it end to end. The experiment started with the retrieval step on its own before touching anything else. Before changing anything I measured the fallback path nobody exercised and that alone explained most of the gap. I rebuilt the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the slowest stage in the pipeline and that alone explained most of the gap. The interesting part turned out to be a small evaluation harness which took longer than the fix itself. Most of the effort went into the slowest stage in the pipeline and the numbers were not what I expected. I rebuilt the fallback path nobody exercised before touching anything else. The interesting part turned out to be the slowest stage in the pipeline before touching anything else.
## What I built

Most of the effort went into the chunking behaviour and the numbers were not what I expected. The interesting part turned out to be the way inputs were normalised which made the regression obvious. Latency landed at 11% once I measured it end to end. I rebuilt a small evaluation harness because the earlier result was not reproducible. I began by isolating the retrieval step on its own which took longer than the fix itself. Most of the effort went into the ranking of candidate passages and that alone explained most of the gap. The experiment started with the assumptions baked into the defaults which made the regression obvious. Most of the effort went into the assumptions baked into the defaults before touching anything else. Most of the effort went into the fallback path nobody exercised and I kept the harness afterwards. I began by isolating the assumptions baked into the defaults and that alone explained most of the gap. The interesting part turned out to be the slowest stage in the pipeline which made the regression obvious.
## What I measured

The number I got for retrieval was 39%. The interesting part turned out to be the ranking of candidate passages which made the regression obvious. Before changing anything I measured the ranking of candidate passages and I kept the harness afterwards. Most of the effort went into a small evaluation harness and that alone explained most of the gap. The experiment started with the fallback path nobody exercised which took longer than the fix itself. I rebuilt a small evaluation harness and the numbers were not what I expected. Before changing anything I measured the slowest stage in the pipeline and the numbers were not what I expected. Before changing anything I measured the retrieval step on its own which made the regression obvious. I rebuilt the ranking of candidate passages which made the regression obvious. I rebuilt the slowest stage in the pipeline which made the regression obvious.
## What I would change

I learned to distrust any improvement that only shows up on the examples I chose myself. I rebuilt the way inputs were normalised which made the regression obvious. Most of the effort went into the chunking behaviour and that alone explained most of the gap. Most of the effort went into the fallback path nobody exercised which made the regression obvious. The experiment started with the ranking of candidate passages so the comparison would actually mean something. The experiment started with the chunking behaviour and the numbers were not what I expected. I began by isolating the chunking behaviour which took longer than the fix itself. I began by isolating the fallback path nobody exercised and the numbers were not what I expected. The experiment started with the retrieval step on its own because the earlier result was not reproducible.
