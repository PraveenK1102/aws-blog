## The problem

That gave retries of 4% change, which I wrote down at the time. Because the consumer fell behind quietly, the first symptom was a customer report. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. The postmortem was more useful than the fix because it changed how we test. On the instrumented run, retries showed 35% change. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. We spent longer agreeing what done meant for the connection pool than on the implementation itself. We now write down what we expect to see before deploying, which catches surprises early. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. The config that caused it had been in place for months without incident. The alert fired correctly and nobody could act on it, which is its own kind of failure. I no longer trust a health check that only proves the process is running. Documenting the failure mode turned out more valuable than documenting the happy path. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse.
## The design

The number I got for retries was 44% change. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. The config that caused it had been in place for months without incident. Every retry we added made the graphs look better and the system behave worse.

The fix was four lines; the diagnosis took two days. An average hid a tail that was doing all the damage. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. Because the consumer fell behind quietly, the first symptom was a customer report. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Making the failure reproducible locally took longer than repairing it. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident.
## Implementation notes

Making the failure reproducible locally took longer than repairing it. Documenting the failure mode turned out more valuable than documenting the happy path. Small boring changes with a measurement attached beat clever changes without one, every time. My recorded figure for retries is 20% change. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. One badly scoped timeout propagated into three services before anyone noticed. Three deploys in, the pattern was obvious: the migration degraded before anything else did. We spent longer agreeing what done meant for the index than on the implementation itself. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. I assumed the database was the bottleneck for two days longer than the evidence supported. The postmortem was more useful than the fix because it changed how we test. Every retry we added made the graphs look better and the system behave worse. We added one metric, waited a week, and the cause became embarrassingly clear. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident.
## What I measured

When I finally measured it, retries was 17% change. We shipped the metric first and the fix a week later, in that deliberate order. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We added one metric, waited a week, and the cause became embarrassingly clear.

Adding a metric to the index before adding a fix is the only reason we caught the regression at all. The index only misbehaved at peak, so the averages stayed healthy right through the incident. I no longer trust a health check that only proves the process is running. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. An average hid a tail that was doing all the damage. Small boring changes with a measurement attached beat clever changes without one, every time. The obvious cause was not the real cause, and the logs had said so if you read them carefully. One badly scoped timeout propagated into three services before anyone noticed.
## The lesson

I assumed the database was the bottleneck for two days longer than the evidence supported. Under normal load the retry path behaved; under peak it did not, and only peak mattered. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.

Measured properly, retries came out at 43% change. The alert fired correctly and nobody could act on it, which is its own kind of failure. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. Retries around the index made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the index broke again. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The fix was four lines; the diagnosis took two days. Idempotency mattered far more than throughput on this path, which inverted our original priorities. We shipped the metric first and the fix a week later, in that deliberate order. We now write down what we expect to see before deploying, which catches surprises early.
