## The problem

The index only misbehaved at peak, so the averages stayed healthy right through the incident. Idempotency mattered far more than throughput on this path, which inverted our original priorities. We now write down what we expect to see before deploying, which catches surprises early. Caching landed at 4% change once I measured it end to end. Small boring changes with a measurement attached beat clever changes without one, every time. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. We added one metric, waited a week, and the cause became embarrassingly clear. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. The postmortem was more useful than the fix because it changed how we test. Documenting the failure mode turned out more valuable than documenting the happy path. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The index only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

One badly scoped timeout propagated into three services before anyone noticed. Measured properly, caching came out at 17% change. The alert fired correctly and nobody could act on it, which is its own kind of failure. An average hid a tail that was doing all the damage. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. Retries around the index made the symptom disappear and the underlying problem considerably worse. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. The fix was four lines; the diagnosis took two days. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. We added one metric, waited a week, and the cause became embarrassingly clear. Small boring changes with a measurement attached beat clever changes without one, every time. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We now write down what we expect to see before deploying, which catches surprises early.
## Implementation notes

I no longer trust a health check that only proves the process is running. The postmortem was more useful than the fix because it changed how we test. The config that caused it had been in place for months without incident.

On the instrumented run, caching showed 5% change. Every retry we added made the graphs look better and the system behave worse. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. We spent longer agreeing what done meant for the retry path than on the implementation itself. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. The config that caused it had been in place for months without incident. An average hid a tail that was doing all the damage. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. I no longer trust a health check that only proves the process is running. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.
## What I measured

Documenting the failure mode turned out more valuable than documenting the happy path. Running it end to end put caching at 22% change. Making the failure reproducible locally took longer than repairing it. We shipped the metric first and the fix a week later, in that deliberate order. I assumed the database was the bottleneck for two days longer than the evidence supported. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. We spent longer agreeing what done meant for the connection pool than on the implementation itself. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We shipped the metric first and the fix a week later, in that deliberate order. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. One badly scoped timeout propagated into three services before anyone noticed. Making the failure reproducible locally took longer than repairing it.
## The lesson

The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. That gave caching of 21% change, which I wrote down at the time. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. Under normal load the retry path behaved; under peak it did not, and only peak mattered. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. Because the consumer fell behind quietly, the first symptom was a customer report. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. Because the consumer fell behind quietly, the first symptom was a customer report. I assumed the database was the bottleneck for two days longer than the evidence supported. Every retry we added made the graphs look better and the system behave worse. The alert fired correctly and nobody could act on it, which is its own kind of failure. The fix was four lines; the diagnosis took two days.
