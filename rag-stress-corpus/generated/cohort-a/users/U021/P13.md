This came out of a production incident, so the details matter.

## The problem

Caching landed at 4% change once I measured it end to end. Instrumentation revealed an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. We spent longer agreeing on what 'done' meant than on the implementation itself. We eventually traced it to a slow query behind a cache miss and it only appeared under load. The fix came down to a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. The first hypothesis was an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. What actually broke was an index that no longer matched the access pattern so the graphs looked healthy throughout. Instrumentation revealed a retry storm masking the real error though the fix was less interesting than the diagnosis. The fix came down to a retry storm masking the real error though the fix was less interesting than the diagnosis. Instrumentation revealed a migration that had never been rehearsed and it only appeared under load. The failure showed up as an index that no longer matched the access pattern and it only appeared under load. Instrumentation revealed an index that no longer matched the access pattern once we stopped trusting the average. The fix came down to a retry storm masking the real error and the logs had said so all along.
## The design

We eventually traced it to a queue consumer falling behind quietly and the logs had said so all along. What actually broke was a slow query behind a cache miss once we stopped trusting the average. Caching landed at 17% change once I measured it end to end. The failure showed up as a migration that had never been rehearsed so the graphs looked healthy throughout. Instrumentation revealed a migration that had never been rehearsed and it has not recurred since. What actually broke was an index that no longer matched the access pattern and it only appeared under load. We eventually traced it to a metric that averaged away the tail and the logs had said so all along. What actually broke was a queue consumer falling behind quietly which is why the rollback mattered. We eventually traced it to an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. What actually broke was an index that no longer matched the access pattern once we stopped trusting the average. We eventually traced it to a slow query behind a cache miss which is why the rollback mattered. What actually broke was a slow query behind a cache miss which is why the rollback mattered.
## Implementation notes

The fix came down to a timeout longer than the caller's which explained the intermittency. Instrumentation revealed a slow query behind a cache miss so the graphs looked healthy throughout. Measured properly, caching came out at 5% change. We eventually traced it to a connection pool exhausted at peak and it only appeared under load. The fix came down to a migration that had never been rehearsed once we stopped trusting the average. The first hypothesis was a metric that averaged away the tail and it only appeared under load. We eventually traced it to a slow query behind a cache miss once we stopped trusting the average. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average. What actually broke was a retry storm masking the real error and it has not recurred since. The fix came down to a metric that averaged away the tail which explained the intermittency. We eventually traced it to a migration that had never been rehearsed and it has not recurred since. What actually broke was a migration that had never been rehearsed and it only appeared under load.
## What I measured

What actually broke was a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. Measured properly, caching came out at 22% change. Instrumentation revealed a metric that averaged away the tail though the fix was less interesting than the diagnosis. The failure showed up as a slow query behind a cache miss and it only appeared under load. The fix came down to a timeout longer than the caller's and it only appeared under load. The fix came down to an index that no longer matched the access pattern and it only appeared under load. The failure showed up as an index that no longer matched the access pattern which explained the intermittency. The first hypothesis was a retry storm masking the real error and it has not recurred since. The first hypothesis was a migration that had never been rehearsed and the logs had said so all along. What actually broke was a slow query behind a cache miss and the logs had said so all along. The first hypothesis was a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. We eventually traced it to a retry storm masking the real error and it only appeared under load.
## The lesson

Caching landed at 21% change once I measured it end to end. The fix came down to a slow query behind a cache miss which is why the rollback mattered. What actually broke was a migration that had never been rehearsed though the fix was less interesting than the diagnosis. Retries made the symptom disappear and the underlying problem worse. Instrumentation revealed a slow query behind a cache miss and it only appeared under load. Adding a metric before adding a fix turned out to be the only reason we caught the regression. Instrumentation revealed a queue consumer falling behind quietly which is why the rollback mattered. The failure showed up as a timeout longer than the caller's and the logs had said so all along. The fix came down to a queue consumer falling behind quietly which is why the rollback mattered. What actually broke was a metric that averaged away the tail and the logs had said so all along. What actually broke was a queue consumer falling behind quietly and the logs had said so all along. The first hypothesis was a retry storm masking the real error which explained the intermittency.
