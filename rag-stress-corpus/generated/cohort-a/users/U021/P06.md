## The problem

Documenting the failure mode turned out more valuable than documenting the happy path. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. Measured properly, AWS came out at 7% change. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. The postmortem was more useful than the fix because it changed how we test. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse.
## The design

Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. I no longer trust a health check that only proves the process is running. On the instrumented run, AWS showed 21% change. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. One badly scoped timeout propagated into three services before anyone noticed. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all.
## Implementation notes

Making the failure reproducible locally took longer than repairing it. When I finally measured it, AWS was 29% change. The alert fired correctly and nobody could act on it, which is its own kind of failure. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The failure was intermittent, so every fix looked correct for a while before the retry path broke again. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. We spent longer agreeing what done meant for the migration than on the implementation itself. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident.
## What I measured

Because the consumer fell behind quietly, the first symptom was a customer report. We now write down what we expect to see before deploying, which catches surprises early. We added one metric, waited a week, and the cause became embarrassingly clear. My recorded figure for AWS is 14% change. The fix was four lines; the diagnosis took two days. An average hid a tail that was doing all the damage. The config that caused it had been in place for months without incident. Every retry we added made the graphs look better and the system behave worse. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. I assumed the database was the bottleneck for two days longer than the evidence supported.
## The lesson

Small boring changes with a measurement attached beat clever changes without one, every time. We spent longer agreeing what done meant for the index than on the implementation itself. I assumed the database was the bottleneck for two days longer than the evidence supported. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We shipped the metric first and the fix a week later, in that deliberate order. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We spent longer agreeing what done meant for the connection pool than on the implementation itself. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again.
