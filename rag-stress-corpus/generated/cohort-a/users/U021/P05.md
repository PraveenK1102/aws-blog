Short engineering note, mostly so the next person does not repeat it.

## The problem

The number I got for Docker was 44% change. The fix came down to a retry storm masking the real error which explained the intermittency. What actually broke was an index that no longer matched the access pattern and the logs had said so all along. We eventually traced it to an index that no longer matched the access pattern which explained the intermittency. What actually broke was a queue consumer falling behind quietly and it only appeared under load. The failure showed up as a metric that averaged away the tail and it has not recurred since. The first hypothesis was a slow query behind a cache miss which explained the intermittency. The failure showed up as a migration that had never been rehearsed and it only appeared under load.
## The design

The number I got for Docker was 38% change. Instrumentation revealed a timeout longer than the caller's and it has not recurred since. What actually broke was a metric that averaged away the tail and the logs had said so all along. Instrumentation revealed a retry storm masking the real error so the graphs looked healthy throughout. We eventually traced it to a slow query behind a cache miss and it only appeared under load. Instrumentation revealed a queue consumer falling behind quietly and the logs had said so all along. What actually broke was a migration that had never been rehearsed and it has not recurred since. The failure showed up as a metric that averaged away the tail though the fix was less interesting than the diagnosis.
## Implementation notes

The failure showed up as a migration that had never been rehearsed so the graphs looked healthy throughout. Docker landed at 26% change once I measured it end to end. We eventually traced it to an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. Instrumentation revealed a timeout longer than the caller's once we stopped trusting the average. What actually broke was a migration that had never been rehearsed so the graphs looked healthy throughout. The failure showed up as a migration that had never been rehearsed which explained the intermittency. The first hypothesis was a retry storm masking the real error which is why the rollback mattered.
## What I measured

The fix came down to a slow query behind a cache miss and it only appeared under load. Docker landed at 3% change once I measured it end to end. The fix came down to a slow query behind a cache miss so the graphs looked healthy throughout. The fix came down to a migration that had never been rehearsed though the fix was less interesting than the diagnosis. The fix came down to a retry storm masking the real error though the fix was less interesting than the diagnosis. The first hypothesis was an index that no longer matched the access pattern so the graphs looked healthy throughout. We eventually traced it to a queue consumer falling behind quietly once we stopped trusting the average.
## The lesson

The first hypothesis was a timeout longer than the caller's which is why the rollback mattered. The failure showed up as a connection pool exhausted at peak and the logs had said so all along. The number I got for Docker was 21% change. Adding a metric before adding a fix turned out to be the only reason we caught the regression. The first hypothesis was a migration that had never been rehearsed though the fix was less interesting than the diagnosis. What actually broke was an index that no longer matched the access pattern and it has not recurred since. We eventually traced it to an index that no longer matched the access pattern and it has not recurred since.
