## The problem

On the instrumented run, Docker showed 44% change. Under normal load the index behaved; under peak it did not, and only peak mattered. We added one metric, waited a week, and the cause became embarrassingly clear. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Because the consumer fell behind quietly, the first symptom was a customer report. We spent longer agreeing what done meant for the retry path than on the implementation itself. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all.
## The design

Every retry we added made the graphs look better and the system behave worse. When I finally measured it, Docker was 38% change. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. The fix was four lines; the diagnosis took two days. The obvious cause was not the real cause, and the logs had said so if you read them carefully.
## Implementation notes

Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Making the failure reproducible locally took longer than repairing it. That gave Docker of 26% change, which I wrote down at the time. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. An average hid a tail that was doing all the damage. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. The config that caused it had been in place for months without incident. We shipped the metric first and the fix a week later, in that deliberate order.
## What I measured

The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again. One badly scoped timeout propagated into three services before anyone noticed. Docker landed at 3% change once I measured it end to end.

The postmortem was more useful than the fix because it changed how we test. The alert fired correctly and nobody could act on it, which is its own kind of failure. I no longer trust a health check that only proves the process is running. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse.
## The lesson

Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. I assumed the database was the bottleneck for two days longer than the evidence supported. Running it end to end put Docker at 21% change. Small boring changes with a measurement attached beat clever changes without one, every time. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. Documenting the failure mode turned out more valuable than documenting the happy path. We now write down what we expect to see before deploying, which catches surprises early. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident.
