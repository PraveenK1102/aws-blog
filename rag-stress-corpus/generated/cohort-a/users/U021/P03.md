This came out of a production incident, so the details matter.

## The problem

Transactions landed at 16% change once I measured it end to end. What actually broke was a metric that averaged away the tail which explained the intermittency. Instrumentation revealed a metric that averaged away the tail once we stopped trusting the average. The failure showed up as a slow query behind a cache miss which is why the rollback mattered. Instrumentation revealed a slow query behind a cache miss which is why the rollback mattered. We eventually traced it to a queue consumer falling behind quietly and it only appeared under load. Instrumentation revealed a connection pool exhausted at peak and it has not recurred since. The failure showed up as a migration that had never been rehearsed though the fix was less interesting than the diagnosis. What actually broke was a slow query behind a cache miss and the logs had said so all along. What actually broke was a metric that averaged away the tail and the logs had said so all along. Instrumentation revealed an index that no longer matched the access pattern and it has not recurred since. The fix came down to a queue consumer falling behind quietly and the logs had said so all along.
## The design

The fix came down to a timeout longer than the caller's and it has not recurred since. Measured properly, transactions came out at 45% change. Instrumentation revealed a queue consumer falling behind quietly and it only appeared under load. The first hypothesis was a slow query behind a cache miss once we stopped trusting the average. The first hypothesis was a slow query behind a cache miss which explained the intermittency. We eventually traced it to a connection pool exhausted at peak and it only appeared under load. We eventually traced it to a slow query behind a cache miss once we stopped trusting the average. The failure showed up as a retry storm masking the real error and it only appeared under load. The fix came down to a connection pool exhausted at peak which is why the rollback mattered. Small, boring changes with a measurement attached beat clever changes without one. Instrumentation revealed a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average.
## Implementation notes

The fix came down to a timeout longer than the caller's once we stopped trusting the average. We eventually traced it to a slow query behind a cache miss though the fix was less interesting than the diagnosis. The number I got for transactions was 38% change. The failure showed up as a migration that had never been rehearsed which explained the intermittency. Instrumentation revealed a timeout longer than the caller's which is why the rollback mattered. The failure showed up as a metric that averaged away the tail and the logs had said so all along. The failure showed up as a connection pool exhausted at peak and the logs had said so all along. The failure showed up as a queue consumer falling behind quietly so the graphs looked healthy throughout. Instrumentation revealed an index that no longer matched the access pattern which explained the intermittency. What actually broke was a slow query behind a cache miss though the fix was less interesting than the diagnosis. The failure showed up as a queue consumer falling behind quietly once we stopped trusting the average. What actually broke was a connection pool exhausted at peak and the logs had said so all along.
## What I measured

Transactions landed at 22% change once I measured it end to end. The first hypothesis was a timeout longer than the caller's which explained the intermittency. What actually broke was a slow query behind a cache miss so the graphs looked healthy throughout. We eventually traced it to an index that no longer matched the access pattern which is why the rollback mattered. The first hypothesis was a slow query behind a cache miss and it only appeared under load. Instrumentation revealed a slow query behind a cache miss though the fix was less interesting than the diagnosis. The fix came down to a slow query behind a cache miss once we stopped trusting the average. The failure showed up as a metric that averaged away the tail and it has not recurred since. What actually broke was a connection pool exhausted at peak and it only appeared under load. The fix came down to a slow query behind a cache miss and it only appeared under load. The first hypothesis was a metric that averaged away the tail which explained the intermittency. Instrumentation revealed a retry storm masking the real error so the graphs looked healthy throughout.
## The lesson

What actually broke was a migration that had never been rehearsed and it has not recurred since. The failure showed up as a connection pool exhausted at peak which is why the rollback mattered. The failure showed up as a timeout longer than the caller's and the logs had said so all along. The failure showed up as a retry storm masking the real error and it has not recurred since. Instrumentation revealed a connection pool exhausted at peak so the graphs looked healthy throughout. We eventually traced it to an index that no longer matched the access pattern and it only appeared under load. The fix came down to a retry storm masking the real error and the logs had said so all along. The first hypothesis was a metric that averaged away the tail and it only appeared under load. What actually broke was a connection pool exhausted at peak and it has not recurred since. What actually broke was an index that no longer matched the access pattern which explained the intermittency.
