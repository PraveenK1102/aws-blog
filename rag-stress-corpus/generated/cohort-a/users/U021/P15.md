## The problem

We shipped the metric first and the fix a week later, in that deliberate order. When I finally measured it, database indexes was 9% change. Because the consumer fell behind quietly, the first symptom was a customer report. We spent longer agreeing what done meant for the index than on the implementation itself. Three deploys in, the pattern was obvious: the index degraded before anything else did. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse.
## The design

Under normal load the migration behaved; under peak it did not, and only peak mattered. We added one metric, waited a week, and the cause became embarrassingly clear. My recorded figure for database indexes is 33% change. Small boring changes with a measurement attached beat clever changes without one, every time.

We now write down what we expect to see before deploying, which catches surprises early. I assumed the database was the bottleneck for two days longer than the evidence supported. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. Documenting the failure mode turned out more valuable than documenting the happy path. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. An average hid a tail that was doing all the damage.
## Implementation notes

Every retry we added made the graphs look better and the system behave worse. Making the failure reproducible locally took longer than repairing it. Database indexes landed at 30% change once I measured it end to end. I no longer trust a health check that only proves the process is running. The fix was four lines; the diagnosis took two days. We spent longer agreeing what done meant for the cache layer than on the implementation itself. Under normal load the index behaved; under peak it did not, and only peak mattered. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## What I measured

Measured properly, database indexes came out at 23% change. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The postmortem was more useful than the fix because it changed how we test. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. One badly scoped timeout propagated into three services before anyone noticed. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. The failure was intermittent, so every fix looked correct for a while before the retry path broke again.
## The lesson

The config that caused it had been in place for months without incident. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Retries around the migration made the symptom disappear and the underlying problem considerably worse. The alert fired correctly and nobody could act on it, which is its own kind of failure. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the timeout budget than on the implementation itself.
