A note to myself, because I will absolutely forget the details.

## The problem

The first hypothesis was a timeout longer than the caller's though the fix was less interesting than the diagnosis. Measured properly, database indexes came out at 9% change. We eventually traced it to a retry storm masking the real error which explained the intermittency. What actually broke was a connection pool exhausted at peak though the fix was less interesting than the diagnosis. Instrumentation revealed a timeout longer than the caller's so the graphs looked healthy throughout. The first hypothesis was a queue consumer falling behind quietly and the logs had said so all along. What actually broke was a timeout longer than the caller's which is why the rollback mattered. We eventually traced it to a slow query behind a cache miss once we stopped trusting the average.
## The design

The fix came down to a connection pool exhausted at peak which explained the intermittency. The number I got for database indexes was 33% change. We eventually traced it to a connection pool exhausted at peak and the logs had said so all along. We eventually traced it to a migration that had never been rehearsed and it only appeared under load. The first hypothesis was a connection pool exhausted at peak so the graphs looked healthy throughout. The first hypothesis was a timeout longer than the caller's which is why the rollback mattered. The failure showed up as a metric that averaged away the tail though the fix was less interesting than the diagnosis. The first hypothesis was an index that no longer matched the access pattern though the fix was less interesting than the diagnosis.
## Implementation notes

The number I got for database indexes was 30% change. Instrumentation revealed a migration that had never been rehearsed so the graphs looked healthy throughout. The failure showed up as a timeout longer than the caller's so the graphs looked healthy throughout. The failure showed up as a queue consumer falling behind quietly which is why the rollback mattered. We eventually traced it to a queue consumer falling behind quietly which explained the intermittency. The first hypothesis was a timeout longer than the caller's and it has not recurred since. The failure showed up as a migration that had never been rehearsed though the fix was less interesting than the diagnosis. We eventually traced it to a slow query behind a cache miss and it only appeared under load.
## What I measured

The fix came down to a connection pool exhausted at peak and it has not recurred since. Measured properly, database indexes came out at 23% change. We eventually traced it to a retry storm masking the real error and the logs had said so all along. Adding a metric before adding a fix turned out to be the only reason we caught the regression. What actually broke was an index that no longer matched the access pattern so the graphs looked healthy throughout. The failure showed up as a queue consumer falling behind quietly and it has not recurred since. The fix came down to a timeout longer than the caller's which is why the rollback mattered. Instrumentation revealed a retry storm masking the real error though the fix was less interesting than the diagnosis.
## The lesson

The first hypothesis was a retry storm masking the real error once we stopped trusting the average. Instrumentation revealed a queue consumer falling behind quietly which explained the intermittency. The failure showed up as a retry storm masking the real error though the fix was less interesting than the diagnosis. We eventually traced it to a slow query behind a cache miss though the fix was less interesting than the diagnosis. The first hypothesis was an index that no longer matched the access pattern so the graphs looked healthy throughout. The first hypothesis was a connection pool exhausted at peak which is why the rollback mattered.
