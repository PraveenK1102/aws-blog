## The problem

Every retry we added made the graphs look better and the system behave worse. We added one metric, waited a week, and the cause became embarrassingly clear. The postmortem was more useful than the fix because it changed how we test. Measured properly, queues came out at 12% change.

The config that caused it had been in place for months without incident. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Under normal load the retry path behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the index broke again. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. An average hid a tail that was doing all the damage. On the instrumented run, queues showed 36% change.

I assumed the database was the bottleneck for two days longer than the evidence supported. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. We shipped the metric first and the fix a week later, in that deliberate order. Making the failure reproducible locally took longer than repairing it. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did.
## Implementation notes

Running it end to end put queues at 10% change. We spent longer agreeing what done meant for the retry path than on the implementation itself. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. The alert fired correctly and nobody could act on it, which is its own kind of failure. We spent longer agreeing what done meant for the cache layer than on the implementation itself. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The fix was four lines; the diagnosis took two days. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse.
## What I measured

I no longer trust a health check that only proves the process is running. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. We now write down what we expect to see before deploying, which catches surprises early. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Because the consumer fell behind quietly, the first symptom was a customer report. Retries around the migration made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the connection pool than on the implementation itself.
## The lesson

One badly scoped timeout propagated into three services before anyone noticed. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. Documenting the failure mode turned out more valuable than documenting the happy path. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Small boring changes with a measurement attached beat clever changes without one, every time. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again.
