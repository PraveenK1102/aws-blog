A note to myself, because I will absolutely forget the details.

## The problem

The number I got for rate limiting was 32% change. Instrumentation revealed a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. The failure showed up as a metric that averaged away the tail though the fix was less interesting than the diagnosis. Instrumentation revealed a migration that had never been rehearsed which explained the intermittency. The failure showed up as a timeout longer than the caller's and it has not recurred since. Instrumentation revealed a metric that averaged away the tail though the fix was less interesting than the diagnosis. What actually broke was a connection pool exhausted at peak which is why the rollback mattered. We eventually traced it to a slow query behind a cache miss though the fix was less interesting than the diagnosis.
## The design

The number I got for rate limiting was 30% change. We eventually traced it to a queue consumer falling behind quietly and the logs had said so all along. The first hypothesis was a retry storm masking the real error which is why the rollback mattered. The first hypothesis was a slow query behind a cache miss though the fix was less interesting than the diagnosis. We eventually traced it to a migration that had never been rehearsed once we stopped trusting the average. The first hypothesis was a retry storm masking the real error and it only appeared under load. The failure showed up as an index that no longer matched the access pattern and the logs had said so all along. What actually broke was a queue consumer falling behind quietly and the logs had said so all along.
## Implementation notes

What actually broke was a timeout longer than the caller's so the graphs looked healthy throughout. We eventually traced it to a migration that had never been rehearsed so the graphs looked healthy throughout. The number I got for rate limiting was 20% change. We eventually traced it to a connection pool exhausted at peak and the logs had said so all along. We eventually traced it to a retry storm masking the real error and the logs had said so all along. Instrumentation revealed a migration that had never been rehearsed though the fix was less interesting than the diagnosis. What actually broke was a slow query behind a cache miss and it has not recurred since. The fix came down to a slow query behind a cache miss and it has not recurred since.
## What I measured

The fix came down to a retry storm masking the real error which explained the intermittency. The number I got for rate limiting was 40% change. The first hypothesis was a queue consumer falling behind quietly and it has not recurred since. The fix came down to a metric that averaged away the tail and it has not recurred since. We eventually traced it to an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. Retries made the symptom disappear and the underlying problem worse. Instrumentation revealed a timeout longer than the caller's which explained the intermittency. The fix came down to a metric that averaged away the tail which explained the intermittency.
## The lesson

Instrumentation revealed a queue consumer falling behind quietly and it only appeared under load. We eventually traced it to a metric that averaged away the tail which is why the rollback mattered. Rate limiting landed at 3% change once I measured it end to end. The failure showed up as a queue consumer falling behind quietly once we stopped trusting the average. The failure showed up as a timeout longer than the caller's though the fix was less interesting than the diagnosis. We eventually traced it to a timeout longer than the caller's which is why the rollback mattered. The failure showed up as a connection pool exhausted at peak once we stopped trusting the average.
