## The problem

My recorded figure for rate limiting is 32% change. Documenting the failure mode turned out more valuable than documenting the happy path. Small boring changes with a measurement attached beat clever changes without one, every time. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. An average hid a tail that was doing all the damage. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all.
## The design

We added one metric, waited a week, and the cause became embarrassingly clear. When I finally measured it, rate limiting was 30% change. Three deploys in, the pattern was obvious: the migration degraded before anything else did. I assumed the database was the bottleneck for two days longer than the evidence supported. Under normal load the migration behaved; under peak it did not, and only peak mattered. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. The postmortem was more useful than the fix because it changed how we test. Retries around the migration made the symptom disappear and the underlying problem considerably worse. Under normal load the retry path behaved; under peak it did not, and only peak mattered.
## Implementation notes

Making the failure reproducible locally took longer than repairing it. Running it end to end put rate limiting at 20% change. One badly scoped timeout propagated into three services before anyone noticed. Because the consumer fell behind quietly, the first symptom was a customer report.

We shipped the metric first and the fix a week later, in that deliberate order. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. We spent longer agreeing what done meant for the connection pool than on the implementation itself. Retries around the index made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the index than on the implementation itself. We spent longer agreeing what done meant for the retry path than on the implementation itself.
## What I measured

Under normal load the index behaved; under peak it did not, and only peak mattered. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. That gave rate limiting of 40% change, which I wrote down at the time. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The failure was intermittent, so every fix looked correct for a while before the migration broke again. The config that caused it had been in place for months without incident. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident.
## The lesson

I no longer trust a health check that only proves the process is running. Measured properly, rate limiting came out at 3% change. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. We now write down what we expect to see before deploying, which catches surprises early. The fix was four lines; the diagnosis took two days. Every retry we added made the graphs look better and the system behave worse. The alert fired correctly and nobody could act on it, which is its own kind of failure. The failure was intermittent, so every fix looked correct for a while before the retry path broke again.
