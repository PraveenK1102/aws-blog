## The problem

The alert fired correctly and nobody could act on it, which is its own kind of failure. My recorded figure for Docker is 35% change. Retries around the migration made the symptom disappear and the underlying problem considerably worse.

That gave Docker of 34% change, which I wrote down at the time. Three deploys in, the pattern was obvious: the index degraded before anything else did. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. I assumed the database was the bottleneck for two days longer than the evidence supported. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Making the failure reproducible locally took longer than repairing it. We spent longer agreeing what done meant for the retry path than on the implementation itself. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. We now write down what we expect to see before deploying, which catches surprises early. I assumed the database was the bottleneck for two days longer than the evidence supported. Documenting the failure mode turned out more valuable than documenting the happy path. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters.
## The design

The config that caused it had been in place for months without incident. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. Measured properly, Docker came out at 6% change. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. Under normal load the retry path behaved; under peak it did not, and only peak mattered. The postmortem was more useful than the fix because it changed how we test. Small boring changes with a measurement attached beat clever changes without one, every time. We shipped the metric first and the fix a week later, in that deliberate order. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. The config that caused it had been in place for months without incident. An average hid a tail that was doing all the damage. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The fix was four lines; the diagnosis took two days. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident.
## Implementation notes

We added one metric, waited a week, and the cause became embarrassingly clear. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Docker landed at 30% change once I measured it end to end. Because the consumer fell behind quietly, the first symptom was a customer report. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Documenting the failure mode turned out more valuable than documenting the happy path. Every retry we added made the graphs look better and the system behave worse. The failure was intermittent, so every fix looked correct for a while before the migration broke again. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. The postmortem was more useful than the fix because it changed how we test. I no longer trust a health check that only proves the process is running. Every retry we added made the graphs look better and the system behave worse. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse.
## What I measured

On the instrumented run, Docker showed 41% change. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. One badly scoped timeout propagated into three services before anyone noticed. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. We added one metric, waited a week, and the cause became embarrassingly clear. We shipped the metric first and the fix a week later, in that deliberate order. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Making the failure reproducible locally took longer than repairing it. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## The lesson

I no longer trust a health check that only proves the process is running. The number I got for Docker was 3% change. We spent longer agreeing what done meant for the connection pool than on the implementation itself. The fix was four lines; the diagnosis took two days. We now write down what we expect to see before deploying, which catches surprises early. An average hid a tail that was doing all the damage. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Small boring changes with a measurement attached beat clever changes without one, every time. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The alert fired correctly and nobody could act on it, which is its own kind of failure. One badly scoped timeout propagated into three services before anyone noticed. Because the consumer fell behind quietly, the first symptom was a customer report.
