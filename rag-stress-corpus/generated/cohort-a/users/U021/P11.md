## The problem

We spent longer agreeing what done meant for the index than on the implementation itself. That gave React of 24% change, which I wrote down at the time. The obvious cause was not the real cause, and the logs had said so if you read them carefully.

The alert fired correctly and nobody could act on it, which is its own kind of failure. We added one metric, waited a week, and the cause became embarrassingly clear. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. We shipped the metric first and the fix a week later, in that deliberate order. We spent longer agreeing what done meant for the migration than on the implementation itself. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Every retry we added made the graphs look better and the system behave worse.
## The design

Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. Idempotency mattered far more than throughput on this path, which inverted our original priorities. React landed at 33% change once I measured it end to end. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Every retry we added made the graphs look better and the system behave worse. We now write down what we expect to see before deploying, which catches surprises early. I assumed the database was the bottleneck for two days longer than the evidence supported. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the index broke again. The index only misbehaved at peak, so the averages stayed healthy right through the incident.
## Implementation notes

The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. On the instrumented run, React showed 23% change. Small boring changes with a measurement attached beat clever changes without one, every time. Making the failure reproducible locally took longer than repairing it. The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again. Documenting the failure mode turned out more valuable than documenting the happy path. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## What I measured

An average hid a tail that was doing all the damage. Running it end to end put React at 37% change. The config that caused it had been in place for months without incident. Three deploys in, the pattern was obvious: the index degraded before anything else did. The postmortem was more useful than the fix because it changed how we test. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Because the consumer fell behind quietly, the first symptom was a customer report. The failure was intermittent, so every fix looked correct for a while before the migration broke again. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. The fix was four lines; the diagnosis took two days.
## The lesson

The number I got for React was 30% change. Under normal load the index behaved; under peak it did not, and only peak mattered. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.

We spent longer agreeing what done meant for the timeout budget than on the implementation itself. I no longer trust a health check that only proves the process is running. The fix was four lines; the diagnosis took two days. One badly scoped timeout propagated into three services before anyone noticed. Retries around the index made the symptom disappear and the underlying problem considerably worse. Under normal load the migration behaved; under peak it did not, and only peak mattered. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. We now write down what we expect to see before deploying, which catches surprises early.
