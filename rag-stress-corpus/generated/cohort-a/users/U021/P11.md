This came out of a production incident, so the details matter.

## The problem

Adding a metric before adding a fix turned out to be the only reason we caught the regression. The number I got for React was 24% change. We eventually traced it to a migration that had never been rehearsed once we stopped trusting the average. Instrumentation revealed a timeout longer than the caller's and it has not recurred since. Instrumentation revealed an index that no longer matched the access pattern and the logs had said so all along. What actually broke was a metric that averaged away the tail which is why the rollback mattered. The fix came down to a metric that averaged away the tail though the fix was less interesting than the diagnosis. The first hypothesis was a slow query behind a cache miss which explained the intermittency. We eventually traced it to a metric that averaged away the tail which is why the rollback mattered.
## The design

What actually broke was a queue consumer falling behind quietly and it has not recurred since. The number I got for React was 33% change. What actually broke was a queue consumer falling behind quietly and the logs had said so all along. The fix came down to a timeout longer than the caller's which explained the intermittency. We eventually traced it to a retry storm masking the real error so the graphs looked healthy throughout. Instrumentation revealed a migration that had never been rehearsed which explained the intermittency. What actually broke was a migration that had never been rehearsed though the fix was less interesting than the diagnosis. What actually broke was a slow query behind a cache miss so the graphs looked healthy throughout. What actually broke was an index that no longer matched the access pattern though the fix was less interesting than the diagnosis.
## Implementation notes

React landed at 23% change once I measured it end to end. The failure showed up as a retry storm masking the real error though the fix was less interesting than the diagnosis. Instrumentation revealed a migration that had never been rehearsed once we stopped trusting the average. The failure showed up as a metric that averaged away the tail and it has not recurred since. We eventually traced it to a timeout longer than the caller's and the logs had said so all along. The failure showed up as a slow query behind a cache miss which explained the intermittency. What actually broke was an index that no longer matched the access pattern so the graphs looked healthy throughout. The failure showed up as a queue consumer falling behind quietly and it only appeared under load. The first hypothesis was a retry storm masking the real error and it only appeared under load.
## What I measured

The first hypothesis was a migration that had never been rehearsed which is why the rollback mattered. Instrumentation revealed a metric that averaged away the tail once we stopped trusting the average. Measured properly, React came out at 37% change. The failure showed up as a retry storm masking the real error which explained the intermittency. The failure showed up as a slow query behind a cache miss and the logs had said so all along. Instrumentation revealed a retry storm masking the real error and it only appeared under load. We eventually traced it to a timeout longer than the caller's and it only appeared under load. Instrumentation revealed a timeout longer than the caller's which explained the intermittency. The failure showed up as a migration that had never been rehearsed so the graphs looked healthy throughout.
## The lesson

The fix came down to a connection pool exhausted at peak once we stopped trusting the average. The first hypothesis was a queue consumer falling behind quietly and it has not recurred since. Measured properly, React came out at 30% change. Instrumentation revealed a retry storm masking the real error once we stopped trusting the average. We eventually traced it to a connection pool exhausted at peak which explained the intermittency. The failure showed up as a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. Instrumentation revealed a connection pool exhausted at peak once we stopped trusting the average. Instrumentation revealed a timeout longer than the caller's and it only appeared under load. The failure showed up as a connection pool exhausted at peak though the fix was less interesting than the diagnosis.
