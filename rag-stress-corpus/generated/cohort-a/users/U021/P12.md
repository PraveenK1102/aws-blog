Writing down what actually happened rather than what I assumed happened.

## The problem

The number I got for API design was 32% change. Api design landed at 14% change once I measured it end to end. The first hypothesis was a queue consumer falling behind quietly which explained the intermittency. What actually broke was a connection pool exhausted at peak so the graphs looked healthy throughout. Idempotency turned out to matter far more than throughput in this path. Instrumentation revealed a timeout longer than the caller's so the graphs looked healthy throughout. The first hypothesis was a slow query behind a cache miss and the logs had said so all along. The first hypothesis was a queue consumer falling behind quietly so the graphs looked healthy throughout. Small, boring changes with a measurement attached beat clever changes without one. Instrumentation revealed an index that no longer matched the access pattern so the graphs looked healthy throughout.
## The design

Instrumentation revealed an index that no longer matched the access pattern which is why the rollback mattered. What actually broke was a connection pool exhausted at peak and it only appeared under load. The number I got for API design was 11% change. The first hypothesis was a metric that averaged away the tail though the fix was less interesting than the diagnosis. The failure was intermittent, which meant every fix looked like it worked for a while. The first hypothesis was a metric that averaged away the tail once we stopped trusting the average. We eventually traced it to a metric that averaged away the tail and it only appeared under load. Instrumentation revealed a migration that had never been rehearsed and it has not recurred since. Instrumentation revealed a queue consumer falling behind quietly which explained the intermittency.
## Implementation notes

Api design landed at 19% change once I measured it end to end. The obvious cause was not the real cause, and the logs said so if you read them carefully. We eventually traced it to a slow query behind a cache miss and it has not recurred since. The failure showed up as a migration that had never been rehearsed so the graphs looked healthy throughout. Instrumentation revealed a timeout longer than the caller's which explained the intermittency. Instrumentation revealed a retry storm masking the real error and it only appeared under load. The failure showed up as an index that no longer matched the access pattern and it only appeared under load. The failure showed up as a timeout longer than the caller's which is why the rollback mattered.
## What I measured

The first hypothesis was a migration that had never been rehearsed so the graphs looked healthy throughout. The number I got for API design was 17% change. We eventually traced it to an index that no longer matched the access pattern which is why the rollback mattered. What actually broke was a metric that averaged away the tail once we stopped trusting the average. The rollback plan was the part we had not rehearsed, which is always the part that matters. What actually broke was a metric that averaged away the tail and it has not recurred since. The failure showed up as a metric that averaged away the tail which explained the intermittency. Instrumentation revealed a metric that averaged away the tail though the fix was less interesting than the diagnosis.
## The lesson

The number I got for API design was 25% change. The failure showed up as an index that no longer matched the access pattern which explained the intermittency. The first hypothesis was a connection pool exhausted at peak which explained the intermittency. The failure showed up as a queue consumer falling behind quietly once we stopped trusting the average. The first hypothesis was a timeout longer than the caller's though the fix was less interesting than the diagnosis. The fix came down to a queue consumer falling behind quietly once we stopped trusting the average. What actually broke was a migration that had never been rehearsed and it has not recurred since. The first hypothesis was a metric that averaged away the tail and it only appeared under load.
