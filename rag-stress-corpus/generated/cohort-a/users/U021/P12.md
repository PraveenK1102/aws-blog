## The problem

The failure was intermittent, so every fix looked correct for a while before the index broke again. The config that caused it had been in place for months without incident. One badly scoped timeout propagated into three services before anyone noticed.

That gave API design of 14% change, which I wrote down at the time. Documenting the failure mode turned out more valuable than documenting the happy path. The number I got for API design was 32% change. Small boring changes with a measurement attached beat clever changes without one, every time. The alert fired correctly and nobody could act on it, which is its own kind of failure. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. My recorded figure for API design is 11% change. Because the consumer fell behind quietly, the first symptom was a customer report. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. Three deploys in, the pattern was obvious: the index degraded before anything else did. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The postmortem was more useful than the fix because it changed how we test. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered.
## Implementation notes

The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. I no longer trust a health check that only proves the process is running. When I finally measured it, API design was 19% change. Making the failure reproducible locally took longer than repairing it. I assumed the database was the bottleneck for two days longer than the evidence supported. The fix was four lines; the diagnosis took two days. An average hid a tail that was doing all the damage. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did.
## What I measured

Under normal load the connection pool behaved; under peak it did not, and only peak mattered. We added one metric, waited a week, and the cause became embarrassingly clear. Running it end to end put API design at 17% change. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. We now write down what we expect to see before deploying, which catches surprises early. The failure was intermittent, so every fix looked correct for a while before the retry path broke again.
## The lesson

Every retry we added made the graphs look better and the system behave worse. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Api design landed at 25% change once I measured it end to end. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. Under normal load the retry path behaved; under peak it did not, and only peak mattered. We shipped the metric first and the fix a week later, in that deliberate order. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. Retries around the migration made the symptom disappear and the underlying problem considerably worse.
