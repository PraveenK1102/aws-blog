## The problem

I no longer trust a health check that only proves the process is running. An average hid a tail that was doing all the damage. We added one metric, waited a week, and the cause became embarrassingly clear.

Queues landed at 35% change once I measured it end to end. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We now write down what we expect to see before deploying, which catches surprises early. The failure was intermittent, so every fix looked correct for a while before the index broke again. I assumed the database was the bottleneck for two days longer than the evidence supported. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Under normal load the index behaved; under peak it did not, and only peak mattered. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Making the failure reproducible locally took longer than repairing it.
## The design

That gave queues of 43% change, which I wrote down at the time. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident.

We spent longer agreeing what done meant for the retry path than on the implementation itself. One badly scoped timeout propagated into three services before anyone noticed. We shipped the metric first and the fix a week later, in that deliberate order. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Retries around the index made the symptom disappear and the underlying problem considerably worse. The fix was four lines; the diagnosis took two days. We shipped the metric first and the fix a week later, in that deliberate order. An average hid a tail that was doing all the damage. The index only misbehaved at peak, so the averages stayed healthy right through the incident.
## Implementation notes

The alert fired correctly and nobody could act on it, which is its own kind of failure. Queues landed at 25% change once I measured it end to end. Documenting the failure mode turned out more valuable than documenting the happy path. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We spent longer agreeing what done meant for the index than on the implementation itself. Every retry we added made the graphs look better and the system behave worse. The config that caused it had been in place for months without incident. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Documenting the failure mode turned out more valuable than documenting the happy path. I no longer trust a health check that only proves the process is running. Every retry we added made the graphs look better and the system behave worse. Because the consumer fell behind quietly, the first symptom was a customer report.
## What I measured

Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. Retries around the migration made the symptom disappear and the underlying problem considerably worse. The fix was four lines; the diagnosis took two days. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. I assumed the database was the bottleneck for two days longer than the evidence supported. The config that caused it had been in place for months without incident.
## The lesson

Idempotency mattered far more than throughput on this path, which inverted our original priorities. Because the consumer fell behind quietly, the first symptom was a customer report. Making the failure reproducible locally took longer than repairing it. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. Small boring changes with a measurement attached beat clever changes without one, every time. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. The postmortem was more useful than the fix because it changed how we test. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. Small boring changes with a measurement attached beat clever changes without one, every time. One badly scoped timeout propagated into three services before anyone noticed. We added one metric, waited a week, and the cause became embarrassingly clear. The alert fired correctly and nobody could act on it, which is its own kind of failure.
