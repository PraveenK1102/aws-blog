Short engineering note, mostly so the next person does not repeat it.

## The problem

The fix came down to a migration that had never been rehearsed and the logs had said so all along. Instrumentation revealed a timeout longer than the caller's which explained the intermittency. Queues landed at 35% change once I measured it end to end. What actually broke was a slow query behind a cache miss though the fix was less interesting than the diagnosis. The first hypothesis was a retry storm masking the real error once we stopped trusting the average. The failure showed up as a timeout longer than the caller's which explained the intermittency. Instrumentation revealed a queue consumer falling behind quietly which is why the rollback mattered. The fix came down to a retry storm masking the real error though the fix was less interesting than the diagnosis. We eventually traced it to a timeout longer than the caller's which explained the intermittency. The first hypothesis was an index that no longer matched the access pattern once we stopped trusting the average. We eventually traced it to a slow query behind a cache miss which explained the intermittency. We eventually traced it to a retry storm masking the real error which is why the rollback mattered.
## The design

Measured properly, queues came out at 43% change. Instrumentation revealed a timeout longer than the caller's and it has not recurred since. We eventually traced it to a timeout longer than the caller's and it has not recurred since. What actually broke was a migration that had never been rehearsed and the logs had said so all along. The first hypothesis was a metric that averaged away the tail which is why the rollback mattered. We eventually traced it to a retry storm masking the real error once we stopped trusting the average. What actually broke was a retry storm masking the real error so the graphs looked healthy throughout. Instrumentation revealed an index that no longer matched the access pattern and the logs had said so all along. We eventually traced it to a slow query behind a cache miss which is why the rollback mattered. The fix came down to a retry storm masking the real error so the graphs looked healthy throughout. What actually broke was a migration that had never been rehearsed so the graphs looked healthy throughout. Instrumentation revealed a timeout longer than the caller's so the graphs looked healthy throughout.
## Implementation notes

The failure showed up as a metric that averaged away the tail so the graphs looked healthy throughout. Instrumentation revealed a slow query behind a cache miss though the fix was less interesting than the diagnosis. Queues landed at 25% change once I measured it end to end. We eventually traced it to a slow query behind a cache miss and it has not recurred since. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average. We eventually traced it to a migration that had never been rehearsed though the fix was less interesting than the diagnosis. Instrumentation revealed a connection pool exhausted at peak and the logs had said so all along. The fix came down to a retry storm masking the real error and it only appeared under load. Instrumentation revealed a timeout longer than the caller's which is why the rollback mattered. The fix came down to a slow query behind a cache miss though the fix was less interesting than the diagnosis. Instrumentation revealed a metric that averaged away the tail and it has not recurred since.
## What I measured

Instrumentation revealed a retry storm masking the real error once we stopped trusting the average. Instrumentation revealed a retry storm masking the real error which is why the rollback mattered. We eventually traced it to a slow query behind a cache miss and the logs had said so all along. The fix came down to an index that no longer matched the access pattern and it only appeared under load. The first hypothesis was a retry storm masking the real error which explained the intermittency. The first hypothesis was a connection pool exhausted at peak and it has not recurred since. The failure showed up as a retry storm masking the real error and the logs had said so all along. We eventually traced it to an index that no longer matched the access pattern and it has not recurred since. We eventually traced it to a timeout longer than the caller's which is why the rollback mattered. The failure showed up as an index that no longer matched the access pattern so the graphs looked healthy throughout.
## The lesson

The first hypothesis was a metric that averaged away the tail though the fix was less interesting than the diagnosis. We eventually traced it to a queue consumer falling behind quietly which is why the rollback mattered. We eventually traced it to a metric that averaged away the tail though the fix was less interesting than the diagnosis. The fix came down to a slow query behind a cache miss which explained the intermittency. We eventually traced it to an index that no longer matched the access pattern and the logs had said so all along. The first hypothesis was a connection pool exhausted at peak which is why the rollback mattered. The fix came down to a slow query behind a cache miss and it only appeared under load. Instrumentation revealed an index that no longer matched the access pattern and it has not recurred since. We eventually traced it to a metric that averaged away the tail and it only appeared under load. The first hypothesis was a retry storm masking the real error and it only appeared under load.
