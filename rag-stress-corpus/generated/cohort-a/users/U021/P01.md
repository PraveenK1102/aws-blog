A note to myself, because I will absolutely forget the details.

## The problem

The failure showed up as a slow query behind a cache miss and the logs had said so all along. The rollback plan was the part we had not rehearsed, which is always the part that matters. The number I got for Redis was 7% change. The failure showed up as a metric that averaged away the tail which explained the intermittency. The first hypothesis was a timeout longer than the caller's though the fix was less interesting than the diagnosis. We eventually traced it to a slow query behind a cache miss which is why the rollback mattered. Adding a metric before adding a fix turned out to be the only reason we caught the regression. The fix came down to a retry storm masking the real error and the logs had said so all along. What actually broke was a timeout longer than the caller's and the logs had said so all along.
## The design

Measured properly, Redis came out at 17% change. The failure showed up as a connection pool exhausted at peak so the graphs looked healthy throughout. The failure showed up as an index that no longer matched the access pattern which explained the intermittency. The fix came down to a timeout longer than the caller's and it has not recurred since. We eventually traced it to a timeout longer than the caller's and it only appeared under load. The first hypothesis was a metric that averaged away the tail and it only appeared under load. We eventually traced it to a slow query behind a cache miss and it has not recurred since. What actually broke was a queue consumer falling behind quietly which explained the intermittency. We eventually traced it to a slow query behind a cache miss so the graphs looked healthy throughout.
## Implementation notes

Instrumentation revealed a retry storm masking the real error and the logs had said so all along. The first hypothesis was a retry storm masking the real error so the graphs looked healthy throughout. The number I got for Redis was 28% change. The failure showed up as a timeout longer than the caller's which is why the rollback mattered. What actually broke was a metric that averaged away the tail which is why the rollback mattered. The first hypothesis was a retry storm masking the real error which explained the intermittency. The first hypothesis was a connection pool exhausted at peak once we stopped trusting the average. Instrumentation revealed a slow query behind a cache miss and it only appeared under load.
## What I measured

The failure showed up as a retry storm masking the real error though the fix was less interesting than the diagnosis. Instrumentation revealed an index that no longer matched the access pattern which is why the rollback mattered. Measured properly, Redis came out at 19% change. The fix came down to a slow query behind a cache miss and it has not recurred since. The first hypothesis was a connection pool exhausted at peak though the fix was less interesting than the diagnosis. We eventually traced it to a migration that had never been rehearsed and it has not recurred since. Instrumentation revealed a timeout longer than the caller's which explained the intermittency. We eventually traced it to an index that no longer matched the access pattern and the logs had said so all along.
## The lesson

The failure showed up as a metric that averaged away the tail and it only appeared under load. Instrumentation revealed an index that no longer matched the access pattern and the logs had said so all along. Instrumentation revealed a retry storm masking the real error which is why the rollback mattered. We eventually traced it to a queue consumer falling behind quietly and the logs had said so all along. The failure showed up as a migration that had never been rehearsed which is why the rollback mattered. What actually broke was a connection pool exhausted at peak and it only appeared under load. The fix came down to an index that no longer matched the access pattern and it has not recurred since.
