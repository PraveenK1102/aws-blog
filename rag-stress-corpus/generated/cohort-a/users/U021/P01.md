## The problem

We now write down what we expect to see before deploying, which catches surprises early. Running it end to end put Redis at 7% change. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Every retry we added made the graphs look better and the system behave worse. Under normal load the migration behaved; under peak it did not, and only peak mattered. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the retry path broke again.
## The design

Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. Measured properly, Redis came out at 17% change. One badly scoped timeout propagated into three services before anyone noticed. We spent longer agreeing what done meant for the retry path than on the implementation itself. I assumed the database was the bottleneck for two days longer than the evidence supported. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. The config that caused it had been in place for months without incident. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse.
## Implementation notes

Making the failure reproducible locally took longer than repairing it. Small boring changes with a measurement attached beat clever changes without one, every time. My recorded figure for Redis is 28% change. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. We spent longer agreeing what done meant for the migration than on the implementation itself. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident.
## What I measured

The number I got for Redis was 19% change. Idempotency mattered far more than throughput on this path, which inverted our original priorities. We shipped the metric first and the fix a week later, in that deliberate order.

Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. The alert fired correctly and nobody could act on it, which is its own kind of failure. I no longer trust a health check that only proves the process is running. Because the consumer fell behind quietly, the first symptom was a customer report. The postmortem was more useful than the fix because it changed how we test. The failure was intermittent, so every fix looked correct for a while before the index broke again. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the retry path degraded before anything else did.
## The lesson

An average hid a tail that was doing all the damage. Documenting the failure mode turned out more valuable than documenting the happy path. The fix was four lines; the diagnosis took two days.

We added one metric, waited a week, and the cause became embarrassingly clear. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all.
