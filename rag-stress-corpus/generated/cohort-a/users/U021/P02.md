Writing down what actually happened rather than what I assumed happened.

## The problem

The first hypothesis was a slow query behind a cache miss and the logs had said so all along. The failure showed up as a connection pool exhausted at peak and the logs had said so all along. Database indexes landed at 21% change once I measured it end to end. The failure showed up as an index that no longer matched the access pattern so the graphs looked healthy throughout. The failure showed up as a migration that had never been rehearsed though the fix was less interesting than the diagnosis. We eventually traced it to a retry storm masking the real error which explained the intermittency. The fix came down to a queue consumer falling behind quietly which explained the intermittency.
## The design

Instrumentation revealed a metric that averaged away the tail and the logs had said so all along. The failure showed up as a timeout longer than the caller's though the fix was less interesting than the diagnosis. Database indexes landed at 14% change once I measured it end to end. The fix came down to a migration that had never been rehearsed which is why the rollback mattered. The failure showed up as a slow query behind a cache miss and it has not recurred since. We eventually traced it to a slow query behind a cache miss once we stopped trusting the average. The fix came down to a metric that averaged away the tail and it only appeared under load.
## Implementation notes

The number I got for database indexes was 28% change. The fix came down to a timeout longer than the caller's which explained the intermittency. The failure showed up as an index that no longer matched the access pattern which is why the rollback mattered. Instrumentation revealed a retry storm masking the real error and it has not recurred since. The fix came down to a timeout longer than the caller's and it only appeared under load. The fix came down to a connection pool exhausted at peak once we stopped trusting the average. The first hypothesis was a migration that had never been rehearsed which is why the rollback mattered.
## What I measured

The fix came down to a timeout longer than the caller's so the graphs looked healthy throughout. The first hypothesis was a retry storm masking the real error though the fix was less interesting than the diagnosis. The fix came down to a migration that had never been rehearsed so the graphs looked healthy throughout. We eventually traced it to a migration that had never been rehearsed though the fix was less interesting than the diagnosis. We eventually traced it to a connection pool exhausted at peak so the graphs looked healthy throughout. Instrumentation revealed a timeout longer than the caller's once we stopped trusting the average.
## The lesson

What actually broke was an index that no longer matched the access pattern and it has not recurred since. The failure showed up as a slow query behind a cache miss and it only appeared under load. The fix came down to an index that no longer matched the access pattern which is why the rollback mattered. We eventually traced it to a connection pool exhausted at peak which is why the rollback mattered. The fix came down to an index that no longer matched the access pattern though the fix was less interesting than the diagnosis.
