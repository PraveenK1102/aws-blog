## The problem

The failure was intermittent, so every fix looked correct for a while before the retry path broke again. Running it end to end put database indexes at 21% change. One badly scoped timeout propagated into three services before anyone noticed. The alert fired correctly and nobody could act on it, which is its own kind of failure. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. The postmortem was more useful than the fix because it changed how we test. We added one metric, waited a week, and the cause became embarrassingly clear. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse.
## The design

I no longer trust a health check that only proves the process is running. My recorded figure for database indexes is 14% change. The config that caused it had been in place for months without incident. An average hid a tail that was doing all the damage. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. Documenting the failure mode turned out more valuable than documenting the happy path. We shipped the metric first and the fix a week later, in that deliberate order. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Under normal load the index behaved; under peak it did not, and only peak mattered.
## Implementation notes

Small boring changes with a measurement attached beat clever changes without one, every time. The fix was four lines; the diagnosis took two days. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Database indexes landed at 28% change once I measured it end to end. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. I assumed the database was the bottleneck for two days longer than the evidence supported. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. We spent longer agreeing what done meant for the migration than on the implementation itself.
## What I measured

The obvious cause was not the real cause, and the logs had said so if you read them carefully. We now write down what we expect to see before deploying, which catches surprises early. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. Because the consumer fell behind quietly, the first symptom was a customer report. Every retry we added made the graphs look better and the system behave worse. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Making the failure reproducible locally took longer than repairing it.
## The lesson

Retries around the migration made the symptom disappear and the underlying problem considerably worse. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
