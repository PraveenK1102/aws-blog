## The problem

When I finally measured it, Redis was 33% change. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Every retry we added made the graphs look better and the system behave worse. Because the consumer fell behind quietly, the first symptom was a customer report. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. Documenting the failure mode turned out more valuable than documenting the happy path. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. The alert fired correctly and nobody could act on it, which is its own kind of failure. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Retries around the migration made the symptom disappear and the underlying problem considerably worse.
## The design

I assumed the database was the bottleneck for two days longer than the evidence supported. Idempotency mattered far more than throughput on this path, which inverted our original priorities. My recorded figure for Redis is 15% change. An average hid a tail that was doing all the damage.

Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. We spent longer agreeing what done meant for the migration than on the implementation itself. One badly scoped timeout propagated into three services before anyone noticed. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the cache layer than on the implementation itself. Under normal load the migration behaved; under peak it did not, and only peak mattered.
## Implementation notes

Small boring changes with a measurement attached beat clever changes without one, every time. We shipped the metric first and the fix a week later, in that deliberate order. Running it end to end put Redis at 35% change. The config that caused it had been in place for months without incident. Retries around the index made the symptom disappear and the underlying problem considerably worse. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Three deploys in, the pattern was obvious: the migration degraded before anything else did. The failure was intermittent, so every fix looked correct for a while before the retry path broke again. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## What I measured

Making the failure reproducible locally took longer than repairing it. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We now write down what we expect to see before deploying, which catches surprises early. On the instrumented run, Redis showed 25% change. We added one metric, waited a week, and the cause became embarrassingly clear. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all.
## The lesson

The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. The postmortem was more useful than the fix because it changed how we test. I no longer trust a health check that only proves the process is running. The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again. The fix was four lines; the diagnosis took two days. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Three deploys in, the pattern was obvious: the index degraded before anything else did.
