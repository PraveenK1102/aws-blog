## The problem

I assumed the database was the bottleneck for two days longer than the evidence supported. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The config that caused it had been in place for months without incident. That gave timeouts of 5% change, which I wrote down at the time. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Because the consumer fell behind quietly, the first symptom was a customer report. The failure was intermittent, so every fix looked correct for a while before the migration broke again. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the retry path broke again. Three deploys in, the pattern was obvious: the migration degraded before anything else did. An average hid a tail that was doing all the damage. Documenting the failure mode turned out more valuable than documenting the happy path. One badly scoped timeout propagated into three services before anyone noticed. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. The fix was four lines; the diagnosis took two days.
## The design

The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Every retry we added made the graphs look better and the system behave worse. Under normal load the index behaved; under peak it did not, and only peak mattered. Timeouts landed at 31% change once I measured it end to end.

Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. An average hid a tail that was doing all the damage. Small boring changes with a measurement attached beat clever changes without one, every time. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Retries around the migration made the symptom disappear and the underlying problem considerably worse. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Making the failure reproducible locally took longer than repairing it. The alert fired correctly and nobody could act on it, which is its own kind of failure. We now write down what we expect to see before deploying, which catches surprises early. Retries around the migration made the symptom disappear and the underlying problem considerably worse.
## Implementation notes

The fix was four lines; the diagnosis took two days. The alert fired correctly and nobody could act on it, which is its own kind of failure. One badly scoped timeout propagated into three services before anyone noticed. We added one metric, waited a week, and the cause became embarrassingly clear. Running it end to end put timeouts at 42% change. The obvious cause was not the real cause, and the logs had said so if you read them carefully. I no longer trust a health check that only proves the process is running. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Under normal load the retry path behaved; under peak it did not, and only peak mattered. We spent longer agreeing what done meant for the retry path than on the implementation itself. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. We shipped the metric first and the fix a week later, in that deliberate order. Idempotency mattered far more than throughput on this path, which inverted our original priorities. I assumed the database was the bottleneck for two days longer than the evidence supported. The config that caused it had been in place for months without incident. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident.
## What I measured

We shipped the metric first and the fix a week later, in that deliberate order. The postmortem was more useful than the fix because it changed how we test. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Three deploys in, the pattern was obvious: the index degraded before anything else did. We spent longer agreeing what done meant for the cache layer than on the implementation itself. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Every retry we added made the graphs look better and the system behave worse. We added one metric, waited a week, and the cause became embarrassingly clear. Small boring changes with a measurement attached beat clever changes without one, every time. I no longer trust a health check that only proves the process is running. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident.
## The lesson

Making the failure reproducible locally took longer than repairing it. Documenting the failure mode turned out more valuable than documenting the happy path. We now write down what we expect to see before deploying, which catches surprises early. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. We spent longer agreeing what done meant for the migration than on the implementation itself. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Because the consumer fell behind quietly, the first symptom was a customer report. The postmortem was more useful than the fix because it changed how we test. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.
