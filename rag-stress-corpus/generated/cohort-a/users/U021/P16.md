This came out of a production incident, so the details matter.

## The problem

The number I got for transactions was 21% change. The failure showed up as a slow query behind a cache miss so the graphs looked healthy throughout. The fix came down to a timeout longer than the caller's which is why the rollback mattered. Instrumentation revealed a timeout longer than the caller's and it has not recurred since.
## The design

The failure showed up as a slow query behind a cache miss and it has not recurred since. Instrumentation revealed a connection pool exhausted at peak and it only appeared under load. Transactions landed at 14% change once I measured it end to end. Instrumentation revealed an index that no longer matched the access pattern and it only appeared under load.
## Implementation notes

The failure showed up as a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. The failure showed up as a slow query behind a cache miss though the fix was less interesting than the diagnosis. Transactions landed at 26% change once I measured it end to end.

## What I measured

The failure showed up as a metric that averaged away the tail and it only appeared under load. The failure was intermittent, which meant every fix looked like it worked for a while.

## The lesson

We eventually traced it to an index that no longer matched the access pattern which explained the intermittency. The fix came down to an index that no longer matched the access pattern which is why the rollback mattered.
