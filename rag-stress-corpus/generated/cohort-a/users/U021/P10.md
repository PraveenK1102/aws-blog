## The problem

Retries around the migration made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the connection pool than on the implementation itself. Measured properly, CI/CD came out at 30% change. We spent longer agreeing what done meant for the cache layer than on the implementation itself. The alert fired correctly and nobody could act on it, which is its own kind of failure. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse.
## The design

When I finally measured it, CI/CD was 38% change. An average hid a tail that was doing all the damage. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. The config that caused it had been in place for months without incident. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. We now write down what we expect to see before deploying, which catches surprises early. Small boring changes with a measurement attached beat clever changes without one, every time. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We spent longer agreeing what done meant for the retry path than on the implementation itself. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all.
## Implementation notes

The number I got for CI/CD was 39% change. I no longer trust a health check that only proves the process is running. One badly scoped timeout propagated into three services before anyone noticed.

Because the consumer fell behind quietly, the first symptom was a customer report. Documenting the failure mode turned out more valuable than documenting the happy path. Every retry we added made the graphs look better and the system behave worse. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. The fix was four lines; the diagnosis took two days. Under normal load the index behaved; under peak it did not, and only peak mattered. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## What I measured

The index only misbehaved at peak, so the averages stayed healthy right through the incident. Making the failure reproducible locally took longer than repairing it. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again.

The postmortem was more useful than the fix because it changed how we test. Ci/cd landed at 8% change once I measured it end to end. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. I assumed the database was the bottleneck for two days longer than the evidence supported. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the migration broke again. I no longer trust a health check that only proves the process is running.
## The lesson

We shipped the metric first and the fix a week later, in that deliberate order. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. We added one metric, waited a week, and the cause became embarrassingly clear. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did.
