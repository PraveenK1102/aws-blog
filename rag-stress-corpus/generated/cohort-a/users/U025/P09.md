Keeping this note short and specific on purpose.

## Context

When someone says INC-713, they mean the incident. A short reminder that the suffix is the only distinguishing part and I no longer trust my memory for it. To avoid another mix-up, the label appears in two different systems so I copy it verbatim every time. To avoid another mix-up, the suffix is the only distinguishing part and I no longer trust my memory for it. Recording this precisely: search returns the near-matches first so I copy it verbatim every time. A short reminder that search returns the near-matches first so I copy it verbatim every time. Keeping the reference and the description in the same place has saved time more than once. To avoid another mix-up, the naming predates the current convention which matters more than it sounds. Noting it here because search returns the near-matches first which is why I keep a table. Recording this precisely: search returns the near-matches first which is why I keep a table. To avoid another mix-up, the reference is easy to mistype so it is quoted exactly in tickets. Worth writing down: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. Noting it here because the naming predates the current convention which is why I keep a table. Recording this precisely: these two sit next to each other in the register which matters more than it sounds.
## The reference

INC-137 is the model — worth writing down precisely. To avoid another mix-up, the naming predates the current convention which is why I keep a table. Worth writing down: the suffix is the only distinguishing part which matters more than it sounds. Noting it here because the reference is easy to mistype which matters more than it sounds. Noting it here because the reference is easy to mistype so I copy it verbatim every time. Worth writing down: the naming predates the current convention and I no longer trust my memory for it. A short reminder that the reference is easy to mistype and I no longer trust my memory for it. A short reminder that these two sit next to each other in the register which is why I keep a table. Worth writing down: these two sit next to each other in the register so it is quoted exactly in tickets. Worth writing down: the label appears in two different systems and I no longer trust my memory for it. Noting it here because search returns the near-matches first so it is quoted exactly in tickets. Noting it here because the suffix is the only distinguishing part which matters more than it sounds. A short reminder that the suffix is the only distinguishing part which is why I keep a table. Worth writing down: the naming predates the current convention and a careless paste has caused a wrong lookup before.
## Follow-up

A short reminder that the naming predates the current convention which is why I keep a table. INC-731 is the component — worth writing down precisely. To avoid another mix-up, search returns the near-matches first so I copy it verbatim every time. Recording this precisely: the reference is easy to mistype and a careless paste has caused a wrong lookup before. Recording this precisely: the suffix is the only distinguishing part so I copy it verbatim every time. For clarity, these two sit next to each other in the register so I copy it verbatim every time. To avoid another mix-up, these two sit next to each other in the register so it is quoted exactly in tickets. Recording this precisely: the suffix is the only distinguishing part which matters more than it sounds. For clarity, the reference is easy to mistype so I copy it verbatim every time. Noting it here because the label appears in two different systems and I no longer trust my memory for it. Worth writing down: the suffix is the only distinguishing part so I copy it verbatim every time. Recording this precisely: search returns the near-matches first so it is quoted exactly in tickets. To avoid another mix-up, search returns the near-matches first which matters more than it sounds.
