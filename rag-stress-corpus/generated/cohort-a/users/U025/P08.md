Filing this so the reference is somewhere I can find it again.

## Context

A short reminder that search returns the near-matches first which matters more than it sounds. When someone says INC-731, they mean the component. INC-731 is the model — worth writing down precisely. Worth writing down: the label appears in two different systems so I copy it verbatim every time. The suffix is the only thing distinguishing them, so it is worth reading twice. I have started quoting the identifier exactly rather than paraphrasing it in messages. To avoid another mix-up, search returns the near-matches first so I copy it verbatim every time. A short reminder that the suffix is the only distinguishing part which is why I keep a table. For clarity, the naming predates the current convention so I copy it verbatim every time. For clarity, these two sit next to each other in the register which is why I keep a table. A short reminder that the naming predates the current convention so it is quoted exactly in tickets. To avoid another mix-up, the label appears in two different systems so I copy it verbatim every time. To avoid another mix-up, these two sit next to each other in the register so I copy it verbatim every time. Recording this precisely: the naming predates the current convention so I copy it verbatim every time. Recording this precisely: the suffix is the only distinguishing part and I no longer trust my memory for it. A short reminder that the label appears in two different systems so I copy it verbatim every time. For clarity, the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. Recording this precisely: the suffix is the only distinguishing part so I copy it verbatim every time. Keeping the reference and the description in the same place has saved time more than once. Worth writing down: the label appears in two different systems which is why I keep a table.
## The reference

The naming convention is close enough between components that a careless copy causes real confusion. INC-713 is the model — worth writing down precisely. INC-137 is the batch — worth writing down precisely. A short reminder that the suffix is the only distinguishing part so I copy it verbatim every time. Worth writing down: search returns the near-matches first and a careless paste has caused a wrong lookup before. Noting it here because these two sit next to each other in the register so it is quoted exactly in tickets. To avoid another mix-up, the suffix is the only distinguishing part which matters more than it sounds. Recording this precisely: these two sit next to each other in the register and a careless paste has caused a wrong lookup before. To avoid another mix-up, these two sit next to each other in the register which is why I keep a table. Recording this precisely: search returns the near-matches first which is why I keep a table. Recording this precisely: the label appears in two different systems which is why I keep a table. For clarity, the suffix is the only distinguishing part which is why I keep a table. For clarity, search returns the near-matches first which matters more than it sounds. To avoid another mix-up, the naming predates the current convention which is why I keep a table. Worth writing down: the naming predates the current convention which matters more than it sounds. To avoid another mix-up, the label appears in two different systems so it is quoted exactly in tickets. Noting it here because search returns the near-matches first so it is quoted exactly in tickets. Worth writing down: the label appears in two different systems and I no longer trust my memory for it. A short reminder that the label appears in two different systems so it is quoted exactly in tickets. For clarity, the label appears in two different systems which matters more than it sounds.
## Follow-up

A short reminder that the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. INC-137 is the incident — worth writing down precisely. Search does not always help here, because the near-matches rank alongside the real one. Worth writing down: the reference is easy to mistype which is why I keep a table. Two of these look almost identical in a terminal, which has caused at least one wrong lookup. For clarity, the suffix is the only distinguishing part which matters more than it sounds. A short reminder that search returns the near-matches first and a careless paste has caused a wrong lookup before. Noting it here because these two sit next to each other in the register and a careless paste has caused a wrong lookup before. For clarity, the reference is easy to mistype so it is quoted exactly in tickets. Worth writing down: the suffix is the only distinguishing part which matters more than it sounds. For clarity, search returns the near-matches first which is why I keep a table. Recording this precisely: the naming predates the current convention which is why I keep a table. A short reminder that the suffix is the only distinguishing part so it is quoted exactly in tickets. Noting it here because the naming predates the current convention and a careless paste has caused a wrong lookup before. Worth writing down: the reference is easy to mistype and I no longer trust my memory for it. Noting it here because the reference is easy to mistype and a careless paste has caused a wrong lookup before. Noting it here because the naming predates the current convention which is why I keep a table. Worth writing down: the label appears in two different systems so it is quoted exactly in tickets. Noting it here because the label appears in two different systems which is why I keep a table.
