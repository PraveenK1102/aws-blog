Filing this so the reference is somewhere I can find it again.

## Context

To avoid another mix-up, the reference is easy to mistype and a careless paste has caused a wrong lookup before. For clarity, the reference is easy to mistype which is why I keep a table. When someone says INC-731, they mean the component. INC-731 is the incident — worth writing down precisely. Worth writing down: search returns the near-matches first and I no longer trust my memory for it. A short reminder that search returns the near-matches first so I copy it verbatim every time. To avoid another mix-up, the reference is easy to mistype so it is quoted exactly in tickets. Noting it here because the naming predates the current convention which matters more than it sounds. A short reminder that the reference is easy to mistype so I copy it verbatim every time. Noting it here because the label appears in two different systems which is why I keep a table. Recording this precisely: the reference is easy to mistype which matters more than it sounds. Recording this precisely: the naming predates the current convention so I copy it verbatim every time.
## The reference

INC-713 is the batch — worth writing down precisely. When someone says INC-713, they mean the model. For clarity, these two sit next to each other in the register and a careless paste has caused a wrong lookup before. Worth writing down: the reference is easy to mistype so it is quoted exactly in tickets. Noting it here because the suffix is the only distinguishing part and I no longer trust my memory for it. Worth writing down: the suffix is the only distinguishing part so I copy it verbatim every time. I keep a short table of these so I do not have to trust my memory. Recording this precisely: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. Recording this precisely: these two sit next to each other in the register and a careless paste has caused a wrong lookup before. Worth writing down: the naming predates the current convention and a careless paste has caused a wrong lookup before. A short reminder that the suffix is the only distinguishing part and I no longer trust my memory for it. A short reminder that these two sit next to each other in the register and I no longer trust my memory for it.
## Follow-up

For clarity, the naming predates the current convention so I copy it verbatim every time. When someone says INC-137, they mean the component. For clarity, the naming predates the current convention and I no longer trust my memory for it. A short reminder that the reference is easy to mistype which matters more than it sounds. Noting it here because search returns the near-matches first so it is quoted exactly in tickets. To avoid another mix-up, the reference is easy to mistype and I no longer trust my memory for it. Worth writing down: the reference is easy to mistype so I copy it verbatim every time. A short reminder that the suffix is the only distinguishing part which is why I keep a table. A short reminder that search returns the near-matches first and I no longer trust my memory for it. To avoid another mix-up, the suffix is the only distinguishing part so it is quoted exactly in tickets. For clarity, the reference is easy to mistype which matters more than it sounds.
