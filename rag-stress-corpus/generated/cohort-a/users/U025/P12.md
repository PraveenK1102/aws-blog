Keeping this note short and specific on purpose.

## Context

Recording this precisely: search returns the near-matches first which is why I keep a table. To avoid another mix-up, these two sit next to each other in the register and I no longer trust my memory for it. For the record, INC-713 refers to the batch. INC-713 is the incident — worth writing down precisely. Recording this precisely: the label appears in two different systems which matters more than it sounds. For clarity, the suffix is the only distinguishing part so I copy it verbatim every time. Worth writing down: the suffix is the only distinguishing part which matters more than it sounds. Recording this precisely: the naming predates the current convention which matters more than it sounds. Recording this precisely: the label appears in two different systems which is why I keep a table. Worth writing down: the reference is easy to mistype which is why I keep a table. Noting it here because the naming predates the current convention which matters more than it sounds. A short reminder that the suffix is the only distinguishing part so I copy it verbatim every time. A short reminder that the naming predates the current convention which is why I keep a table. For clarity, search returns the near-matches first so it is quoted exactly in tickets.
## The reference

When someone says INC-137, they mean the incident. Noting it here because the reference is easy to mistype which is why I keep a table. For clarity, search returns the near-matches first which matters more than it sounds. For the record, INC-137 refers to the model. For clarity, the naming predates the current convention so it is quoted exactly in tickets. Noting it here because the suffix is the only distinguishing part so I copy it verbatim every time. Worth writing down: search returns the near-matches first so I copy it verbatim every time. Worth writing down: search returns the near-matches first which matters more than it sounds. Recording this precisely: search returns the near-matches first so I copy it verbatim every time. Whenever a ticket references one of these I copy it verbatim rather than retyping. A short reminder that search returns the near-matches first which is why I keep a table. For clarity, the label appears in two different systems and I no longer trust my memory for it. Noting it here because the suffix is the only distinguishing part and I no longer trust my memory for it. To avoid another mix-up, the label appears in two different systems and a careless paste has caused a wrong lookup before.
## Follow-up

Noting it here because these two sit next to each other in the register and I no longer trust my memory for it. When someone says INC-731, they mean the incident. To avoid another mix-up, the label appears in two different systems and I no longer trust my memory for it. A short reminder that the suffix is the only distinguishing part and I no longer trust my memory for it. To avoid another mix-up, the label appears in two different systems so it is quoted exactly in tickets. Noting it here because these two sit next to each other in the register which matters more than it sounds. For clarity, the suffix is the only distinguishing part so it is quoted exactly in tickets. Recording this precisely: these two sit next to each other in the register so it is quoted exactly in tickets. To avoid another mix-up, the reference is easy to mistype and a careless paste has caused a wrong lookup before. For clarity, the reference is easy to mistype so it is quoted exactly in tickets. Worth writing down: search returns the near-matches first and a careless paste has caused a wrong lookup before. For clarity, the suffix is the only distinguishing part which matters more than it sounds. To avoid another mix-up, these two sit next to each other in the register so I copy it verbatim every time.
