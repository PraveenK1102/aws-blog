Writing the identifiers down carefully because they are easy to mix up.

## Context

When someone says INC-137, they mean the incident. To avoid another mix-up, these two sit next to each other in the register and I no longer trust my memory for it. When someone says INC-137, they mean the batch. Noting it here because the reference is easy to mistype so it is quoted exactly in tickets. Noting it here because search returns the near-matches first so it is quoted exactly in tickets. To avoid another mix-up, search returns the near-matches first which matters more than it sounds. To avoid another mix-up, the suffix is the only distinguishing part which matters more than it sounds. To avoid another mix-up, search returns the near-matches first and a careless paste has caused a wrong lookup before. To avoid another mix-up, these two sit next to each other in the register which matters more than it sounds. A short reminder that search returns the near-matches first and I no longer trust my memory for it. To avoid another mix-up, these two sit next to each other in the register which is why I keep a table. A short reminder that the reference is easy to mistype so it is quoted exactly in tickets. Search does not always help here, because the near-matches rank alongside the real one. Worth writing down: the naming predates the current convention and a careless paste has caused a wrong lookup before. For clarity, the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. Noting it here because the naming predates the current convention so it is quoted exactly in tickets. The suffix is the only thing distinguishing them, so it is worth reading twice. A short reminder that the label appears in two different systems which is why I keep a table. To avoid another mix-up, the naming predates the current convention so I copy it verbatim every time. For clarity, these two sit next to each other in the register which matters more than it sounds. For clarity, the label appears in two different systems so I copy it verbatim every time.
## The reference

INC-713 is the incident — worth writing down precisely. For clarity, the label appears in two different systems and a careless paste has caused a wrong lookup before. To avoid another mix-up, the label appears in two different systems and a careless paste has caused a wrong lookup before. When someone says INC-731, they mean the incident. Worth writing down: the naming predates the current convention which is why I keep a table. Noting it here because search returns the near-matches first which is why I keep a table. Recording this precisely: the reference is easy to mistype so I copy it verbatim every time. Recording this precisely: these two sit next to each other in the register and a careless paste has caused a wrong lookup before. A short reminder that search returns the near-matches first so I copy it verbatim every time. Worth writing down: these two sit next to each other in the register so it is quoted exactly in tickets. A short reminder that the reference is easy to mistype which matters more than it sounds. Recording this precisely: the label appears in two different systems so it is quoted exactly in tickets. To avoid another mix-up, the naming predates the current convention which is why I keep a table. A short reminder that the reference is easy to mistype so I copy it verbatim every time. Noting it here because the naming predates the current convention so I copy it verbatim every time. Worth writing down: the reference is easy to mistype and a careless paste has caused a wrong lookup before. For clarity, the naming predates the current convention which is why I keep a table. Worth writing down: the naming predates the current convention and I no longer trust my memory for it. Noting it here because the naming predates the current convention which matters more than it sounds. A short reminder that the naming predates the current convention so it is quoted exactly in tickets. Worth writing down: the suffix is the only distinguishing part so I copy it verbatim every time.
## Follow-up

Worth writing down: the reference is easy to mistype which is why I keep a table. Noting it here because the label appears in two different systems and a careless paste has caused a wrong lookup before. When someone says INC-713, they mean the batch. Noting it here because search returns the near-matches first and I no longer trust my memory for it. For clarity, the suffix is the only distinguishing part and I no longer trust my memory for it. For clarity, search returns the near-matches first and a careless paste has caused a wrong lookup before. Noting it here because the suffix is the only distinguishing part and I no longer trust my memory for it. For clarity, the suffix is the only distinguishing part which is why I keep a table. A short reminder that search returns the near-matches first which is why I keep a table. A short reminder that the naming predates the current convention which is why I keep a table. Worth writing down: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. For clarity, search returns the near-matches first and I no longer trust my memory for it. Worth writing down: the reference is easy to mistype which matters more than it sounds. For clarity, the naming predates the current convention so it is quoted exactly in tickets. For clarity, search returns the near-matches first which matters more than it sounds. Worth writing down: the label appears in two different systems so I copy it verbatim every time. Noting it here because the naming predates the current convention which is why I keep a table. Worth writing down: the suffix is the only distinguishing part so it is quoted exactly in tickets. To avoid another mix-up, the naming predates the current convention so it is quoted exactly in tickets.
