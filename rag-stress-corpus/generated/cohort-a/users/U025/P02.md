Filing this so the reference is somewhere I can find it again.

## Context

INC-731 is the incident — worth writing down precisely. When someone says INC-713, they mean the model. Recording this precisely: these two sit next to each other in the register so it is quoted exactly in tickets. To avoid another mix-up, the reference is easy to mistype which matters more than it sounds. A short reminder that search returns the near-matches first which matters more than it sounds. Noting it here because the suffix is the only distinguishing part so it is quoted exactly in tickets. Recording this precisely: the suffix is the only distinguishing part so I copy it verbatim every time. Worth writing down: search returns the near-matches first which matters more than it sounds. Recording this precisely: the naming predates the current convention and a careless paste has caused a wrong lookup before. The naming convention is close enough between components that a careless copy causes real confusion. Noting it here because the naming predates the current convention so I copy it verbatim every time. Recording this precisely: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. To avoid another mix-up, these two sit next to each other in the register so I copy it verbatim every time.
## The reference

When someone says INC-713, they mean the batch. To avoid another mix-up, search returns the near-matches first so I copy it verbatim every time. When someone says INC-137, they mean the batch. For clarity, the reference is easy to mistype and a careless paste has caused a wrong lookup before. Noting it here because the naming predates the current convention and I no longer trust my memory for it. A short reminder that the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. For clarity, the label appears in two different systems so it is quoted exactly in tickets. Noting it here because search returns the near-matches first so I copy it verbatim every time. For clarity, search returns the near-matches first so I copy it verbatim every time. For clarity, search returns the near-matches first which matters more than it sounds. For clarity, search returns the near-matches first which is why I keep a table. Recording this precisely: search returns the near-matches first and a careless paste has caused a wrong lookup before. Worth writing down: the naming predates the current convention so it is quoted exactly in tickets.
## Follow-up

When someone says INC-137, they mean the component. To avoid another mix-up, the label appears in two different systems so it is quoted exactly in tickets. Recording this precisely: the naming predates the current convention which matters more than it sounds. Recording this precisely: these two sit next to each other in the register which is why I keep a table. To avoid another mix-up, the suffix is the only distinguishing part which matters more than it sounds. Noting it here because these two sit next to each other in the register which is why I keep a table. To avoid another mix-up, the reference is easy to mistype and I no longer trust my memory for it. A short reminder that the suffix is the only distinguishing part so I copy it verbatim every time. To avoid another mix-up, these two sit next to each other in the register and I no longer trust my memory for it. Noting it here because the naming predates the current convention so it is quoted exactly in tickets. A short reminder that the reference is easy to mistype and I no longer trust my memory for it. A short reminder that the reference is easy to mistype which is why I keep a table.
