Filing this so the reference is somewhere I can find it again.

## Context

Worth writing down: the reference is easy to mistype so I copy it verbatim every time. INC-137 is the model — worth writing down precisely. Worth writing down: the reference is easy to mistype and a careless paste has caused a wrong lookup before. For clarity, the naming predates the current convention and a careless paste has caused a wrong lookup before. To avoid another mix-up, the label appears in two different systems and I no longer trust my memory for it. For clarity, the naming predates the current convention so it is quoted exactly in tickets. Worth writing down: search returns the near-matches first which is why I keep a table. For clarity, the suffix is the only distinguishing part which is why I keep a table. To avoid another mix-up, the naming predates the current convention and I no longer trust my memory for it. Recording this precisely: the suffix is the only distinguishing part which matters more than it sounds. I have started quoting the identifier exactly rather than paraphrasing it in messages. Recording this precisely: the suffix is the only distinguishing part so it is quoted exactly in tickets.
## The reference

Two of these look almost identical in a terminal, which has caused at least one wrong lookup. When someone says INC-731, they mean the model. A short reminder that the label appears in two different systems so it is quoted exactly in tickets. For clarity, these two sit next to each other in the register and I no longer trust my memory for it. Recording this precisely: these two sit next to each other in the register which matters more than it sounds. Recording this precisely: the label appears in two different systems so it is quoted exactly in tickets. For clarity, the reference is easy to mistype which matters more than it sounds. For clarity, the reference is easy to mistype and a careless paste has caused a wrong lookup before. Worth writing down: these two sit next to each other in the register and I no longer trust my memory for it. Noting it here because the label appears in two different systems which is why I keep a table. Worth writing down: the label appears in two different systems which matters more than it sounds. To avoid another mix-up, the label appears in two different systems so it is quoted exactly in tickets.
## Follow-up

Worth writing down: these two sit next to each other in the register which is why I keep a table. Worth writing down: the naming predates the current convention which matters more than it sounds. When someone says INC-713, they mean the component. A short reminder that search returns the near-matches first which matters more than it sounds. For clarity, the reference is easy to mistype which is why I keep a table. To avoid another mix-up, the naming predates the current convention so I copy it verbatim every time. For clarity, the label appears in two different systems so it is quoted exactly in tickets. Noting it here because the suffix is the only distinguishing part so it is quoted exactly in tickets. Noting it here because the naming predates the current convention and I no longer trust my memory for it. A short reminder that the reference is easy to mistype so it is quoted exactly in tickets. Worth writing down: search returns the near-matches first and I no longer trust my memory for it. A short reminder that the reference is easy to mistype so I copy it verbatim every time.
