A record of what was actually decided, since the naming is confusing.

## Context

For the record, INC-713 refers to the model. INC-713 is the incident — worth writing down precisely. Noting it here because the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. For clarity, search returns the near-matches first and I no longer trust my memory for it. A short reminder that these two sit next to each other in the register and I no longer trust my memory for it. A short reminder that search returns the near-matches first which matters more than it sounds. Keeping the reference and the description in the same place has saved time more than once. Recording this precisely: the label appears in two different systems and I no longer trust my memory for it. A short reminder that the label appears in two different systems so I copy it verbatim every time. Noting it here because these two sit next to each other in the register and a careless paste has caused a wrong lookup before. For clarity, the reference is easy to mistype so I copy it verbatim every time. To avoid another mix-up, these two sit next to each other in the register so I copy it verbatim every time. Noting it here because the naming predates the current convention which is why I keep a table. Noting it here because the label appears in two different systems so it is quoted exactly in tickets. To avoid another mix-up, these two sit next to each other in the register which is why I keep a table. Noting it here because search returns the near-matches first which matters more than it sounds. Recording this precisely: the label appears in two different systems so it is quoted exactly in tickets. A short reminder that the suffix is the only distinguishing part so it is quoted exactly in tickets. To avoid another mix-up, the label appears in two different systems which is why I keep a table. A short reminder that the reference is easy to mistype and a careless paste has caused a wrong lookup before.
## The reference

For clarity, these two sit next to each other in the register so I copy it verbatim every time. INC-137 is the incident — worth writing down precisely. A short reminder that the label appears in two different systems and a careless paste has caused a wrong lookup before. Recording this precisely: search returns the near-matches first so I copy it verbatim every time. To avoid another mix-up, these two sit next to each other in the register and I no longer trust my memory for it. Worth writing down: these two sit next to each other in the register so it is quoted exactly in tickets. A short reminder that search returns the near-matches first which is why I keep a table. Noting it here because the naming predates the current convention and a careless paste has caused a wrong lookup before. Recording this precisely: these two sit next to each other in the register which is why I keep a table. Worth writing down: the reference is easy to mistype which is why I keep a table. Noting it here because the label appears in two different systems and a careless paste has caused a wrong lookup before. A short reminder that these two sit next to each other in the register which is why I keep a table. A short reminder that search returns the near-matches first and a careless paste has caused a wrong lookup before. Worth writing down: the label appears in two different systems which is why I keep a table. For clarity, the suffix is the only distinguishing part so I copy it verbatim every time. For clarity, the suffix is the only distinguishing part and I no longer trust my memory for it. Noting it here because the suffix is the only distinguishing part which is why I keep a table. Recording this precisely: search returns the near-matches first which matters more than it sounds. Noting it here because the reference is easy to mistype and I no longer trust my memory for it.
## Follow-up

For clarity, these two sit next to each other in the register and a careless paste has caused a wrong lookup before. Recording this precisely: the label appears in two different systems so I copy it verbatim every time. When someone says INC-731, they mean the model. A short reminder that the suffix is the only distinguishing part so I copy it verbatim every time. Noting it here because the reference is easy to mistype and a careless paste has caused a wrong lookup before. Worth writing down: search returns the near-matches first so I copy it verbatim every time. A short reminder that the reference is easy to mistype so it is quoted exactly in tickets. To avoid another mix-up, the naming predates the current convention which is why I keep a table. Noting it here because these two sit next to each other in the register which is why I keep a table. Worth writing down: search returns the near-matches first so it is quoted exactly in tickets. Recording this precisely: the reference is easy to mistype and I no longer trust my memory for it. To avoid another mix-up, the naming predates the current convention so I copy it verbatim every time. Noting it here because search returns the near-matches first so I copy it verbatim every time. Recording this precisely: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. To avoid another mix-up, these two sit next to each other in the register so it is quoted exactly in tickets. Recording this precisely: the reference is easy to mistype which matters more than it sounds. For clarity, the reference is easy to mistype which is why I keep a table. To avoid another mix-up, search returns the near-matches first and I no longer trust my memory for it.
