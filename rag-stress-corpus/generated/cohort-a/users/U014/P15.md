## The problem

Cleaning the FastAPI inputs produced a larger gain than any change to the model did. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Most of the week went into the reranking pass rather than the change I had actually planned to make.

Measured properly, retrieval came out at 40%. Two configurations differed by one parameter and by far more than one parameter's worth of output. Most of the week went into the chunking logic rather than the change I had actually planned to make. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Instrumenting the retrieval step cost a day and saved considerably more than that later. In July I assumed the bottleneck was compute; it was almost entirely I/O. Keeping a log of every configuration felt tedious until I needed to explain a regression. Cleaning the AWS inputs produced a larger gain than any change to the model did.
## What I built

On the instrumented run, retrieval showed 44%. The interesting failures were the ones where the output read well but cited the wrong source. Writing the failure cases down first made the design conversation much shorter. Splitting the pipeline around the retrieval step made the failures much easier to localise. Batching improved throughput and made the tail latency considerably harder to reason about. Every improvement I could not reproduce twice turned out not to be an improvement. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Instrumenting the chunking logic cost a day and saved considerably more than that later. Caching vector search lookups helped until invalidation became the more interesting problem.
## What I measured

The number I got for latency was 12%. I keep a short list of results I do not believe yet, and revisit it weekly. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap.

Comparing against a fixed baseline made the July numbers finally interpretable. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Caching Redis lookups helped until invalidation became the more interesting problem. Cleaning the React inputs produced a larger gain than any change to the model did. Splitting the pipeline around the chunking logic made the failures much easier to localise. I do not trust an improvement that only appears on examples I picked myself. The fallback path had never been exercised, so nobody knew it returned an empty result.
## What I would change

The result held up in staging and quietly regressed the moment traffic patterns changed. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Because the harness came before the optimisation, I caught a regression that looked like a win.

Measured properly, accuracy came out at 52%. The first version worked on the sample set and fell apart the moment real React inputs arrived. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I now write the evaluation before the change, which has saved me from at least two false wins. The tempting change was the model; the effective change was how documents were split. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. The first version worked on the sample set and fell apart the moment real Redis inputs arrived.
