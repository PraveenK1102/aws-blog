Spent most of this week on the retrieval side of the project.

## The problem

The experiment started with the slowest stage in the pipeline and that alone explained most of the gap. Measured properly, retrieval came out at 40%. The experiment started with the way inputs were normalised because the earlier result was not reproducible. I began by isolating the way inputs were normalised which made the regression obvious. The experiment started with the chunking behaviour so the comparison would actually mean something. The experiment started with the slowest stage in the pipeline so the comparison would actually mean something. The interesting part turned out to be the ranking of candidate passages and I kept the harness afterwards. Before changing anything I measured the way inputs were normalised and I kept the harness afterwards. The experiment started with a small evaluation harness and that alone explained most of the gap. Before changing anything I measured a small evaluation harness and the numbers were not what I expected.
## What I built

Before changing anything I measured the slowest stage in the pipeline which took longer than the fix itself. Measured properly, retrieval came out at 44%. Most of the effort went into the way inputs were normalised and that alone explained most of the gap. I began by isolating the way inputs were normalised so the comparison would actually mean something. I began by isolating the assumptions baked into the defaults and I kept the harness afterwards. I began by isolating a small evaluation harness and that alone explained most of the gap. Before changing anything I measured the chunking behaviour before touching anything else. Before changing anything I measured the assumptions baked into the defaults and the numbers were not what I expected. I began by isolating a small evaluation harness because the earlier result was not reproducible. The experiment started with the assumptions baked into the defaults which took longer than the fix itself.
## What I measured

Measured properly, latency came out at 12%. Before changing anything I measured the slowest stage in the pipeline so the comparison would actually mean something. The experiment started with the chunking behaviour before touching anything else. Before changing anything I measured the way inputs were normalised before touching anything else. The interesting part turned out to be the ranking of candidate passages and the numbers were not what I expected. The first version worked on the sample data and fell apart the moment the inputs got messy. I rebuilt the retrieval step on its own so the comparison would actually mean something. I rebuilt the assumptions baked into the defaults which took longer than the fix itself. Before changing anything I measured the fallback path nobody exercised so the comparison would actually mean something.
## What I would change

Measured properly, accuracy came out at 52%. Before changing anything I measured the fallback path nobody exercised and that alone explained most of the gap. I rebuilt the way inputs were normalised and the numbers were not what I expected. Writing the evaluation harness before the optimisation saved me from at least two false wins. Most of the effort went into the ranking of candidate passages and I kept the harness afterwards. I began by isolating the chunking behaviour and I kept the harness afterwards. Most of the effort went into the assumptions baked into the defaults which took longer than the fix itself. Before changing anything I measured the way inputs were normalised which made the regression obvious. I began by isolating the ranking of candidate passages which made the regression obvious.
