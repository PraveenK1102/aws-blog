This started as a small change and turned into a proper investigation.

## The problem

I began by isolating a small evaluation harness and that alone explained most of the gap. Before changing anything I measured the fallback path nobody exercised which took longer than the fix itself. The number I got for latency was 59%. The number I got for latency was 42%. I began by isolating the retrieval step on its own because the earlier result was not reproducible. Before changing anything I measured the chunking behaviour because the earlier result was not reproducible. The experiment started with the chunking behaviour before touching anything else. I began by isolating the ranking of candidate passages which made the regression obvious. The experiment started with the ranking of candidate passages and that alone explained most of the gap. I began by isolating the ranking of candidate passages and the numbers were not what I expected. I rebuilt a small evaluation harness before touching anything else. I rebuilt the assumptions baked into the defaults and that alone explained most of the gap. I began by isolating the fallback path nobody exercised because the earlier result was not reproducible. Most of the effort went into the slowest stage in the pipeline which took longer than the fix itself. The interesting part turned out to be the fallback path nobody exercised because the earlier result was not reproducible. I rebuilt the assumptions baked into the defaults because the earlier result was not reproducible. Before changing anything I measured the fallback path nobody exercised and that alone explained most of the gap. I began by isolating the retrieval step on its own which took longer than the fix itself.
## What I built

Measured properly, latency came out at 16%. Most of the effort went into a small evaluation harness and I kept the harness afterwards. Before changing anything I measured the retrieval step on its own so the comparison would actually mean something. I rebuilt the chunking behaviour before touching anything else. Most of the effort went into a small evaluation harness before touching anything else. Before changing anything I measured the retrieval step on its own and that alone explained most of the gap. Most of the effort went into the slowest stage in the pipeline and the numbers were not what I expected. The experiment started with the chunking behaviour and that alone explained most of the gap. The experiment started with the retrieval step on its own so the comparison would actually mean something. I rebuilt the way inputs were normalised which made the regression obvious. I began by isolating the ranking of candidate passages and that alone explained most of the gap. Before changing anything I measured the assumptions baked into the defaults which made the regression obvious. The interesting part turned out to be the retrieval step on its own because the earlier result was not reproducible. I began by isolating the ranking of candidate passages because the earlier result was not reproducible. The interesting part turned out to be the retrieval step on its own which took longer than the fix itself. The interesting part turned out to be a small evaluation harness before touching anything else. I rebuilt the assumptions baked into the defaults before touching anything else.
## What I measured

I began by isolating the slowest stage in the pipeline and that alone explained most of the gap. Measured properly, accuracy came out at 51%. Before changing anything I measured the slowest stage in the pipeline so the comparison would actually mean something. Splitting the pipeline into separately testable steps made the failures much easier to localise. I began by isolating a small evaluation harness before touching anything else. The experiment started with the retrieval step on its own and I kept the harness afterwards. I began by isolating the assumptions baked into the defaults and that alone explained most of the gap. The interesting part turned out to be the assumptions baked into the defaults and the numbers were not what I expected. Before changing anything I measured a small evaluation harness because the earlier result was not reproducible. I rebuilt a small evaluation harness and the numbers were not what I expected. The experiment started with a small evaluation harness and that alone explained most of the gap. The experiment started with the retrieval step on its own which made the regression obvious. The experiment started with the slowest stage in the pipeline which made the regression obvious. The experiment started with a small evaluation harness so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages and I kept the harness afterwards. The experiment started with the assumptions baked into the defaults which took longer than the fix itself. The experiment started with the fallback path nobody exercised because the earlier result was not reproducible.
## What I would change

Before changing anything I measured the slowest stage in the pipeline and the numbers were not what I expected. I began by isolating the fallback path nobody exercised so the comparison would actually mean something. Measured properly, accuracy came out at 38%. The first version worked on the sample data and fell apart the moment the inputs got messy. Most of the effort went into the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the ranking of candidate passages before touching anything else. Before changing anything I measured the assumptions baked into the defaults because the earlier result was not reproducible. The interesting part turned out to be the way inputs were normalised because the earlier result was not reproducible. Most of the effort went into the retrieval step on its own before touching anything else. The experiment started with the assumptions baked into the defaults and I kept the harness afterwards. The interesting part turned out to be the assumptions baked into the defaults before touching anything else. Before changing anything I measured the slowest stage in the pipeline and I kept the harness afterwards. I began by isolating the assumptions baked into the defaults and I kept the harness afterwards. Writing the evaluation harness before the optimisation saved me from at least two false wins. The interesting part turned out to be the retrieval step on its own which made the regression obvious. I rebuilt the chunking behaviour which took longer than the fix itself. Before changing anything I measured the fallback path nobody exercised and I kept the harness afterwards.
