## The problem

Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Every improvement I could not reproduce twice turned out not to be an improvement. My recorded figure for latency is 42%. Running it end to end put latency at 59%. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Most of the week went into the fallback branch rather than the change I had actually planned to make. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Instrumenting the ingestion path cost a day and saved considerably more than that later. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Instrumenting the scoring function cost a day and saved considerably more than that later. Caching AWS lookups helped until invalidation became the more interesting problem. Cleaning the Redis inputs produced a larger gain than any change to the model did. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Tuning Redis helped marginally; fixing how documents were split helped substantially. Tuning AWS helped marginally; fixing how documents were split helped substantially. Keeping a log of every configuration felt tedious until I needed to explain a regression. In May I assumed the bottleneck was compute; it was almost entirely I/O. Caching AWS lookups helped until invalidation became the more interesting problem.
## What I built

Instrumenting the reranking pass cost a day and saved considerably more than that later. The number I got for latency was 16%. Two configurations differed by one parameter and by far more than one parameter's worth of output. Writing the failure cases down first made the design conversation much shorter. Caching vector search lookups helped until invalidation became the more interesting problem. The result held up in staging and quietly regressed the moment traffic patterns changed. Most of the week went into the embedding cache rather than the change I had actually planned to make. Comparing against a fixed baseline made the May numbers finally interpretable. I keep a short list of results I do not believe yet, and revisit it weekly. The fallback path had never been exercised, so nobody knew it returned an empty result. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Most of the week went into the scoring function rather than the change I had actually planned to make. Cleaning the vector search inputs produced a larger gain than any change to the model did. Cleaning the React inputs produced a larger gain than any change to the model did. Cleaning the AWS inputs produced a larger gain than any change to the model did. The result held up in staging and quietly regressed the moment traffic patterns changed. Two configurations differed by one parameter and by far more than one parameter's worth of output. I do not trust an improvement that only appears on examples I picked myself.
## What I measured

Measured properly, accuracy came out at 51%. The tempting change was the model; the effective change was how documents were split. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I now write the evaluation before the change, which has saved me from at least two false wins. The interesting failures were the ones where the output read well but cited the wrong source. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Because the harness came before the optimisation, I caught a regression that looked like a win. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning vector search helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Instrumenting the embedding cache cost a day and saved considerably more than that later. Instrumenting the retrieval step cost a day and saved considerably more than that later. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. I now write the evaluation before the change, which has saved me from at least two false wins. Because the harness came before the optimisation, I caught a regression that looked like a win. Comparing against a fixed baseline made the May numbers finally interpretable.
## What I would change

Tuning React helped marginally; fixing how documents were split helped substantially. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. Batching improved throughput and made the tail latency considerably harder to reason about.

Accuracy landed at 38% once I measured it end to end. I do not trust an improvement that only appears on examples I picked myself. In May I assumed the bottleneck was compute; it was almost entirely I/O. Splitting the pipeline around the reranking pass made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Splitting the pipeline around the embedding cache made the failures much easier to localise. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Splitting the pipeline around the ingestion path made the failures much easier to localise. Caching Redis lookups helped until invalidation became the more interesting problem. Caching React lookups helped until invalidation became the more interesting problem. Caching FastAPI lookups helped until invalidation became the more interesting problem. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. Writing the failure cases down first made the design conversation much shorter. I spent an afternoon convinced the model was wrong before finding a bug in the loader.
