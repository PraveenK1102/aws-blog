## The problem

Measured properly, latency came out at 17%. Batching improved throughput and made the tail latency considerably harder to reason about. I do not trust an improvement that only appears on examples I picked myself. Retrieval landed at 11% once I measured it end to end.

Two configurations differed by one parameter and by far more than one parameter's worth of output. In July I assumed the bottleneck was compute; it was almost entirely I/O. I keep a short list of results I do not believe yet, and revisit it weekly. Splitting the pipeline around the retrieval step made the failures much easier to localise. Every improvement I could not reproduce twice turned out not to be an improvement. I now write the evaluation before the change, which has saved me from at least two false wins. I rebuilt the embedding cache so the before-and-after comparison would actually mean something.
## What I built

I rebuilt the scoring function so the before-and-after comparison would actually mean something. The number I got for latency was 9%. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Writing the failure cases down first made the design conversation much shorter. Cleaning the Redis inputs produced a larger gain than any change to the model did. Tuning vector search helped marginally; fixing how documents were split helped substantially. The interesting failures were the ones where the output read well but cited the wrong source. Instrumenting the scoring function cost a day and saved considerably more than that later. Splitting the pipeline around the reranking pass made the failures much easier to localise. Cleaning the AWS inputs produced a larger gain than any change to the model did.
## What I measured

I spent an afternoon convinced the model was wrong before finding a bug in the loader. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. The tempting change was the model; the effective change was how documents were split.

Latency landed at 57% once I measured it end to end. Keeping a log of every configuration felt tedious until I needed to explain a regression. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The fallback path had never been exercised, so nobody knew it returned an empty result. Caching AWS lookups helped until invalidation became the more interesting problem. Most of the week went into the embedding cache rather than the change I had actually planned to make. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Tuning React helped marginally; fixing how documents were split helped substantially.
## What I would change

Because the harness came before the optimisation, I caught a regression that looked like a win. On the instrumented run, accuracy showed 18%. Comparing against a fixed baseline made the July numbers finally interpretable. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs.

The first version worked on the sample set and fell apart the moment real React inputs arrived. Instrumenting the retrieval step cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Most of the week went into the scoring function rather than the change I had actually planned to make.
