This started as a small change and turned into a proper investigation.

## The problem

The interesting part turned out to be the ranking of candidate passages which took longer than the fix itself. Measured properly, retrieval came out at 11%. I rebuilt the way inputs were normalised which made the regression obvious. Latency landed at 17% once I measured it end to end. I rebuilt the fallback path nobody exercised so the comparison would actually mean something. I began by isolating the fallback path nobody exercised which made the regression obvious. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. Most of the effort went into the slowest stage in the pipeline and the numbers were not what I expected. I rebuilt the assumptions baked into the defaults so the comparison would actually mean something. The experiment started with the chunking behaviour before touching anything else. Most of the effort went into the fallback path nobody exercised which took longer than the fix itself.
## What I built

Measured properly, latency came out at 9%. Before changing anything I measured the chunking behaviour which took longer than the fix itself. The interesting part turned out to be the ranking of candidate passages so the comparison would actually mean something. I began by isolating the way inputs were normalised before touching anything else. I rebuilt a small evaluation harness before touching anything else. The experiment started with the fallback path nobody exercised which made the regression obvious. Most of the effort went into a small evaluation harness which made the regression obvious. The interesting part turned out to be a small evaluation harness which took longer than the fix itself. The experiment started with the assumptions baked into the defaults and the numbers were not what I expected. Most of the effort went into a small evaluation harness because the earlier result was not reproducible.
## What I measured

Measured properly, latency came out at 57%. The experiment started with the fallback path nobody exercised and the numbers were not what I expected. Most of the effort went into a small evaluation harness so the comparison would actually mean something. Most of the effort went into the fallback path nobody exercised before touching anything else. The experiment started with the way inputs were normalised because the earlier result was not reproducible. I rebuilt the chunking behaviour so the comparison would actually mean something. Before changing anything I measured the assumptions baked into the defaults and the numbers were not what I expected. The experiment started with the ranking of candidate passages which made the regression obvious. I rebuilt the fallback path nobody exercised and that alone explained most of the gap. Most of the effort went into the retrieval step on its own and that alone explained most of the gap.
## What I would change

The experiment started with a small evaluation harness which took longer than the fix itself. Most of the effort went into the fallback path nobody exercised so the comparison would actually mean something. Measured properly, accuracy came out at 18%. I began by isolating the way inputs were normalised so the comparison would actually mean something. Before changing anything I measured a small evaluation harness and the numbers were not what I expected. I learned to distrust any improvement that only shows up on the examples I chose myself. Before changing anything I measured the chunking behaviour and the numbers were not what I expected. I began by isolating the slowest stage in the pipeline which took longer than the fix itself. The experiment started with the retrieval step on its own which made the regression obvious.
