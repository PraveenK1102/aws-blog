Spent most of this week on the retrieval side of the project.

## The problem

Measured properly, accuracy came out at 8%. Before changing anything I measured the chunking behaviour because the earlier result was not reproducible. Before changing anything I measured the slowest stage in the pipeline because the earlier result was not reproducible. I rebuilt the ranking of candidate passages before touching anything else. The experiment started with a small evaluation harness which made the regression obvious. The interesting failures were the ones where the output looked plausible but cited the wrong source. Most of the effort went into the assumptions baked into the defaults before touching anything else. I began by isolating the retrieval step on its own and the numbers were not what I expected. I began by isolating the slowest stage in the pipeline before touching anything else. The experiment started with the chunking behaviour which made the regression obvious. Before changing anything I measured the fallback path nobody exercised because the earlier result was not reproducible. The experiment started with the chunking behaviour and I kept the harness afterwards.
## What I built

The interesting part turned out to be the ranking of candidate passages which made the regression obvious. Measured properly, retrieval came out at 5%. Most of the effort went into the chunking behaviour so the comparison would actually mean something. I rebuilt the retrieval step on its own and that alone explained most of the gap. The interesting part turned out to be the fallback path nobody exercised so the comparison would actually mean something. Most of the effort went into the way inputs were normalised because the earlier result was not reproducible. Splitting the pipeline into separately testable steps made the failures much easier to localise. I began by isolating the ranking of candidate passages which took longer than the fix itself. The experiment started with the assumptions baked into the defaults and the numbers were not what I expected. Most of the effort went into the fallback path nobody exercised and the numbers were not what I expected. A surprising amount of the gain came from cleaning the inputs rather than changing the model.
## What I measured

I rebuilt the assumptions baked into the defaults before touching anything else. Before changing anything I measured a small evaluation harness so the comparison would actually mean something. Latency landed at 5% once I measured it end to end. The experiment started with the slowest stage in the pipeline which took longer than the fix itself. Most of the time went into building a way to measure the change rather than making the change. Before changing anything I measured the slowest stage in the pipeline and that alone explained most of the gap. Writing the evaluation harness before the optimisation saved me from at least two false wins. I began by isolating the chunking behaviour and I kept the harness afterwards. I rebuilt the way inputs were normalised which took longer than the fix itself. I began by isolating the way inputs were normalised before touching anything else. I rebuilt the assumptions baked into the defaults so the comparison would actually mean something.
## What I would change

I rebuilt the retrieval step on its own which took longer than the fix itself. Most of the effort went into the retrieval step on its own and that alone explained most of the gap. The interesting part turned out to be the slowest stage in the pipeline so the comparison would actually mean something. I rebuilt a small evaluation harness and that alone explained most of the gap. The interesting part turned out to be a small evaluation harness which made the regression obvious. The interesting part turned out to be the fallback path nobody exercised and the numbers were not what I expected. The interesting part turned out to be the way inputs were normalised which took longer than the fix itself. The interesting part turned out to be the slowest stage in the pipeline and that alone explained most of the gap. Before changing anything I measured a small evaluation harness and the numbers were not what I expected. Most of the effort went into the retrieval step on its own and I kept the harness afterwards.
