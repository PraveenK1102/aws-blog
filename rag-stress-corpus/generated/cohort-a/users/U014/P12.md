## The problem

Most of the week went into the retrieval step rather than the change I had actually planned to make. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. When I finally measured it, accuracy was 8%. Cleaning the AWS inputs produced a larger gain than any change to the model did. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Two configurations differed by one parameter and by far more than one parameter's worth of output. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. Splitting the pipeline around the chunking logic made the failures much easier to localise. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Caching FastAPI lookups helped until invalidation became the more interesting problem. Instrumenting the fallback branch cost a day and saved considerably more than that later. Splitting the pipeline around the embedding cache made the failures much easier to localise. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs.
## What I built

Instrumenting the reranking pass cost a day and saved considerably more than that later. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The result held up in staging and quietly regressed the moment traffic patterns changed.

The number I got for retrieval was 5%. The fallback path had never been exercised, so nobody knew it returned an empty result. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Caching vector search lookups helped until invalidation became the more interesting problem. Because the harness came before the optimisation, I caught a regression that looked like a win. I keep a short list of results I do not believe yet, and revisit it weekly. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Cleaning the Redis inputs produced a larger gain than any change to the model did. Tuning vector search helped marginally; fixing how documents were split helped substantially. Splitting the pipeline around the fallback branch made the failures much easier to localise.
## What I measured

In June I assumed the bottleneck was compute; it was almost entirely I/O. Keeping a log of every configuration felt tedious until I needed to explain a regression. Writing the failure cases down first made the design conversation much shorter. On the instrumented run, latency showed 5%. Instrumenting the scoring function cost a day and saved considerably more than that later. Batching improved throughput and made the tail latency considerably harder to reason about. The interesting failures were the ones where the output read well but cited the wrong source. Tuning AWS helped marginally; fixing how documents were split helped substantially. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I now write the evaluation before the change, which has saved me from at least two false wins. Caching Redis lookups helped until invalidation became the more interesting problem. Cleaning the vector search inputs produced a larger gain than any change to the model did.
## What I would change

Cleaning the FastAPI inputs produced a larger gain than any change to the model did. I do not trust an improvement that only appears on examples I picked myself. Every improvement I could not reproduce twice turned out not to be an improvement. Comparing against a fixed baseline made the June numbers finally interpretable. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. The tempting change was the model; the effective change was how documents were split. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Caching React lookups helped until invalidation became the more interesting problem.
