Spent most of this week on the retrieval side of the project.

## The problem

Most of the effort went into the retrieval step on its own which took longer than the fix itself. Measured properly, retrieval came out at 25%. Latency landed at 5% once I measured it end to end. I rebuilt the retrieval step on its own and the numbers were not what I expected. I began by isolating the chunking behaviour and I kept the harness afterwards. Before changing anything I measured the chunking behaviour before touching anything else. Before changing anything I measured the assumptions baked into the defaults which made the regression obvious. Before changing anything I measured the assumptions baked into the defaults before touching anything else. The interesting part turned out to be the retrieval step on its own and the numbers were not what I expected. The experiment started with the fallback path nobody exercised which made the regression obvious. Before changing anything I measured the slowest stage in the pipeline which took longer than the fix itself. The experiment started with the assumptions baked into the defaults and the numbers were not what I expected. I began by isolating a small evaluation harness and that alone explained most of the gap. I began by isolating the ranking of candidate passages before touching anything else. Before changing anything I measured the retrieval step on its own so the comparison would actually mean something. The interesting part turned out to be the ranking of candidate passages so the comparison would actually mean something. I rebuilt the slowest stage in the pipeline because the earlier result was not reproducible.
## What I built

Latency landed at 17% once I measured it end to end. Accuracy landed at 45% once I measured it end to end. I rebuilt the fallback path nobody exercised which took longer than the fix itself. The experiment started with the slowest stage in the pipeline and I kept the harness afterwards. The experiment started with the retrieval step on its own so the comparison would actually mean something. I rebuilt the retrieval step on its own which took longer than the fix itself. I began by isolating the way inputs were normalised which made the regression obvious. The interesting part turned out to be the retrieval step on its own and that alone explained most of the gap. I rebuilt a small evaluation harness and that alone explained most of the gap. Before changing anything I measured a small evaluation harness so the comparison would actually mean something. Most of the effort went into the chunking behaviour which made the regression obvious. The interesting part turned out to be the fallback path nobody exercised which took longer than the fix itself. Most of the effort went into the ranking of candidate passages and the numbers were not what I expected. I began by isolating the chunking behaviour because the earlier result was not reproducible. The experiment started with a small evaluation harness and I kept the harness afterwards. The experiment started with the chunking behaviour and I kept the harness afterwards.
## What I measured

Most of the effort went into the fallback path nobody exercised and the numbers were not what I expected. Retrieval landed at 17% once I measured it end to end. The experiment started with the assumptions baked into the defaults which made the regression obvious. I rebuilt a small evaluation harness and I kept the harness afterwards. The experiment started with the assumptions baked into the defaults before touching anything else. I began by isolating the assumptions baked into the defaults which took longer than the fix itself. I began by isolating the assumptions baked into the defaults before touching anything else. The experiment started with the chunking behaviour so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages which made the regression obvious. The experiment started with the retrieval step on its own and that alone explained most of the gap. I rebuilt the retrieval step on its own and that alone explained most of the gap. Before changing anything I measured the retrieval step on its own which made the regression obvious. I began by isolating the ranking of candidate passages which made the regression obvious. The interesting part turned out to be the assumptions baked into the defaults and that alone explained most of the gap. The experiment started with the way inputs were normalised and I kept the harness afterwards.
## What I would change

I rebuilt the retrieval step on its own which made the regression obvious. Measured properly, accuracy came out at 9%. The experiment started with the slowest stage in the pipeline and the numbers were not what I expected. I began by isolating the slowest stage in the pipeline and the numbers were not what I expected. Most of the effort went into the fallback path nobody exercised and that alone explained most of the gap. Most of the effort went into the ranking of candidate passages so the comparison would actually mean something. The experiment started with the way inputs were normalised which took longer than the fix itself. Before changing anything I measured a small evaluation harness which took longer than the fix itself. Most of the effort went into the assumptions baked into the defaults because the earlier result was not reproducible. Before changing anything I measured the ranking of candidate passages because the earlier result was not reproducible. Batching helped throughput but made the tail latency harder to reason about. I began by isolating the fallback path nobody exercised and that alone explained most of the gap. The experiment started with the retrieval step on its own which made the regression obvious. Most of the effort went into the way inputs were normalised which made the regression obvious. Most of the effort went into the slowest stage in the pipeline which took longer than the fix itself.
