## The problem

That gave retrieval of 25%, which I wrote down at the time. Measured properly, latency came out at 5%. Keeping a log of every configuration felt tedious until I needed to explain a regression. The interesting failures were the ones where the output read well but cited the wrong source. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Writing the failure cases down first made the design conversation much shorter. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Because the harness came before the optimisation, I caught a regression that looked like a win. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Instrumenting the ingestion path cost a day and saved considerably more than that later. Caching React lookups helped until invalidation became the more interesting problem. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Instrumenting the chunking logic cost a day and saved considerably more than that later. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Tuning vector search helped marginally; fixing how documents were split helped substantially. Because the harness came before the optimisation, I caught a regression that looked like a win.
## What I built

When I finally measured it, latency was 17%. Accuracy landed at 45% once I measured it end to end. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I now write the evaluation before the change, which has saved me from at least two false wins.

The fallback path had never been exercised, so nobody knew it returned an empty result. Cleaning the Redis inputs produced a larger gain than any change to the model did. I do not trust an improvement that only appears on examples I picked myself. Caching vector search lookups helped until invalidation became the more interesting problem. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Comparing against a fixed baseline made the August numbers finally interpretable. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Cleaning the AWS inputs produced a larger gain than any change to the model did. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Most of the week went into the chunking logic rather than the change I had actually planned to make. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Tuning Redis helped marginally; fixing how documents were split helped substantially.
## What I measured

Measured properly, retrieval came out at 17%. The result held up in staging and quietly regressed the moment traffic patterns changed. I spent an afternoon convinced the model was wrong before finding a bug in the loader.

The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Most of the week went into the ingestion path rather than the change I had actually planned to make. In August I assumed the bottleneck was compute; it was almost entirely I/O. Most of the week went into the retrieval step rather than the change I had actually planned to make. I keep a short list of results I do not believe yet, and revisit it weekly. Two configurations differed by one parameter and by far more than one parameter's worth of output. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Splitting the pipeline around the scoring function made the failures much easier to localise. Caching Redis lookups helped until invalidation became the more interesting problem. Tuning AWS helped marginally; fixing how documents were split helped substantially. Caching AWS lookups helped until invalidation became the more interesting problem. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. I now write the evaluation before the change, which has saved me from at least two false wins.
## What I would change

That gave accuracy of 9%, which I wrote down at the time. The tempting change was the model; the effective change was how documents were split. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Batching improved throughput and made the tail latency considerably harder to reason about. Tuning React helped marginally; fixing how documents were split helped substantially. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Instrumenting the retrieval step cost a day and saved considerably more than that later. Splitting the pipeline around the chunking logic made the failures much easier to localise. Every improvement I could not reproduce twice turned out not to be an improvement. Splitting the pipeline around the retrieval step made the failures much easier to localise. Tuning Redis helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Cleaning the vector search inputs produced a larger gain than any change to the model did. Cleaning the React inputs produced a larger gain than any change to the model did. Instrumenting the scoring function cost a day and saved considerably more than that later. The fallback path had never been exercised, so nobody knew it returned an empty result.
