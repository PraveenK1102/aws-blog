Writing this up mostly to force myself to be precise about the numbers.

## The problem

Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. I rebuilt the fallback path nobody exercised and that alone explained most of the gap. The number I got for accuracy was 53%. The interesting part turned out to be the assumptions baked into the defaults and that alone explained most of the gap. Splitting the pipeline into separately testable steps made the failures much easier to localise. The interesting part turned out to be a small evaluation harness and that alone explained most of the gap. Most of the effort went into the slowest stage in the pipeline which made the regression obvious. I began by isolating the way inputs were normalised and the numbers were not what I expected. Before changing anything I measured the way inputs were normalised because the earlier result was not reproducible. I began by isolating the chunking behaviour before touching anything else. I began by isolating a small evaluation harness before touching anything else. Before changing anything I measured the chunking behaviour which took longer than the fix itself.
## What I built

Latency landed at 28% once I measured it end to end. A surprising amount of the gain came from cleaning the inputs rather than changing the model. The experiment started with the retrieval step on its own and that alone explained most of the gap. The experiment started with the slowest stage in the pipeline and that alone explained most of the gap. Most of the effort went into the assumptions baked into the defaults and the numbers were not what I expected. The experiment started with the fallback path nobody exercised which took longer than the fix itself. The experiment started with the way inputs were normalised which took longer than the fix itself. Most of the effort went into the retrieval step on its own because the earlier result was not reproducible. Before changing anything I measured the way inputs were normalised and that alone explained most of the gap. I rebuilt the chunking behaviour so the comparison would actually mean something. Before changing anything I measured the fallback path nobody exercised and the numbers were not what I expected. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression.
## What I measured

The experiment started with a small evaluation harness so the comparison would actually mean something. The interesting part turned out to be the fallback path nobody exercised and I kept the harness afterwards. The number I got for accuracy was 18%. I began by isolating the retrieval step on its own because the earlier result was not reproducible. Before changing anything I measured the slowest stage in the pipeline because the earlier result was not reproducible. The experiment started with the assumptions baked into the defaults and I kept the harness afterwards. The experiment started with the chunking behaviour which made the regression obvious. I began by isolating the assumptions baked into the defaults and that alone explained most of the gap. The experiment started with the retrieval step on its own which made the regression obvious. I began by isolating the way inputs were normalised and I kept the harness afterwards. Most of the time went into building a way to measure the change rather than making the change. The interesting part turned out to be the assumptions baked into the defaults so the comparison would actually mean something.
## What I would change

Most of the effort went into the assumptions baked into the defaults and I kept the harness afterwards. Measured properly, retrieval came out at 32%. I rebuilt the chunking behaviour because the earlier result was not reproducible. Before changing anything I measured the chunking behaviour so the comparison would actually mean something. The interesting part turned out to be the ranking of candidate passages and the numbers were not what I expected. I rebuilt the retrieval step on its own before touching anything else. The experiment started with the fallback path nobody exercised and I kept the harness afterwards. I rebuilt the ranking of candidate passages so the comparison would actually mean something. Before changing anything I measured the way inputs were normalised so the comparison would actually mean something. I rebuilt a small evaluation harness and I kept the harness afterwards. I rebuilt the fallback path nobody exercised which took longer than the fix itself.
