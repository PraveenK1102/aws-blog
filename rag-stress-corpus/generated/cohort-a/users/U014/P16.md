## The problem

The fallback path had never been exercised, so nobody knew it returned an empty result. That gave accuracy of 53%, which I wrote down at the time. In August I assumed the bottleneck was compute; it was almost entirely I/O. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I now write the evaluation before the change, which has saved me from at least two false wins. Most of the week went into the ingestion path rather than the change I had actually planned to make. Writing the failure cases down first made the design conversation much shorter. The interesting failures were the ones where the output read well but cited the wrong source. Splitting the pipeline around the reranking pass made the failures much easier to localise. Caching Redis lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Caching vector search lookups helped until invalidation became the more interesting problem. Cleaning the AWS inputs produced a larger gain than any change to the model did.
## What I built

Batching improved throughput and made the tail latency considerably harder to reason about. The result held up in staging and quietly regressed the moment traffic patterns changed. That gave latency of 28%, which I wrote down at the time.

I keep a short list of results I do not believe yet, and revisit it weekly. Caching AWS lookups helped until invalidation became the more interesting problem. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Keeping a log of every configuration felt tedious until I needed to explain a regression. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Tuning Redis helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Splitting the pipeline around the scoring function made the failures much easier to localise.
## What I measured

The number I got for accuracy was 18%. Most of the week went into the retrieval step rather than the change I had actually planned to make. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The tempting change was the model; the effective change was how documents were split. Tuning vector search helped marginally; fixing how documents were split helped substantially. Because the harness came before the optimisation, I caught a regression that looked like a win. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Comparing against a fixed baseline made the August numbers finally interpretable. Instrumenting the scoring function cost a day and saved considerably more than that later. Splitting the pipeline around the ingestion path made the failures much easier to localise. Instrumenting the retrieval step cost a day and saved considerably more than that later. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something.
## What I would change

Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Two configurations differed by one parameter and by far more than one parameter's worth of output. My recorded figure for retrieval is 32%. Instrumenting the evaluation harness cost a day and saved considerably more than that later. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. I do not trust an improvement that only appears on examples I picked myself. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Every improvement I could not reproduce twice turned out not to be an improvement. Cleaning the Redis inputs produced a larger gain than any change to the model did. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Cleaning the vector search inputs produced a larger gain than any change to the model did. Tuning AWS helped marginally; fixing how documents were split helped substantially.
