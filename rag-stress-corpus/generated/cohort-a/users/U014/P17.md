## The problem

Caching Redis lookups helped until invalidation became the more interesting problem. Cleaning the Redis inputs produced a larger gain than any change to the model did. Measured properly, accuracy came out at 31%. Running it end to end put accuracy at 46%.

The interesting failures were the ones where the output read well but cited the wrong source. The fallback path had never been exercised, so nobody knew it returned an empty result. Two configurations differed by one parameter and by far more than one parameter's worth of output. Splitting the pipeline around the fallback branch made the failures much easier to localise. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. I do not trust an improvement that only appears on examples I picked myself. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. I keep a short list of results I do not believe yet, and revisit it weekly.
## What I built

That gave retrieval of 9%, which I wrote down at the time. On the instrumented run, latency showed 49%. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Caching AWS lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the retrieval step made the failures much easier to localise. Every improvement I could not reproduce twice turned out not to be an improvement. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.
## What I measured

When I finally measured it, latency was 38%. Writing the failure cases down first made the design conversation much shorter. Instrumenting the fallback branch cost a day and saved considerably more than that later. Because the harness came before the optimisation, I caught a regression that looked like a win. Caching vector search lookups helped until invalidation became the more interesting problem. I now write the evaluation before the change, which has saved me from at least two false wins. The tempting change was the model; the effective change was how documents were split. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Tuning React helped marginally; fixing how documents were split helped substantially. Comparing against a fixed baseline made the August numbers finally interpretable.
## What I would change

Measured properly, retrieval came out at 18%. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Most of the week went into the scoring function rather than the change I had actually planned to make. Keeping a log of every configuration felt tedious until I needed to explain a regression. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Batching improved throughput and made the tail latency considerably harder to reason about. The result held up in staging and quietly regressed the moment traffic patterns changed. Instrumenting the evaluation harness cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. In August I assumed the bottleneck was compute; it was almost entirely I/O.
