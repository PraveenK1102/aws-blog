## The problem

Splitting the pipeline around the reranking pass made the failures much easier to localise. The number I got for latency was 30%. I keep a short list of results I do not believe yet, and revisit it weekly.

Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Cleaning the React inputs produced a larger gain than any change to the model did. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. The tempting change was the model; the effective change was how documents were split. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Splitting the pipeline around the fallback branch made the failures much easier to localise. Instrumenting the reranking pass cost a day and saved considerably more than that later. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Tuning Redis helped marginally; fixing how documents were split helped substantially.
## What I built

Running it end to end put latency at 41%. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. The fallback path had never been exercised, so nobody knew it returned an empty result. The result held up in staging and quietly regressed the moment traffic patterns changed. I now write the evaluation before the change, which has saved me from at least two false wins. The interesting failures were the ones where the output read well but cited the wrong source. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Cleaning the AWS inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Cleaning the Redis inputs produced a larger gain than any change to the model did. Instrumenting the scoring function cost a day and saved considerably more than that later.
## What I measured

I do not trust an improvement that only appears on examples I picked myself. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Batching improved throughput and made the tail latency considerably harder to reason about. On the instrumented run, latency showed 9%.

Caching FastAPI lookups helped until invalidation became the more interesting problem. Writing the failure cases down first made the design conversation much shorter. Two configurations differed by one parameter and by far more than one parameter's worth of output. Tuning vector search helped marginally; fixing how documents were split helped substantially. Most of the week went into the ingestion path rather than the change I had actually planned to make. In July I assumed the bottleneck was compute; it was almost entirely I/O. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap.
## What I would change

Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Because the harness came before the optimisation, I caught a regression that looked like a win. Keeping a log of every configuration felt tedious until I needed to explain a regression.

My recorded figure for accuracy is 60%. Comparing against a fixed baseline made the July numbers finally interpretable. Most of the week went into the retrieval step rather than the change I had actually planned to make. Every improvement I could not reproduce twice turned out not to be an improvement. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Caching React lookups helped until invalidation became the more interesting problem. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something.
