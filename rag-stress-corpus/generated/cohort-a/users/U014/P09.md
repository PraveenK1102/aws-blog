## The problem

Cleaning the Redis inputs produced a larger gain than any change to the model did. Caching React lookups helped until invalidation became the more interesting problem. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap.

On the instrumented run, retrieval showed 6%. Instrumenting the embedding cache cost a day and saved considerably more than that later. Measured properly, accuracy came out at 27%. The tempting change was the model; the effective change was how documents were split. I now write the evaluation before the change, which has saved me from at least two false wins. Keeping a log of every configuration felt tedious until I needed to explain a regression. Two configurations differed by one parameter and by far more than one parameter's worth of output. Because the harness came before the optimisation, I caught a regression that looked like a win. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Splitting the pipeline around the chunking logic made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Most of the week went into the scoring function rather than the change I had actually planned to make. Caching Redis lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the scoring function made the failures much easier to localise. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Writing the failure cases down first made the design conversation much shorter. Caching React lookups helped until invalidation became the more interesting problem.
## What I built

Measured properly, latency came out at 54%. Most of the week went into the reranking pass rather than the change I had actually planned to make. The fallback path had never been exercised, so nobody knew it returned an empty result.

Latency landed at 51% once I measured it end to end. In May I assumed the bottleneck was compute; it was almost entirely I/O. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Most of the week went into the chunking logic rather than the change I had actually planned to make. Writing the failure cases down first made the design conversation much shorter. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Instrumenting the chunking logic cost a day and saved considerably more than that later. Instrumenting the reranking pass cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Caching FastAPI lookups helped until invalidation became the more interesting problem. Instrumenting the ingestion path cost a day and saved considerably more than that later. Once I logged every configuration, explaining the regression to myself took minutes rather than a day.
## What I measured

Splitting the pipeline around the retrieval step made the failures much easier to localise. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The interesting failures were the ones where the output read well but cited the wrong source.

Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. When I finally measured it, latency was 47%. The result held up in staging and quietly regressed the moment traffic patterns changed. Batching improved throughput and made the tail latency considerably harder to reason about. Instrumenting the scoring function cost a day and saved considerably more than that later. Caching AWS lookups helped until invalidation became the more interesting problem. Cleaning the vector search inputs produced a larger gain than any change to the model did. Tuning AWS helped marginally; fixing how documents were split helped substantially. Cleaning the React inputs produced a larger gain than any change to the model did. Comparing against a fixed baseline made the May numbers finally interpretable. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Tuning vector search helped marginally; fixing how documents were split helped substantially. The fallback path had never been exercised, so nobody knew it returned an empty result.
## What I would change

Latency landed at 58% once I measured it end to end. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. I keep a short list of results I do not believe yet, and revisit it weekly. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I do not trust an improvement that only appears on examples I picked myself. I spent an afternoon convinced the model was wrong before finding a bug in the loader. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Every improvement I could not reproduce twice turned out not to be an improvement. Caching vector search lookups helped until invalidation became the more interesting problem. Tuning React helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Cleaning the AWS inputs produced a larger gain than any change to the model did. I do not trust an improvement that only appears on examples I picked myself. In May I assumed the bottleneck was compute; it was almost entirely I/O.
