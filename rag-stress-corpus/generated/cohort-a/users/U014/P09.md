Spent most of this week on the retrieval side of the project.

## The problem

Most of the effort went into a small evaluation harness and the numbers were not what I expected. The experiment started with the slowest stage in the pipeline so the comparison would actually mean something. Measured properly, accuracy came out at 27%. Measured properly, retrieval came out at 6%. I began by isolating the way inputs were normalised because the earlier result was not reproducible. Before changing anything I measured the fallback path nobody exercised which took longer than the fix itself. Most of the effort went into the slowest stage in the pipeline before touching anything else. Most of the effort went into the assumptions baked into the defaults which took longer than the fix itself. Before changing anything I measured the assumptions baked into the defaults which took longer than the fix itself. The interesting part turned out to be the fallback path nobody exercised and the numbers were not what I expected. I rebuilt the chunking behaviour which took longer than the fix itself. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression. The experiment started with the fallback path nobody exercised which took longer than the fix itself. Before changing anything I measured the way inputs were normalised which took longer than the fix itself. I rebuilt the ranking of candidate passages and that alone explained most of the gap. Before changing anything I measured the ranking of candidate passages because the earlier result was not reproducible. The interesting part turned out to be the ranking of candidate passages because the earlier result was not reproducible.
## What I built

Before changing anything I measured the way inputs were normalised and I kept the harness afterwards. I began by isolating the ranking of candidate passages which took longer than the fix itself. Measured properly, latency came out at 54%. Latency landed at 51% once I measured it end to end. I began by isolating the fallback path nobody exercised and I kept the harness afterwards. The interesting part turned out to be the way inputs were normalised which made the regression obvious. I rebuilt a small evaluation harness so the comparison would actually mean something. I rebuilt the assumptions baked into the defaults and I kept the harness afterwards. I rebuilt the slowest stage in the pipeline and that alone explained most of the gap. I rebuilt the retrieval step on its own which made the regression obvious. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. I began by isolating the way inputs were normalised and the numbers were not what I expected. I began by isolating the way inputs were normalised before touching anything else. Most of the effort went into the chunking behaviour because the earlier result was not reproducible. I rebuilt the chunking behaviour because the earlier result was not reproducible. Before changing anything I measured the ranking of candidate passages and that alone explained most of the gap. A surprising amount of the gain came from cleaning the inputs rather than changing the model.
## What I measured

I began by isolating the retrieval step on its own and I kept the harness afterwards. Before changing anything I measured the chunking behaviour which made the regression obvious. The number I got for latency was 47%. Most of the effort went into the fallback path nobody exercised before touching anything else. Most of the effort went into the retrieval step on its own because the earlier result was not reproducible. Before changing anything I measured the retrieval step on its own which made the regression obvious. I rebuilt the fallback path nobody exercised and that alone explained most of the gap. I began by isolating the chunking behaviour before touching anything else. The interesting part turned out to be a small evaluation harness which took longer than the fix itself. Before changing anything I measured the way inputs were normalised because the earlier result was not reproducible. Most of the effort went into the fallback path nobody exercised so the comparison would actually mean something. I began by isolating the ranking of candidate passages so the comparison would actually mean something. The experiment started with the way inputs were normalised and the numbers were not what I expected. I rebuilt the retrieval step on its own and I kept the harness afterwards. Most of the effort went into the way inputs were normalised so the comparison would actually mean something.
## What I would change

The interesting part turned out to be the chunking behaviour which took longer than the fix itself. Before changing anything I measured the ranking of candidate passages which made the regression obvious. Measured properly, latency came out at 58%. I began by isolating the chunking behaviour which made the regression obvious. The experiment started with the chunking behaviour and the numbers were not what I expected. I rebuilt the slowest stage in the pipeline so the comparison would actually mean something. I began by isolating the ranking of candidate passages before touching anything else. The interesting part turned out to be the chunking behaviour and I kept the harness afterwards. I rebuilt the way inputs were normalised and that alone explained most of the gap. I began by isolating the ranking of candidate passages because the earlier result was not reproducible. I rebuilt the fallback path nobody exercised and the numbers were not what I expected. I rebuilt a small evaluation harness because the earlier result was not reproducible. Before changing anything I measured the assumptions baked into the defaults and I kept the harness afterwards. The experiment started with the fallback path nobody exercised before touching anything else. I rebuilt the retrieval step on its own and that alone explained most of the gap.
