## The problem

The tempting change was the model; the effective change was how documents were split. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Measured properly, latency came out at 27%. Keeping a log of every configuration felt tedious until I needed to explain a regression.

Batching improved throughput and made the tail latency considerably harder to reason about. Splitting the pipeline around the scoring function made the failures much easier to localise. I now write the evaluation before the change, which has saved me from at least two false wins. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. I do not trust an improvement that only appears on examples I picked myself. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Instrumenting the retrieval step cost a day and saved considerably more than that later. Instrumenting the scoring function cost a day and saved considerably more than that later. Tuning vector search helped marginally; fixing how documents were split helped substantially. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. Once I logged every configuration, explaining the regression to myself took minutes rather than a day.
## What I built

I keep a short list of results I do not believe yet, and revisit it weekly. Comparing against a fixed baseline made the April numbers finally interpretable. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.

The number I got for accuracy was 7%. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Instrumenting the chunking logic cost a day and saved considerably more than that later. Because the harness came before the optimisation, I caught a regression that looked like a win. Splitting the pipeline around the retrieval step made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Tuning AWS helped marginally; fixing how documents were split helped substantially. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Splitting the pipeline around the embedding cache made the failures much easier to localise. Caching React lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real React inputs arrived. Tuning Redis helped marginally; fixing how documents were split helped substantially. The tempting change was the model; the effective change was how documents were split. Tuning AWS helped marginally; fixing how documents were split helped substantially. Comparing against a fixed baseline made the April numbers finally interpretable.
## What I measured

The fallback path had never been exercised, so nobody knew it returned an empty result. That gave accuracy of 55%, which I wrote down at the time. Caching Redis lookups helped until invalidation became the more interesting problem.

Writing the failure cases down first made the design conversation much shorter. Most of the week went into the scoring function rather than the change I had actually planned to make. In April I assumed the bottleneck was compute; it was almost entirely I/O. Every improvement I could not reproduce twice turned out not to be an improvement. Cleaning the Redis inputs produced a larger gain than any change to the model did. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Caching AWS lookups helped until invalidation became the more interesting problem. Cleaning the AWS inputs produced a larger gain than any change to the model did. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Cleaning the React inputs produced a larger gain than any change to the model did. Instrumenting the embedding cache cost a day and saved considerably more than that later. Caching FastAPI lookups helped until invalidation became the more interesting problem. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Cleaning the Redis inputs produced a larger gain than any change to the model did. In April I assumed the bottleneck was compute; it was almost entirely I/O.
## What I would change

The interesting failures were the ones where the output read well but cited the wrong source. The result held up in staging and quietly regressed the moment traffic patterns changed. Retrieval landed at 56% once I measured it end to end. Two configurations differed by one parameter and by far more than one parameter's worth of output. Most of the week went into the embedding cache rather than the change I had actually planned to make. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Tuning React helped marginally; fixing how documents were split helped substantially. Caching vector search lookups helped until invalidation became the more interesting problem. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Most of the week went into the evaluation harness rather than the change I had actually planned to make. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Cleaning the vector search inputs produced a larger gain than any change to the model did. Keeping a log of every configuration felt tedious until I needed to explain a regression. I do not trust an improvement that only appears on examples I picked myself. I now write the evaluation before the change, which has saved me from at least two false wins.
