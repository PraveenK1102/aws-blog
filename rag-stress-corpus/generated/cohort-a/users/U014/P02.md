## The problem

The tempting change was the model; the effective change was how documents were split. That gave accuracy of 48%, which I wrote down at the time. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Writing the failure cases down first made the design conversation much shorter. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. In March I assumed the bottleneck was compute; it was almost entirely I/O. Two configurations differed by one parameter and by far more than one parameter's worth of output. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Instrumenting the ingestion path cost a day and saved considerably more than that later. The fallback path had never been exercised, so nobody knew it returned an empty result. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Caching Redis lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the embedding cache made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real vector search inputs arrived.
## What I built

Running it end to end put latency at 11%. Keeping a log of every configuration felt tedious until I needed to explain a regression. Batching improved throughput and made the tail latency considerably harder to reason about. Because the harness came before the optimisation, I caught a regression that looked like a win. Tuning React helped marginally; fixing how documents were split helped substantially. Splitting the pipeline around the ingestion path made the failures much easier to localise. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Caching React lookups helped until invalidation became the more interesting problem. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Cleaning the Redis inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. Tuning vector search helped marginally; fixing how documents were split helped substantially.
## What I measured

My recorded figure for latency is 53%. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The interesting failures were the ones where the output read well but cited the wrong source. Tuning AWS helped marginally; fixing how documents were split helped substantially.

Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Comparing against a fixed baseline made the March numbers finally interpretable. Cleaning the React inputs produced a larger gain than any change to the model did. I keep a short list of results I do not believe yet, and revisit it weekly. Cleaning the vector search inputs produced a larger gain than any change to the model did. I now write the evaluation before the change, which has saved me from at least two false wins. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The result held up in staging and quietly regressed the moment traffic patterns changed. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap.
## What I would change

Caching AWS lookups helped until invalidation became the more interesting problem. Most of the week went into the retrieval step rather than the change I had actually planned to make. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. I do not trust an improvement that only appears on examples I picked myself. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Every improvement I could not reproduce twice turned out not to be an improvement. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Tuning Redis helped marginally; fixing how documents were split helped substantially. Instrumenting the reranking pass cost a day and saved considerably more than that later. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Instrumenting the fallback branch cost a day and saved considerably more than that later.
