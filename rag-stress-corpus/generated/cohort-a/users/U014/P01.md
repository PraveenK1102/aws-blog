## The problem

My recorded figure for latency is 56%. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The tempting change was the model; the effective change was how documents were split. Instrumenting the chunking logic cost a day and saved considerably more than that later. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the reranking pass cost a day and saved considerably more than that later. Because the harness came before the optimisation, I caught a regression that looked like a win. Writing the failure cases down first made the design conversation much shorter. Caching Redis lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs.
## What I built

Batching improved throughput and made the tail latency considerably harder to reason about. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Accuracy landed at 8% once I measured it end to end. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. I do not trust an improvement that only appears on examples I picked myself. The interesting failures were the ones where the output read well but cited the wrong source. Tuning AWS helped marginally; fixing how documents were split helped substantially. Most of the week went into the retrieval step rather than the change I had actually planned to make. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. The result held up in staging and quietly regressed the moment traffic patterns changed. The first version worked on the sample set and fell apart the moment real vector search inputs arrived.
## What I measured

Measured properly, latency came out at 59%. Comparing against a fixed baseline made the March numbers finally interpretable. Tuning Redis helped marginally; fixing how documents were split helped substantially. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Every improvement I could not reproduce twice turned out not to be an improvement. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Caching vector search lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. Caching AWS lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Tuning React helped marginally; fixing how documents were split helped substantially. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Cleaning the Redis inputs produced a larger gain than any change to the model did.
## What I would change

Most of the week went into the chunking logic rather than the change I had actually planned to make. The fallback path had never been exercised, so nobody knew it returned an empty result. The number I got for accuracy was 28%.

Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Two configurations differed by one parameter and by far more than one parameter's worth of output. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. I keep a short list of results I do not believe yet, and revisit it weekly. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. In March I assumed the bottleneck was compute; it was almost entirely I/O. Keeping a log of every configuration felt tedious until I needed to explain a regression. Cleaning the React inputs produced a larger gain than any change to the model did. Splitting the pipeline around the embedding cache made the failures much easier to localise. I rebuilt the scoring function so the before-and-after comparison would actually mean something.
