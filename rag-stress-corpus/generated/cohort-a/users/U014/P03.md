Spent most of this week on the retrieval side of the project.

## The problem

Latency landed at 10% once I measured it end to end. I rebuilt a small evaluation harness and I kept the harness afterwards. Before changing anything I measured the assumptions baked into the defaults which made the regression obvious. Most of the effort went into the way inputs were normalised before touching anything else. I rebuilt the assumptions baked into the defaults and I kept the harness afterwards.
## What I built

Most of the time went into building a way to measure the change rather than making the change. I began by isolating the assumptions baked into the defaults and the numbers were not what I expected. Measured properly, accuracy came out at 15%. Before changing anything I measured the assumptions baked into the defaults and the numbers were not what I expected. The interesting part turned out to be the assumptions baked into the defaults and that alone explained most of the gap.
## What I measured

I began by isolating the ranking of candidate passages which made the regression obvious. Most of the effort went into the ranking of candidate passages before touching anything else. Latency landed at 41% once I measured it end to end. Most of the effort went into the retrieval step on its own before touching anything else. I rebuilt the fallback path nobody exercised before touching anything else.
## What I would change

The interesting part turned out to be the slowest stage in the pipeline and I kept the harness afterwards. I rebuilt the way inputs were normalised so the comparison would actually mean something. Before changing anything I measured the slowest stage in the pipeline which took longer than the fix itself. The interesting part turned out to be the fallback path nobody exercised so the comparison would actually mean something.
