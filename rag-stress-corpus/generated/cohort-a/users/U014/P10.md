## The problem

Measured properly, latency came out at 34%. Because the harness came before the optimisation, I caught a regression that looked like a win. Running it end to end put retrieval at 15%. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Cleaning the Redis inputs produced a larger gain than any change to the model did.

I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Comparing against a fixed baseline made the June numbers finally interpretable. The fallback path had never been exercised, so nobody knew it returned an empty result. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Most of the week went into the fallback branch rather than the change I had actually planned to make. Every improvement I could not reproduce twice turned out not to be an improvement. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Instrumenting the reranking pass cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. Caching FastAPI lookups helped until invalidation became the more interesting problem. Tuning vector search helped marginally; fixing how documents were split helped substantially. Tuning Redis helped marginally; fixing how documents were split helped substantially. Caching AWS lookups helped until invalidation became the more interesting problem. Two configurations differed by one parameter and by far more than one parameter's worth of output. I now write the evaluation before the change, which has saved me from at least two false wins. The tempting change was the model; the effective change was how documents were split.
## What I built

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. In June I assumed the bottleneck was compute; it was almost entirely I/O. My recorded figure for accuracy is 23%. I now write the evaluation before the change, which has saved me from at least two false wins. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Batching improved throughput and made the tail latency considerably harder to reason about. The result held up in staging and quietly regressed the moment traffic patterns changed. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Tuning AWS helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Instrumenting the scoring function cost a day and saved considerably more than that later. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Instrumenting the retrieval step cost a day and saved considerably more than that later. Instrumenting the ingestion path cost a day and saved considerably more than that later. Because the harness came before the optimisation, I caught a regression that looked like a win. Every improvement I could not reproduce twice turned out not to be an improvement.
## What I measured

Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning React helped marginally; fixing how documents were split helped substantially. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. Measured properly, accuracy came out at 16%. Caching vector search lookups helped until invalidation became the more interesting problem. I do not trust an improvement that only appears on examples I picked myself. Splitting the pipeline around the scoring function made the failures much easier to localise. I keep a short list of results I do not believe yet, and revisit it weekly. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Most of the week went into the scoring function rather than the change I had actually planned to make. The first version worked on the sample set and fell apart the moment real React inputs arrived. Caching React lookups helped until invalidation became the more interesting problem. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Keeping a log of every configuration felt tedious until I needed to explain a regression. Comparing against a fixed baseline made the June numbers finally interpretable.
## What I would change

Two configurations differed by one parameter and by far more than one parameter's worth of output. The number I got for retrieval was 42%. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. The tempting change was the model; the effective change was how documents were split. Writing the failure cases down first made the design conversation much shorter. Most of the week went into the retrieval step rather than the change I had actually planned to make. The interesting failures were the ones where the output read well but cited the wrong source. Cleaning the AWS inputs produced a larger gain than any change to the model did. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Caching Redis lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Splitting the pipeline around the embedding cache made the failures much easier to localise. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Cleaning the vector search inputs produced a larger gain than any change to the model did. Cleaning the React inputs produced a larger gain than any change to the model did. Writing the failure cases down first made the design conversation much shorter. I spent an afternoon convinced the model was wrong before finding a bug in the loader. In June I assumed the bottleneck was compute; it was almost entirely I/O.
