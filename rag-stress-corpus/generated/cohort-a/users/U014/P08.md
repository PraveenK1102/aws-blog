Writing this up mostly to force myself to be precise about the numbers.

## The problem

Measured properly, latency came out at 21%. I began by isolating a small evaluation harness before touching anything else. Retrieval landed at 33% once I measured it end to end. The experiment started with the fallback path nobody exercised which made the regression obvious. Before changing anything I measured a small evaluation harness so the comparison would actually mean something. The interesting part turned out to be the slowest stage in the pipeline before touching anything else. I rebuilt the ranking of candidate passages which took longer than the fix itself. Before changing anything I measured the way inputs were normalised before touching anything else. Before changing anything I measured the chunking behaviour because the earlier result was not reproducible. The experiment started with the ranking of candidate passages so the comparison would actually mean something.
## What I built

Retrieval landed at 42% once I measured it end to end. Most of the effort went into the chunking behaviour because the earlier result was not reproducible. The number I got for accuracy was 25%. The interesting part turned out to be the retrieval step on its own which took longer than the fix itself. Before changing anything I measured the chunking behaviour which made the regression obvious. Most of the effort went into the ranking of candidate passages and I kept the harness afterwards. The interesting part turned out to be the fallback path nobody exercised and the numbers were not what I expected. The interesting part turned out to be the chunking behaviour which made the regression obvious. I began by isolating the retrieval step on its own before touching anything else. I began by isolating the retrieval step on its own and that alone explained most of the gap.
## What I measured

Retrieval landed at 12% once I measured it end to end. The interesting part turned out to be the retrieval step on its own because the earlier result was not reproducible. I rebuilt the assumptions baked into the defaults because the earlier result was not reproducible. I began by isolating the ranking of candidate passages which took longer than the fix itself. Most of the effort went into the ranking of candidate passages because the earlier result was not reproducible. I began by isolating the assumptions baked into the defaults before touching anything else. Splitting the pipeline into separately testable steps made the failures much easier to localise. I rebuilt the chunking behaviour before touching anything else. I rebuilt a small evaluation harness before touching anything else.
## What I would change

Most of the effort went into the retrieval step on its own so the comparison would actually mean something. The experiment started with the way inputs were normalised which made the regression obvious. Measured properly, latency came out at 7%. The experiment started with the assumptions baked into the defaults so the comparison would actually mean something. I began by isolating the slowest stage in the pipeline and that alone explained most of the gap. I began by isolating the ranking of candidate passages and I kept the harness afterwards. The experiment started with the retrieval step on its own and I kept the harness afterwards. Most of the effort went into the fallback path nobody exercised because the earlier result was not reproducible. I began by isolating the chunking behaviour which made the regression obvious.
