## The problem

My recorded figure for retrieval is 33%. Measured properly, latency came out at 21%. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Batching improved throughput and made the tail latency considerably harder to reason about. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. I keep a short list of results I do not believe yet, and revisit it weekly. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. The first version worked on the sample set and fell apart the moment real AWS inputs arrived.
## What I built

Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Two configurations differed by one parameter and by far more than one parameter's worth of output. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. When I finally measured it, retrieval was 42%.

The number I got for accuracy was 25%. In May I assumed the bottleneck was compute; it was almost entirely I/O. Every improvement I could not reproduce twice turned out not to be an improvement. Caching FastAPI lookups helped until invalidation became the more interesting problem. Most of the week went into the chunking logic rather than the change I had actually planned to make. I now write the evaluation before the change, which has saved me from at least two false wins.
## What I measured

Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Most of the week went into the scoring function rather than the change I had actually planned to make. Splitting the pipeline around the reranking pass made the failures much easier to localise.

That gave retrieval of 12%, which I wrote down at the time. Comparing against a fixed baseline made the May numbers finally interpretable. I do not trust an improvement that only appears on examples I picked myself. Splitting the pipeline around the fallback branch made the failures much easier to localise. The interesting failures were the ones where the output read well but cited the wrong source. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Because the harness came before the optimisation, I caught a regression that looked like a win.
## What I would change

The result held up in staging and quietly regressed the moment traffic patterns changed. The tempting change was the model; the effective change was how documents were split. Keeping a log of every configuration felt tedious until I needed to explain a regression. Cleaning the vector search inputs produced a larger gain than any change to the model did.

Running it end to end put latency at 7%. The fallback path had never been exercised, so nobody knew it returned an empty result. Tuning AWS helped marginally; fixing how documents were split helped substantially. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Cleaning the Redis inputs produced a larger gain than any change to the model did. Caching vector search lookups helped until invalidation became the more interesting problem.
