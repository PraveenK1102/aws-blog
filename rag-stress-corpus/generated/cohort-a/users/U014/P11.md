Notes from a few days of experiments, written while they are still fresh.

## The problem

Accuracy landed at 24% once I measured it end to end. I began by isolating the way inputs were normalised and the numbers were not what I expected. I rebuilt the retrieval step on its own before touching anything else. Retrieval landed at 56% once I measured it end to end. I rebuilt the retrieval step on its own which took longer than the fix itself. The interesting part turned out to be the chunking behaviour and I kept the harness afterwards. Before changing anything I measured the retrieval step on its own which made the regression obvious. I began by isolating the slowest stage in the pipeline because the earlier result was not reproducible. Most of the effort went into the way inputs were normalised because the earlier result was not reproducible. I began by isolating the fallback path nobody exercised and that alone explained most of the gap. The interesting part turned out to be the fallback path nobody exercised and the numbers were not what I expected. The interesting part turned out to be the ranking of candidate passages and I kept the harness afterwards. Most of the effort went into the chunking behaviour which took longer than the fix itself. I began by isolating the chunking behaviour before touching anything else. I began by isolating the fallback path nobody exercised and I kept the harness afterwards. Most of the effort went into the retrieval step on its own so the comparison would actually mean something. Latency turned out to be dominated by a step I had assumed was cheap.
## What I built

Before changing anything I measured the ranking of candidate passages and I kept the harness afterwards. A surprising amount of the gain came from cleaning the inputs rather than changing the model. Retrieval landed at 50% once I measured it end to end. I began by isolating a small evaluation harness which made the regression obvious. I began by isolating the assumptions baked into the defaults because the earlier result was not reproducible. The experiment started with the ranking of candidate passages and I kept the harness afterwards. I rebuilt the fallback path nobody exercised and the numbers were not what I expected. Before changing anything I measured a small evaluation harness and that alone explained most of the gap. I began by isolating the retrieval step on its own which made the regression obvious. I rebuilt a small evaluation harness and I kept the harness afterwards. I began by isolating the chunking behaviour which made the regression obvious. The interesting part turned out to be the fallback path nobody exercised which took longer than the fix itself. The experiment started with the retrieval step on its own and I kept the harness afterwards. The experiment started with a small evaluation harness and that alone explained most of the gap. The experiment started with the assumptions baked into the defaults so the comparison would actually mean something.
## What I measured

I rebuilt the slowest stage in the pipeline and I kept the harness afterwards. Most of the effort went into a small evaluation harness and I kept the harness afterwards. The number I got for retrieval was 30%. The interesting part turned out to be the ranking of candidate passages which took longer than the fix itself. The interesting part turned out to be the fallback path nobody exercised and that alone explained most of the gap. The experiment started with the slowest stage in the pipeline so the comparison would actually mean something. The experiment started with the fallback path nobody exercised because the earlier result was not reproducible. I began by isolating the retrieval step on its own so the comparison would actually mean something. I began by isolating a small evaluation harness and the numbers were not what I expected. I rebuilt the ranking of candidate passages which took longer than the fix itself. I began by isolating the way inputs were normalised and I kept the harness afterwards. I rebuilt a small evaluation harness so the comparison would actually mean something. I rebuilt the assumptions baked into the defaults and I kept the harness afterwards. Most of the effort went into the slowest stage in the pipeline and I kept the harness afterwards. Before changing anything I measured the slowest stage in the pipeline and I kept the harness afterwards.
## What I would change

Before changing anything I measured the retrieval step on its own before touching anything else. The number I got for retrieval was 59%. Most of the time went into building a way to measure the change rather than making the change. The interesting part turned out to be a small evaluation harness which took longer than the fix itself. The experiment started with the fallback path nobody exercised before touching anything else. I began by isolating the chunking behaviour and I kept the harness afterwards. Before changing anything I measured the chunking behaviour and the numbers were not what I expected. I began by isolating the retrieval step on its own because the earlier result was not reproducible. I began by isolating the ranking of candidate passages which took longer than the fix itself. The experiment started with the slowest stage in the pipeline and that alone explained most of the gap. Before changing anything I measured the way inputs were normalised which made the regression obvious. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. The experiment started with the way inputs were normalised so the comparison would actually mean something. The experiment started with the retrieval step on its own which took longer than the fix itself. The interesting part turned out to be the fallback path nobody exercised and I kept the harness afterwards.
