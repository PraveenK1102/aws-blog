## The problem

Two configurations differed by one parameter and by far more than one parameter's worth of output. Running it end to end put accuracy at 24%. Latency turned out to be dominated by the scoring function, which I had assumed was cheap.

I spent an afternoon convinced the model was wrong before finding a bug in the loader. In June I assumed the bottleneck was compute; it was almost entirely I/O. My recorded figure for retrieval is 56%. Tuning Redis helped marginally; fixing how documents were split helped substantially. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. The interesting failures were the ones where the output read well but cited the wrong source. Every improvement I could not reproduce twice turned out not to be an improvement. Writing the failure cases down first made the design conversation much shorter. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Instrumenting the fallback branch cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Tuning AWS helped marginally; fixing how documents were split helped substantially. Tuning vector search helped marginally; fixing how documents were split helped substantially. The result held up in staging and quietly regressed the moment traffic patterns changed.
## What I built

The result held up in staging and quietly regressed the moment traffic patterns changed. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. The tempting change was the model; the effective change was how documents were split.

When I finally measured it, retrieval was 50%. Batching improved throughput and made the tail latency considerably harder to reason about. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Because the harness came before the optimisation, I caught a regression that looked like a win. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Splitting the pipeline around the fallback branch made the failures much easier to localise. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Splitting the pipeline around the ingestion path made the failures much easier to localise. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Caching vector search lookups helped until invalidation became the more interesting problem. Instrumenting the embedding cache cost a day and saved considerably more than that later. Every improvement I could not reproduce twice turned out not to be an improvement. I do not trust an improvement that only appears on examples I picked myself.
## What I measured

Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Comparing against a fixed baseline made the June numbers finally interpretable. Keeping a log of every configuration felt tedious until I needed to explain a regression. On the instrumented run, retrieval showed 30%. The fallback path had never been exercised, so nobody knew it returned an empty result. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the ingestion path cost a day and saved considerably more than that later. I keep a short list of results I do not believe yet, and revisit it weekly. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Caching AWS lookups helped until invalidation became the more interesting problem. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Cleaning the AWS inputs produced a larger gain than any change to the model did. Instrumenting the reranking pass cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Caching FastAPI lookups helped until invalidation became the more interesting problem. In June I assumed the bottleneck was compute; it was almost entirely I/O. Writing the failure cases down first made the design conversation much shorter.
## What I would change

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I do not trust an improvement that only appears on examples I picked myself. Instrumenting the chunking logic cost a day and saved considerably more than that later.

Most of the week went into the evaluation harness rather than the change I had actually planned to make. The number I got for retrieval was 59%. Caching React lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Most of the week went into the ingestion path rather than the change I had actually planned to make. Cleaning the Redis inputs produced a larger gain than any change to the model did. Tuning React helped marginally; fixing how documents were split helped substantially. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Caching Redis lookups helped until invalidation became the more interesting problem. Most of the week went into the chunking logic rather than the change I had actually planned to make. Cleaning the vector search inputs produced a larger gain than any change to the model did. Splitting the pipeline around the retrieval step made the failures much easier to localise. Cleaning the React inputs produced a larger gain than any change to the model did. Keeping a log of every configuration felt tedious until I needed to explain a regression.
