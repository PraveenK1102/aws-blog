## The problem

Keeping a log of every configuration felt tedious until I needed to explain a regression. Comparing against a fixed baseline made the April numbers finally interpretable. Batching improved throughput and made the tail latency considerably harder to reason about.

Measured properly, latency came out at 58%. My recorded figure for retrieval is 8%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Cleaning the React inputs produced a larger gain than any change to the model did. Every improvement I could not reproduce twice turned out not to be an improvement. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. The tempting change was the model; the effective change was how documents were split. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Caching FastAPI lookups helped until invalidation became the more interesting problem.
## What I built

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Caching React lookups helped until invalidation became the more interesting problem. Measured properly, accuracy came out at 13%. I now write the evaluation before the change, which has saved me from at least two false wins. The fallback path had never been exercised, so nobody knew it returned an empty result. Tuning Redis helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. Two configurations differed by one parameter and by far more than one parameter's worth of output. Splitting the pipeline around the fallback branch made the failures much easier to localise. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Most of the week went into the retrieval step rather than the change I had actually planned to make.
## What I measured

Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Accuracy landed at 53% once I measured it end to end. I keep a short list of results I do not believe yet, and revisit it weekly.

Because the harness came before the optimisation, I caught a regression that looked like a win. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The result held up in staging and quietly regressed the moment traffic patterns changed. Instrumenting the retrieval step cost a day and saved considerably more than that later. Tuning vector search helped marginally; fixing how documents were split helped substantially. I do not trust an improvement that only appears on examples I picked myself.
## What I would change

The number I got for latency was 55%. Splitting the pipeline around the evaluation harness made the failures much easier to localise. In April I assumed the bottleneck was compute; it was almost entirely I/O. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The interesting failures were the ones where the output read well but cited the wrong source. Writing the failure cases down first made the design conversation much shorter. Most of the week went into the chunking logic rather than the change I had actually planned to make. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real AWS inputs arrived.
