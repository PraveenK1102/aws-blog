## The problem

Running it end to end put retrieval at 42%. Caching Redis lookups helped until invalidation became the more interesting problem. Two configurations differed by one parameter and by far more than one parameter's worth of output. That gave retrieval of 14%, which I wrote down at the time.

Every improvement I could not reproduce twice turned out not to be an improvement. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. I keep a short list of results I do not believe yet, and revisit it weekly. Comparing against a fixed baseline made the April numbers finally interpretable. Splitting the pipeline around the fallback branch made the failures much easier to localise. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Caching AWS lookups helped until invalidation became the more interesting problem. Instrumenting the scoring function cost a day and saved considerably more than that later. Tuning AWS helped marginally; fixing how documents were split helped substantially.
## What I built

The tempting change was the model; the effective change was how documents were split. The fallback path had never been exercised, so nobody knew it returned an empty result. Once I logged every configuration, explaining the regression to myself took minutes rather than a day.

Accuracy landed at 6% once I measured it end to end. On the instrumented run, latency showed 22%. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Batching improved throughput and made the tail latency considerably harder to reason about. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Keeping a log of every configuration felt tedious until I needed to explain a regression. Most of the week went into the retrieval step rather than the change I had actually planned to make. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Caching FastAPI lookups helped until invalidation became the more interesting problem.
## What I measured

Measured properly, latency came out at 28%. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. Writing the failure cases down first made the design conversation much shorter. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Tuning React helped marginally; fixing how documents were split helped substantially. I do not trust an improvement that only appears on examples I picked myself. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. The result held up in staging and quietly regressed the moment traffic patterns changed. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Cleaning the vector search inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap.
## What I would change

My recorded figure for retrieval is 57%. Because the harness came before the optimisation, I caught a regression that looked like a win. I now write the evaluation before the change, which has saved me from at least two false wins. The interesting failures were the ones where the output read well but cited the wrong source. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Instrumenting the evaluation harness cost a day and saved considerably more than that later. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Tuning vector search helped marginally; fixing how documents were split helped substantially. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. In April I assumed the bottleneck was compute; it was almost entirely I/O. Most of the week went into the fallback branch rather than the change I had actually planned to make.
