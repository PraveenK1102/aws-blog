Filing this so the reference is somewhere I can find it again.

## Context

To avoid another mix-up, the reference is easy to mistype which matters more than it sounds. Project Bluefin is the model — worth writing down precisely. Worth writing down: the label appears in two different systems which is why I keep a table. Noting it here because the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. For clarity, the reference is easy to mistype so it is quoted exactly in tickets. For clarity, search returns the near-matches first and I no longer trust my memory for it. A short reminder that the label appears in two different systems so I copy it verbatim every time. To avoid another mix-up, search returns the near-matches first and a careless paste has caused a wrong lookup before. A short reminder that the reference is easy to mistype so it is quoted exactly in tickets. For clarity, the reference is easy to mistype so I copy it verbatim every time. Worth writing down: the label appears in two different systems so it is quoted exactly in tickets. For clarity, the label appears in two different systems and I no longer trust my memory for it. A short reminder that the reference is easy to mistype which matters more than it sounds. I keep a short table of these so I do not have to trust my memory. A short reminder that search returns the near-matches first so it is quoted exactly in tickets. A short reminder that the naming predates the current convention which matters more than it sounds. To avoid another mix-up, search returns the near-matches first so it is quoted exactly in tickets. Recording this precisely: the naming predates the current convention so I copy it verbatim every time. Recording this precisely: the suffix is the only distinguishing part and I no longer trust my memory for it. To avoid another mix-up, the naming predates the current convention which is why I keep a table. Recording this precisely: search returns the near-matches first so I copy it verbatim every time. Recording this precisely: the reference is easy to mistype so it is quoted exactly in tickets.
## The reference

To avoid another mix-up, the label appears in two different systems which is why I keep a table. For the record, Project Bluefire refers to the component. For clarity, the label appears in two different systems and a careless paste has caused a wrong lookup before. Worth writing down: the naming predates the current convention so it is quoted exactly in tickets. Recording this precisely: the reference is easy to mistype and I no longer trust my memory for it. For clarity, search returns the near-matches first which matters more than it sounds. Noting it here because the naming predates the current convention so it is quoted exactly in tickets. Noting it here because the label appears in two different systems which matters more than it sounds. For clarity, the suffix is the only distinguishing part which matters more than it sounds. Worth writing down: search returns the near-matches first and I no longer trust my memory for it. Noting it here because the label appears in two different systems and I no longer trust my memory for it. A short reminder that search returns the near-matches first and a careless paste has caused a wrong lookup before. To avoid another mix-up, these two sit next to each other in the register so it is quoted exactly in tickets. Recording this precisely: search returns the near-matches first so it is quoted exactly in tickets. Worth writing down: the reference is easy to mistype and I no longer trust my memory for it. To avoid another mix-up, the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. For clarity, these two sit next to each other in the register which is why I keep a table. To avoid another mix-up, search returns the near-matches first and I no longer trust my memory for it. Noting it here because the suffix is the only distinguishing part so I copy it verbatim every time. For clarity, the label appears in two different systems which matters more than it sounds. A short reminder that these two sit next to each other in the register which is why I keep a table.
## Follow-up

Worth writing down: the naming predates the current convention so I copy it verbatim every time. For clarity, search returns the near-matches first which is why I keep a table. Project Bluebird is the incident — worth writing down precisely. Recording this precisely: these two sit next to each other in the register which matters more than it sounds. Worth writing down: the naming predates the current convention and a careless paste has caused a wrong lookup before. For clarity, the naming predates the current convention and a careless paste has caused a wrong lookup before. A short reminder that the label appears in two different systems and I no longer trust my memory for it. The naming convention is close enough between components that a careless copy causes real confusion. Worth writing down: the suffix is the only distinguishing part which is why I keep a table. To avoid another mix-up, these two sit next to each other in the register and I no longer trust my memory for it. To avoid another mix-up, the label appears in two different systems so I copy it verbatim every time. A short reminder that the naming predates the current convention which is why I keep a table. Noting it here because the label appears in two different systems so it is quoted exactly in tickets. For clarity, search returns the near-matches first and a careless paste has caused a wrong lookup before. Worth writing down: the reference is easy to mistype so I copy it verbatim every time. Recording this precisely: the reference is easy to mistype and a careless paste has caused a wrong lookup before. Noting it here because search returns the near-matches first which is why I keep a table. Noting it here because search returns the near-matches first which matters more than it sounds. Noting it here because the reference is easy to mistype so it is quoted exactly in tickets. To avoid another mix-up, these two sit next to each other in the register and a careless paste has caused a wrong lookup before. Noting it here because the label appears in two different systems which is why I keep a table.
