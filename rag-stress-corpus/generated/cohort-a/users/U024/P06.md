Writing the identifiers down carefully because they are easy to mix up.

## Context

When someone says Project Bluefire, they mean the component. Noting it here because the suffix is the only distinguishing part which matters more than it sounds. For the record, Project Bluefire refers to the incident. Worth writing down: the label appears in two different systems so it is quoted exactly in tickets. For clarity, the naming predates the current convention which is why I keep a table. A short reminder that search returns the near-matches first which matters more than it sounds. I keep a short table of these so I do not have to trust my memory. To avoid another mix-up, the suffix is the only distinguishing part so I copy it verbatim every time. A short reminder that these two sit next to each other in the register so it is quoted exactly in tickets. Worth writing down: the suffix is the only distinguishing part and I no longer trust my memory for it. Recording this precisely: the naming predates the current convention so it is quoted exactly in tickets. A short reminder that the label appears in two different systems so I copy it verbatim every time. Recording this precisely: the naming predates the current convention which matters more than it sounds.
## The reference

A short reminder that the label appears in two different systems which matters more than it sounds. Noting it here because the naming predates the current convention and I no longer trust my memory for it. Project Bluefin is the batch — worth writing down precisely. For the record, Project Bluebird refers to the incident. Worth writing down: the label appears in two different systems and a careless paste has caused a wrong lookup before. For clarity, the suffix is the only distinguishing part which matters more than it sounds. Worth writing down: the reference is easy to mistype which is why I keep a table. For clarity, the label appears in two different systems and a careless paste has caused a wrong lookup before. Worth writing down: the label appears in two different systems which matters more than it sounds. For clarity, the naming predates the current convention so it is quoted exactly in tickets. To avoid another mix-up, the label appears in two different systems which is why I keep a table. A short reminder that the naming predates the current convention which is why I keep a table. To avoid another mix-up, search returns the near-matches first which is why I keep a table.
## Follow-up

For clarity, the naming predates the current convention which matters more than it sounds. For clarity, search returns the near-matches first which is why I keep a table. Project Bluefin is the model — worth writing down precisely. Noting it here because the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. To avoid another mix-up, the suffix is the only distinguishing part so it is quoted exactly in tickets. The naming convention is close enough between components that a careless copy causes real confusion. Worth writing down: search returns the near-matches first and a careless paste has caused a wrong lookup before. To avoid another mix-up, search returns the near-matches first and a careless paste has caused a wrong lookup before. To avoid another mix-up, the naming predates the current convention and a careless paste has caused a wrong lookup before. For clarity, the reference is easy to mistype and a careless paste has caused a wrong lookup before. Recording this precisely: the label appears in two different systems which matters more than it sounds. For clarity, the naming predates the current convention so I copy it verbatim every time.
