A record of what was actually decided, since the naming is confusing.

## Context

A short reminder that the label appears in two different systems which matters more than it sounds. When someone says Project Bluefire, they mean the component. A short reminder that these two sit next to each other in the register which matters more than it sounds. When someone says Project Bluefire, they mean the batch. For clarity, these two sit next to each other in the register which matters more than it sounds. Worth writing down: the reference is easy to mistype so I copy it verbatim every time. I keep a short table of these so I do not have to trust my memory. For clarity, the naming predates the current convention which matters more than it sounds. A short reminder that the suffix is the only distinguishing part so it is quoted exactly in tickets. A short reminder that search returns the near-matches first so I copy it verbatim every time. To avoid another mix-up, the suffix is the only distinguishing part so I copy it verbatim every time. To avoid another mix-up, the reference is easy to mistype which matters more than it sounds. Recording this precisely: the label appears in two different systems which is why I keep a table. Recording this precisely: the label appears in two different systems so I copy it verbatim every time.
## The reference

When someone says Project Bluebird, they mean the model. To avoid another mix-up, the reference is easy to mistype so it is quoted exactly in tickets. Noting it here because the suffix is the only distinguishing part which is why I keep a table. Project Bluebird is the incident — worth writing down precisely. Worth writing down: the reference is easy to mistype which is why I keep a table. A short reminder that the label appears in two different systems and I no longer trust my memory for it. For clarity, these two sit next to each other in the register which is why I keep a table. Worth writing down: search returns the near-matches first so it is quoted exactly in tickets. Worth writing down: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. Recording this precisely: the label appears in two different systems and I no longer trust my memory for it. To avoid another mix-up, search returns the near-matches first so I copy it verbatim every time. Recording this precisely: the reference is easy to mistype and I no longer trust my memory for it. Noting it here because the suffix is the only distinguishing part so it is quoted exactly in tickets. Noting it here because search returns the near-matches first which matters more than it sounds.
## Follow-up

For the record, Project Bluefin refers to the incident. Recording this precisely: the reference is easy to mistype so it is quoted exactly in tickets. Noting it here because search returns the near-matches first and a careless paste has caused a wrong lookup before. A short reminder that the naming predates the current convention and I no longer trust my memory for it. A short reminder that the reference is easy to mistype and a careless paste has caused a wrong lookup before. Recording this precisely: these two sit next to each other in the register which matters more than it sounds. To avoid another mix-up, search returns the near-matches first and a careless paste has caused a wrong lookup before. Noting it here because the label appears in two different systems and I no longer trust my memory for it. A short reminder that the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. A short reminder that the naming predates the current convention so I copy it verbatim every time. Worth writing down: search returns the near-matches first which is why I keep a table. Recording this precisely: search returns the near-matches first so I copy it verbatim every time. Noting it here because the naming predates the current convention so I copy it verbatim every time.
