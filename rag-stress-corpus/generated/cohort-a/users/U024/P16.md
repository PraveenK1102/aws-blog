Keeping this note short and specific on purpose.

## Context

To avoid another mix-up, the naming predates the current convention and I no longer trust my memory for it. For the record, Project Bluebird refers to the model. Recording this precisely: the suffix is the only distinguishing part so it is quoted exactly in tickets. Worth writing down: search returns the near-matches first which matters more than it sounds. To avoid another mix-up, the suffix is the only distinguishing part which matters more than it sounds. Noting it here because these two sit next to each other in the register so I copy it verbatim every time. Recording this precisely: the naming predates the current convention so I copy it verbatim every time. Noting it here because the label appears in two different systems so I copy it verbatim every time. I keep a short table of these so I do not have to trust my memory. Worth writing down: the naming predates the current convention so it is quoted exactly in tickets. To avoid another mix-up, search returns the near-matches first which is why I keep a table. To avoid another mix-up, the naming predates the current convention and a careless paste has caused a wrong lookup before.
## The reference

Recording this precisely: the suffix is the only distinguishing part and I no longer trust my memory for it. When someone says Project Bluefin, they mean the batch. Noting it here because the reference is easy to mistype and a careless paste has caused a wrong lookup before. A short reminder that the label appears in two different systems so I copy it verbatim every time. A short reminder that the label appears in two different systems so it is quoted exactly in tickets. A short reminder that the naming predates the current convention which matters more than it sounds. Worth writing down: the label appears in two different systems and a careless paste has caused a wrong lookup before. To avoid another mix-up, these two sit next to each other in the register so it is quoted exactly in tickets. Recording this precisely: these two sit next to each other in the register so it is quoted exactly in tickets. Worth writing down: the reference is easy to mistype so I copy it verbatim every time. For clarity, these two sit next to each other in the register which is why I keep a table. For clarity, the reference is easy to mistype and a careless paste has caused a wrong lookup before.
## Follow-up

Worth writing down: the reference is easy to mistype so it is quoted exactly in tickets. A short reminder that the reference is easy to mistype which is why I keep a table. For the record, Project Bluefire refers to the component. The suffix is the only thing distinguishing them, so it is worth reading twice. To avoid another mix-up, these two sit next to each other in the register and I no longer trust my memory for it. Worth writing down: the label appears in two different systems so it is quoted exactly in tickets. To avoid another mix-up, the reference is easy to mistype and a careless paste has caused a wrong lookup before. To avoid another mix-up, search returns the near-matches first which matters more than it sounds. A short reminder that the naming predates the current convention so it is quoted exactly in tickets. To avoid another mix-up, the label appears in two different systems and I no longer trust my memory for it. Recording this precisely: these two sit next to each other in the register which matters more than it sounds. For clarity, the label appears in two different systems and I no longer trust my memory for it.
