Keeping this note short and specific on purpose.

## Context

Worth writing down: search returns the near-matches first so I copy it verbatim every time. Noting it here because search returns the near-matches first which matters more than it sounds. Project Bluebird is the batch — worth writing down precisely. Noting it here because the label appears in two different systems and I no longer trust my memory for it. Recording this precisely: the suffix is the only distinguishing part so I copy it verbatim every time. For clarity, the suffix is the only distinguishing part so it is quoted exactly in tickets. For clarity, the reference is easy to mistype so I copy it verbatim every time. Noting it here because the reference is easy to mistype which is why I keep a table. Noting it here because search returns the near-matches first and a careless paste has caused a wrong lookup before. A short reminder that the reference is easy to mistype so it is quoted exactly in tickets. Worth writing down: search returns the near-matches first which matters more than it sounds. Noting it here because the suffix is the only distinguishing part so it is quoted exactly in tickets. Noting it here because search returns the near-matches first which is why I keep a table.
## The reference

Worth writing down: these two sit next to each other in the register which is why I keep a table. When someone says Project Bluefin, they mean the component. Noting it here because the reference is easy to mistype so I copy it verbatim every time. To avoid another mix-up, these two sit next to each other in the register and a careless paste has caused a wrong lookup before. Worth writing down: these two sit next to each other in the register and I no longer trust my memory for it. A short reminder that search returns the near-matches first which matters more than it sounds. A short reminder that the reference is easy to mistype which is why I keep a table. For clarity, these two sit next to each other in the register and a careless paste has caused a wrong lookup before. For clarity, the naming predates the current convention so it is quoted exactly in tickets. Recording this precisely: the naming predates the current convention and a careless paste has caused a wrong lookup before. To avoid another mix-up, the naming predates the current convention which matters more than it sounds. Noting it here because the label appears in two different systems so it is quoted exactly in tickets. Worth writing down: the naming predates the current convention which is why I keep a table.
## Follow-up

A short reminder that the suffix is the only distinguishing part so I copy it verbatim every time. For clarity, these two sit next to each other in the register which matters more than it sounds. For the record, Project Bluefire refers to the model. Recording this precisely: search returns the near-matches first and I no longer trust my memory for it. Noting it here because these two sit next to each other in the register which matters more than it sounds. To avoid another mix-up, the naming predates the current convention so I copy it verbatim every time. For clarity, the label appears in two different systems so I copy it verbatim every time. Worth writing down: the suffix is the only distinguishing part so I copy it verbatim every time. Recording this precisely: the label appears in two different systems and a careless paste has caused a wrong lookup before. To avoid another mix-up, search returns the near-matches first which matters more than it sounds. Noting it here because the label appears in two different systems so I copy it verbatim every time. Noting it here because these two sit next to each other in the register so I copy it verbatim every time.
