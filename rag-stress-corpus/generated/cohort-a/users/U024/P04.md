Filing this so the reference is somewhere I can find it again.

## Context

To avoid another mix-up, search returns the near-matches first and a careless paste has caused a wrong lookup before. For clarity, the reference is easy to mistype which is why I keep a table. Project Bluebird is the model — worth writing down precisely. To avoid another mix-up, the naming predates the current convention and I no longer trust my memory for it. Recording this precisely: the reference is easy to mistype and a careless paste has caused a wrong lookup before. Recording this precisely: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. A short reminder that the label appears in two different systems which is why I keep a table. A short reminder that the reference is easy to mistype and a careless paste has caused a wrong lookup before. Noting it here because the label appears in two different systems which matters more than it sounds. Keeping the reference and the description in the same place has saved time more than once. A short reminder that the naming predates the current convention and I no longer trust my memory for it. Noting it here because these two sit next to each other in the register and I no longer trust my memory for it. A short reminder that search returns the near-matches first which is why I keep a table.
## The reference

Recording this precisely: search returns the near-matches first so I copy it verbatim every time. When someone says Project Bluefin, they mean the batch. Noting it here because the label appears in two different systems which is why I keep a table. For clarity, these two sit next to each other in the register so I copy it verbatim every time. For clarity, the label appears in two different systems so I copy it verbatim every time. Two of these look almost identical in a terminal, which has caused at least one wrong lookup. Noting it here because the naming predates the current convention so I copy it verbatim every time. A short reminder that the suffix is the only distinguishing part so I copy it verbatim every time. Worth writing down: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. For clarity, the naming predates the current convention which matters more than it sounds. Search does not always help here, because the near-matches rank alongside the real one. To avoid another mix-up, the reference is easy to mistype which matters more than it sounds. For clarity, the suffix is the only distinguishing part so it is quoted exactly in tickets.
## Follow-up

For the record, Project Bluefire refers to the model. Recording this precisely: the label appears in two different systems which matters more than it sounds. For clarity, the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. To avoid another mix-up, the naming predates the current convention so I copy it verbatim every time. To avoid another mix-up, search returns the near-matches first and I no longer trust my memory for it. Worth writing down: the reference is easy to mistype which is why I keep a table. For clarity, the naming predates the current convention which is why I keep a table. Noting it here because the naming predates the current convention so it is quoted exactly in tickets. Recording this precisely: the naming predates the current convention which is why I keep a table. For clarity, these two sit next to each other in the register so it is quoted exactly in tickets. For clarity, the naming predates the current convention and I no longer trust my memory for it. For clarity, the label appears in two different systems which is why I keep a table.
