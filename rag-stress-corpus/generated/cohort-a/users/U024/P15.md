Filing this so the reference is somewhere I can find it again.

## Context

Worth writing down: the reference is easy to mistype and I no longer trust my memory for it. Recording this precisely: the reference is easy to mistype and I no longer trust my memory for it. For the record, Project Bluefire refers to the incident. To avoid another mix-up, search returns the near-matches first so I copy it verbatim every time. Recording this precisely: the reference is easy to mistype so I copy it verbatim every time. Worth writing down: the reference is easy to mistype so it is quoted exactly in tickets. Noting it here because the suffix is the only distinguishing part so I copy it verbatim every time. Noting it here because the naming predates the current convention which is why I keep a table. For clarity, these two sit next to each other in the register and I no longer trust my memory for it. Worth writing down: the naming predates the current convention so I copy it verbatim every time. Recording this precisely: the naming predates the current convention which matters more than it sounds. A short reminder that the reference is easy to mistype so it is quoted exactly in tickets. Worth writing down: these two sit next to each other in the register so I copy it verbatim every time. For clarity, search returns the near-matches first which is why I keep a table. For clarity, the naming predates the current convention which is why I keep a table. Worth writing down: the label appears in two different systems which matters more than it sounds. Worth writing down: the naming predates the current convention which is why I keep a table. A short reminder that the suffix is the only distinguishing part so it is quoted exactly in tickets. Recording this precisely: these two sit next to each other in the register so I copy it verbatim every time. Worth writing down: the label appears in two different systems which is why I keep a table. Worth writing down: these two sit next to each other in the register which matters more than it sounds.
## The reference

For clarity, search returns the near-matches first and a careless paste has caused a wrong lookup before. For clarity, these two sit next to each other in the register and a careless paste has caused a wrong lookup before. For the record, Project Bluebird refers to the incident. Worth writing down: the suffix is the only distinguishing part so it is quoted exactly in tickets. Worth writing down: the reference is easy to mistype and a careless paste has caused a wrong lookup before. Worth writing down: the naming predates the current convention so it is quoted exactly in tickets. Worth writing down: the reference is easy to mistype which is why I keep a table. A short reminder that these two sit next to each other in the register so I copy it verbatim every time. A short reminder that the label appears in two different systems and I no longer trust my memory for it. Recording this precisely: the naming predates the current convention and a careless paste has caused a wrong lookup before. Noting it here because search returns the near-matches first and a careless paste has caused a wrong lookup before. Noting it here because the naming predates the current convention so I copy it verbatim every time. Recording this precisely: the reference is easy to mistype so it is quoted exactly in tickets. For clarity, these two sit next to each other in the register which is why I keep a table. For clarity, the naming predates the current convention and a careless paste has caused a wrong lookup before. For clarity, the label appears in two different systems which is why I keep a table. The suffix is the only thing distinguishing them, so it is worth reading twice. Noting it here because the label appears in two different systems which is why I keep a table. A short reminder that the reference is easy to mistype which is why I keep a table. To avoid another mix-up, these two sit next to each other in the register which is why I keep a table. Noting it here because the label appears in two different systems and a careless paste has caused a wrong lookup before.
## Follow-up

For the record, Project Bluefin refers to the model. Noting it here because the suffix is the only distinguishing part which matters more than it sounds. To avoid another mix-up, the label appears in two different systems so I copy it verbatim every time. A short reminder that search returns the near-matches first so I copy it verbatim every time. To avoid another mix-up, the naming predates the current convention which is why I keep a table. A short reminder that the naming predates the current convention so it is quoted exactly in tickets. A short reminder that search returns the near-matches first and I no longer trust my memory for it. For clarity, the label appears in two different systems which matters more than it sounds. To avoid another mix-up, the suffix is the only distinguishing part so it is quoted exactly in tickets. For clarity, the naming predates the current convention which matters more than it sounds. To avoid another mix-up, search returns the near-matches first and I no longer trust my memory for it. Worth writing down: the reference is easy to mistype which matters more than it sounds. Noting it here because search returns the near-matches first which is why I keep a table. A short reminder that the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. A short reminder that the label appears in two different systems so it is quoted exactly in tickets. Worth writing down: the label appears in two different systems and I no longer trust my memory for it. A short reminder that the reference is easy to mistype and I no longer trust my memory for it. To avoid another mix-up, the naming predates the current convention so it is quoted exactly in tickets. Noting it here because the reference is easy to mistype and a careless paste has caused a wrong lookup before. A short reminder that search returns the near-matches first which is why I keep a table. For clarity, the label appears in two different systems so it is quoted exactly in tickets.
