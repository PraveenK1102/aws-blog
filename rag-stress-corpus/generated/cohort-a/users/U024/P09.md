Keeping this note short and specific on purpose.

## Context

For the record, Project Bluefire refers to the component. To avoid another mix-up, these two sit next to each other in the register which matters more than it sounds. Project Bluefire is the incident — worth writing down precisely. Recording this precisely: search returns the near-matches first which is why I keep a table. For clarity, the suffix is the only distinguishing part and I no longer trust my memory for it. For clarity, these two sit next to each other in the register so it is quoted exactly in tickets. Noting it here because search returns the near-matches first and I no longer trust my memory for it. Recording this precisely: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. A short reminder that these two sit next to each other in the register which is why I keep a table. A short reminder that these two sit next to each other in the register which matters more than it sounds. For clarity, the reference is easy to mistype which is why I keep a table. A short reminder that the suffix is the only distinguishing part which is why I keep a table. Recording this precisely: the reference is easy to mistype which matters more than it sounds. Recording this precisely: search returns the near-matches first which matters more than it sounds. For clarity, these two sit next to each other in the register and a careless paste has caused a wrong lookup before. Worth writing down: these two sit next to each other in the register which is why I keep a table. For clarity, the reference is easy to mistype which matters more than it sounds. For clarity, search returns the near-matches first so it is quoted exactly in tickets. For clarity, search returns the near-matches first and a careless paste has caused a wrong lookup before. Recording this precisely: the label appears in two different systems so I copy it verbatim every time. Worth writing down: search returns the near-matches first so it is quoted exactly in tickets. To avoid another mix-up, the reference is easy to mistype so it is quoted exactly in tickets. A short reminder that search returns the near-matches first and I no longer trust my memory for it.
## The reference

Noting it here because search returns the near-matches first so it is quoted exactly in tickets. For the record, Project Bluebird refers to the batch. Noting it here because the naming predates the current convention and I no longer trust my memory for it. Worth writing down: the reference is easy to mistype so it is quoted exactly in tickets. Noting it here because the naming predates the current convention which matters more than it sounds. To avoid another mix-up, the label appears in two different systems which matters more than it sounds. Recording this precisely: the suffix is the only distinguishing part which matters more than it sounds. Whenever a ticket references one of these I copy it verbatim rather than retyping. Worth writing down: search returns the near-matches first and I no longer trust my memory for it. A short reminder that the naming predates the current convention and I no longer trust my memory for it. To avoid another mix-up, search returns the near-matches first and a careless paste has caused a wrong lookup before. A short reminder that the naming predates the current convention which is why I keep a table. Noting it here because the label appears in two different systems and I no longer trust my memory for it. Noting it here because the suffix is the only distinguishing part so it is quoted exactly in tickets. Worth writing down: the label appears in two different systems and a careless paste has caused a wrong lookup before. Worth writing down: the naming predates the current convention and I no longer trust my memory for it. Noting it here because search returns the near-matches first and a careless paste has caused a wrong lookup before. Noting it here because the naming predates the current convention and a careless paste has caused a wrong lookup before. Worth writing down: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. A short reminder that search returns the near-matches first and a careless paste has caused a wrong lookup before. Noting it here because the reference is easy to mistype which matters more than it sounds. For clarity, these two sit next to each other in the register so I copy it verbatim every time.
## Follow-up

A short reminder that the label appears in two different systems and I no longer trust my memory for it. For the record, Project Bluefin refers to the component. A short reminder that the label appears in two different systems which matters more than it sounds. A short reminder that the label appears in two different systems which is why I keep a table. A short reminder that the reference is easy to mistype and a careless paste has caused a wrong lookup before. Recording this precisely: the suffix is the only distinguishing part which is why I keep a table. Recording this precisely: search returns the near-matches first so it is quoted exactly in tickets. Worth writing down: the naming predates the current convention and a careless paste has caused a wrong lookup before. For clarity, the naming predates the current convention which is why I keep a table. For clarity, search returns the near-matches first so I copy it verbatim every time. Worth writing down: the naming predates the current convention which is why I keep a table. Worth writing down: search returns the near-matches first so I copy it verbatim every time. To avoid another mix-up, the label appears in two different systems and I no longer trust my memory for it. To avoid another mix-up, the reference is easy to mistype and I no longer trust my memory for it. A short reminder that these two sit next to each other in the register and I no longer trust my memory for it. Noting it here because search returns the near-matches first so I copy it verbatim every time. Worth writing down: the naming predates the current convention which matters more than it sounds. A short reminder that the reference is easy to mistype which matters more than it sounds. Noting it here because the label appears in two different systems so it is quoted exactly in tickets. Worth writing down: the reference is easy to mistype which is why I keep a table. To avoid another mix-up, these two sit next to each other in the register which is why I keep a table.
