Filing this so the reference is somewhere I can find it again.

## Context

Worth writing down: the label appears in two different systems so I copy it verbatim every time. For the record, Project Bluefire refers to the incident. A short reminder that the naming predates the current convention so it is quoted exactly in tickets. Project Bluebird is the component — worth writing down precisely. A short reminder that the reference is easy to mistype and I no longer trust my memory for it. Worth writing down: the label appears in two different systems and a careless paste has caused a wrong lookup before. A short reminder that the naming predates the current convention so I copy it verbatim every time. To avoid another mix-up, the reference is easy to mistype so I copy it verbatim every time. Noting it here because these two sit next to each other in the register so I copy it verbatim every time. Noting it here because the naming predates the current convention so I copy it verbatim every time. Noting it here because the naming predates the current convention which matters more than it sounds. Noting it here because the label appears in two different systems so it is quoted exactly in tickets. Two of these look almost identical in a terminal, which has caused at least one wrong lookup. A short reminder that these two sit next to each other in the register so I copy it verbatim every time.
## The reference

To avoid another mix-up, the naming predates the current convention so I copy it verbatim every time. Noting it here because the naming predates the current convention so it is quoted exactly in tickets. When someone says Project Bluebird, they mean the incident. To avoid another mix-up, the reference is easy to mistype and I no longer trust my memory for it. Worth writing down: the reference is easy to mistype so it is quoted exactly in tickets. For clarity, the naming predates the current convention which is why I keep a table. Worth writing down: search returns the near-matches first and I no longer trust my memory for it. To avoid another mix-up, the suffix is the only distinguishing part which matters more than it sounds. A short reminder that the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. To avoid another mix-up, these two sit next to each other in the register which matters more than it sounds. Recording this precisely: the suffix is the only distinguishing part which matters more than it sounds. To avoid another mix-up, the reference is easy to mistype and a careless paste has caused a wrong lookup before. Recording this precisely: these two sit next to each other in the register and I no longer trust my memory for it.
## Follow-up

Noting it here because the suffix is the only distinguishing part so it is quoted exactly in tickets. For clarity, search returns the near-matches first and I no longer trust my memory for it. For the record, Project Bluefin refers to the batch. Recording this precisely: the suffix is the only distinguishing part so it is quoted exactly in tickets. For clarity, the label appears in two different systems and a careless paste has caused a wrong lookup before. Worth writing down: the label appears in two different systems and I no longer trust my memory for it. Recording this precisely: the reference is easy to mistype and I no longer trust my memory for it. A short reminder that the reference is easy to mistype so I copy it verbatim every time. For clarity, the label appears in two different systems and I no longer trust my memory for it. Worth writing down: search returns the near-matches first so it is quoted exactly in tickets. A short reminder that the reference is easy to mistype which matters more than it sounds. Noting it here because search returns the near-matches first and a careless paste has caused a wrong lookup before.
