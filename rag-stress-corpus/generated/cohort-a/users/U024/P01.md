Filing this so the reference is somewhere I can find it again.

## Context

Project Bluebird is the component — worth writing down precisely. For the record, Project Bluebird refers to the model. Worth writing down: the reference is easy to mistype and a careless paste has caused a wrong lookup before. A short reminder that the label appears in two different systems so it is quoted exactly in tickets. Noting it here because these two sit next to each other in the register which is why I keep a table. For clarity, the naming predates the current convention so it is quoted exactly in tickets. To avoid another mix-up, the reference is easy to mistype so it is quoted exactly in tickets. To avoid another mix-up, the naming predates the current convention so I copy it verbatim every time. A short reminder that the naming predates the current convention and a careless paste has caused a wrong lookup before. Worth writing down: the label appears in two different systems which is why I keep a table. Recording this precisely: the reference is easy to mistype and I no longer trust my memory for it. For clarity, the label appears in two different systems so it is quoted exactly in tickets. Recording this precisely: the reference is easy to mistype so it is quoted exactly in tickets. For clarity, these two sit next to each other in the register which is why I keep a table. A short reminder that these two sit next to each other in the register so I copy it verbatim every time. A short reminder that the label appears in two different systems which is why I keep a table.
## The reference

To avoid another mix-up, the naming predates the current convention and a careless paste has caused a wrong lookup before. For the record, Project Bluefin refers to the component. To avoid another mix-up, the reference is easy to mistype which matters more than it sounds. Keeping the reference and the description in the same place has saved time more than once. Noting it here because the suffix is the only distinguishing part so it is quoted exactly in tickets. Noting it here because search returns the near-matches first so I copy it verbatim every time. Worth writing down: these two sit next to each other in the register so I copy it verbatim every time. Recording this precisely: search returns the near-matches first which matters more than it sounds. Noting it here because these two sit next to each other in the register and a careless paste has caused a wrong lookup before. Recording this precisely: the naming predates the current convention which matters more than it sounds. Two of these look almost identical in a terminal, which has caused at least one wrong lookup. A short reminder that the suffix is the only distinguishing part and I no longer trust my memory for it. Recording this precisely: the suffix is the only distinguishing part and I no longer trust my memory for it. For clarity, search returns the near-matches first so it is quoted exactly in tickets.
## Follow-up

Noting it here because the label appears in two different systems and I no longer trust my memory for it. A short reminder that search returns the near-matches first so I copy it verbatim every time. Project Bluefire is the model — worth writing down precisely. A short reminder that the naming predates the current convention so it is quoted exactly in tickets. To avoid another mix-up, search returns the near-matches first and a careless paste has caused a wrong lookup before. To avoid another mix-up, the reference is easy to mistype so I copy it verbatim every time. Worth writing down: search returns the near-matches first which is why I keep a table. Worth writing down: the suffix is the only distinguishing part which is why I keep a table. Noting it here because search returns the near-matches first and a careless paste has caused a wrong lookup before. To avoid another mix-up, the label appears in two different systems so I copy it verbatim every time. To avoid another mix-up, the reference is easy to mistype and a careless paste has caused a wrong lookup before. Noting it here because the reference is easy to mistype and I no longer trust my memory for it. Noting it here because the label appears in two different systems which matters more than it sounds. A short reminder that search returns the near-matches first so it is quoted exactly in tickets.
