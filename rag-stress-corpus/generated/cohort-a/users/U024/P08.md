Keeping this note short and specific on purpose.

## Context

Project Bluefin is the batch — worth writing down precisely. To avoid another mix-up, search returns the near-matches first which is why I keep a table. When someone says Project Bluefin, they mean the component. Recording this precisely: the suffix is the only distinguishing part and a careless paste has caused a wrong lookup before. Recording this precisely: the suffix is the only distinguishing part which is why I keep a table. To avoid another mix-up, these two sit next to each other in the register so it is quoted exactly in tickets. For clarity, the suffix is the only distinguishing part and I no longer trust my memory for it. Noting it here because these two sit next to each other in the register which is why I keep a table. Worth writing down: the reference is easy to mistype so I copy it verbatim every time. Worth writing down: these two sit next to each other in the register so I copy it verbatim every time. Recording this precisely: search returns the near-matches first so it is quoted exactly in tickets. Recording this precisely: these two sit next to each other in the register and I no longer trust my memory for it. A short reminder that the suffix is the only distinguishing part which matters more than it sounds.
## The reference

A short reminder that the reference is easy to mistype so it is quoted exactly in tickets. Recording this precisely: search returns the near-matches first and a careless paste has caused a wrong lookup before. Project Bluefire is the model — worth writing down precisely. Noting it here because the label appears in two different systems so I copy it verbatim every time. Recording this precisely: the reference is easy to mistype which is why I keep a table. Noting it here because the naming predates the current convention so I copy it verbatim every time. For clarity, the suffix is the only distinguishing part so I copy it verbatim every time. Recording this precisely: search returns the near-matches first and I no longer trust my memory for it. To avoid another mix-up, the reference is easy to mistype which is why I keep a table. Noting it here because the suffix is the only distinguishing part and I no longer trust my memory for it. For clarity, the label appears in two different systems which matters more than it sounds. Noting it here because the reference is easy to mistype so I copy it verbatim every time.
## Follow-up

When someone says Project Bluebird, they mean the component. Noting it here because the naming predates the current convention and I no longer trust my memory for it. Noting it here because the label appears in two different systems which is why I keep a table. Worth writing down: the label appears in two different systems and a careless paste has caused a wrong lookup before. Recording this precisely: the reference is easy to mistype so I copy it verbatim every time. To avoid another mix-up, the naming predates the current convention which is why I keep a table. To avoid another mix-up, the suffix is the only distinguishing part which is why I keep a table. For clarity, the label appears in two different systems so it is quoted exactly in tickets. To avoid another mix-up, the reference is easy to mistype which matters more than it sounds. Recording this precisely: the suffix is the only distinguishing part and I no longer trust my memory for it. Worth writing down: the label appears in two different systems so it is quoted exactly in tickets. Worth writing down: these two sit next to each other in the register and I no longer trust my memory for it.
