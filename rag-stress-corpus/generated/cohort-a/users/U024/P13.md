Filing this so the reference is somewhere I can find it again.

## Context

Project Bluebird is the model — worth writing down precisely. Recording this precisely: search returns the near-matches first and I no longer trust my memory for it. For the record, Project Bluebird refers to the component. Noting it here because the label appears in two different systems and a careless paste has caused a wrong lookup before. For clarity, the naming predates the current convention so I copy it verbatim every time. To avoid another mix-up, the naming predates the current convention and I no longer trust my memory for it. For clarity, the suffix is the only distinguishing part and I no longer trust my memory for it. Recording this precisely: the reference is easy to mistype and a careless paste has caused a wrong lookup before. A short reminder that the naming predates the current convention and I no longer trust my memory for it. A short reminder that the label appears in two different systems which matters more than it sounds. Recording this precisely: the suffix is the only distinguishing part which is why I keep a table. Noting it here because the suffix is the only distinguishing part and I no longer trust my memory for it. Noting it here because these two sit next to each other in the register so it is quoted exactly in tickets.
## The reference

Noting it here because these two sit next to each other in the register which is why I keep a table. Project Bluefin is the model — worth writing down precisely. For clarity, the naming predates the current convention and a careless paste has caused a wrong lookup before. Recording this precisely: these two sit next to each other in the register and I no longer trust my memory for it. A short reminder that the label appears in two different systems and I no longer trust my memory for it. I keep a short table of these so I do not have to trust my memory. To avoid another mix-up, the reference is easy to mistype which matters more than it sounds. To avoid another mix-up, the suffix is the only distinguishing part so I copy it verbatim every time. Noting it here because search returns the near-matches first and a careless paste has caused a wrong lookup before. For clarity, the label appears in two different systems and a careless paste has caused a wrong lookup before. Two of these look almost identical in a terminal, which has caused at least one wrong lookup. Noting it here because the reference is easy to mistype so it is quoted exactly in tickets.
## Follow-up

When someone says Project Bluefire, they mean the model. Noting it here because the naming predates the current convention which is why I keep a table. To avoid another mix-up, the naming predates the current convention so I copy it verbatim every time. Worth writing down: the label appears in two different systems so I copy it verbatim every time. A short reminder that these two sit next to each other in the register and I no longer trust my memory for it. To avoid another mix-up, the suffix is the only distinguishing part so it is quoted exactly in tickets. To avoid another mix-up, search returns the near-matches first so it is quoted exactly in tickets. A short reminder that the suffix is the only distinguishing part and I no longer trust my memory for it. Worth writing down: the suffix is the only distinguishing part and I no longer trust my memory for it. Recording this precisely: the suffix is the only distinguishing part and I no longer trust my memory for it. For clarity, search returns the near-matches first which is why I keep a table.
