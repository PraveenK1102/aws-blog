## The problem

I do not trust an improvement that only appears on examples I picked myself. Running it end to end put retrieval at 30%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day.

Latency landed at 5% once I measured it end to end. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. The result held up in staging and quietly regressed the moment traffic patterns changed. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Instrumenting the embedding cache cost a day and saved considerably more than that later. Cleaning the AWS inputs produced a larger gain than any change to the model did. I keep a short list of results I do not believe yet, and revisit it weekly. Instrumenting the retrieval step cost a day and saved considerably more than that later. Caching vector search lookups helped until invalidation became the more interesting problem. Cleaning the RAG inputs produced a larger gain than any change to the model did. Tuning RAG helped marginally; fixing how documents were split helped substantially. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Writing the failure cases down first made the design conversation much shorter. Batching improved throughput and made the tail latency considerably harder to reason about. Cleaning the vector search inputs produced a larger gain than any change to the model did. Two configurations differed by one parameter and by far more than one parameter's worth of output.
## What I built

The number I got for latency was 21%. In March I assumed the bottleneck was compute; it was almost entirely I/O. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Comparing against a fixed baseline made the March numbers finally interpretable. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Two configurations differed by one parameter and by far more than one parameter's worth of output. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. I now write the evaluation before the change, which has saved me from at least two false wins. Most of the week went into the embedding cache rather than the change I had actually planned to make. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Splitting the pipeline around the embedding cache made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. Most of the week went into the scoring function rather than the change I had actually planned to make. Cleaning the vector search inputs produced a larger gain than any change to the model did. Splitting the pipeline around the ingestion path made the failures much easier to localise. Splitting the pipeline around the reranking pass made the failures much easier to localise. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Keeping a log of every configuration felt tedious until I needed to explain a regression. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March.
## What I measured

My recorded figure for latency is 42%. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. The interesting failures were the ones where the output read well but cited the wrong source. Keeping a log of every configuration felt tedious until I needed to explain a regression. Every improvement I could not reproduce twice turned out not to be an improvement. Batching improved throughput and made the tail latency considerably harder to reason about. The tempting change was the model; the effective change was how documents were split. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. The first version worked on the sample set and fell apart the moment real RAG inputs arrived. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Tuning React helped marginally; fixing how documents were split helped substantially. Cleaning the React inputs produced a larger gain than any change to the model did. Tuning AWS helped marginally; fixing how documents were split helped substantially. Instrumenting the fallback branch cost a day and saved considerably more than that later. Instrumenting the scoring function cost a day and saved considerably more than that later. Instrumenting the ingestion path cost a day and saved considerably more than that later. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. The interesting failures were the ones where the output read well but cited the wrong source. I now write the evaluation before the change, which has saved me from at least two false wins. Caching vector search lookups helped until invalidation became the more interesting problem.
## What I would change

Because the harness came before the optimisation, I caught a regression that looked like a win. Latency landed at 50% once I measured it end to end. Tuning vector search helped marginally; fixing how documents were split helped substantially. Most of the week went into the ingestion path rather than the change I had actually planned to make. The fallback path had never been exercised, so nobody knew it returned an empty result. Writing the failure cases down first made the design conversation much shorter. Caching RAG lookups helped until invalidation became the more interesting problem. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Caching AWS lookups helped until invalidation became the more interesting problem. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. The first version worked on the sample set and fell apart the moment real React inputs arrived. Caching React lookups helped until invalidation became the more interesting problem. The tempting change was the model; the effective change was how documents were split. Because the harness came before the optimisation, I caught a regression that looked like a win. Comparing against a fixed baseline made the March numbers finally interpretable. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.
