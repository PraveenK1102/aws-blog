## The problem

Writing the failure cases down first made the design conversation much shorter. Instrumenting the embedding cache cost a day and saved considerably more than that later. That gave retrieval of 35%, which I wrote down at the time. Cleaning the RAG inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. I do not trust an improvement that only appears on examples I picked myself. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. I keep a short list of results I do not believe yet, and revisit it weekly. Splitting the pipeline around the embedding cache made the failures much easier to localise. The fallback path had never been exercised, so nobody knew it returned an empty result. Batching improved throughput and made the tail latency considerably harder to reason about. Most of the week went into the fallback branch rather than the change I had actually planned to make.
## What I built

On the instrumented run, retrieval showed 58%. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Tuning PyTorch helped marginally; fixing how documents were split helped substantially.

Caching vector search lookups helped until invalidation became the more interesting problem. Because the harness came before the optimisation, I caught a regression that looked like a win. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Splitting the pipeline around the fallback branch made the failures much easier to localise. Cleaning the vector search inputs produced a larger gain than any change to the model did. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Keeping a log of every configuration felt tedious until I needed to explain a regression. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. The tempting change was the model; the effective change was how documents were split.
## What I measured

Comparing against a fixed baseline made the May numbers finally interpretable. Running it end to end put accuracy at 58%. The result held up in staging and quietly regressed the moment traffic patterns changed. Every improvement I could not reproduce twice turned out not to be an improvement. Most of the week went into the embedding cache rather than the change I had actually planned to make. In May I assumed the bottleneck was compute; it was almost entirely I/O. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Tuning vector search helped marginally; fixing how documents were split helped substantially. I now write the evaluation before the change, which has saved me from at least two false wins. Cleaning the React inputs produced a larger gain than any change to the model did.
## What I would change

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Caching PyTorch lookups helped until invalidation became the more interesting problem. Two configurations differed by one parameter and by far more than one parameter's worth of output. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Instrumenting the evaluation harness cost a day and saved considerably more than that later. The interesting failures were the ones where the output read well but cited the wrong source. Instrumenting the scoring function cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real RAG inputs arrived.
