This started as a small change and turned into a proper investigation.

## The problem

I began by isolating the fallback path nobody exercised and I kept the harness afterwards. Before changing anything I measured the retrieval step on its own and the numbers were not what I expected. Retrieval landed at 35% once I measured it end to end. I began by isolating the assumptions baked into the defaults and the numbers were not what I expected. I began by isolating the slowest stage in the pipeline before touching anything else. The experiment started with the slowest stage in the pipeline which took longer than the fix itself. The experiment started with a small evaluation harness and I kept the harness afterwards. The experiment started with the assumptions baked into the defaults and the numbers were not what I expected. I began by isolating the retrieval step on its own before touching anything else. The experiment started with the ranking of candidate passages before touching anything else. Most of the effort went into the slowest stage in the pipeline before touching anything else.
## What I built

The experiment started with a small evaluation harness and the numbers were not what I expected. I began by isolating the chunking behaviour and the numbers were not what I expected. The number I got for retrieval was 58%. The interesting part turned out to be the retrieval step on its own before touching anything else. I rebuilt the ranking of candidate passages and that alone explained most of the gap. The experiment started with the ranking of candidate passages and that alone explained most of the gap. Before changing anything I measured the slowest stage in the pipeline which made the regression obvious. Before changing anything I measured the assumptions baked into the defaults and I kept the harness afterwards. Most of the effort went into the retrieval step on its own and that alone explained most of the gap. The interesting part turned out to be the ranking of candidate passages which made the regression obvious. Most of the effort went into the way inputs were normalised which made the regression obvious.
## What I measured

Accuracy landed at 58% once I measured it end to end. I began by isolating a small evaluation harness which made the regression obvious. Most of the effort went into the ranking of candidate passages so the comparison would actually mean something. Most of the effort went into the chunking behaviour before touching anything else. Before changing anything I measured the retrieval step on its own because the earlier result was not reproducible. Before changing anything I measured the chunking behaviour and the numbers were not what I expected. Before changing anything I measured the fallback path nobody exercised because the earlier result was not reproducible. Most of the effort went into the chunking behaviour which took longer than the fix itself. Before changing anything I measured the fallback path nobody exercised which made the regression obvious. The interesting part turned out to be the fallback path nobody exercised before touching anything else. I rebuilt the retrieval step on its own which made the regression obvious.
## What I would change

Most of the effort went into a small evaluation harness because the earlier result was not reproducible. I rebuilt the chunking behaviour and I kept the harness afterwards. The interesting part turned out to be a small evaluation harness so the comparison would actually mean something. I rebuilt the ranking of candidate passages and the numbers were not what I expected. I began by isolating the fallback path nobody exercised and the numbers were not what I expected. I rebuilt the retrieval step on its own and the numbers were not what I expected. I rebuilt the assumptions baked into the defaults and that alone explained most of the gap. Latency turned out to be dominated by a step I had assumed was cheap. I began by isolating the assumptions baked into the defaults so the comparison would actually mean something.
