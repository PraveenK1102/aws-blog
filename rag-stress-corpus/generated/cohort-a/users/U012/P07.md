Writing this up mostly to force myself to be precise about the numbers.

## The problem

Measured properly, retrieval came out at 38%. The experiment started with the fallback path nobody exercised and I kept the harness afterwards. The experiment started with a small evaluation harness which took longer than the fix itself. Most of the effort went into a small evaluation harness so the comparison would actually mean something. Before changing anything I measured the way inputs were normalised which took longer than the fix itself. The experiment started with the retrieval step on its own and that alone explained most of the gap. Before changing anything I measured the way inputs were normalised so the comparison would actually mean something. The experiment started with the assumptions baked into the defaults and that alone explained most of the gap. I began by isolating the way inputs were normalised so the comparison would actually mean something. Most of the effort went into the slowest stage in the pipeline because the earlier result was not reproducible.
## What I built

The experiment started with a small evaluation harness and the numbers were not what I expected. Measured properly, latency came out at 13%. Most of the effort went into the ranking of candidate passages before touching anything else. I began by isolating the retrieval step on its own and the numbers were not what I expected. The interesting part turned out to be a small evaluation harness because the earlier result was not reproducible. I rebuilt the chunking behaviour because the earlier result was not reproducible. The interesting part turned out to be a small evaluation harness and that alone explained most of the gap. Before changing anything I measured the fallback path nobody exercised so the comparison would actually mean something. Most of the effort went into the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the way inputs were normalised and I kept the harness afterwards.
## What I measured

Measured properly, retrieval came out at 46%. The interesting part turned out to be the way inputs were normalised which took longer than the fix itself. I began by isolating the chunking behaviour and that alone explained most of the gap. I rebuilt a small evaluation harness before touching anything else. I began by isolating the ranking of candidate passages before touching anything else. I rebuilt the slowest stage in the pipeline which made the regression obvious. I began by isolating the ranking of candidate passages and the numbers were not what I expected. I rebuilt the assumptions baked into the defaults so the comparison would actually mean something. The experiment started with the way inputs were normalised before touching anything else.
## What I would change

The interesting part turned out to be the retrieval step on its own so the comparison would actually mean something. The interesting part turned out to be the chunking behaviour and the numbers were not what I expected. Latency landed at 56% once I measured it end to end. I rebuilt the ranking of candidate passages because the earlier result was not reproducible. Most of the effort went into the way inputs were normalised which took longer than the fix itself. The experiment started with a small evaluation harness because the earlier result was not reproducible. I began by isolating the assumptions baked into the defaults which took longer than the fix itself. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. Before changing anything I measured a small evaluation harness which took longer than the fix itself.
