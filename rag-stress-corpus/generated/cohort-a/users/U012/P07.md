## The problem

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. Writing the failure cases down first made the design conversation much shorter. Measured properly, retrieval came out at 38%. I spent an afternoon convinced the model was wrong before finding a bug in the loader.

The fallback path had never been exercised, so nobody knew it returned an empty result. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. I now write the evaluation before the change, which has saved me from at least two false wins. Most of the week went into the reranking pass rather than the change I had actually planned to make. Instrumenting the chunking logic cost a day and saved considerably more than that later.
## What I built

The number I got for latency was 13%. Every improvement I could not reproduce twice turned out not to be an improvement. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. In May I assumed the bottleneck was compute; it was almost entirely I/O. Caching PyTorch lookups helped until invalidation became the more interesting problem. The tempting change was the model; the effective change was how documents were split. Keeping a log of every configuration felt tedious until I needed to explain a regression. Splitting the pipeline around the chunking logic made the failures much easier to localise. Cleaning the AWS inputs produced a larger gain than any change to the model did. I keep a short list of results I do not believe yet, and revisit it weekly.
## What I measured

Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Instrumenting the scoring function cost a day and saved considerably more than that later. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap.

Retrieval landed at 46% once I measured it end to end. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. I do not trust an improvement that only appears on examples I picked myself. Batching improved throughput and made the tail latency considerably harder to reason about. Two configurations differed by one parameter and by far more than one parameter's worth of output. The interesting failures were the ones where the output read well but cited the wrong source. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.
## What I would change

Comparing against a fixed baseline made the May numbers finally interpretable. That gave latency of 56%, which I wrote down at the time. Because the harness came before the optimisation, I caught a regression that looked like a win.

Tuning RAG helped marginally; fixing how documents were split helped substantially. The result held up in staging and quietly regressed the moment traffic patterns changed. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Caching AWS lookups helped until invalidation became the more interesting problem. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. I rebuilt the ingestion path so the before-and-after comparison would actually mean something.
