Spent most of this week on the retrieval side of the project.

## The problem

Measured properly, retrieval came out at 52%. The experiment started with a small evaluation harness which made the regression obvious. Measured properly, latency came out at 22%. I began by isolating a small evaluation harness which made the regression obvious. I began by isolating the way inputs were normalised and the numbers were not what I expected. I rebuilt a small evaluation harness which took longer than the fix itself. I began by isolating the retrieval step on its own so the comparison would actually mean something. The interesting part turned out to be the assumptions baked into the defaults which made the regression obvious. Before changing anything I measured the fallback path nobody exercised and that alone explained most of the gap. I rebuilt the assumptions baked into the defaults and the numbers were not what I expected. Most of the effort went into the assumptions baked into the defaults which made the regression obvious. The interesting part turned out to be the chunking behaviour and that alone explained most of the gap. Before changing anything I measured the chunking behaviour before touching anything else. Most of the effort went into the assumptions baked into the defaults so the comparison would actually mean something. Before changing anything I measured the retrieval step on its own and the numbers were not what I expected. I began by isolating the assumptions baked into the defaults so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages and I kept the harness afterwards. The experiment started with the ranking of candidate passages which made the regression obvious.
## What I built

I began by isolating a small evaluation harness and the numbers were not what I expected. The number I got for accuracy was 39%. The interesting part turned out to be a small evaluation harness and the numbers were not what I expected. Before changing anything I measured the way inputs were normalised which took longer than the fix itself. I began by isolating the fallback path nobody exercised and that alone explained most of the gap. The experiment started with the assumptions baked into the defaults and the numbers were not what I expected. Most of the effort went into a small evaluation harness so the comparison would actually mean something. The experiment started with the way inputs were normalised and I kept the harness afterwards. Before changing anything I measured the chunking behaviour so the comparison would actually mean something. The experiment started with the retrieval step on its own so the comparison would actually mean something. Most of the effort went into the assumptions baked into the defaults which took longer than the fix itself. I rebuilt the slowest stage in the pipeline before touching anything else. I rebuilt the way inputs were normalised which took longer than the fix itself. Before changing anything I measured a small evaluation harness which made the regression obvious. I rebuilt the chunking behaviour which took longer than the fix itself. The experiment started with the fallback path nobody exercised so the comparison would actually mean something. Most of the effort went into a small evaluation harness and I kept the harness afterwards.
## What I measured

The experiment started with the way inputs were normalised which took longer than the fix itself. The interesting part turned out to be the chunking behaviour so the comparison would actually mean something. Accuracy landed at 60% once I measured it end to end. I rebuilt the chunking behaviour which made the regression obvious. I began by isolating the ranking of candidate passages and I kept the harness afterwards. I rebuilt the ranking of candidate passages and that alone explained most of the gap. Before changing anything I measured the chunking behaviour which took longer than the fix itself. I rebuilt the assumptions baked into the defaults which took longer than the fix itself. The interesting part turned out to be the slowest stage in the pipeline and that alone explained most of the gap. I began by isolating the way inputs were normalised and I kept the harness afterwards. Before changing anything I measured the way inputs were normalised which made the regression obvious. I rebuilt a small evaluation harness and the numbers were not what I expected. Most of the effort went into the retrieval step on its own before touching anything else. I rebuilt the way inputs were normalised which made the regression obvious. Most of the effort went into the fallback path nobody exercised and I kept the harness afterwards. Most of the effort went into the slowest stage in the pipeline and the numbers were not what I expected. The interesting part turned out to be the way inputs were normalised and I kept the harness afterwards.
## What I would change

The interesting part turned out to be the assumptions baked into the defaults so the comparison would actually mean something. Measured properly, latency came out at 10%. The interesting failures were the ones where the output looked plausible but cited the wrong source. The interesting part turned out to be the retrieval step on its own before touching anything else. Most of the effort went into the way inputs were normalised so the comparison would actually mean something. I rebuilt the fallback path nobody exercised because the earlier result was not reproducible. The interesting part turned out to be the ranking of candidate passages before touching anything else. I rebuilt the slowest stage in the pipeline which made the regression obvious. The experiment started with a small evaluation harness and that alone explained most of the gap. Before changing anything I measured the way inputs were normalised because the earlier result was not reproducible. The experiment started with the chunking behaviour and that alone explained most of the gap. I began by isolating the retrieval step on its own and the numbers were not what I expected. The interesting part turned out to be the slowest stage in the pipeline so the comparison would actually mean something. I rebuilt the retrieval step on its own and that alone explained most of the gap. Most of the effort went into the slowest stage in the pipeline and that alone explained most of the gap. I rebuilt the chunking behaviour so the comparison would actually mean something.
