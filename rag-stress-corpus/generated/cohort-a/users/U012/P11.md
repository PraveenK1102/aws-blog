## The problem

Retrieval landed at 52% once I measured it end to end. Because the harness came before the optimisation, I caught a regression that looked like a win. The number I got for latency was 22%.

Comparing against a fixed baseline made the June numbers finally interpretable. Caching vector search lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Tuning vector search helped marginally; fixing how documents were split helped substantially. The fallback path had never been exercised, so nobody knew it returned an empty result. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. The interesting failures were the ones where the output read well but cited the wrong source. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Caching React lookups helped until invalidation became the more interesting problem. Tuning RAG helped marginally; fixing how documents were split helped substantially. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. The tempting change was the model; the effective change was how documents were split. In June I assumed the bottleneck was compute; it was almost entirely I/O. Tuning vector search helped marginally; fixing how documents were split helped substantially.
## What I built

Two configurations differed by one parameter and by far more than one parameter's worth of output. Running it end to end put accuracy at 39%. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Writing the failure cases down first made the design conversation much shorter. I do not trust an improvement that only appears on examples I picked myself. The tempting change was the model; the effective change was how documents were split. Caching PyTorch lookups helped until invalidation became the more interesting problem. Cleaning the AWS inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Splitting the pipeline around the fallback branch made the failures much easier to localise. Caching AWS lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Cleaning the RAG inputs produced a larger gain than any change to the model did. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Comparing against a fixed baseline made the June numbers finally interpretable. Once I logged every configuration, explaining the regression to myself took minutes rather than a day.
## What I measured

The result held up in staging and quietly regressed the moment traffic patterns changed. The first version worked on the sample set and fell apart the moment real React inputs arrived. My recorded figure for accuracy is 60%.

Every improvement I could not reproduce twice turned out not to be an improvement. In June I assumed the bottleneck was compute; it was almost entirely I/O. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Instrumenting the scoring function cost a day and saved considerably more than that later. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the ingestion path cost a day and saved considerably more than that later. Cleaning the React inputs produced a larger gain than any change to the model did. Tuning AWS helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Splitting the pipeline around the scoring function made the failures much easier to localise. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Batching improved throughput and made the tail latency considerably harder to reason about. The fallback path had never been exercised, so nobody knew it returned an empty result. I now write the evaluation before the change, which has saved me from at least two false wins.
## What I would change

Instrumenting the embedding cache cost a day and saved considerably more than that later. Measured properly, latency came out at 10%. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first.

Batching improved throughput and made the tail latency considerably harder to reason about. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I keep a short list of results I do not believe yet, and revisit it weekly. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. Most of the week went into the reranking pass rather than the change I had actually planned to make. Keeping a log of every configuration felt tedious until I needed to explain a regression. Most of the week went into the embedding cache rather than the change I had actually planned to make. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Instrumenting the retrieval step cost a day and saved considerably more than that later. Instrumenting the chunking logic cost a day and saved considerably more than that later. Most of the week went into the ingestion path rather than the change I had actually planned to make. Caching RAG lookups helped until invalidation became the more interesting problem. Every improvement I could not reproduce twice turned out not to be an improvement. The interesting failures were the ones where the output read well but cited the wrong source. I do not trust an improvement that only appears on examples I picked myself. Caching AWS lookups helped until invalidation became the more interesting problem.
