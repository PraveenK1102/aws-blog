## The problem

I now write the evaluation before the change, which has saved me from at least two false wins. The interesting failures were the ones where the output read well but cited the wrong source. Most of the week went into the chunking logic rather than the change I had actually planned to make. Measured properly, retrieval came out at 59%. I spent an afternoon convinced the model was wrong before finding a bug in the loader.

When I finally measured it, latency was 6%. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Was the gain from the model or from cleaning the RAG inputs? Mostly the inputs. Every improvement I could not reproduce twice turned out not to be an improvement. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. Instrumenting the retrieval step cost a day and saved considerably more than that later. Tuning RAG helped marginally; fixing how documents were split helped substantially. Caching RAG lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the ingestion path made the failures much easier to localise.
## What I built

In April I assumed the bottleneck was compute; it was almost entirely I/O. I do not trust an improvement that only appears on examples I picked myself. That gave latency of 57%, which I wrote down at the time. Latency landed at 60% once I measured it end to end.

Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. The fallback path had never been exercised, so nobody knew it returned an empty result. I keep a short list of results I do not believe yet, and revisit it weekly. The tempting change was the model; the effective change was how documents were split. Batching improved throughput and made the tail latency considerably harder to reason about. Because the harness came before the optimisation, I caught a regression that looked like a win. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Cleaning the RAG inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real vector search inputs arrived.
## What I measured

The result held up in staging and quietly regressed the moment traffic patterns changed. The first version worked on the sample set and fell apart the moment real React inputs arrived. That gave accuracy of 11%, which I wrote down at the time.

Comparing against a fixed baseline made the April numbers finally interpretable. Writing the failure cases down first made the design conversation much shorter. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning React helped marginally; fixing how documents were split helped substantially. Caching PyTorch lookups helped until invalidation became the more interesting problem. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Splitting the pipeline around the chunking logic made the failures much easier to localise. I rebuilt the scoring function so the before-and-after comparison would actually mean something.
## What I would change

Most of the week went into the fallback branch rather than the change I had actually planned to make. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Instrumenting the ingestion path cost a day and saved considerably more than that later.

Measured properly, accuracy came out at 32%. Two configurations differed by one parameter and by far more than one parameter's worth of output. Cleaning the AWS inputs produced a larger gain than any change to the model did. Instrumenting the reranking pass cost a day and saved considerably more than that later. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Instrumenting the chunking logic cost a day and saved considerably more than that later.
