## The problem

The result held up in staging and quietly regressed the moment traffic patterns changed. When I finally measured it, accuracy was 36%. Cleaning the AWS inputs produced a larger gain than any change to the model did. I spent an afternoon convinced the model was wrong before finding a bug in the loader. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The tempting change was the model; the effective change was how documents were split. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. The first version worked on the sample set and fell apart the moment real RAG inputs arrived. Because the harness came before the optimisation, I caught a regression that looked like a win. Most of the week went into the scoring function rather than the change I had actually planned to make. Caching React lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the embedding cache made the failures much easier to localise.
## What I built

The fallback path had never been exercised, so nobody knew it returned an empty result. I now write the evaluation before the change, which has saved me from at least two false wins. Accuracy landed at 31% once I measured it end to end.

Every improvement I could not reproduce twice turned out not to be an improvement. In March I assumed the bottleneck was compute; it was almost entirely I/O. Comparing against a fixed baseline made the March numbers finally interpretable. Cleaning the vector search inputs produced a larger gain than any change to the model did. I keep a short list of results I do not believe yet, and revisit it weekly. Splitting the pipeline around the reranking pass made the failures much easier to localise. I rebuilt the scoring function so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Was the gain from the model or from cleaning the RAG inputs? Mostly the inputs.
## What I measured

Instrumenting the chunking logic cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. I do not trust an improvement that only appears on examples I picked myself. Two configurations differed by one parameter and by far more than one parameter's worth of output.

That gave retrieval of 27%, which I wrote down at the time. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Splitting the pipeline around the retrieval step made the failures much easier to localise. Batching improved throughput and made the tail latency considerably harder to reason about. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Tuning vector search helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Cleaning the React inputs produced a larger gain than any change to the model did. Most of the week went into the retrieval step rather than the change I had actually planned to make.
## What I would change

Accuracy landed at 59% once I measured it end to end. The interesting failures were the ones where the output read well but cited the wrong source. Keeping a log of every configuration felt tedious until I needed to explain a regression. Most of the week went into the reranking pass rather than the change I had actually planned to make.

Writing the failure cases down first made the design conversation much shorter. Instrumenting the embedding cache cost a day and saved considerably more than that later. Tuning RAG helped marginally; fixing how documents were split helped substantially. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Caching AWS lookups helped until invalidation became the more interesting problem. Instrumenting the ingestion path cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Instrumenting the retrieval step cost a day and saved considerably more than that later.
