Spent most of this week on the retrieval side of the project.

## The problem

I began by isolating the way inputs were normalised before touching anything else. Measured properly, accuracy came out at 36%. Before changing anything I measured the fallback path nobody exercised which took longer than the fix itself. I began by isolating the assumptions baked into the defaults and the numbers were not what I expected. The interesting part turned out to be the chunking behaviour because the earlier result was not reproducible. The experiment started with the chunking behaviour so the comparison would actually mean something. Splitting the pipeline into separately testable steps made the failures much easier to localise. The interesting part turned out to be the slowest stage in the pipeline and the numbers were not what I expected. I rebuilt the chunking behaviour which took longer than the fix itself. The experiment started with the retrieval step on its own and I kept the harness afterwards. The interesting part turned out to be the assumptions baked into the defaults so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages so the comparison would actually mean something.
## What I built

The experiment started with the ranking of candidate passages and that alone explained most of the gap. The number I got for accuracy was 31%. The experiment started with the fallback path nobody exercised so the comparison would actually mean something. I rebuilt the ranking of candidate passages so the comparison would actually mean something. I began by isolating the ranking of candidate passages and that alone explained most of the gap. I began by isolating the slowest stage in the pipeline and that alone explained most of the gap. I began by isolating the fallback path nobody exercised which took longer than the fix itself. Before changing anything I measured the fallback path nobody exercised and I kept the harness afterwards. Before changing anything I measured the assumptions baked into the defaults and I kept the harness afterwards. Most of the effort went into the way inputs were normalised which took longer than the fix itself. Before changing anything I measured the assumptions baked into the defaults which made the regression obvious.
## What I measured

Before changing anything I measured a small evaluation harness so the comparison would actually mean something. The interesting part turned out to be the chunking behaviour which made the regression obvious. Retrieval landed at 27% once I measured it end to end. Most of the time went into building a way to measure the change rather than making the change. I rebuilt the retrieval step on its own so the comparison would actually mean something. The interesting part turned out to be the retrieval step on its own and I kept the harness afterwards. I rebuilt the slowest stage in the pipeline which took longer than the fix itself. Most of the effort went into the assumptions baked into the defaults which took longer than the fix itself. Before changing anything I measured a small evaluation harness and the numbers were not what I expected. I rebuilt the slowest stage in the pipeline so the comparison would actually mean something. The interesting part turned out to be a small evaluation harness so the comparison would actually mean something.
## What I would change

I rebuilt the assumptions baked into the defaults and the numbers were not what I expected. The number I got for accuracy was 59%. I rebuilt the fallback path nobody exercised and I kept the harness afterwards. Before changing anything I measured the ranking of candidate passages so the comparison would actually mean something. The experiment started with the way inputs were normalised and the numbers were not what I expected. The experiment started with the chunking behaviour because the earlier result was not reproducible. Most of the effort went into the way inputs were normalised because the earlier result was not reproducible. I rebuilt the retrieval step on its own before touching anything else. The experiment started with the fallback path nobody exercised before touching anything else. Most of the effort went into a small evaluation harness and that alone explained most of the gap. Most of the effort went into the slowest stage in the pipeline so the comparison would actually mean something.
