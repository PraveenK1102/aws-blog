## The problem

In June I assumed the bottleneck was compute; it was almost entirely I/O. That gave latency of 7%, which I wrote down at the time. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap.

Running it end to end put latency at 45%. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. Batching improved throughput and made the tail latency considerably harder to reason about. Writing the failure cases down first made the design conversation much shorter. Keeping a log of every configuration felt tedious until I needed to explain a regression. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. Cleaning the React inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. Caching React lookups helped until invalidation became the more interesting problem. Caching vector search lookups helped until invalidation became the more interesting problem.
## What I built

Splitting the pipeline around the scoring function made the failures much easier to localise. The fallback path had never been exercised, so nobody knew it returned an empty result. Because the harness came before the optimisation, I caught a regression that looked like a win. Latency landed at 14% once I measured it end to end. I now write the evaluation before the change, which has saved me from at least two false wins. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I do not trust an improvement that only appears on examples I picked myself. Caching RAG lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the fallback branch made the failures much easier to localise. The result held up in staging and quietly regressed the moment traffic patterns changed. Every improvement I could not reproduce twice turned out not to be an improvement. Instrumenting the embedding cache cost a day and saved considerably more than that later. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. Most of the week went into the reranking pass rather than the change I had actually planned to make.
## What I measured

Caching AWS lookups helped until invalidation became the more interesting problem. The tempting change was the model; the effective change was how documents were split. Most of the week went into the chunking logic rather than the change I had actually planned to make. Cleaning the PyTorch inputs produced a larger gain than any change to the model did.

On the instrumented run, retrieval showed 42%. Cleaning the RAG inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Instrumenting the scoring function cost a day and saved considerably more than that later. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. The interesting failures were the ones where the output read well but cited the wrong source. Instrumenting the retrieval step cost a day and saved considerably more than that later.
## What I would change

The number I got for accuracy was 11%. Two configurations differed by one parameter and by far more than one parameter's worth of output. I keep a short list of results I do not believe yet, and revisit it weekly. Comparing against a fixed baseline made the June numbers finally interpretable. Most of the week went into the ingestion path rather than the change I had actually planned to make. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Was the gain from the model or from cleaning the RAG inputs? Mostly the inputs. Tuning RAG helped marginally; fixing how documents were split helped substantially. The first version worked on the sample set and fell apart the moment real RAG inputs arrived. Tuning AWS helped marginally; fixing how documents were split helped substantially. I rebuilt the embedding cache so the before-and-after comparison would actually mean something.
