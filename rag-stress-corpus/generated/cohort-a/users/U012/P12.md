Spent most of this week on the retrieval side of the project.

## The problem

The experiment started with the fallback path nobody exercised before touching anything else. Measured properly, latency came out at 7%. Before changing anything I measured the fallback path nobody exercised and I kept the harness afterwards. The number I got for latency was 45%. I began by isolating a small evaluation harness before touching anything else. Most of the effort went into the chunking behaviour and I kept the harness afterwards. I rebuilt the ranking of candidate passages because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults and I kept the harness afterwards. I began by isolating a small evaluation harness so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages and I kept the harness afterwards. I began by isolating a small evaluation harness and the numbers were not what I expected. Most of the effort went into the fallback path nobody exercised and that alone explained most of the gap. I began by isolating the way inputs were normalised before touching anything else.
## What I built

The experiment started with the ranking of candidate passages before touching anything else. The number I got for latency was 14%. The experiment started with the ranking of candidate passages and I kept the harness afterwards. Most of the effort went into the slowest stage in the pipeline so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages before touching anything else. Before changing anything I measured the assumptions baked into the defaults and that alone explained most of the gap. Before changing anything I measured the chunking behaviour and that alone explained most of the gap. I began by isolating the retrieval step on its own because the earlier result was not reproducible. I began by isolating the ranking of candidate passages and I kept the harness afterwards. I began by isolating the ranking of candidate passages before touching anything else. The interesting part turned out to be the slowest stage in the pipeline which took longer than the fix itself. Most of the effort went into the way inputs were normalised so the comparison would actually mean something.
## What I measured

Before changing anything I measured the way inputs were normalised and the numbers were not what I expected. Measured properly, retrieval came out at 42%. I began by isolating the assumptions baked into the defaults which took longer than the fix itself. Before changing anything I measured the slowest stage in the pipeline which made the regression obvious. The interesting part turned out to be the retrieval step on its own and the numbers were not what I expected. The experiment started with the ranking of candidate passages which took longer than the fix itself. I rebuilt the retrieval step on its own and I kept the harness afterwards. Before changing anything I measured the assumptions baked into the defaults because the earlier result was not reproducible. I rebuilt the slowest stage in the pipeline and the numbers were not what I expected. Most of the effort went into the assumptions baked into the defaults which made the regression obvious. The experiment started with the fallback path nobody exercised because the earlier result was not reproducible.
## What I would change

Most of the effort went into the assumptions baked into the defaults so the comparison would actually mean something. Accuracy landed at 11% once I measured it end to end. Most of the effort went into the retrieval step on its own because the earlier result was not reproducible. The experiment started with the way inputs were normalised so the comparison would actually mean something. The experiment started with the chunking behaviour because the earlier result was not reproducible. I began by isolating the way inputs were normalised and the numbers were not what I expected. I rebuilt the ranking of candidate passages and I kept the harness afterwards. The experiment started with the ranking of candidate passages and that alone explained most of the gap. I rebuilt the retrieval step on its own so the comparison would actually mean something. I rebuilt the retrieval step on its own which took longer than the fix itself. I began by isolating the assumptions baked into the defaults so the comparison would actually mean something.
