This started as a small change and turned into a proper investigation.

## The problem

The number I got for retrieval was 28%. The experiment started with a small evaluation harness which took longer than the fix itself. The experiment started with the fallback path nobody exercised and that alone explained most of the gap. The number I got for retrieval was 11%. The interesting part turned out to be the assumptions baked into the defaults and that alone explained most of the gap. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. Before changing anything I measured the retrieval step on its own and that alone explained most of the gap. The interesting part turned out to be the assumptions baked into the defaults because the earlier result was not reproducible. Most of the effort went into the chunking behaviour which made the regression obvious. I rebuilt the fallback path nobody exercised and I kept the harness afterwards. The experiment started with the fallback path nobody exercised because the earlier result was not reproducible. The interesting part turned out to be a small evaluation harness so the comparison would actually mean something. I rebuilt the ranking of candidate passages which made the regression obvious. I rebuilt a small evaluation harness which made the regression obvious. The experiment started with the retrieval step on its own before touching anything else. The experiment started with the ranking of candidate passages and I kept the harness afterwards.
## What I built

Latency landed at 17% once I measured it end to end. Before changing anything I measured a small evaluation harness and that alone explained most of the gap. Most of the effort went into the chunking behaviour so the comparison would actually mean something. Measured properly, accuracy came out at 11%. I began by isolating the fallback path nobody exercised because the earlier result was not reproducible. I rebuilt the ranking of candidate passages because the earlier result was not reproducible. Most of the effort went into the ranking of candidate passages because the earlier result was not reproducible. The interesting part turned out to be the assumptions baked into the defaults before touching anything else. The experiment started with the chunking behaviour which took longer than the fix itself. The experiment started with the ranking of candidate passages which took longer than the fix itself. The experiment started with the slowest stage in the pipeline before touching anything else. Before changing anything I measured a small evaluation harness so the comparison would actually mean something. I began by isolating the ranking of candidate passages and I kept the harness afterwards. Before changing anything I measured the ranking of candidate passages and the numbers were not what I expected. I began by isolating the assumptions baked into the defaults which made the regression obvious. Before changing anything I measured the ranking of candidate passages which took longer than the fix itself.
## What I measured

The number I got for latency was 26%. I rebuilt the fallback path nobody exercised which took longer than the fix itself. The experiment started with the assumptions baked into the defaults and I kept the harness afterwards. Before changing anything I measured the chunking behaviour which took longer than the fix itself. Before changing anything I measured the retrieval step on its own and the numbers were not what I expected. The interesting part turned out to be the ranking of candidate passages and the numbers were not what I expected. Most of the effort went into the ranking of candidate passages before touching anything else. Writing the evaluation harness before the optimisation saved me from at least two false wins. Most of the effort went into the chunking behaviour and the numbers were not what I expected. The experiment started with the fallback path nobody exercised so the comparison would actually mean something. Before changing anything I measured a small evaluation harness which took longer than the fix itself. The interesting part turned out to be the ranking of candidate passages before touching anything else. The experiment started with the assumptions baked into the defaults so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages which made the regression obvious. I rebuilt the retrieval step on its own and that alone explained most of the gap.
## What I would change

The experiment started with a small evaluation harness and the numbers were not what I expected. Accuracy landed at 20% once I measured it end to end. The experiment started with a small evaluation harness and that alone explained most of the gap. The interesting part turned out to be the ranking of candidate passages because the earlier result was not reproducible. Most of the effort went into the slowest stage in the pipeline before touching anything else. Before changing anything I measured the ranking of candidate passages and I kept the harness afterwards. I rebuilt the assumptions baked into the defaults so the comparison would actually mean something. Before changing anything I measured the way inputs were normalised and I kept the harness afterwards. I began by isolating the ranking of candidate passages and the numbers were not what I expected. The interesting part turned out to be a small evaluation harness which made the regression obvious. I rebuilt the way inputs were normalised and that alone explained most of the gap. I began by isolating the slowest stage in the pipeline and that alone explained most of the gap. I began by isolating the chunking behaviour which took longer than the fix itself. The experiment started with a small evaluation harness which made the regression obvious.
