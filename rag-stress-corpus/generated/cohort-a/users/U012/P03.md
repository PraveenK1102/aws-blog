## The problem

I do not trust an improvement that only appears on examples I picked myself. Batching improved throughput and made the tail latency considerably harder to reason about. Running it end to end put retrieval at 11%. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap.

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. That gave retrieval of 28%, which I wrote down at the time. Cleaning the vector search inputs produced a larger gain than any change to the model did. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Most of the week went into the chunking logic rather than the change I had actually planned to make. In March I assumed the bottleneck was compute; it was almost entirely I/O. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Cleaning the React inputs produced a larger gain than any change to the model did. I rebuilt the scoring function so the before-and-after comparison would actually mean something. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Tuning RAG helped marginally; fixing how documents were split helped substantially. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Once I logged every configuration, explaining the regression to myself took minutes rather than a day.
## What I built

The number I got for accuracy was 11%. The fallback path had never been exercised, so nobody knew it returned an empty result. Writing the failure cases down first made the design conversation much shorter. Splitting the pipeline around the fallback branch made the failures much easier to localise.

Latency landed at 17% once I measured it end to end. Tuning React helped marginally; fixing how documents were split helped substantially. I keep a short list of results I do not believe yet, and revisit it weekly. Comparing against a fixed baseline made the March numbers finally interpretable. I now write the evaluation before the change, which has saved me from at least two false wins. The tempting change was the model; the effective change was how documents were split. Every improvement I could not reproduce twice turned out not to be an improvement. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Instrumenting the reranking pass cost a day and saved considerably more than that later. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Instrumenting the chunking logic cost a day and saved considerably more than that later. Instrumenting the evaluation harness cost a day and saved considerably more than that later. I do not trust an improvement that only appears on examples I picked myself. Comparing against a fixed baseline made the March numbers finally interpretable.
## What I measured

Caching AWS lookups helped until invalidation became the more interesting problem. My recorded figure for latency is 26%. The interesting failures were the ones where the output read well but cited the wrong source.

Most of the week went into the ingestion path rather than the change I had actually planned to make. The first version worked on the sample set and fell apart the moment real React inputs arrived. The result held up in staging and quietly regressed the moment traffic patterns changed. Because the harness came before the optimisation, I caught a regression that looked like a win. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Was the gain from the model or from cleaning the RAG inputs? Mostly the inputs. Tuning vector search helped marginally; fixing how documents were split helped substantially. Splitting the pipeline around the chunking logic made the failures much easier to localise. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Cleaning the AWS inputs produced a larger gain than any change to the model did. Caching RAG lookups helped until invalidation became the more interesting problem.
## What I would change

Two configurations differed by one parameter and by far more than one parameter's worth of output. Instrumenting the scoring function cost a day and saved considerably more than that later. When I finally measured it, accuracy was 20%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Tuning AWS helped marginally; fixing how documents were split helped substantially. Keeping a log of every configuration felt tedious until I needed to explain a regression. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. Caching PyTorch lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Instrumenting the ingestion path cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real RAG inputs arrived. Caching RAG lookups helped until invalidation became the more interesting problem. Caching React lookups helped until invalidation became the more interesting problem. Two configurations differed by one parameter and by far more than one parameter's worth of output.
