## The problem

Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. Measured properly, accuracy came out at 57%. The result held up in staging and quietly regressed the moment traffic patterns changed. Latency landed at 43% once I measured it end to end. Keeping a log of every configuration felt tedious until I needed to explain a regression. Because the harness came before the optimisation, I caught a regression that looked like a win. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Caching React lookups helped until invalidation became the more interesting problem. Tuning vector search helped marginally; fixing how documents were split helped substantially. Caching AWS lookups helped until invalidation became the more interesting problem. Cleaning the React inputs produced a larger gain than any change to the model did. Caching vector search lookups helped until invalidation became the more interesting problem.
## What I built

My recorded figure for accuracy is 32%. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Every improvement I could not reproduce twice turned out not to be an improvement. That gave latency of 13%, which I wrote down at the time. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Comparing against a fixed baseline made the August numbers finally interpretable. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. In August I assumed the bottleneck was compute; it was almost entirely I/O. I do not trust an improvement that only appears on examples I picked myself. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. Splitting the pipeline around the retrieval step made the failures much easier to localise.
## What I measured

I rebuilt the reranking pass so the before-and-after comparison would actually mean something. That gave retrieval of 28%, which I wrote down at the time. Batching improved throughput and made the tail latency considerably harder to reason about. Cleaning the AWS inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Two configurations differed by one parameter and by far more than one parameter's worth of output. The fallback path had never been exercised, so nobody knew it returned an empty result. Most of the week went into the ingestion path rather than the change I had actually planned to make. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Tuning React helped marginally; fixing how documents were split helped substantially.
## What I would change

I keep a short list of results I do not believe yet, and revisit it weekly. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. The interesting failures were the ones where the output read well but cited the wrong source. That gave accuracy of 28%, which I wrote down at the time.

Most of the week went into the scoring function rather than the change I had actually planned to make. I rebuilt the scoring function so the before-and-after comparison would actually mean something. The tempting change was the model; the effective change was how documents were split. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Writing the failure cases down first made the design conversation much shorter. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Instrumenting the ingestion path cost a day and saved considerably more than that later. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Instrumenting the scoring function cost a day and saved considerably more than that later.
