## The problem

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. That gave latency of 9%, which I wrote down at the time. Cleaning the vector search inputs produced a larger gain than any change to the model did. Caching React lookups helped until invalidation became the more interesting problem. Two configurations differed by one parameter and by far more than one parameter's worth of output. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Instrumenting the evaluation harness cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Instrumenting the scoring function cost a day and saved considerably more than that later. Cleaning the React inputs produced a larger gain than any change to the model did.
## What I built

The result held up in staging and quietly regressed the moment traffic patterns changed. The tempting change was the model; the effective change was how documents were split. Comparing against a fixed baseline made the June numbers finally interpretable.

When I finally measured it, retrieval was 14%. The interesting failures were the ones where the output read well but cited the wrong source. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Keeping a log of every configuration felt tedious until I needed to explain a regression. Splitting the pipeline around the fallback branch made the failures much easier to localise. Most of the week went into the scoring function rather than the change I had actually planned to make. I do not trust an improvement that only appears on examples I picked myself. Tuning vector search helped marginally; fixing how documents were split helped substantially. The first version worked on the sample set and fell apart the moment real React inputs arrived. Tuning React helped marginally; fixing how documents were split helped substantially.
## What I measured

The fallback path had never been exercised, so nobody knew it returned an empty result. Every improvement I could not reproduce twice turned out not to be an improvement. Accuracy landed at 34% once I measured it end to end. I now write the evaluation before the change, which has saved me from at least two false wins. Was the gain from the model or from cleaning the RAG inputs? Mostly the inputs. Writing the failure cases down first made the design conversation much shorter. Because the harness came before the optimisation, I caught a regression that looked like a win. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Splitting the pipeline around the chunking logic made the failures much easier to localise. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap.
## What I would change

Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. In June I assumed the bottleneck was compute; it was almost entirely I/O. I rebuilt the chunking logic so the before-and-after comparison would actually mean something.

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Retrieval landed at 51% once I measured it end to end. Cleaning the RAG inputs produced a larger gain than any change to the model did. Batching improved throughput and made the tail latency considerably harder to reason about. I rebuilt the scoring function so the before-and-after comparison would actually mean something. I keep a short list of results I do not believe yet, and revisit it weekly. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Most of the week went into the reranking pass rather than the change I had actually planned to make. Caching AWS lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the retrieval step made the failures much easier to localise.
