This started as a small change and turned into a proper investigation.

## The problem

Before changing anything I measured the fallback path nobody exercised because the earlier result was not reproducible. The number I got for latency was 9%. I began by isolating the retrieval step on its own which made the regression obvious. Before changing anything I measured the ranking of candidate passages so the comparison would actually mean something. The interesting part turned out to be a small evaluation harness and that alone explained most of the gap. I rebuilt the ranking of candidate passages which made the regression obvious. The interesting part turned out to be the chunking behaviour because the earlier result was not reproducible. The interesting part turned out to be the assumptions baked into the defaults because the earlier result was not reproducible. Before changing anything I measured the fallback path nobody exercised and the numbers were not what I expected. Most of the effort went into the fallback path nobody exercised which took longer than the fix itself. Before changing anything I measured the assumptions baked into the defaults which took longer than the fix itself.
## What I built

Before changing anything I measured the fallback path nobody exercised before touching anything else. The experiment started with a small evaluation harness and that alone explained most of the gap. Measured properly, retrieval came out at 14%. The interesting part turned out to be the fallback path nobody exercised and that alone explained most of the gap. I rebuilt the chunking behaviour and that alone explained most of the gap. Before changing anything I measured the slowest stage in the pipeline and I kept the harness afterwards. Before changing anything I measured the retrieval step on its own which made the regression obvious. The interesting part turned out to be the way inputs were normalised because the earlier result was not reproducible. Most of the effort went into the fallback path nobody exercised and that alone explained most of the gap. I began by isolating the ranking of candidate passages which took longer than the fix itself. Before changing anything I measured the ranking of candidate passages and I kept the harness afterwards.
## What I measured

The number I got for accuracy was 34%. The interesting part turned out to be the fallback path nobody exercised which took longer than the fix itself. I rebuilt the chunking behaviour before touching anything else. Before changing anything I measured the ranking of candidate passages and the numbers were not what I expected. Before changing anything I measured the chunking behaviour before touching anything else. I rebuilt the chunking behaviour so the comparison would actually mean something. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression. The experiment started with a small evaluation harness and the numbers were not what I expected. I learned to distrust any improvement that only shows up on the examples I chose myself. I began by isolating the way inputs were normalised because the earlier result was not reproducible. Before changing anything I measured the slowest stage in the pipeline which took longer than the fix itself.
## What I would change

The number I got for retrieval was 51%. Most of the effort went into the retrieval step on its own because the earlier result was not reproducible. The interesting part turned out to be the retrieval step on its own so the comparison would actually mean something. I began by isolating the fallback path nobody exercised so the comparison would actually mean something. Before changing anything I measured the slowest stage in the pipeline and that alone explained most of the gap. I rebuilt the ranking of candidate passages which took longer than the fix itself. Most of the effort went into the chunking behaviour before touching anything else. I began by isolating the chunking behaviour before touching anything else. Most of the effort went into the way inputs were normalised which made the regression obvious. I rebuilt the ranking of candidate passages so the comparison would actually mean something. I began by isolating the way inputs were normalised and I kept the harness afterwards.
