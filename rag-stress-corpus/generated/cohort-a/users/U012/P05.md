## The problem

The number I got for retrieval was 42%. My recorded figure for latency is 37%. The tempting change was the model; the effective change was how documents were split. The first version worked on the sample set and fell apart the moment real vector search inputs arrived.

Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Two configurations differed by one parameter and by far more than one parameter's worth of output. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. The result held up in staging and quietly regressed the moment traffic patterns changed. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. I spent an afternoon convinced the model was wrong before finding a bug in the loader. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs.
## What I built

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. On the instrumented run, retrieval showed 25%. The interesting failures were the ones where the output read well but cited the wrong source. When I finally measured it, latency was 16%. I now write the evaluation before the change, which has saved me from at least two false wins. In April I assumed the bottleneck was compute; it was almost entirely I/O. Because the harness came before the optimisation, I caught a regression that looked like a win. The first version worked on the sample set and fell apart the moment real RAG inputs arrived. Caching PyTorch lookups helped until invalidation became the more interesting problem. Tuning React helped marginally; fixing how documents were split helped substantially. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.
## What I measured

Caching RAG lookups helped until invalidation became the more interesting problem. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The fallback path had never been exercised, so nobody knew it returned an empty result. Measured properly, accuracy came out at 5%. Batching improved throughput and made the tail latency considerably harder to reason about. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Instrumenting the fallback branch cost a day and saved considerably more than that later. Every improvement I could not reproduce twice turned out not to be an improvement. Comparing against a fixed baseline made the April numbers finally interpretable. I do not trust an improvement that only appears on examples I picked myself. Caching AWS lookups helped until invalidation became the more interesting problem.
## What I would change

Keeping a log of every configuration felt tedious until I needed to explain a regression. Running it end to end put retrieval at 46%. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Writing the failure cases down first made the design conversation much shorter. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. I keep a short list of results I do not believe yet, and revisit it weekly. Splitting the pipeline around the chunking logic made the failures much easier to localise. Instrumenting the scoring function cost a day and saved considerably more than that later. Most of the week went into the ingestion path rather than the change I had actually planned to make. Instrumenting the reranking pass cost a day and saved considerably more than that later.
