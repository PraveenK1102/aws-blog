Notes from a few days of experiments, written while they are still fresh.

## The problem

Measured properly, retrieval came out at 30%. The experiment started with the assumptions baked into the defaults before touching anything else. Before changing anything I measured the slowest stage in the pipeline before touching anything else. Before changing anything I measured the assumptions baked into the defaults and the numbers were not what I expected. I rebuilt the ranking of candidate passages because the earlier result was not reproducible. Before changing anything I measured the assumptions baked into the defaults and I kept the harness afterwards. Before changing anything I measured the ranking of candidate passages so the comparison would actually mean something. Most of the effort went into the slowest stage in the pipeline and that alone explained most of the gap. The interesting part turned out to be a small evaluation harness because the earlier result was not reproducible. Before changing anything I measured a small evaluation harness and I kept the harness afterwards.
## What I built

I rebuilt the ranking of candidate passages before touching anything else. I began by isolating the assumptions baked into the defaults so the comparison would actually mean something. The number I got for retrieval was 43%. I rebuilt the assumptions baked into the defaults and I kept the harness afterwards. I rebuilt a small evaluation harness and the numbers were not what I expected. The experiment started with the slowest stage in the pipeline before touching anything else. Most of the effort went into the retrieval step on its own and that alone explained most of the gap. Most of the effort went into the fallback path nobody exercised which took longer than the fix itself. The experiment started with the ranking of candidate passages and the numbers were not what I expected. The interesting part turned out to be the way inputs were normalised so the comparison would actually mean something.
## What I measured

Measured properly, retrieval came out at 26%. I rebuilt the slowest stage in the pipeline which took longer than the fix itself. I began by isolating the ranking of candidate passages which made the regression obvious. The interesting part turned out to be a small evaluation harness which made the regression obvious. The experiment started with the way inputs were normalised because the earlier result was not reproducible. The experiment started with the slowest stage in the pipeline so the comparison would actually mean something. Before changing anything I measured the fallback path nobody exercised and that alone explained most of the gap. The interesting part turned out to be the assumptions baked into the defaults and I kept the harness afterwards. I began by isolating a small evaluation harness which made the regression obvious.
## What I would change

I rebuilt the way inputs were normalised because the earlier result was not reproducible. I rebuilt the way inputs were normalised which took longer than the fix itself. I began by isolating the retrieval step on its own and I kept the harness afterwards. Before changing anything I measured the retrieval step on its own and I kept the harness afterwards. I began by isolating the slowest stage in the pipeline and the numbers were not what I expected. Before changing anything I measured the fallback path nobody exercised and I kept the harness afterwards. The experiment started with the ranking of candidate passages because the earlier result was not reproducible. The experiment started with a small evaluation harness and that alone explained most of the gap.
