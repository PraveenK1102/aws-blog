## The problem

Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Instrumenting the evaluation harness cost a day and saved considerably more than that later. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Measured properly, retrieval came out at 30%. I do not trust an improvement that only appears on examples I picked myself. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Most of the week went into the scoring function rather than the change I had actually planned to make. Instrumenting the fallback branch cost a day and saved considerably more than that later. The interesting failures were the ones where the output read well but cited the wrong source. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Cleaning the vector search inputs produced a larger gain than any change to the model did.
## What I built

Because the harness came before the optimisation, I caught a regression that looked like a win. Comparing against a fixed baseline made the May numbers finally interpretable. My recorded figure for retrieval is 43%.

Every improvement I could not reproduce twice turned out not to be an improvement. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. Writing the failure cases down first made the design conversation much shorter. The result held up in staging and quietly regressed the moment traffic patterns changed. Tuning AWS helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. I rebuilt the embedding cache so the before-and-after comparison would actually mean something.
## What I measured

Retrieval landed at 26% once I measured it end to end. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The tempting change was the model; the effective change was how documents were split. Keeping a log of every configuration felt tedious until I needed to explain a regression. I now write the evaluation before the change, which has saved me from at least two false wins. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Two configurations differed by one parameter and by far more than one parameter's worth of output. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The first version worked on the sample set and fell apart the moment real React inputs arrived. Tuning RAG helped marginally; fixing how documents were split helped substantially.
## What I would change

Cleaning the React inputs produced a larger gain than any change to the model did. Caching PyTorch lookups helped until invalidation became the more interesting problem. Batching improved throughput and made the tail latency considerably harder to reason about. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. In May I assumed the bottleneck was compute; it was almost entirely I/O. I keep a short list of results I do not believe yet, and revisit it weekly. Caching React lookups helped until invalidation became the more interesting problem. Instrumenting the reranking pass cost a day and saved considerably more than that later. Splitting the pipeline around the scoring function made the failures much easier to localise.
