Writing this up mostly to force myself to be precise about the numbers.

## The problem

I began by isolating a small evaluation harness and I kept the harness afterwards. The number I got for latency was 25%. The experiment started with the fallback path nobody exercised so the comparison would actually mean something. I began by isolating the way inputs were normalised before touching anything else. Most of the effort went into the way inputs were normalised so the comparison would actually mean something. The interesting part turned out to be the assumptions baked into the defaults and I kept the harness afterwards. The experiment started with a small evaluation harness which made the regression obvious. I began by isolating the fallback path nobody exercised which took longer than the fix itself. I rebuilt the assumptions baked into the defaults and that alone explained most of the gap. Most of the effort went into the fallback path nobody exercised which made the regression obvious. I rebuilt the retrieval step on its own and I kept the harness afterwards. Before changing anything I measured a small evaluation harness so the comparison would actually mean something. Before changing anything I measured the way inputs were normalised which made the regression obvious. I began by isolating the way inputs were normalised because the earlier result was not reproducible. The interesting part turned out to be the ranking of candidate passages and that alone explained most of the gap. Before changing anything I measured the ranking of candidate passages which made the regression obvious. I began by isolating a small evaluation harness because the earlier result was not reproducible. The interesting part turned out to be the retrieval step on its own before touching anything else.
## What I built

I began by isolating the retrieval step on its own and I kept the harness afterwards. Latency landed at 30% once I measured it end to end. Before changing anything I measured the retrieval step on its own which took longer than the fix itself. I rebuilt the ranking of candidate passages which made the regression obvious. I began by isolating the assumptions baked into the defaults and the numbers were not what I expected. Most of the effort went into the retrieval step on its own which took longer than the fix itself. The experiment started with the fallback path nobody exercised before touching anything else. I rebuilt the slowest stage in the pipeline which took longer than the fix itself. Most of the effort went into the way inputs were normalised because the earlier result was not reproducible. I rebuilt the fallback path nobody exercised and that alone explained most of the gap. The experiment started with a small evaluation harness and I kept the harness afterwards. Most of the effort went into the chunking behaviour before touching anything else. I began by isolating the way inputs were normalised so the comparison would actually mean something. I began by isolating the assumptions baked into the defaults before touching anything else. I rebuilt the way inputs were normalised before touching anything else. I rebuilt the way inputs were normalised which took longer than the fix itself. The interesting part turned out to be the ranking of candidate passages so the comparison would actually mean something. The interesting part turned out to be the way inputs were normalised which took longer than the fix itself.
## What I measured

Latency landed at 20% once I measured it end to end. The interesting part turned out to be the assumptions baked into the defaults so the comparison would actually mean something. I rebuilt the assumptions baked into the defaults which made the regression obvious. The experiment started with the retrieval step on its own because the earlier result was not reproducible. I rebuilt the way inputs were normalised and that alone explained most of the gap. The interesting part turned out to be the retrieval step on its own and the numbers were not what I expected. I began by isolating the retrieval step on its own which took longer than the fix itself. The interesting failures were the ones where the output looked plausible but cited the wrong source. The interesting part turned out to be the chunking behaviour which took longer than the fix itself. I began by isolating the chunking behaviour because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults which took longer than the fix itself. The interesting part turned out to be the fallback path nobody exercised and the numbers were not what I expected. A surprising amount of the gain came from cleaning the inputs rather than changing the model. Before changing anything I measured the way inputs were normalised which took longer than the fix itself. I rebuilt the fallback path nobody exercised before touching anything else. I began by isolating the ranking of candidate passages which made the regression obvious. Before changing anything I measured the assumptions baked into the defaults and I kept the harness afterwards.
## What I would change

The interesting part turned out to be a small evaluation harness because the earlier result was not reproducible. Latency landed at 29% once I measured it end to end. I began by isolating the assumptions baked into the defaults which took longer than the fix itself. The experiment started with the way inputs were normalised and the numbers were not what I expected. I rebuilt the chunking behaviour and I kept the harness afterwards. Most of the effort went into the assumptions baked into the defaults before touching anything else. Most of the effort went into the fallback path nobody exercised before touching anything else. Before changing anything I measured the slowest stage in the pipeline so the comparison would actually mean something. I rebuilt the slowest stage in the pipeline and that alone explained most of the gap. Most of the time went into building a way to measure the change rather than making the change. Most of the effort went into a small evaluation harness which made the regression obvious. The experiment started with a small evaluation harness which took longer than the fix itself. Most of the effort went into the ranking of candidate passages because the earlier result was not reproducible. The experiment started with the fallback path nobody exercised which made the regression obvious. Before changing anything I measured the ranking of candidate passages and that alone explained most of the gap. The interesting part turned out to be a small evaluation harness which took longer than the fix itself. Before changing anything I measured the retrieval step on its own because the earlier result was not reproducible.
