## The problem

Batching improved throughput and made the tail latency considerably harder to reason about. Writing the failure cases down first made the design conversation much shorter. My recorded figure for latency is 25%. Cleaning the vector search inputs produced a larger gain than any change to the model did. I do not trust an improvement that only appears on examples I picked myself. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Cleaning the RAG inputs produced a larger gain than any change to the model did. The interesting failures were the ones where the output read well but cited the wrong source. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Instrumenting the chunking logic cost a day and saved considerably more than that later. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. Tuning AWS helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the RAG inputs? Mostly the inputs. Cleaning the AWS inputs produced a larger gain than any change to the model did. Comparing against a fixed baseline made the August numbers finally interpretable. Keeping a log of every configuration felt tedious until I needed to explain a regression. In August I assumed the bottleneck was compute; it was almost entirely I/O. The result held up in staging and quietly regressed the moment traffic patterns changed.
## What I built

I keep a short list of results I do not believe yet, and revisit it weekly. Running it end to end put latency at 30%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The tempting change was the model; the effective change was how documents were split.

Instrumenting the fallback branch cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real RAG inputs arrived. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Because the harness came before the optimisation, I caught a regression that looked like a win. Keeping a log of every configuration felt tedious until I needed to explain a regression. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Splitting the pipeline around the retrieval step made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. Caching AWS lookups helped until invalidation became the more interesting problem. Caching PyTorch lookups helped until invalidation became the more interesting problem. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Because the harness came before the optimisation, I caught a regression that looked like a win. Every improvement I could not reproduce twice turned out not to be an improvement. Writing the failure cases down first made the design conversation much shorter.
## What I measured

That gave latency of 20%, which I wrote down at the time. Two configurations differed by one parameter and by far more than one parameter's worth of output. The fallback path had never been exercised, so nobody knew it returned an empty result.

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Tuning React helped marginally; fixing how documents were split helped substantially. Every improvement I could not reproduce twice turned out not to be an improvement. In August I assumed the bottleneck was compute; it was almost entirely I/O. Instrumenting the embedding cache cost a day and saved considerably more than that later. Splitting the pipeline around the fallback branch made the failures much easier to localise. Caching React lookups helped until invalidation became the more interesting problem. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. The result held up in staging and quietly regressed the moment traffic patterns changed. Instrumenting the scoring function cost a day and saved considerably more than that later. Cleaning the React inputs produced a larger gain than any change to the model did. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Tuning RAG helped marginally; fixing how documents were split helped substantially. Caching vector search lookups helped until invalidation became the more interesting problem. Batching improved throughput and made the tail latency considerably harder to reason about. I spent an afternoon convinced the model was wrong before finding a bug in the loader. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.
## What I would change

Most of the week went into the chunking logic rather than the change I had actually planned to make. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. On the instrumented run, latency showed 29%. Splitting the pipeline around the scoring function made the failures much easier to localise. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. Tuning vector search helped marginally; fixing how documents were split helped substantially. I now write the evaluation before the change, which has saved me from at least two false wins. Comparing against a fixed baseline made the August numbers finally interpretable. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Caching vector search lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Most of the week went into the ingestion path rather than the change I had actually planned to make. I rebuilt the scoring function so the before-and-after comparison would actually mean something. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Cleaning the React inputs produced a larger gain than any change to the model did. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August.
