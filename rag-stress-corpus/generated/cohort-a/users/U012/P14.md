Notes from a few days of experiments, written while they are still fresh.

## The problem

Measured properly, retrieval came out at 18%. Measured properly, latency came out at 35%. Before changing anything I measured the ranking of candidate passages before touching anything else. I rebuilt the chunking behaviour and the numbers were not what I expected. I began by isolating the way inputs were normalised before touching anything else. The experiment started with the slowest stage in the pipeline which took longer than the fix itself. Before changing anything I measured the slowest stage in the pipeline before touching anything else. The experiment started with the slowest stage in the pipeline and the numbers were not what I expected. The interesting part turned out to be the way inputs were normalised because the earlier result was not reproducible. Most of the effort went into the retrieval step on its own which made the regression obvious. I rebuilt the slowest stage in the pipeline and the numbers were not what I expected. The experiment started with a small evaluation harness which made the regression obvious.
## What I built

The interesting part turned out to be the way inputs were normalised which made the regression obvious. The experiment started with the slowest stage in the pipeline because the earlier result was not reproducible. Accuracy landed at 9% once I measured it end to end. Latency landed at 19% once I measured it end to end. Most of the effort went into a small evaluation harness and that alone explained most of the gap. Most of the effort went into a small evaluation harness which made the regression obvious. Before changing anything I measured the chunking behaviour which made the regression obvious. The experiment started with the retrieval step on its own because the earlier result was not reproducible. I began by isolating the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the retrieval step on its own because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. The interesting part turned out to be the chunking behaviour which took longer than the fix itself.
## What I measured

The number I got for retrieval was 46%. Most of the effort went into the slowest stage in the pipeline before touching anything else. The interesting part turned out to be the assumptions baked into the defaults so the comparison would actually mean something. The interesting part turned out to be the retrieval step on its own and the numbers were not what I expected. The experiment started with a small evaluation harness and I kept the harness afterwards. The interesting part turned out to be the assumptions baked into the defaults and that alone explained most of the gap. I began by isolating a small evaluation harness which made the regression obvious. Before changing anything I measured the way inputs were normalised so the comparison would actually mean something. The interesting part turned out to be the chunking behaviour because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults so the comparison would actually mean something.
## What I would change

The interesting part turned out to be a small evaluation harness which made the regression obvious. I rebuilt the fallback path nobody exercised which made the regression obvious. The number I got for latency was 57%. I began by isolating the way inputs were normalised so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages and that alone explained most of the gap. The interesting part turned out to be the retrieval step on its own so the comparison would actually mean something. The experiment started with the retrieval step on its own which made the regression obvious. The experiment started with the ranking of candidate passages so the comparison would actually mean something. The interesting part turned out to be the assumptions baked into the defaults which made the regression obvious. I began by isolating the assumptions baked into the defaults because the earlier result was not reproducible.
