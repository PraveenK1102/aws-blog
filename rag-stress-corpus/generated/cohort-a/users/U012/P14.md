## The problem

Retrieval landed at 18% once I measured it end to end. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Instrumenting the embedding cache cost a day and saved considerably more than that later.

That gave latency of 35%, which I wrote down at the time. Caching vector search lookups helped until invalidation became the more interesting problem. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Tuning RAG helped marginally; fixing how documents were split helped substantially. Splitting the pipeline around the fallback branch made the failures much easier to localise. Writing the failure cases down first made the design conversation much shorter. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Instrumenting the reranking pass cost a day and saved considerably more than that later.
## What I built

I rebuilt the fallback branch so the before-and-after comparison would actually mean something. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs.

Accuracy landed at 9% once I measured it end to end. On the instrumented run, latency showed 19%. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Most of the week went into the retrieval step rather than the change I had actually planned to make. Instrumenting the evaluation harness cost a day and saved considerably more than that later. I do not trust an improvement that only appears on examples I picked myself. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The first version worked on the sample set and fell apart the moment real RAG inputs arrived. Splitting the pipeline around the embedding cache made the failures much easier to localise. Caching PyTorch lookups helped until invalidation became the more interesting problem.
## What I measured

Most of the week went into the fallback branch rather than the change I had actually planned to make. When I finally measured it, retrieval was 46%. I now write the evaluation before the change, which has saved me from at least two false wins. The interesting failures were the ones where the output read well but cited the wrong source. The result held up in staging and quietly regressed the moment traffic patterns changed. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. I keep a short list of results I do not believe yet, and revisit it weekly. Comparing against a fixed baseline made the July numbers finally interpretable. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Cleaning the React inputs produced a larger gain than any change to the model did. The fallback path had never been exercised, so nobody knew it returned an empty result. Cleaning the RAG inputs produced a larger gain than any change to the model did.
## What I would change

The number I got for latency was 57%. Keeping a log of every configuration felt tedious until I needed to explain a regression. Was the gain from the model or from cleaning the RAG inputs? Mostly the inputs. Batching improved throughput and made the tail latency considerably harder to reason about. The tempting change was the model; the effective change was how documents were split. Every improvement I could not reproduce twice turned out not to be an improvement. In July I assumed the bottleneck was compute; it was almost entirely I/O. Because the harness came before the optimisation, I caught a regression that looked like a win. Two configurations differed by one parameter and by far more than one parameter's worth of output. Tuning AWS helped marginally; fixing how documents were split helped substantially. I rebuilt the scoring function so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real React inputs arrived.
