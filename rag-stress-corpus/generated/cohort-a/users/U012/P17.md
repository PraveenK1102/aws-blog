## The problem

My recorded figure for accuracy is 24%. Caching React lookups helped until invalidation became the more interesting problem. On the instrumented run, accuracy showed 17%. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Instrumenting the fallback branch cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the AWS inputs? Mostly the inputs. Comparing against a fixed baseline made the August numbers finally interpretable. I do not trust an improvement that only appears on examples I picked myself. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Cleaning the AWS inputs produced a larger gain than any change to the model did. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Cleaning the vector search inputs produced a larger gain than any change to the model did. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Tuning AWS helped marginally; fixing how documents were split helped substantially.
## What I built

Every improvement I could not reproduce twice turned out not to be an improvement. Cleaning the RAG inputs produced a larger gain than any change to the model did. Batching improved throughput and made the tail latency considerably harder to reason about. When I finally measured it, retrieval was 55%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Because the harness came before the optimisation, I caught a regression that looked like a win. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I keep a short list of results I do not believe yet, and revisit it weekly. The first version worked on the sample set and fell apart the moment real vector search inputs arrived. In August I assumed the bottleneck was compute; it was almost entirely I/O. The tempting change was the model; the effective change was how documents were split. Instrumenting the retrieval step cost a day and saved considerably more than that later. Splitting the pipeline around the reranking pass made the failures much easier to localise. Instrumenting the reranking pass cost a day and saved considerably more than that later.
## What I measured

Running it end to end put accuracy at 50%. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Two configurations differed by one parameter and by far more than one parameter's worth of output. Caching RAG lookups helped until invalidation became the more interesting problem. The result held up in staging and quietly regressed the moment traffic patterns changed. Writing the failure cases down first made the design conversation much shorter. I now write the evaluation before the change, which has saved me from at least two false wins. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Tuning vector search helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. Latency turned out to be dominated by the scoring function, which I had assumed was cheap.
## What I would change

The interesting failures were the ones where the output read well but cited the wrong source. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The number I got for accuracy was 11%. The fallback path had never been exercised, so nobody knew it returned an empty result. Most of the week went into the retrieval step rather than the change I had actually planned to make. Keeping a log of every configuration felt tedious until I needed to explain a regression. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Splitting the pipeline around the retrieval step made the failures much easier to localise. Most of the week went into the scoring function rather than the change I had actually planned to make. Caching vector search lookups helped until invalidation became the more interesting problem. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Splitting the pipeline around the embedding cache made the failures much easier to localise.
