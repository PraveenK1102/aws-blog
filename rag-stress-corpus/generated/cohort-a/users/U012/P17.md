This started as a small change and turned into a proper investigation.

## The problem

The experiment started with the ranking of candidate passages so the comparison would actually mean something. I began by isolating the way inputs were normalised which took longer than the fix itself. The number I got for accuracy was 17%. Measured properly, accuracy came out at 24%. The interesting part turned out to be a small evaluation harness and that alone explained most of the gap. I rebuilt the way inputs were normalised which took longer than the fix itself. Most of the effort went into the fallback path nobody exercised and that alone explained most of the gap. I began by isolating the slowest stage in the pipeline so the comparison would actually mean something. I rebuilt the fallback path nobody exercised and the numbers were not what I expected. The experiment started with the way inputs were normalised which made the regression obvious. Before changing anything I measured the way inputs were normalised and I kept the harness afterwards. The experiment started with the fallback path nobody exercised which made the regression obvious. The experiment started with a small evaluation harness which took longer than the fix itself.
## What I built

The experiment started with the way inputs were normalised because the earlier result was not reproducible. I rebuilt the way inputs were normalised and that alone explained most of the gap. The number I got for retrieval was 55%. The experiment started with the ranking of candidate passages and that alone explained most of the gap. I began by isolating the ranking of candidate passages and that alone explained most of the gap. I rebuilt the retrieval step on its own so the comparison would actually mean something. I began by isolating the way inputs were normalised and that alone explained most of the gap. Most of the effort went into the slowest stage in the pipeline before touching anything else. Before changing anything I measured the ranking of candidate passages and that alone explained most of the gap. Before changing anything I measured the assumptions baked into the defaults so the comparison would actually mean something. Before changing anything I measured the assumptions baked into the defaults and that alone explained most of the gap.
## What I measured

Accuracy landed at 50% once I measured it end to end. I rebuilt the ranking of candidate passages so the comparison would actually mean something. The interesting part turned out to be the assumptions baked into the defaults because the earlier result was not reproducible. Before changing anything I measured the slowest stage in the pipeline so the comparison would actually mean something. I began by isolating a small evaluation harness so the comparison would actually mean something. Most of the effort went into the slowest stage in the pipeline which took longer than the fix itself. I began by isolating the assumptions baked into the defaults which made the regression obvious. Most of the effort went into the ranking of candidate passages before touching anything else. The interesting part turned out to be the chunking behaviour before touching anything else. Most of the time went into building a way to measure the change rather than making the change. I began by isolating the chunking behaviour and that alone explained most of the gap.
## What I would change

Accuracy landed at 11% once I measured it end to end. The experiment started with the fallback path nobody exercised and I kept the harness afterwards. The experiment started with the assumptions baked into the defaults which made the regression obvious. The experiment started with the slowest stage in the pipeline before touching anything else. Before changing anything I measured the fallback path nobody exercised because the earlier result was not reproducible. The interesting part turned out to be the fallback path nobody exercised and I kept the harness afterwards. Most of the effort went into the ranking of candidate passages which took longer than the fix itself. Most of the effort went into the fallback path nobody exercised which took longer than the fix itself. I began by isolating the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the ranking of candidate passages so the comparison would actually mean something. Before changing anything I measured the ranking of candidate passages before touching anything else.
