## The problem

Because the harness came before the optimisation, I caught a regression that looked like a win. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Once I logged every configuration, explaining the regression to myself took minutes rather than a day.

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The number I got for accuracy was 54%. Splitting the pipeline around the chunking logic made the failures much easier to localise. The interesting failures were the ones where the output read well but cited the wrong source. The first version worked on the sample set and fell apart the moment real RAG inputs arrived. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Tuning AWS helped marginally; fixing how documents were split helped substantially. I do not trust an improvement that only appears on examples I picked myself. Tuning RAG helped marginally; fixing how documents were split helped substantially. Splitting the pipeline around the fallback branch made the failures much easier to localise. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. Caching AWS lookups helped until invalidation became the more interesting problem. Caching RAG lookups helped until invalidation became the more interesting problem. Caching PyTorch lookups helped until invalidation became the more interesting problem. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Batching improved throughput and made the tail latency considerably harder to reason about.
## What I built

The first version worked on the sample set and fell apart the moment real React inputs arrived. The result held up in staging and quietly regressed the moment traffic patterns changed. Keeping a log of every configuration felt tedious until I needed to explain a regression.

Writing the failure cases down first made the design conversation much shorter. Running it end to end put retrieval at 43%. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. The fallback path had never been exercised, so nobody knew it returned an empty result. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Cleaning the React inputs produced a larger gain than any change to the model did. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Cleaning the RAG inputs produced a larger gain than any change to the model did. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Cleaning the AWS inputs produced a larger gain than any change to the model did. Caching RAG lookups helped until invalidation became the more interesting problem. Keeping a log of every configuration felt tedious until I needed to explain a regression. Every improvement I could not reproduce twice turned out not to be an improvement.
## What I measured

Measured properly, accuracy came out at 22%. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Instrumenting the chunking logic cost a day and saved considerably more than that later. Every improvement I could not reproduce twice turned out not to be an improvement. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. The tempting change was the model; the effective change was how documents were split. Comparing against a fixed baseline made the July numbers finally interpretable. Batching improved throughput and made the tail latency considerably harder to reason about. Caching React lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the vector search inputs? Mostly the inputs. Tuning React helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Instrumenting the scoring function cost a day and saved considerably more than that later. Tuning vector search helped marginally; fixing how documents were split helped substantially. The interesting failures were the ones where the output read well but cited the wrong source. The fallback path had never been exercised, so nobody knew it returned an empty result. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Two configurations differed by one parameter and by far more than one parameter's worth of output.
## What I would change

I keep a short list of results I do not believe yet, and revisit it weekly. Was the gain from the model or from cleaning the RAG inputs? Mostly the inputs. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first.

Two configurations differed by one parameter and by far more than one parameter's worth of output. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the ingestion path cost a day and saved considerably more than that later. Most of the week went into the scoring function rather than the change I had actually planned to make. In July I assumed the bottleneck was compute; it was almost entirely I/O. Most of the week went into the embedding cache rather than the change I had actually planned to make. Splitting the pipeline around the scoring function made the failures much easier to localise. Most of the week went into the chunking logic rather than the change I had actually planned to make. Caching vector search lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Cleaning the vector search inputs produced a larger gain than any change to the model did. Instrumenting the reranking pass cost a day and saved considerably more than that later. Writing the failure cases down first made the design conversation much shorter. I do not trust an improvement that only appears on examples I picked myself. The tempting change was the model; the effective change was how documents were split.
