## The problem

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. When I finally measured it, retrieval was 36%. Cleaning the vector search inputs produced a larger gain than any change to the model did. My recorded figure for latency is 60%. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The result held up in staging and quietly regressed the moment traffic patterns changed. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The first version worked on the sample set and fell apart the moment real RAG inputs arrived. In July I assumed the bottleneck was compute; it was almost entirely I/O. Instrumenting the retrieval step cost a day and saved considerably more than that later. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Most of the week went into the fallback branch rather than the change I had actually planned to make. Cleaning the RAG inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. Cleaning the React inputs produced a larger gain than any change to the model did. Splitting the pipeline around the reranking pass made the failures much easier to localise. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. The fallback path had never been exercised, so nobody knew it returned an empty result. The tempting change was the model; the effective change was how documents were split. Because the harness came before the optimisation, I caught a regression that looked like a win.
## What I built

Caching React lookups helped until invalidation became the more interesting problem. Instrumenting the fallback branch cost a day and saved considerably more than that later. The interesting failures were the ones where the output read well but cited the wrong source.

Measured properly, latency came out at 6%. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Every improvement I could not reproduce twice turned out not to be an improvement. Comparing against a fixed baseline made the July numbers finally interpretable. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Keeping a log of every configuration felt tedious until I needed to explain a regression. Caching PyTorch lookups helped until invalidation became the more interesting problem. Because the harness came before the optimisation, I caught a regression that looked like a win. Instrumenting the ingestion path cost a day and saved considerably more than that later. Caching AWS lookups helped until invalidation became the more interesting problem. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real React inputs arrived. I do not trust an improvement that only appears on examples I picked myself. Tuning RAG helped marginally; fixing how documents were split helped substantially. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Keeping a log of every configuration felt tedious until I needed to explain a regression.
## What I measured

Tuning React helped marginally; fixing how documents were split helped substantially. The first version worked on the sample set and fell apart the moment real AWS inputs arrived. Latency landed at 30% once I measured it end to end. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Caching RAG lookups helped until invalidation became the more interesting problem. The tempting change was the model; the effective change was how documents were split. Writing the failure cases down first made the design conversation much shorter. Two configurations differed by one parameter and by far more than one parameter's worth of output. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Tuning RAG helped marginally; fixing how documents were split helped substantially. Instrumenting the reranking pass cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Instrumenting the scoring function cost a day and saved considerably more than that later. Tuning vector search helped marginally; fixing how documents were split helped substantially. Two configurations differed by one parameter and by far more than one parameter's worth of output. I keep a short list of results I do not believe yet, and revisit it weekly. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Caching PyTorch lookups helped until invalidation became the more interesting problem.
## What I would change

I do not trust an improvement that only appears on examples I picked myself. Measured properly, retrieval came out at 59%. The fallback path had never been exercised, so nobody knew it returned an empty result. Splitting the pipeline around the ingestion path made the failures much easier to localise. Batching improved throughput and made the tail latency considerably harder to reason about. I now write the evaluation before the change, which has saved me from at least two false wins. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Most of the week went into the scoring function rather than the change I had actually planned to make. Cleaning the AWS inputs produced a larger gain than any change to the model did. I keep a short list of results I do not believe yet, and revisit it weekly. Was the gain from the model or from cleaning the React inputs? Mostly the inputs. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Most of the week went into the chunking logic rather than the change I had actually planned to make. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Caching vector search lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the RAG inputs? Mostly the inputs. In July I assumed the bottleneck was compute; it was almost entirely I/O. Writing the failure cases down first made the design conversation much shorter. Comparing against a fixed baseline made the July numbers finally interpretable.
