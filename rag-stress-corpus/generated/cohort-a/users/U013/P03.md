## The problem

That gave retrieval of 40%, which I wrote down at the time. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The result held up in staging and quietly regressed the moment traffic patterns changed. Retrieval landed at 44% once I measured it end to end.

Caching Docker lookups helped until invalidation became the more interesting problem. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real Python inputs arrived. In March I assumed the bottleneck was compute; it was almost entirely I/O. Most of the week went into the reranking pass rather than the change I had actually planned to make. Tuning Redis helped marginally; fixing how documents were split helped substantially. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning Qdrant helped marginally; fixing how documents were split helped substantially. Instrumenting the ingestion path cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Splitting the pipeline around the retrieval step made the failures much easier to localise. Caching Redis lookups helped until invalidation became the more interesting problem. Keeping a log of every configuration felt tedious until I needed to explain a regression.
## What I built

My recorded figure for latency is 16%. Latency landed at 45% once I measured it end to end. I now write the evaluation before the change, which has saved me from at least two false wins. The interesting failures were the ones where the output read well but cited the wrong source.

Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Two configurations differed by one parameter and by far more than one parameter's worth of output. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Comparing against a fixed baseline made the March numbers finally interpretable. Instrumenting the chunking logic cost a day and saved considerably more than that later. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Caching Qdrant lookups helped until invalidation became the more interesting problem. Cleaning the Qdrant inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Writing the failure cases down first made the design conversation much shorter.
## What I measured

I do not trust an improvement that only appears on examples I picked myself. The fallback path had never been exercised, so nobody knew it returned an empty result. Because the harness came before the optimisation, I caught a regression that looked like a win.

The number I got for latency was 9%. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. The tempting change was the model; the effective change was how documents were split. Cleaning the Python inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs. Cleaning the Redis inputs produced a larger gain than any change to the model did. Caching Python lookups helped until invalidation became the more interesting problem. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Qdrant inputs? Mostly the inputs. Most of the week went into the fallback branch rather than the change I had actually planned to make. Splitting the pipeline around the embedding cache made the failures much easier to localise. Caching FastAPI lookups helped until invalidation became the more interesting problem. Cleaning the Docker inputs produced a larger gain than any change to the model did. Tuning Python helped marginally; fixing how documents were split helped substantially. Batching improved throughput and made the tail latency considerably harder to reason about.
## What I would change

Writing the failure cases down first made the design conversation much shorter. When I finally measured it, latency was 35%. Most of the week went into the scoring function rather than the change I had actually planned to make. I keep a short list of results I do not believe yet, and revisit it weekly. I spent an afternoon convinced the model was wrong before finding a bug in the loader. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Batching improved throughput and made the tail latency considerably harder to reason about. Every improvement I could not reproduce twice turned out not to be an improvement. Splitting the pipeline around the reranking pass made the failures much easier to localise. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Tuning Docker helped marginally; fixing how documents were split helped substantially. Instrumenting the retrieval step cost a day and saved considerably more than that later. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Instrumenting the reranking pass cost a day and saved considerably more than that later. Instrumenting the scoring function cost a day and saved considerably more than that later. Two configurations differed by one parameter and by far more than one parameter's worth of output.
