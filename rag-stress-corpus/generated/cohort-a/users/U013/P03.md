Notes from a few days of experiments, written while they are still fresh.

## The problem

Before changing anything I measured the retrieval step on its own before touching anything else. I rebuilt a small evaluation harness which made the regression obvious. Retrieval landed at 44% once I measured it end to end. The number I got for retrieval was 40%. The experiment started with the chunking behaviour so the comparison would actually mean something. I rebuilt the fallback path nobody exercised before touching anything else. Before changing anything I measured the retrieval step on its own and I kept the harness afterwards. The interesting part turned out to be the slowest stage in the pipeline and that alone explained most of the gap. I began by isolating the slowest stage in the pipeline before touching anything else. I rebuilt the chunking behaviour and the numbers were not what I expected. The interesting part turned out to be the retrieval step on its own which made the regression obvious. Before changing anything I measured the ranking of candidate passages and I kept the harness afterwards. Most of the effort went into the fallback path nobody exercised and that alone explained most of the gap. The experiment started with the chunking behaviour which took longer than the fix itself. The experiment started with the retrieval step on its own and that alone explained most of the gap. I began by isolating the chunking behaviour and I kept the harness afterwards. Before changing anything I measured the assumptions baked into the defaults which took longer than the fix itself.
## What I built

I rebuilt the ranking of candidate passages because the earlier result was not reproducible. The interesting part turned out to be the way inputs were normalised and I kept the harness afterwards. Latency landed at 45% once I measured it end to end. The number I got for latency was 16%. I rebuilt the way inputs were normalised which took longer than the fix itself. The interesting part turned out to be the way inputs were normalised before touching anything else. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression. I rebuilt the way inputs were normalised so the comparison would actually mean something. The experiment started with the fallback path nobody exercised before touching anything else. The interesting part turned out to be the chunking behaviour and that alone explained most of the gap. The experiment started with the ranking of candidate passages because the earlier result was not reproducible. The experiment started with the fallback path nobody exercised and I kept the harness afterwards. I rebuilt the retrieval step on its own and I kept the harness afterwards. I rebuilt the ranking of candidate passages and I kept the harness afterwards. The interesting part turned out to be the fallback path nobody exercised before touching anything else. I began by isolating the fallback path nobody exercised and that alone explained most of the gap.
## What I measured

Before changing anything I measured the slowest stage in the pipeline and the numbers were not what I expected. I rebuilt the assumptions baked into the defaults which made the regression obvious. Measured properly, latency came out at 9%. The interesting part turned out to be a small evaluation harness before touching anything else. I began by isolating the slowest stage in the pipeline and the numbers were not what I expected. The interesting part turned out to be the way inputs were normalised and that alone explained most of the gap. Before changing anything I measured the retrieval step on its own which made the regression obvious. Before changing anything I measured a small evaluation harness before touching anything else. I began by isolating the fallback path nobody exercised before touching anything else. The experiment started with the fallback path nobody exercised which took longer than the fix itself. I rebuilt the slowest stage in the pipeline and that alone explained most of the gap. Most of the effort went into the way inputs were normalised which made the regression obvious. I began by isolating the slowest stage in the pipeline and that alone explained most of the gap. Most of the effort went into a small evaluation harness and that alone explained most of the gap. The interesting part turned out to be the fallback path nobody exercised which took longer than the fix itself.
## What I would change

The experiment started with the slowest stage in the pipeline and that alone explained most of the gap. The number I got for latency was 35%. Most of the effort went into the chunking behaviour and I kept the harness afterwards. The experiment started with the retrieval step on its own and I kept the harness afterwards. I began by isolating a small evaluation harness and I kept the harness afterwards. I rebuilt the ranking of candidate passages and the numbers were not what I expected. Before changing anything I measured the way inputs were normalised before touching anything else. The interesting part turned out to be a small evaluation harness and I kept the harness afterwards. The experiment started with a small evaluation harness which made the regression obvious. I began by isolating a small evaluation harness because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. The interesting part turned out to be the way inputs were normalised so the comparison would actually mean something. The experiment started with the way inputs were normalised before touching anything else. I began by isolating the chunking behaviour which took longer than the fix itself. The experiment started with the way inputs were normalised so the comparison would actually mean something.
