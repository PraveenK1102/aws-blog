## The problem

Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Measured properly, retrieval came out at 26%. In June I assumed the bottleneck was compute; it was almost entirely I/O. I keep a short list of results I do not believe yet, and revisit it weekly. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The tempting change was the model; the effective change was how documents were split. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. I now write the evaluation before the change, which has saved me from at least two false wins. Most of the week went into the scoring function rather than the change I had actually planned to make. Caching Redis lookups helped until invalidation became the more interesting problem. Most of the week went into the reranking pass rather than the change I had actually planned to make.
## What I built

The fallback path had never been exercised, so nobody knew it returned an empty result. Running it end to end put retrieval at 59%. I do not trust an improvement that only appears on examples I picked myself. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Every improvement I could not reproduce twice turned out not to be an improvement. The result held up in staging and quietly regressed the moment traffic patterns changed. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Batching improved throughput and made the tail latency considerably harder to reason about. Writing the failure cases down first made the design conversation much shorter. The interesting failures were the ones where the output read well but cited the wrong source. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap.
## What I measured

Splitting the pipeline around the chunking logic made the failures much easier to localise. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. When I finally measured it, retrieval was 51%. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Cleaning the Redis inputs produced a larger gain than any change to the model did. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. Because the harness came before the optimisation, I caught a regression that looked like a win. Two configurations differed by one parameter and by far more than one parameter's worth of output. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Cleaning the Python inputs produced a larger gain than any change to the model did. I rebuilt the scoring function so the before-and-after comparison would actually mean something.
## What I would change

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Caching Python lookups helped until invalidation became the more interesting problem. Instrumenting the fallback branch cost a day and saved considerably more than that later. Keeping a log of every configuration felt tedious until I needed to explain a regression. Comparing against a fixed baseline made the June numbers finally interpretable. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Caching Docker lookups helped until invalidation became the more interesting problem. Tuning Redis helped marginally; fixing how documents were split helped substantially. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Tuning Docker helped marginally; fixing how documents were split helped substantially.
