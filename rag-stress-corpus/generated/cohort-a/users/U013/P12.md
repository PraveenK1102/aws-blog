## The problem

I now write the evaluation before the change, which has saved me from at least two false wins. Because the harness came before the optimisation, I caught a regression that looked like a win. Comparing against a fixed baseline made the June numbers finally interpretable. When I finally measured it, accuracy was 23%. In June I assumed the bottleneck was compute; it was almost entirely I/O. Caching Docker lookups helped until invalidation became the more interesting problem. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Splitting the pipeline around the retrieval step made the failures much easier to localise. Tuning Python helped marginally; fixing how documents were split helped substantially. I spent an afternoon convinced the model was wrong before finding a bug in the loader.
## What I built

Instrumenting the reranking pass cost a day and saved considerably more than that later. The tempting change was the model; the effective change was how documents were split. Caching FastAPI lookups helped until invalidation became the more interesting problem. That gave accuracy of 36%, which I wrote down at the time.

The interesting failures were the ones where the output read well but cited the wrong source. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. The result held up in staging and quietly regressed the moment traffic patterns changed. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. Caching Python lookups helped until invalidation became the more interesting problem. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Qdrant inputs? Mostly the inputs.
## What I measured

Batching improved throughput and made the tail latency considerably harder to reason about. My recorded figure for latency is 59%. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Cleaning the Redis inputs produced a larger gain than any change to the model did. I rebuilt the scoring function so the before-and-after comparison would actually mean something. I keep a short list of results I do not believe yet, and revisit it weekly. Instrumenting the ingestion path cost a day and saved considerably more than that later. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Most of the week went into the reranking pass rather than the change I had actually planned to make.
## What I would change

The fallback path had never been exercised, so nobody knew it returned an empty result. Most of the week went into the fallback branch rather than the change I had actually planned to make. Writing the failure cases down first made the design conversation much shorter. On the instrumented run, retrieval showed 12%. Two configurations differed by one parameter and by far more than one parameter's worth of output. Keeping a log of every configuration felt tedious until I needed to explain a regression. Every improvement I could not reproduce twice turned out not to be an improvement. The first version worked on the sample set and fell apart the moment real Qdrant inputs arrived. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. I do not trust an improvement that only appears on examples I picked myself.
