## The problem

The fallback path had never been exercised, so nobody knew it returned an empty result. The tempting change was the model; the effective change was how documents were split. I keep a short list of results I do not believe yet, and revisit it weekly.

On the instrumented run, latency showed 46%. Accuracy landed at 13% once I measured it end to end. Comparing against a fixed baseline made the May numbers finally interpretable. I now write the evaluation before the change, which has saved me from at least two false wins. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Because the harness came before the optimisation, I caught a regression that looked like a win. Most of the week went into the retrieval step rather than the change I had actually planned to make. Two configurations differed by one parameter and by far more than one parameter's worth of output. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Splitting the pipeline around the ingestion path made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Tuning Redis helped marginally; fixing how documents were split helped substantially. Tuning Qdrant helped marginally; fixing how documents were split helped substantially. The interesting failures were the ones where the output read well but cited the wrong source. Instrumenting the embedding cache cost a day and saved considerably more than that later.
## What I built

Caching Python lookups helped until invalidation became the more interesting problem. Cleaning the Docker inputs produced a larger gain than any change to the model did. That gave retrieval of 49%, which I wrote down at the time.

Measured properly, retrieval came out at 30%. Splitting the pipeline around the chunking logic made the failures much easier to localise. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The interesting failures were the ones where the output read well but cited the wrong source. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Instrumenting the retrieval step cost a day and saved considerably more than that later. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.
## What I measured

Batching improved throughput and made the tail latency considerably harder to reason about. Latency landed at 44% once I measured it end to end. Instrumenting the reranking pass cost a day and saved considerably more than that later. In May I assumed the bottleneck was compute; it was almost entirely I/O. Every improvement I could not reproduce twice turned out not to be an improvement. Most of the week went into the fallback branch rather than the change I had actually planned to make. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. I do not trust an improvement that only appears on examples I picked myself. Writing the failure cases down first made the design conversation much shorter. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. The first version worked on the sample set and fell apart the moment real Qdrant inputs arrived. Tuning Python helped marginally; fixing how documents were split helped substantially. Instrumenting the embedding cache cost a day and saved considerably more than that later. Caching Redis lookups helped until invalidation became the more interesting problem. Instrumenting the fallback branch cost a day and saved considerably more than that later. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Comparing against a fixed baseline made the May numbers finally interpretable.
## What I would change

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Running it end to end put accuracy at 26%.

The result held up in staging and quietly regressed the moment traffic patterns changed. Caching Docker lookups helped until invalidation became the more interesting problem. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. Keeping a log of every configuration felt tedious until I needed to explain a regression. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Tuning Docker helped marginally; fixing how documents were split helped substantially. Caching Qdrant lookups helped until invalidation became the more interesting problem. Was the gain from the model or from cleaning the Qdrant inputs? Mostly the inputs. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Cleaning the Redis inputs produced a larger gain than any change to the model did. Caching FastAPI lookups helped until invalidation became the more interesting problem. Cleaning the Python inputs produced a larger gain than any change to the model did. Tuning Qdrant helped marginally; fixing how documents were split helped substantially.
