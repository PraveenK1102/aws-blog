## The problem

I spent an afternoon convinced the model was wrong before finding a bug in the loader. Comparing against a fixed baseline made the May numbers finally interpretable. That gave latency of 12%, which I wrote down at the time. Caching Python lookups helped until invalidation became the more interesting problem. Cleaning the Qdrant inputs produced a larger gain than any change to the model did.

My recorded figure for accuracy is 54%. I now write the evaluation before the change, which has saved me from at least two false wins. The fallback path had never been exercised, so nobody knew it returned an empty result. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. Splitting the pipeline around the embedding cache made the failures much easier to localise. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Caching Docker lookups helped until invalidation became the more interesting problem. Cleaning the Python inputs produced a larger gain than any change to the model did. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap.
## What I built

I rebuilt the reranking pass so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Qdrant inputs arrived. Latency landed at 14% once I measured it end to end. Keeping a log of every configuration felt tedious until I needed to explain a regression. The tempting change was the model; the effective change was how documents were split. Every improvement I could not reproduce twice turned out not to be an improvement. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Instrumenting the embedding cache cost a day and saved considerably more than that later. I keep a short list of results I do not believe yet, and revisit it weekly. I do not trust an improvement that only appears on examples I picked myself. Instrumenting the fallback branch cost a day and saved considerably more than that later. Caching Qdrant lookups helped until invalidation became the more interesting problem. I rebuilt the retrieval step so the before-and-after comparison would actually mean something.
## What I measured

Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Running it end to end put accuracy at 13%. Because the harness came before the optimisation, I caught a regression that looked like a win. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Most of the week went into the embedding cache rather than the change I had actually planned to make. Writing the failure cases down first made the design conversation much shorter. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Most of the week went into the retrieval step rather than the change I had actually planned to make. Tuning Redis helped marginally; fixing how documents were split helped substantially. Cleaning the Docker inputs produced a larger gain than any change to the model did.
## What I would change

Batching improved throughput and made the tail latency considerably harder to reason about. In May I assumed the bottleneck was compute; it was almost entirely I/O. On the instrumented run, latency showed 15%. Two configurations differed by one parameter and by far more than one parameter's worth of output. The result held up in staging and quietly regressed the moment traffic patterns changed. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The interesting failures were the ones where the output read well but cited the wrong source. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Splitting the pipeline around the reranking pass made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs.
