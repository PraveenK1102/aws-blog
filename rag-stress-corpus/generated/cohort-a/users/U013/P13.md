Spent most of this week on the retrieval side of the project.

## The problem

I rebuilt the slowest stage in the pipeline and that alone explained most of the gap. Retrieval landed at 32% once I measured it end to end. Most of the effort went into the retrieval step on its own so the comparison would actually mean something. I began by isolating a small evaluation harness before touching anything else. I began by isolating the way inputs were normalised because the earlier result was not reproducible. The experiment started with the ranking of candidate passages because the earlier result was not reproducible. The interesting part turned out to be the ranking of candidate passages and I kept the harness afterwards. The experiment started with the chunking behaviour which took longer than the fix itself. I rebuilt the retrieval step on its own and the numbers were not what I expected. Most of the effort went into the fallback path nobody exercised before touching anything else.
## What I built

Before changing anything I measured the way inputs were normalised which took longer than the fix itself. Measured properly, latency came out at 58%. Most of the effort went into the slowest stage in the pipeline and the numbers were not what I expected. Latency turned out to be dominated by a step I had assumed was cheap. I rebuilt a small evaluation harness which took longer than the fix itself. Most of the effort went into a small evaluation harness and the numbers were not what I expected. The experiment started with the way inputs were normalised because the earlier result was not reproducible. Most of the effort went into the fallback path nobody exercised and that alone explained most of the gap. The experiment started with the assumptions baked into the defaults because the earlier result was not reproducible. Most of the effort went into the way inputs were normalised so the comparison would actually mean something.
## What I measured

Most of the effort went into a small evaluation harness before touching anything else. Measured properly, accuracy came out at 41%. I rebuilt the way inputs were normalised because the earlier result was not reproducible. Before changing anything I measured a small evaluation harness and I kept the harness afterwards. Before changing anything I measured the ranking of candidate passages which took longer than the fix itself. The interesting part turned out to be the ranking of candidate passages before touching anything else. I began by isolating the fallback path nobody exercised which took longer than the fix itself. The interesting part turned out to be the retrieval step on its own which made the regression obvious. The interesting part turned out to be the fallback path nobody exercised because the earlier result was not reproducible. I rebuilt the chunking behaviour and that alone explained most of the gap.
## What I would change

I began by isolating the way inputs were normalised before touching anything else. Most of the effort went into the chunking behaviour before touching anything else. Measured properly, accuracy came out at 26%. I rebuilt the slowest stage in the pipeline and the numbers were not what I expected. The interesting part turned out to be the way inputs were normalised so the comparison would actually mean something. I began by isolating the fallback path nobody exercised and I kept the harness afterwards. I rebuilt the chunking behaviour so the comparison would actually mean something. I rebuilt the slowest stage in the pipeline and I kept the harness afterwards. Before changing anything I measured the assumptions baked into the defaults because the earlier result was not reproducible. Most of the effort went into the ranking of candidate passages which made the regression obvious.
