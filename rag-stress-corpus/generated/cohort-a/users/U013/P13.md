## The problem

Two configurations differed by one parameter and by far more than one parameter's worth of output. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Caching Redis lookups helped until invalidation became the more interesting problem. Measured properly, retrieval came out at 32%.

Batching improved throughput and made the tail latency considerably harder to reason about. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the fallback branch cost a day and saved considerably more than that later. Most of the week went into the ingestion path rather than the change I had actually planned to make. Instrumenting the retrieval step cost a day and saved considerably more than that later. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap.
## What I built

Once I logged every configuration, explaining the regression to myself took minutes rather than a day. When I finally measured it, latency was 58%. Tuning Docker helped marginally; fixing how documents were split helped substantially. Comparing against a fixed baseline made the July numbers finally interpretable. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. In July I assumed the bottleneck was compute; it was almost entirely I/O. I do not trust an improvement that only appears on examples I picked myself. Writing the failure cases down first made the design conversation much shorter. Caching Docker lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the reranking pass made the failures much easier to localise.
## What I measured

The result held up in staging and quietly regressed the moment traffic patterns changed. Measured properly, accuracy came out at 41%. The interesting failures were the ones where the output read well but cited the wrong source.

I rebuilt the embedding cache so the before-and-after comparison would actually mean something. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The tempting change was the model; the effective change was how documents were split. Caching FastAPI lookups helped until invalidation became the more interesting problem. Because the harness came before the optimisation, I caught a regression that looked like a win. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Cleaning the Qdrant inputs produced a larger gain than any change to the model did. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Most of the week went into the fallback branch rather than the change I had actually planned to make.
## What I would change

The number I got for accuracy was 26%. I keep a short list of results I do not believe yet, and revisit it weekly. Keeping a log of every configuration felt tedious until I needed to explain a regression. The fallback path had never been exercised, so nobody knew it returned an empty result. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Instrumenting the scoring function cost a day and saved considerably more than that later.
