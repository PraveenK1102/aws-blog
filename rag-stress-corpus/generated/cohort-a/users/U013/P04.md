## The problem

The number I got for retrieval was 42%. Accuracy landed at 32% once I measured it end to end. Batching improved throughput and made the tail latency considerably harder to reason about. Every improvement I could not reproduce twice turned out not to be an improvement. The interesting failures were the ones where the output read well but cited the wrong source. Most of the week went into the reranking pass rather than the change I had actually planned to make. Splitting the pipeline around the ingestion path made the failures much easier to localise. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. I do not trust an improvement that only appears on examples I picked myself. Writing the failure cases down first made the design conversation much shorter. Instrumenting the retrieval step cost a day and saved considerably more than that later.
## What I built

I keep a short list of results I do not believe yet, and revisit it weekly. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Comparing against a fixed baseline made the April numbers finally interpretable. On the instrumented run, accuracy showed 21%. The tempting change was the model; the effective change was how documents were split.

When I finally measured it, latency was 24%. Tuning Qdrant helped marginally; fixing how documents were split helped substantially. The fallback path had never been exercised, so nobody knew it returned an empty result. I now write the evaluation before the change, which has saved me from at least two false wins. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Caching Redis lookups helped until invalidation became the more interesting problem. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. I spent an afternoon convinced the model was wrong before finding a bug in the loader.
## What I measured

Instrumenting the scoring function cost a day and saved considerably more than that later. Measured properly, accuracy came out at 9%. Two configurations differed by one parameter and by far more than one parameter's worth of output. Cleaning the Redis inputs produced a larger gain than any change to the model did. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. In April I assumed the bottleneck was compute; it was almost entirely I/O. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Cleaning the Qdrant inputs produced a larger gain than any change to the model did. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Caching Python lookups helped until invalidation became the more interesting problem.
## What I would change

The first version worked on the sample set and fell apart the moment real Docker inputs arrived. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Measured properly, retrieval came out at 22%.

The result held up in staging and quietly regressed the moment traffic patterns changed. Because the harness came before the optimisation, I caught a regression that looked like a win. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Most of the week went into the ingestion path rather than the change I had actually planned to make. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs.
