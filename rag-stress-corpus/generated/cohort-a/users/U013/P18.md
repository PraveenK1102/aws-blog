## The problem

Most of the week went into the fallback branch rather than the change I had actually planned to make. Measured properly, accuracy came out at 36%. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Accuracy landed at 55% once I measured it end to end.

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Most of the week went into the reranking pass rather than the change I had actually planned to make. Instrumenting the retrieval step cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Every improvement I could not reproduce twice turned out not to be an improvement. Instrumenting the ingestion path cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Writing the failure cases down first made the design conversation much shorter. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Cleaning the Redis inputs produced a larger gain than any change to the model did. Instrumenting the chunking logic cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the Qdrant inputs? Mostly the inputs. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. I now write the evaluation before the change, which has saved me from at least two false wins.
## What I built

Caching FastAPI lookups helped until invalidation became the more interesting problem. Tuning Docker helped marginally; fixing how documents were split helped substantially. That gave latency of 44%, which I wrote down at the time. The interesting failures were the ones where the output read well but cited the wrong source.

The fallback path had never been exercised, so nobody knew it returned an empty result. On the instrumented run, accuracy showed 49%. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Tuning Redis helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Instrumenting the scoring function cost a day and saved considerably more than that later. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Cleaning the Qdrant inputs produced a larger gain than any change to the model did. Cleaning the Docker inputs produced a larger gain than any change to the model did. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August.
## What I measured

Splitting the pipeline around the embedding cache made the failures much easier to localise. The number I got for latency was 35%. Batching improved throughput and made the tail latency considerably harder to reason about.

In August I assumed the bottleneck was compute; it was almost entirely I/O. I keep a short list of results I do not believe yet, and revisit it weekly. Comparing against a fixed baseline made the August numbers finally interpretable. The tempting change was the model; the effective change was how documents were split. The result held up in staging and quietly regressed the moment traffic patterns changed. Two configurations differed by one parameter and by far more than one parameter's worth of output. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Cleaning the Python inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Splitting the pipeline around the ingestion path made the failures much easier to localise. Tuning Qdrant helped marginally; fixing how documents were split helped substantially. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Caching Docker lookups helped until invalidation became the more interesting problem. Because the harness came before the optimisation, I caught a regression that looked like a win.
## What I would change

The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. I now write the evaluation before the change, which has saved me from at least two false wins. My recorded figure for accuracy is 33%. Because the harness came before the optimisation, I caught a regression that looked like a win. Splitting the pipeline around the scoring function made the failures much easier to localise. I do not trust an improvement that only appears on examples I picked myself. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Caching Qdrant lookups helped until invalidation became the more interesting problem. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. Tuning Python helped marginally; fixing how documents were split helped substantially. Caching Redis lookups helped until invalidation became the more interesting problem. Caching Python lookups helped until invalidation became the more interesting problem. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Batching improved throughput and made the tail latency considerably harder to reason about.
