## The problem

Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Keeping a log of every configuration felt tedious until I needed to explain a regression. On the instrumented run, accuracy showed 14%. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Latency landed at 35% once I measured it end to end. Caching Docker lookups helped until invalidation became the more interesting problem. I keep a short list of results I do not believe yet, and revisit it weekly. I do not trust an improvement that only appears on examples I picked myself. In August I assumed the bottleneck was compute; it was almost entirely I/O. Splitting the pipeline around the reranking pass made the failures much easier to localise. Cleaning the Qdrant inputs produced a larger gain than any change to the model did. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Caching Python lookups helped until invalidation became the more interesting problem.
## What I built

The first version worked on the sample set and fell apart the moment real Docker inputs arrived. Comparing against a fixed baseline made the August numbers finally interpretable. My recorded figure for retrieval is 50%. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The interesting failures were the ones where the output read well but cited the wrong source. Tuning Docker helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. I now write the evaluation before the change, which has saved me from at least two false wins. Tuning Redis helped marginally; fixing how documents were split helped substantially. The first version worked on the sample set and fell apart the moment real Redis inputs arrived.
## What I measured

The result held up in staging and quietly regressed the moment traffic patterns changed. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Cleaning the Docker inputs produced a larger gain than any change to the model did. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. That gave latency of 23%, which I wrote down at the time. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Two configurations differed by one parameter and by far more than one parameter's worth of output. Most of the week went into the fallback branch rather than the change I had actually planned to make. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. The fallback path had never been exercised, so nobody knew it returned an empty result. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap.
## What I would change

Because the harness came before the optimisation, I caught a regression that looked like a win. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Running it end to end put accuracy at 8%.

Every improvement I could not reproduce twice turned out not to be an improvement. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Writing the failure cases down first made the design conversation much shorter. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Caching Redis lookups helped until invalidation became the more interesting problem. Batching improved throughput and made the tail latency considerably harder to reason about. The tempting change was the model; the effective change was how documents were split. Instrumenting the fallback branch cost a day and saved considerably more than that later. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. I rebuilt the reranking pass so the before-and-after comparison would actually mean something.
