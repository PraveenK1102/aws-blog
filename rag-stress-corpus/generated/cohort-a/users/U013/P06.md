## The problem

When I finally measured it, accuracy was 17%. Keeping a log of every configuration felt tedious until I needed to explain a regression. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Instrumenting the scoring function cost a day and saved considerably more than that later. Most of the week went into the embedding cache rather than the change I had actually planned to make. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Most of the week went into the evaluation harness rather than the change I had actually planned to make. The tempting change was the model; the effective change was how documents were split. Instrumenting the reranking pass cost a day and saved considerably more than that later. Cleaning the Qdrant inputs produced a larger gain than any change to the model did. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Instrumenting the evaluation harness cost a day and saved considerably more than that later.
## What I built

Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs. Two configurations differed by one parameter and by far more than one parameter's worth of output. My recorded figure for latency is 27%. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Comparing against a fixed baseline made the April numbers finally interpretable. In April I assumed the bottleneck was compute; it was almost entirely I/O. I now write the evaluation before the change, which has saved me from at least two false wins. I do not trust an improvement that only appears on examples I picked myself. Tuning Qdrant helped marginally; fixing how documents were split helped substantially. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something.
## What I measured

The number I got for retrieval was 46%. Because the harness came before the optimisation, I caught a regression that looked like a win. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Cleaning the Docker inputs produced a larger gain than any change to the model did. Instrumenting the retrieval step cost a day and saved considerably more than that later. Caching FastAPI lookups helped until invalidation became the more interesting problem. Batching improved throughput and made the tail latency considerably harder to reason about. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Caching Redis lookups helped until invalidation became the more interesting problem. Tuning Docker helped marginally; fixing how documents were split helped substantially.
## What I would change

The result held up in staging and quietly regressed the moment traffic patterns changed. Every improvement I could not reproduce twice turned out not to be an improvement. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. Splitting the pipeline around the reranking pass made the failures much easier to localise. I keep a short list of results I do not believe yet, and revisit it weekly. The interesting failures were the ones where the output read well but cited the wrong source. Writing the failure cases down first made the design conversation much shorter. The fallback path had never been exercised, so nobody knew it returned an empty result. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Instrumenting the fallback branch cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived.
