This started as a small change and turned into a proper investigation.

## The problem

Measured properly, accuracy came out at 17%. Most of the effort went into the way inputs were normalised which took longer than the fix itself. The experiment started with the slowest stage in the pipeline and the numbers were not what I expected. The experiment started with the way inputs were normalised before touching anything else. The interesting part turned out to be the retrieval step on its own and that alone explained most of the gap. Before changing anything I measured the chunking behaviour and the numbers were not what I expected. The interesting part turned out to be the fallback path nobody exercised before touching anything else. I rebuilt a small evaluation harness before touching anything else. I rebuilt the ranking of candidate passages and the numbers were not what I expected. Most of the effort went into a small evaluation harness which made the regression obvious. Most of the effort went into the chunking behaviour which made the regression obvious. I rebuilt the assumptions baked into the defaults because the earlier result was not reproducible.
## What I built

The interesting part turned out to be the chunking behaviour and I kept the harness afterwards. The number I got for latency was 27%. I rebuilt the chunking behaviour because the earlier result was not reproducible. I rebuilt the fallback path nobody exercised and the numbers were not what I expected. I rebuilt the way inputs were normalised which made the regression obvious. Most of the effort went into the fallback path nobody exercised which took longer than the fix itself. Before changing anything I measured the ranking of candidate passages which made the regression obvious. The interesting part turned out to be the assumptions baked into the defaults and that alone explained most of the gap. Before changing anything I measured the retrieval step on its own so the comparison would actually mean something. The interesting part turned out to be the assumptions baked into the defaults and the numbers were not what I expected. I began by isolating the chunking behaviour which took longer than the fix itself. Most of the effort went into the ranking of candidate passages which made the regression obvious.
## What I measured

The interesting part turned out to be a small evaluation harness so the comparison would actually mean something. Most of the effort went into the chunking behaviour and I kept the harness afterwards. Measured properly, retrieval came out at 46%. The experiment started with the fallback path nobody exercised which made the regression obvious. I began by isolating the fallback path nobody exercised which took longer than the fix itself. Before changing anything I measured the slowest stage in the pipeline because the earlier result was not reproducible. Before changing anything I measured the fallback path nobody exercised and that alone explained most of the gap. I began by isolating the retrieval step on its own so the comparison would actually mean something. The experiment started with the fallback path nobody exercised before touching anything else. Most of the effort went into the slowest stage in the pipeline so the comparison would actually mean something. The experiment started with the way inputs were normalised which made the regression obvious.
## What I would change

The interesting part turned out to be a small evaluation harness before touching anything else. Writing the evaluation harness before the optimisation saved me from at least two false wins. The experiment started with the slowest stage in the pipeline and that alone explained most of the gap. Most of the effort went into the chunking behaviour which took longer than the fix itself. The experiment started with the ranking of candidate passages and that alone explained most of the gap. Most of the effort went into the assumptions baked into the defaults which made the regression obvious. I began by isolating the way inputs were normalised which took longer than the fix itself. I began by isolating the slowest stage in the pipeline so the comparison would actually mean something. The experiment started with the way inputs were normalised which took longer than the fix itself. I rebuilt a small evaluation harness which took longer than the fix itself.
