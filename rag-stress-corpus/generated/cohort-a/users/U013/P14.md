## The problem

I now write the evaluation before the change, which has saved me from at least two false wins. The result held up in staging and quietly regressed the moment traffic patterns changed. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July.

I do not trust an improvement that only appears on examples I picked myself. Accuracy landed at 35% once I measured it end to end. The first version worked on the sample set and fell apart the moment real Python inputs arrived. The interesting failures were the ones where the output read well but cited the wrong source. Batching improved throughput and made the tail latency considerably harder to reason about. Keeping a log of every configuration felt tedious until I needed to explain a regression. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Comparing against a fixed baseline made the July numbers finally interpretable. Caching Qdrant lookups helped until invalidation became the more interesting problem.
## What I built

Because the harness came before the optimisation, I caught a regression that looked like a win. On the instrumented run, latency showed 8%. Writing the failure cases down first made the design conversation much shorter. Cleaning the Python inputs produced a larger gain than any change to the model did. Instrumenting the retrieval step cost a day and saved considerably more than that later. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Two configurations differed by one parameter and by far more than one parameter's worth of output. Splitting the pipeline around the reranking pass made the failures much easier to localise. The tempting change was the model; the effective change was how documents were split. Most of the week went into the fallback branch rather than the change I had actually planned to make. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived.
## What I measured

I spent an afternoon convinced the model was wrong before finding a bug in the loader. I keep a short list of results I do not believe yet, and revisit it weekly. Running it end to end put accuracy at 45%. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Every improvement I could not reproduce twice turned out not to be an improvement. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Caching Python lookups helped until invalidation became the more interesting problem. The fallback path had never been exercised, so nobody knew it returned an empty result. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Tuning Python helped marginally; fixing how documents were split helped substantially.
## What I would change

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Caching Redis lookups helped until invalidation became the more interesting problem. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Most of the week went into the ingestion path rather than the change I had actually planned to make. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Instrumenting the reranking pass cost a day and saved considerably more than that later. In July I assumed the bottleneck was compute; it was almost entirely I/O. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Cleaning the Redis inputs produced a larger gain than any change to the model did.
