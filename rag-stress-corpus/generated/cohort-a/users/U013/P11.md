## The problem

When I finally measured it, retrieval was 23%. Tuning Redis helped marginally; fixing how documents were split helped substantially. Most of the week went into the scoring function rather than the change I had actually planned to make. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real Qdrant inputs arrived. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The result held up in staging and quietly regressed the moment traffic patterns changed. The tempting change was the model; the effective change was how documents were split. Caching Redis lookups helped until invalidation became the more interesting problem. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Caching Docker lookups helped until invalidation became the more interesting problem. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Tuning Qdrant helped marginally; fixing how documents were split helped substantially. Tuning Python helped marginally; fixing how documents were split helped substantially. Writing the failure cases down first made the design conversation much shorter. Two configurations differed by one parameter and by far more than one parameter's worth of output.
## What I built

Comparing against a fixed baseline made the June numbers finally interpretable. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs. Instrumenting the ingestion path cost a day and saved considerably more than that later.

My recorded figure for latency is 60%. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Cleaning the Python inputs produced a larger gain than any change to the model did. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. Two configurations differed by one parameter and by far more than one parameter's worth of output. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Caching FastAPI lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the scoring function made the failures much easier to localise. Caching Python lookups helped until invalidation became the more interesting problem. Caching Qdrant lookups helped until invalidation became the more interesting problem. Because the harness came before the optimisation, I caught a regression that looked like a win. The tempting change was the model; the effective change was how documents were split.
## What I measured

Every improvement I could not reproduce twice turned out not to be an improvement. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Accuracy landed at 55% once I measured it end to end. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. I now write the evaluation before the change, which has saved me from at least two false wins. Writing the failure cases down first made the design conversation much shorter. Splitting the pipeline around the ingestion path made the failures much easier to localise. I do not trust an improvement that only appears on examples I picked myself. In June I assumed the bottleneck was compute; it was almost entirely I/O. Keeping a log of every configuration felt tedious until I needed to explain a regression. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Most of the week went into the embedding cache rather than the change I had actually planned to make. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. Cleaning the Redis inputs produced a larger gain than any change to the model did. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Keeping a log of every configuration felt tedious until I needed to explain a regression. The fallback path had never been exercised, so nobody knew it returned an empty result.
## What I would change

The fallback path had never been exercised, so nobody knew it returned an empty result. Most of the week went into the retrieval step rather than the change I had actually planned to make. The interesting failures were the ones where the output read well but cited the wrong source. I keep a short list of results I do not believe yet, and revisit it weekly. Batching improved throughput and made the tail latency considerably harder to reason about. Because the harness came before the optimisation, I caught a regression that looked like a win. Instrumenting the reranking pass cost a day and saved considerably more than that later. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Instrumenting the scoring function cost a day and saved considerably more than that later. Tuning Docker helped marginally; fixing how documents were split helped substantially. Instrumenting the fallback branch cost a day and saved considerably more than that later. Cleaning the Docker inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Cleaning the Qdrant inputs produced a larger gain than any change to the model did. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I keep a short list of results I do not believe yet, and revisit it weekly.
