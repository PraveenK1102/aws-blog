Spent most of this week on the retrieval side of the project.

## The problem

Measured properly, retrieval came out at 23%. I began by isolating the ranking of candidate passages and I kept the harness afterwards. Before changing anything I measured the ranking of candidate passages which made the regression obvious. I began by isolating the way inputs were normalised before touching anything else. I rebuilt the chunking behaviour and that alone explained most of the gap. Before changing anything I measured the fallback path nobody exercised before touching anything else. Most of the effort went into a small evaluation harness which made the regression obvious. I began by isolating the way inputs were normalised and the numbers were not what I expected. I rebuilt the fallback path nobody exercised because the earlier result was not reproducible. The experiment started with the chunking behaviour which made the regression obvious. Most of the effort went into the way inputs were normalised and the numbers were not what I expected. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression. The interesting part turned out to be the fallback path nobody exercised and that alone explained most of the gap. Most of the effort went into the fallback path nobody exercised because the earlier result was not reproducible. I began by isolating the fallback path nobody exercised so the comparison would actually mean something. Before changing anything I measured the way inputs were normalised and that alone explained most of the gap.
## What I built

The number I got for latency was 60%. Before changing anything I measured the way inputs were normalised and the numbers were not what I expected. The interesting part turned out to be the assumptions baked into the defaults and the numbers were not what I expected. The experiment started with the chunking behaviour and the numbers were not what I expected. Most of the effort went into the fallback path nobody exercised and I kept the harness afterwards. I began by isolating the slowest stage in the pipeline which took longer than the fix itself. Before changing anything I measured a small evaluation harness which made the regression obvious. The interesting part turned out to be the assumptions baked into the defaults which made the regression obvious. I rebuilt the retrieval step on its own so the comparison would actually mean something. I rebuilt the slowest stage in the pipeline and the numbers were not what I expected. I began by isolating a small evaluation harness and that alone explained most of the gap. The experiment started with the assumptions baked into the defaults which took longer than the fix itself. Before changing anything I measured the retrieval step on its own and the numbers were not what I expected. The experiment started with the fallback path nobody exercised so the comparison would actually mean something. I rebuilt the assumptions baked into the defaults so the comparison would actually mean something. The interesting part turned out to be the way inputs were normalised and I kept the harness afterwards.
## What I measured

The interesting part turned out to be the retrieval step on its own before touching anything else. The number I got for accuracy was 55%. The experiment started with the way inputs were normalised and I kept the harness afterwards. The interesting part turned out to be the ranking of candidate passages and that alone explained most of the gap. Most of the effort went into the way inputs were normalised which made the regression obvious. The experiment started with a small evaluation harness before touching anything else. The interesting part turned out to be the ranking of candidate passages and I kept the harness afterwards. Most of the time went into building a way to measure the change rather than making the change. Most of the effort went into the ranking of candidate passages which made the regression obvious. The interesting part turned out to be the fallback path nobody exercised and I kept the harness afterwards. The experiment started with the retrieval step on its own and the numbers were not what I expected. I began by isolating the retrieval step on its own and the numbers were not what I expected. I began by isolating a small evaluation harness which made the regression obvious. Most of the effort went into the retrieval step on its own which took longer than the fix itself. Before changing anything I measured the chunking behaviour and the numbers were not what I expected. The experiment started with the slowest stage in the pipeline because the earlier result was not reproducible.
## What I would change

I rebuilt the chunking behaviour which took longer than the fix itself. I rebuilt the retrieval step on its own which took longer than the fix itself. Before changing anything I measured the assumptions baked into the defaults because the earlier result was not reproducible. Most of the effort went into a small evaluation harness and the numbers were not what I expected. The experiment started with the assumptions baked into the defaults which made the regression obvious. I rebuilt the ranking of candidate passages because the earlier result was not reproducible. The interesting part turned out to be a small evaluation harness and the numbers were not what I expected. I began by isolating the way inputs were normalised so the comparison would actually mean something. The experiment started with the chunking behaviour and I kept the harness afterwards. The interesting part turned out to be the way inputs were normalised which made the regression obvious. Most of the effort went into the fallback path nobody exercised and that alone explained most of the gap. Before changing anything I measured the retrieval step on its own and that alone explained most of the gap. Most of the effort went into the chunking behaviour which took longer than the fix itself. I rebuilt the slowest stage in the pipeline so the comparison would actually mean something.
