## The problem

Comparing against a fixed baseline made the March numbers finally interpretable. Tuning Redis helped marginally; fixing how documents were split helped substantially. That gave latency of 22%, which I wrote down at the time. Measured properly, accuracy came out at 29%.

Because the harness came before the optimisation, I caught a regression that looked like a win. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. I do not trust an improvement that only appears on examples I picked myself. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Keeping a log of every configuration felt tedious until I needed to explain a regression. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. The interesting failures were the ones where the output read well but cited the wrong source. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first.
## What I built

The number I got for latency was 8%. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I keep a short list of results I do not believe yet, and revisit it weekly. Writing the failure cases down first made the design conversation much shorter. Most of the week went into the scoring function rather than the change I had actually planned to make. Caching Redis lookups helped until invalidation became the more interesting problem. I now write the evaluation before the change, which has saved me from at least two false wins. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Caching FastAPI lookups helped until invalidation became the more interesting problem. Splitting the pipeline around the embedding cache made the failures much easier to localise.
## What I measured

Batching improved throughput and made the tail latency considerably harder to reason about. Instrumenting the scoring function cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Measured properly, latency came out at 29%. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Two configurations differed by one parameter and by far more than one parameter's worth of output. Instrumenting the reranking pass cost a day and saved considerably more than that later. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Python inputs arrived. The fallback path had never been exercised, so nobody knew it returned an empty result. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Most of the week went into the chunking logic rather than the change I had actually planned to make.
## What I would change

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Every improvement I could not reproduce twice turned out not to be an improvement. That gave accuracy of 25%, which I wrote down at the time. The tempting change was the model; the effective change was how documents were split. Tuning Docker helped marginally; fixing how documents were split helped substantially. In March I assumed the bottleneck was compute; it was almost entirely I/O. The result held up in staging and quietly regressed the moment traffic patterns changed. Splitting the pipeline around the reranking pass made the failures much easier to localise. Cleaning the Python inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived.
