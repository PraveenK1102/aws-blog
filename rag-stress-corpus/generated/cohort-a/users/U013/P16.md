## The problem

I now write the evaluation before the change, which has saved me from at least two false wins. On the instrumented run, retrieval showed 45%. Because the harness came before the optimisation, I caught a regression that looked like a win. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Writing the failure cases down first made the design conversation much shorter. The fallback path had never been exercised, so nobody knew it returned an empty result. Comparing against a fixed baseline made the August numbers finally interpretable. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Cleaning the Python inputs produced a larger gain than any change to the model did. The interesting failures were the ones where the output read well but cited the wrong source.
## What I built

Running it end to end put latency at 29%. Every improvement I could not reproduce twice turned out not to be an improvement. I rebuilt the ingestion path so the before-and-after comparison would actually mean something.

Tuning Redis helped marginally; fixing how documents were split helped substantially. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Instrumenting the retrieval step cost a day and saved considerably more than that later. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The tempting change was the model; the effective change was how documents were split. Splitting the pipeline around the embedding cache made the failures much easier to localise. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Caching Qdrant lookups helped until invalidation became the more interesting problem.
## What I measured

In August I assumed the bottleneck was compute; it was almost entirely I/O. Accuracy landed at 21% once I measured it end to end. Batching improved throughput and made the tail latency considerably harder to reason about.

Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. I do not trust an improvement that only appears on examples I picked myself. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Most of the week went into the scoring function rather than the change I had actually planned to make. Caching Redis lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. I keep a short list of results I do not believe yet, and revisit it weekly. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs.
## What I would change

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning Python helped marginally; fixing how documents were split helped substantially. Two configurations differed by one parameter and by far more than one parameter's worth of output. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. The result held up in staging and quietly regressed the moment traffic patterns changed. Instrumenting the fallback branch cost a day and saved considerably more than that later.
