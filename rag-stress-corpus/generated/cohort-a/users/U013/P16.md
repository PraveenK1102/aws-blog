This started as a small change and turned into a proper investigation.

## The problem

I rebuilt the slowest stage in the pipeline and I kept the harness afterwards. Retrieval landed at 45% once I measured it end to end. I rebuilt the slowest stage in the pipeline which took longer than the fix itself. Before changing anything I measured the chunking behaviour which made the regression obvious. The interesting part turned out to be the assumptions baked into the defaults so the comparison would actually mean something. The experiment started with the assumptions baked into the defaults which took longer than the fix itself. I began by isolating the ranking of candidate passages because the earlier result was not reproducible. The interesting part turned out to be the ranking of candidate passages which took longer than the fix itself. I began by isolating the retrieval step on its own before touching anything else.
## What I built

The number I got for latency was 29%. The interesting part turned out to be the retrieval step on its own before touching anything else. The experiment started with the ranking of candidate passages because the earlier result was not reproducible. I began by isolating the assumptions baked into the defaults which took longer than the fix itself. The experiment started with the retrieval step on its own which took longer than the fix itself. I began by isolating the assumptions baked into the defaults and I kept the harness afterwards. Before changing anything I measured the chunking behaviour because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults which made the regression obvious. I rebuilt a small evaluation harness because the earlier result was not reproducible.
## What I measured

Most of the effort went into the retrieval step on its own which took longer than the fix itself. The interesting part turned out to be the retrieval step on its own so the comparison would actually mean something. Accuracy landed at 21% once I measured it end to end. The interesting part turned out to be the assumptions baked into the defaults and that alone explained most of the gap. Most of the effort went into a small evaluation harness which took longer than the fix itself. The experiment started with the chunking behaviour and that alone explained most of the gap. The interesting part turned out to be the way inputs were normalised and I kept the harness afterwards. I began by isolating a small evaluation harness so the comparison would actually mean something. Splitting the pipeline into separately testable steps made the failures much easier to localise.
## What I would change

The interesting part turned out to be the assumptions baked into the defaults which made the regression obvious. Most of the effort went into the ranking of candidate passages and that alone explained most of the gap. Latency turned out to be dominated by a step I had assumed was cheap. I rebuilt the slowest stage in the pipeline because the earlier result was not reproducible. Most of the effort went into a small evaluation harness so the comparison would actually mean something. The experiment started with the ranking of candidate passages so the comparison would actually mean something. The experiment started with the chunking behaviour so the comparison would actually mean something. I began by isolating a small evaluation harness and the numbers were not what I expected.
