## The problem

I keep a short list of results I do not believe yet, and revisit it weekly. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Measured properly, retrieval came out at 14%. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Every improvement I could not reproduce twice turned out not to be an improvement. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Comparing against a fixed baseline made the March numbers finally interpretable. The fallback path had never been exercised, so nobody knew it returned an empty result. Instrumenting the chunking logic cost a day and saved considerably more than that later. I rebuilt the chunking logic so the before-and-after comparison would actually mean something.
## What I built

I now write the evaluation before the change, which has saved me from at least two false wins. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. When I finally measured it, accuracy was 30%. Writing the failure cases down first made the design conversation much shorter. The interesting failures were the ones where the output read well but cited the wrong source. Most of the week went into the embedding cache rather than the change I had actually planned to make. Tuning Qdrant helped marginally; fixing how documents were split helped substantially. Caching Docker lookups helped until invalidation became the more interesting problem. The result held up in staging and quietly regressed the moment traffic patterns changed. Tuning Python helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Tuning Docker helped marginally; fixing how documents were split helped substantially.
## What I measured

I do not trust an improvement that only appears on examples I picked myself. Keeping a log of every configuration felt tedious until I needed to explain a regression. The number I got for accuracy was 57%. Splitting the pipeline around the ingestion path made the failures much easier to localise. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Splitting the pipeline around the fallback branch made the failures much easier to localise. The tempting change was the model; the effective change was how documents were split. The first version worked on the sample set and fell apart the moment real Qdrant inputs arrived. Cleaning the Python inputs produced a larger gain than any change to the model did. Caching Qdrant lookups helped until invalidation became the more interesting problem. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Caching FastAPI lookups helped until invalidation became the more interesting problem.
## What I would change

In March I assumed the bottleneck was compute; it was almost entirely I/O. Two configurations differed by one parameter and by far more than one parameter's worth of output. Because the harness came before the optimisation, I caught a regression that looked like a win. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Batching improved throughput and made the tail latency considerably harder to reason about. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Instrumenting the reranking pass cost a day and saved considerably more than that later. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Most of the week went into the retrieval step rather than the change I had actually planned to make. Cleaning the Docker inputs produced a larger gain than any change to the model did.
