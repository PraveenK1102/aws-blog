Writing this up mostly to force myself to be precise about the numbers.

## The problem

The experiment started with the slowest stage in the pipeline which took longer than the fix itself. The number I got for accuracy was 9%. Before changing anything I measured the retrieval step on its own and that alone explained most of the gap. Before changing anything I measured the chunking behaviour which took longer than the fix itself. The interesting part turned out to be the chunking behaviour which took longer than the fix itself. I rebuilt the ranking of candidate passages and I kept the harness afterwards. Before changing anything I measured a small evaluation harness which took longer than the fix itself. Before changing anything I measured the ranking of candidate passages because the earlier result was not reproducible. The experiment started with the assumptions baked into the defaults because the earlier result was not reproducible. Before changing anything I measured the way inputs were normalised so the comparison would actually mean something. Most of the effort went into the fallback path nobody exercised which made the regression obvious. Before changing anything I measured the retrieval step on its own which made the regression obvious. I rebuilt the ranking of candidate passages so the comparison would actually mean something. Most of the effort went into the chunking behaviour and I kept the harness afterwards. I rebuilt the fallback path nobody exercised so the comparison would actually mean something. The interesting part turned out to be the fallback path nobody exercised because the earlier result was not reproducible.
## What I built

I rebuilt the slowest stage in the pipeline and the numbers were not what I expected. A surprising amount of the gain came from cleaning the inputs rather than changing the model. Latency landed at 24% once I measured it end to end. Before changing anything I measured the fallback path nobody exercised because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. I began by isolating the ranking of candidate passages which took longer than the fix itself. The experiment started with the fallback path nobody exercised which took longer than the fix itself. I rebuilt the retrieval step on its own and that alone explained most of the gap. I began by isolating the chunking behaviour before touching anything else. Most of the effort went into a small evaluation harness so the comparison would actually mean something. I began by isolating the retrieval step on its own and I kept the harness afterwards. The interesting part turned out to be the assumptions baked into the defaults and that alone explained most of the gap. The interesting part turned out to be a small evaluation harness which took longer than the fix itself. I began by isolating the way inputs were normalised and I kept the harness afterwards. I began by isolating the slowest stage in the pipeline and that alone explained most of the gap. The experiment started with the slowest stage in the pipeline so the comparison would actually mean something.
## What I measured

I began by isolating a small evaluation harness because the earlier result was not reproducible. Measured properly, accuracy came out at 21%. Most of the effort went into the way inputs were normalised and that alone explained most of the gap. Most of the effort went into the chunking behaviour so the comparison would actually mean something. The experiment started with the assumptions baked into the defaults which took longer than the fix itself. I rebuilt the slowest stage in the pipeline which made the regression obvious. Latency turned out to be dominated by a step I had assumed was cheap. The experiment started with the slowest stage in the pipeline because the earlier result was not reproducible. The interesting part turned out to be the assumptions baked into the defaults and the numbers were not what I expected. I learned to distrust any improvement that only shows up on the examples I chose myself. The interesting part turned out to be a small evaluation harness and that alone explained most of the gap. I began by isolating the ranking of candidate passages and the numbers were not what I expected. I rebuilt the retrieval step on its own which took longer than the fix itself. The experiment started with the retrieval step on its own which took longer than the fix itself. I began by isolating the slowest stage in the pipeline which made the regression obvious. Most of the effort went into a small evaluation harness which made the regression obvious.
## What I would change

Measured properly, latency came out at 25%. I rebuilt the assumptions baked into the defaults and that alone explained most of the gap. The interesting part turned out to be the assumptions baked into the defaults and I kept the harness afterwards. The experiment started with the fallback path nobody exercised because the earlier result was not reproducible. The experiment started with the slowest stage in the pipeline before touching anything else. I began by isolating the assumptions baked into the defaults which took longer than the fix itself. I began by isolating the retrieval step on its own and the numbers were not what I expected. The interesting part turned out to be the slowest stage in the pipeline and the numbers were not what I expected. The interesting part turned out to be the fallback path nobody exercised so the comparison would actually mean something. Before changing anything I measured the fallback path nobody exercised so the comparison would actually mean something. The experiment started with the chunking behaviour which took longer than the fix itself. Before changing anything I measured the chunking behaviour before touching anything else. The experiment started with the retrieval step on its own so the comparison would actually mean something. Most of the effort went into the retrieval step on its own and the numbers were not what I expected. The interesting part turned out to be the retrieval step on its own so the comparison would actually mean something. Most of the effort went into the fallback path nobody exercised which took longer than the fix itself.
