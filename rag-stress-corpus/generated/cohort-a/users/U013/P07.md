## The problem

Tuning FastAPI helped marginally; fixing how documents were split helped substantially. I do not trust an improvement that only appears on examples I picked myself. That gave accuracy of 9%, which I wrote down at the time. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Cleaning the Redis inputs produced a larger gain than any change to the model did. In May I assumed the bottleneck was compute; it was almost entirely I/O. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. Comparing against a fixed baseline made the May numbers finally interpretable. Tuning Docker helped marginally; fixing how documents were split helped substantially. Every improvement I could not reproduce twice turned out not to be an improvement. Tuning Python helped marginally; fixing how documents were split helped substantially. Most of the week went into the reranking pass rather than the change I had actually planned to make. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Instrumenting the retrieval step cost a day and saved considerably more than that later. Caching FastAPI lookups helped until invalidation became the more interesting problem. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The fallback path had never been exercised, so nobody knew it returned an empty result. I now write the evaluation before the change, which has saved me from at least two false wins.
## What I built

I spent an afternoon convinced the model was wrong before finding a bug in the loader. When I finally measured it, latency was 24%. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap.

The result held up in staging and quietly regressed the moment traffic patterns changed. Keeping a log of every configuration felt tedious until I needed to explain a regression. Caching Docker lookups helped until invalidation became the more interesting problem. The interesting failures were the ones where the output read well but cited the wrong source. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Caching Redis lookups helped until invalidation became the more interesting problem. Cleaning the Docker inputs produced a larger gain than any change to the model did. Caching Python lookups helped until invalidation became the more interesting problem. Cleaning the Qdrant inputs produced a larger gain than any change to the model did. Tuning Qdrant helped marginally; fixing how documents were split helped substantially. Caching Qdrant lookups helped until invalidation became the more interesting problem. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Two configurations differed by one parameter and by far more than one parameter's worth of output.
## What I measured

The fallback path had never been exercised, so nobody knew it returned an empty result. Measured properly, accuracy came out at 21%. I keep a short list of results I do not believe yet, and revisit it weekly. Because the harness came before the optimisation, I caught a regression that looked like a win.

The tempting change was the model; the effective change was how documents were split. Writing the failure cases down first made the design conversation much shorter. Batching improved throughput and made the tail latency considerably harder to reason about. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Instrumenting the embedding cache cost a day and saved considerably more than that later. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Instrumenting the scoring function cost a day and saved considerably more than that later. Cleaning the Python inputs produced a larger gain than any change to the model did. Splitting the pipeline around the chunking logic made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. The result held up in staging and quietly regressed the moment traffic patterns changed. Writing the failure cases down first made the design conversation much shorter.
## What I would change

Instrumenting the chunking logic cost a day and saved considerably more than that later. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. My recorded figure for latency is 25%. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived.

Splitting the pipeline around the scoring function made the failures much easier to localise. Two configurations differed by one parameter and by far more than one parameter's worth of output. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I now write the evaluation before the change, which has saved me from at least two false wins. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Most of the week went into the fallback branch rather than the change I had actually planned to make. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Was the gain from the model or from cleaning the Qdrant inputs? Mostly the inputs. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Tuning Redis helped marginally; fixing how documents were split helped substantially. Because the harness came before the optimisation, I caught a regression that looked like a win. The interesting failures were the ones where the output read well but cited the wrong source. Caching Docker lookups helped until invalidation became the more interesting problem.
