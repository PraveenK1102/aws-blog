## The problem

Running it end to end put accuracy at 14%. Every improvement I could not reproduce twice turned out not to be an improvement. Comparing against a fixed baseline made the July numbers finally interpretable. Two configurations differed by one parameter and by far more than one parameter's worth of output.

Because the harness came before the optimisation, I caught a regression that looked like a win. Measured properly, retrieval came out at 59%. I do not trust an improvement that only appears on examples I picked myself. Most of the week went into the embedding cache rather than the change I had actually planned to make. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Tuning FastAPI helped marginally; fixing how documents were split helped substantially. Caching FastAPI lookups helped until invalidation became the more interesting problem. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Instrumenting the embedding cache cost a day and saved considerably more than that later. Tuning Docker helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the Qdrant inputs? Mostly the inputs. The first version worked on the sample set and fell apart the moment real Qdrant inputs arrived. Tuning Python helped marginally; fixing how documents were split helped substantially. Tuning Redis helped marginally; fixing how documents were split helped substantially. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Two configurations differed by one parameter and by far more than one parameter's worth of output.
## What I built

Cleaning the Docker inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Accuracy landed at 11% once I measured it end to end. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Writing the failure cases down first made the design conversation much shorter. I keep a short list of results I do not believe yet, and revisit it weekly. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. I spent an afternoon convinced the model was wrong before finding a bug in the loader. The first version worked on the sample set and fell apart the moment real FastAPI inputs arrived. Keeping a log of every configuration felt tedious until I needed to explain a regression. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Tuning Qdrant helped marginally; fixing how documents were split helped substantially. Cleaning the FastAPI inputs produced a larger gain than any change to the model did. Cleaning the Python inputs produced a larger gain than any change to the model did. Instrumenting the scoring function cost a day and saved considerably more than that later. Comparing against a fixed baseline made the July numbers finally interpretable. Because the harness came before the optimisation, I caught a regression that looked like a win.
## What I measured

Once I logged every configuration, explaining the regression to myself took minutes rather than a day. I now write the evaluation before the change, which has saved me from at least two false wins. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Measured properly, latency came out at 33%. Was the gain from the model or from cleaning the FastAPI inputs? Mostly the inputs. Splitting the pipeline around the embedding cache made the failures much easier to localise. The tempting change was the model; the effective change was how documents were split. Batching improved throughput and made the tail latency considerably harder to reason about. The result held up in staging and quietly regressed the moment traffic patterns changed. Splitting the pipeline around the evaluation harness made the failures much easier to localise. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Instrumenting the retrieval step cost a day and saved considerably more than that later. Splitting the pipeline around the fallback branch made the failures much easier to localise. Instrumenting the ingestion path cost a day and saved considerably more than that later. I rebuilt the scoring function so the before-and-after comparison would actually mean something. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. I spent an afternoon convinced the model was wrong before finding a bug in the loader. I now write the evaluation before the change, which has saved me from at least two false wins.
## What I would change

In July I assumed the bottleneck was compute; it was almost entirely I/O. The interesting failures were the ones where the output read well but cited the wrong source. When I finally measured it, latency was 34%. Caching Qdrant lookups helped until invalidation became the more interesting problem. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Instrumenting the reranking pass cost a day and saved considerably more than that later. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. The fallback path had never been exercised, so nobody knew it returned an empty result. Cleaning the Qdrant inputs produced a larger gain than any change to the model did. Caching Docker lookups helped until invalidation became the more interesting problem. Cleaning the Redis inputs produced a larger gain than any change to the model did. Caching Python lookups helped until invalidation became the more interesting problem. Most of the week went into the retrieval step rather than the change I had actually planned to make. Caching Redis lookups helped until invalidation became the more interesting problem. In July I assumed the bottleneck was compute; it was almost entirely I/O. Keeping a log of every configuration felt tedious until I needed to explain a regression. Every improvement I could not reproduce twice turned out not to be an improvement.
