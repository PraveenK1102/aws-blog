Notes from a few days of experiments, written while they are still fresh.

## The problem

The experiment started with the way inputs were normalised so the comparison would actually mean something. The number I got for retrieval was 47%. I rebuilt the retrieval step on its own before touching anything else. I rebuilt the assumptions baked into the defaults which took longer than the fix itself. Before changing anything I measured the chunking behaviour before touching anything else. Most of the effort went into the ranking of candidate passages and the numbers were not what I expected. I began by isolating the chunking behaviour and that alone explained most of the gap. I began by isolating a small evaluation harness and the numbers were not what I expected. Writing the evaluation harness before the optimisation saved me from at least two false wins.
## What I built

The interesting part turned out to be the assumptions baked into the defaults which took longer than the fix itself. The experiment started with the assumptions baked into the defaults and I kept the harness afterwards. Measured properly, latency came out at 25%. Most of the time went into building a way to measure the change rather than making the change. Before changing anything I measured the retrieval step on its own and the numbers were not what I expected. Latency turned out to be dominated by a step I had assumed was cheap. The experiment started with the chunking behaviour because the earlier result was not reproducible. Most of the effort went into the slowest stage in the pipeline which took longer than the fix itself. Before changing anything I measured the ranking of candidate passages and the numbers were not what I expected.
## What I measured

Before changing anything I measured the ranking of candidate passages and that alone explained most of the gap. The number I got for latency was 8%. The interesting part turned out to be the fallback path nobody exercised and that alone explained most of the gap. The interesting part turned out to be the retrieval step on its own because the earlier result was not reproducible. Most of the effort went into the slowest stage in the pipeline and that alone explained most of the gap. Before changing anything I measured the way inputs were normalised which made the regression obvious. The experiment started with the ranking of candidate passages and I kept the harness afterwards. Before changing anything I measured the fallback path nobody exercised which took longer than the fix itself. A surprising amount of the gain came from cleaning the inputs rather than changing the model.
## What I would change

Most of the effort went into the ranking of candidate passages which took longer than the fix itself. Latency landed at 7% once I measured it end to end. I began by isolating the fallback path nobody exercised which took longer than the fix itself. Before changing anything I measured the way inputs were normalised and the numbers were not what I expected. Before changing anything I measured the slowest stage in the pipeline before touching anything else. The interesting part turned out to be a small evaluation harness and I kept the harness afterwards. The experiment started with the retrieval step on its own and the numbers were not what I expected. Most of the effort went into the fallback path nobody exercised before touching anything else. I began by isolating the retrieval step on its own so the comparison would actually mean something.
