## The problem

The fallback path had never been exercised, so nobody knew it returned an empty result. Every improvement I could not reproduce twice turned out not to be an improvement. On the instrumented run, retrieval showed 47%. I do not trust an improvement that only appears on examples I picked myself. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the reranking pass cost a day and saved considerably more than that later. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. The result held up in staging and quietly regressed the moment traffic patterns changed. Cleaning the Docker inputs produced a larger gain than any change to the model did. Most of the week went into the reranking pass rather than the change I had actually planned to make. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs.
## What I built

Keeping a log of every configuration felt tedious until I needed to explain a regression. I keep a short list of results I do not believe yet, and revisit it weekly. I spent an afternoon convinced the model was wrong before finding a bug in the loader. That gave latency of 25%, which I wrote down at the time. Caching Python lookups helped until invalidation became the more interesting problem. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Caching PostgreSQL lookups helped until invalidation became the more interesting problem. Tuning Docker helped marginally; fixing how documents were split helped substantially. Instrumenting the fallback branch cost a day and saved considerably more than that later. Cleaning the Redis inputs produced a larger gain than any change to the model did.
## What I measured

Cleaning the Python inputs produced a larger gain than any change to the model did. Tuning Redis helped marginally; fixing how documents were split helped substantially. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. The number I got for latency was 8%. In April I assumed the bottleneck was compute; it was almost entirely I/O. Two configurations differed by one parameter and by far more than one parameter's worth of output. Writing the failure cases down first made the design conversation much shorter. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real PostgreSQL inputs arrived. Comparing against a fixed baseline made the April numbers finally interpretable.
## What I would change

The tempting change was the model; the effective change was how documents were split. Because the harness came before the optimisation, I caught a regression that looked like a win. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived.

When I finally measured it, latency was 7%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially. Most of the week went into the chunking logic rather than the change I had actually planned to make. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Batching improved throughput and made the tail latency considerably harder to reason about.
