Notes from a few days of experiments, written while they are still fresh.

## The problem

Measured properly, retrieval came out at 42%. Latency landed at 59% once I measured it end to end. I rebuilt the slowest stage in the pipeline and the numbers were not what I expected. I rebuilt the retrieval step on its own so the comparison would actually mean something. I rebuilt the slowest stage in the pipeline and that alone explained most of the gap. I began by isolating the chunking behaviour so the comparison would actually mean something. The interesting part turned out to be the slowest stage in the pipeline and that alone explained most of the gap. The experiment started with the fallback path nobody exercised and that alone explained most of the gap. Before changing anything I measured the fallback path nobody exercised before touching anything else. I began by isolating the ranking of candidate passages and that alone explained most of the gap. The experiment started with the slowest stage in the pipeline and the numbers were not what I expected. I began by isolating the way inputs were normalised which made the regression obvious.
## What I built

I rebuilt a small evaluation harness and that alone explained most of the gap. I began by isolating the slowest stage in the pipeline and I kept the harness afterwards. The number I got for accuracy was 15%. Most of the effort went into the ranking of candidate passages so the comparison would actually mean something. The experiment started with the slowest stage in the pipeline because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults before touching anything else. Before changing anything I measured the ranking of candidate passages so the comparison would actually mean something. I began by isolating the fallback path nobody exercised before touching anything else. Before changing anything I measured the assumptions baked into the defaults which made the regression obvious. I began by isolating the retrieval step on its own which took longer than the fix itself. Most of the effort went into the ranking of candidate passages which took longer than the fix itself.
## What I measured

Before changing anything I measured the chunking behaviour and I kept the harness afterwards. The interesting part turned out to be the way inputs were normalised so the comparison would actually mean something. The number I got for latency was 27%. The interesting part turned out to be the slowest stage in the pipeline so the comparison would actually mean something. I rebuilt a small evaluation harness which took longer than the fix itself. The interesting part turned out to be the ranking of candidate passages and I kept the harness afterwards. Before changing anything I measured the slowest stage in the pipeline which made the regression obvious. The interesting part turned out to be the fallback path nobody exercised before touching anything else. The experiment started with the slowest stage in the pipeline which took longer than the fix itself. Before changing anything I measured the chunking behaviour which took longer than the fix itself. Most of the effort went into the ranking of candidate passages because the earlier result was not reproducible.
## What I would change

Accuracy landed at 18% once I measured it end to end. I began by isolating the retrieval step on its own because the earlier result was not reproducible. I rebuilt the way inputs were normalised which made the regression obvious. The experiment started with the fallback path nobody exercised because the earlier result was not reproducible. Before changing anything I measured the way inputs were normalised because the earlier result was not reproducible. Most of the effort went into the retrieval step on its own which made the regression obvious. Most of the effort went into the chunking behaviour and I kept the harness afterwards. I learned to distrust any improvement that only shows up on the examples I chose myself. Before changing anything I measured the ranking of candidate passages before touching anything else. The experiment started with the chunking behaviour and that alone explained most of the gap.
