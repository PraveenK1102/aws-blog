Spent most of this week on the retrieval side of the project.

## The problem

I rebuilt a small evaluation harness and that alone explained most of the gap. I began by isolating the slowest stage in the pipeline before touching anything else. The number I got for retrieval was 60%. I rebuilt the ranking of candidate passages and I kept the harness afterwards. The experiment started with the ranking of candidate passages and the numbers were not what I expected. I rebuilt the retrieval step on its own before touching anything else. The experiment started with the fallback path nobody exercised before touching anything else. I began by isolating the retrieval step on its own because the earlier result was not reproducible. Most of the effort went into the chunking behaviour which took longer than the fix itself. Before changing anything I measured the retrieval step on its own and I kept the harness afterwards. Most of the effort went into the way inputs were normalised and the numbers were not what I expected. I began by isolating the assumptions baked into the defaults and the numbers were not what I expected. The interesting part turned out to be the way inputs were normalised and that alone explained most of the gap.
## What I built

I began by isolating the chunking behaviour before touching anything else. Before changing anything I measured the slowest stage in the pipeline and the numbers were not what I expected. Measured properly, accuracy came out at 60%. Most of the effort went into the slowest stage in the pipeline which took longer than the fix itself. A surprising amount of the gain came from cleaning the inputs rather than changing the model. The experiment started with the way inputs were normalised and I kept the harness afterwards. I began by isolating the slowest stage in the pipeline and I kept the harness afterwards. The interesting part turned out to be the retrieval step on its own and that alone explained most of the gap. I rebuilt the retrieval step on its own because the earlier result was not reproducible. Before changing anything I measured a small evaluation harness so the comparison would actually mean something. Most of the effort went into the chunking behaviour and that alone explained most of the gap. Most of the effort went into a small evaluation harness and I kept the harness afterwards.
## What I measured

Measured properly, accuracy came out at 29%. I began by isolating the chunking behaviour and that alone explained most of the gap. I began by isolating the way inputs were normalised which made the regression obvious. Splitting the pipeline into separately testable steps made the failures much easier to localise. The experiment started with a small evaluation harness before touching anything else. Before changing anything I measured the ranking of candidate passages and I kept the harness afterwards. Before changing anything I measured the ranking of candidate passages and the numbers were not what I expected. The experiment started with a small evaluation harness which took longer than the fix itself. I rebuilt a small evaluation harness and I kept the harness afterwards. I began by isolating the fallback path nobody exercised which took longer than the fix itself. The experiment started with the chunking behaviour because the earlier result was not reproducible. The experiment started with a small evaluation harness and I kept the harness afterwards.
## What I would change

I began by isolating the chunking behaviour and I kept the harness afterwards. The interesting part turned out to be the fallback path nobody exercised and the numbers were not what I expected. The experiment started with the slowest stage in the pipeline because the earlier result was not reproducible. Most of the effort went into the ranking of candidate passages which made the regression obvious. I rebuilt the assumptions baked into the defaults and I kept the harness afterwards. The experiment started with the way inputs were normalised which took longer than the fix itself. The interesting part turned out to be the retrieval step on its own and I kept the harness afterwards. I rebuilt the retrieval step on its own and the numbers were not what I expected. The experiment started with a small evaluation harness and that alone explained most of the gap. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression. I began by isolating the slowest stage in the pipeline because the earlier result was not reproducible.
