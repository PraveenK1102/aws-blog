## The problem

Most of the week went into the chunking logic rather than the change I had actually planned to make. Splitting the pipeline around the reranking pass made the failures much easier to localise. Was the gain from the model or from cleaning the PostgreSQL inputs? Mostly the inputs.

On the instrumented run, accuracy showed 43%. Retrieval landed at 19% once I measured it end to end. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. The fallback path had never been exercised, so nobody knew it returned an empty result. Instrumenting the ingestion path cost a day and saved considerably more than that later. Writing the failure cases down first made the design conversation much shorter. Instrumenting the scoring function cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Every improvement I could not reproduce twice turned out not to be an improvement. Caching PyTorch lookups helped until invalidation became the more interesting problem. Most of the week went into the reranking pass rather than the change I had actually planned to make.
## What I built

I rebuilt the ingestion path so the before-and-after comparison would actually mean something. When I finally measured it, latency was 40%. Two configurations differed by one parameter and by far more than one parameter's worth of output. Measured properly, accuracy came out at 36%.

Cleaning the Redis inputs produced a larger gain than any change to the model did. I now write the evaluation before the change, which has saved me from at least two false wins. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Tuning Docker helped marginally; fixing how documents were split helped substantially. Batching improved throughput and made the tail latency considerably harder to reason about. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Cleaning the Python inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap.
## What I measured

Running it end to end put accuracy at 9%. Because the harness came before the optimisation, I caught a regression that looked like a win. The interesting failures were the ones where the output read well but cited the wrong source. The first version worked on the sample set and fell apart the moment real PostgreSQL inputs arrived. The result held up in staging and quietly regressed the moment traffic patterns changed. Most of the week went into the scoring function rather than the change I had actually planned to make. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Keeping a log of every configuration felt tedious until I needed to explain a regression. In May I assumed the bottleneck was compute; it was almost entirely I/O. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially.
## What I would change

I do not trust an improvement that only appears on examples I picked myself. My recorded figure for accuracy is 41%. Instrumenting the fallback branch cost a day and saved considerably more than that later. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Caching Python lookups helped until invalidation became the more interesting problem. Comparing against a fixed baseline made the May numbers finally interpretable. I keep a short list of results I do not believe yet, and revisit it weekly. Splitting the pipeline around the evaluation harness made the failures much easier to localise. The tempting change was the model; the effective change was how documents were split. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs.
