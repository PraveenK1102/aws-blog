Writing this up mostly to force myself to be precise about the numbers.

## The problem

The interesting part turned out to be the fallback path nobody exercised because the earlier result was not reproducible. Measured properly, latency came out at 43%. Measured properly, retrieval came out at 44%. The interesting part turned out to be the ranking of candidate passages because the earlier result was not reproducible. The interesting part turned out to be the ranking of candidate passages and I kept the harness afterwards. The interesting part turned out to be the ranking of candidate passages before touching anything else. I began by isolating the slowest stage in the pipeline which took longer than the fix itself. I rebuilt the retrieval step on its own before touching anything else. I began by isolating the way inputs were normalised and I kept the harness afterwards. Before changing anything I measured the way inputs were normalised and that alone explained most of the gap. The experiment started with the retrieval step on its own and that alone explained most of the gap.
## What I built

I began by isolating the fallback path nobody exercised because the earlier result was not reproducible. The number I got for latency was 22%. Before changing anything I measured the retrieval step on its own and the numbers were not what I expected. I began by isolating the chunking behaviour and that alone explained most of the gap. I rebuilt the ranking of candidate passages because the earlier result was not reproducible. The interesting part turned out to be the slowest stage in the pipeline and that alone explained most of the gap. The experiment started with the retrieval step on its own and I kept the harness afterwards. I began by isolating the way inputs were normalised which took longer than the fix itself. Most of the effort went into the retrieval step on its own before touching anything else. The experiment started with the ranking of candidate passages and I kept the harness afterwards.
## What I measured

I rebuilt the slowest stage in the pipeline because the earlier result was not reproducible. I rebuilt the ranking of candidate passages and the numbers were not what I expected. The number I got for retrieval was 37%. I learned to distrust any improvement that only shows up on the examples I chose myself. Before changing anything I measured a small evaluation harness and I kept the harness afterwards. The interesting part turned out to be the retrieval step on its own which took longer than the fix itself. Before changing anything I measured a small evaluation harness which made the regression obvious. Before changing anything I measured the retrieval step on its own before touching anything else. Before changing anything I measured the fallback path nobody exercised which made the regression obvious. Before changing anything I measured the chunking behaviour and I kept the harness afterwards.
## What I would change

The experiment started with the ranking of candidate passages so the comparison would actually mean something. Most of the effort went into the way inputs were normalised which took longer than the fix itself. Accuracy landed at 20% once I measured it end to end. Before changing anything I measured the assumptions baked into the defaults and the numbers were not what I expected. I began by isolating the fallback path nobody exercised so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages and the numbers were not what I expected. The experiment started with the chunking behaviour which made the regression obvious. Before changing anything I measured the way inputs were normalised before touching anything else. The interesting part turned out to be a small evaluation harness and I kept the harness afterwards.
