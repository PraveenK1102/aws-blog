## The problem

Splitting the pipeline around the chunking logic made the failures much easier to localise. Instrumenting the reranking pass cost a day and saved considerably more than that later. Measured properly, latency came out at 43%.

Two configurations differed by one parameter and by far more than one parameter's worth of output. When I finally measured it, retrieval was 44%. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Instrumenting the chunking logic cost a day and saved considerably more than that later. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Caching Redis lookups helped until invalidation became the more interesting problem. Instrumenting the scoring function cost a day and saved considerably more than that later. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap.
## What I built

In August I assumed the bottleneck was compute; it was almost entirely I/O. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Keeping a log of every configuration felt tedious until I needed to explain a regression. My recorded figure for latency is 22%. I keep a short list of results I do not believe yet, and revisit it weekly. Because the harness came before the optimisation, I caught a regression that looked like a win. Most of the week went into the reranking pass rather than the change I had actually planned to make. Splitting the pipeline around the retrieval step made the failures much easier to localise. I do not trust an improvement that only appears on examples I picked myself. Writing the failure cases down first made the design conversation much shorter. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did.
## What I measured

I spent an afternoon convinced the model was wrong before finding a bug in the loader. Most of the week went into the chunking logic rather than the change I had actually planned to make. Every improvement I could not reproduce twice turned out not to be an improvement.

I now write the evaluation before the change, which has saved me from at least two false wins. Running it end to end put retrieval at 37%. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. The tempting change was the model; the effective change was how documents were split. The interesting failures were the ones where the output read well but cited the wrong source. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Batching improved throughput and made the tail latency considerably harder to reason about.
## What I would change

Comparing against a fixed baseline made the August numbers finally interpretable. On the instrumented run, accuracy showed 20%. The result held up in staging and quietly regressed the moment traffic patterns changed. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. The fallback path had never been exercised, so nobody knew it returned an empty result. Tuning Redis helped marginally; fixing how documents were split helped substantially. Cleaning the Python inputs produced a larger gain than any change to the model did. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Caching Python lookups helped until invalidation became the more interesting problem.
