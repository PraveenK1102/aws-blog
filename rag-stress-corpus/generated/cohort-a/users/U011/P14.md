Writing this up mostly to force myself to be precise about the numbers.

## The problem

I began by isolating the way inputs were normalised and the numbers were not what I expected. I kept a log of every configuration I tried, which felt tedious until I needed to explain a regression. The number I got for latency was 12%. Measured properly, accuracy came out at 16%. The interesting part turned out to be the chunking behaviour which took longer than the fix itself. The experiment started with the chunking behaviour which made the regression obvious. Most of the effort went into the way inputs were normalised and I kept the harness afterwards. Batching helped throughput but made the tail latency harder to reason about. I began by isolating the retrieval step on its own so the comparison would actually mean something. The experiment started with the chunking behaviour and I kept the harness afterwards. Before changing anything I measured a small evaluation harness which made the regression obvious. Before changing anything I measured a small evaluation harness which took longer than the fix itself. The interesting part turned out to be the slowest stage in the pipeline and the numbers were not what I expected. Most of the effort went into the retrieval step on its own before touching anything else. I rebuilt the chunking behaviour which took longer than the fix itself. The interesting part turned out to be the retrieval step on its own and the numbers were not what I expected. The experiment started with a small evaluation harness which made the regression obvious.
## What I built

Measured properly, latency came out at 40%. Most of the effort went into the fallback path nobody exercised which took longer than the fix itself. The interesting part turned out to be a small evaluation harness which made the regression obvious. I began by isolating the slowest stage in the pipeline and the numbers were not what I expected. I rebuilt the way inputs were normalised so the comparison would actually mean something. The first version worked on the sample data and fell apart the moment the inputs got messy. Before changing anything I measured the slowest stage in the pipeline and I kept the harness afterwards. I began by isolating the ranking of candidate passages and I kept the harness afterwards. The experiment started with the assumptions baked into the defaults which made the regression obvious. The experiment started with the slowest stage in the pipeline and I kept the harness afterwards. Before changing anything I measured the fallback path nobody exercised and the numbers were not what I expected. I began by isolating a small evaluation harness which took longer than the fix itself. I began by isolating the slowest stage in the pipeline because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults and that alone explained most of the gap. I rebuilt the way inputs were normalised which made the regression obvious.
## What I measured

Retrieval landed at 53% once I measured it end to end. The experiment started with the assumptions baked into the defaults which took longer than the fix itself. I rebuilt the way inputs were normalised and that alone explained most of the gap. Before changing anything I measured the assumptions baked into the defaults and that alone explained most of the gap. I rebuilt the retrieval step on its own which took longer than the fix itself. Before changing anything I measured the way inputs were normalised before touching anything else. Before changing anything I measured the chunking behaviour before touching anything else. The interesting part turned out to be the slowest stage in the pipeline which made the regression obvious. I learned to distrust any improvement that only shows up on the examples I chose myself. I began by isolating the slowest stage in the pipeline and I kept the harness afterwards. I rebuilt a small evaluation harness and that alone explained most of the gap. A surprising amount of the gain came from cleaning the inputs rather than changing the model. The experiment started with the fallback path nobody exercised and that alone explained most of the gap. The interesting part turned out to be the way inputs were normalised which made the regression obvious. Most of the effort went into the chunking behaviour which made the regression obvious.
## What I would change

Measured properly, accuracy came out at 43%. Most of the effort went into the retrieval step on its own and the numbers were not what I expected. Most of the effort went into the retrieval step on its own because the earlier result was not reproducible. The interesting part turned out to be the chunking behaviour because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults and the numbers were not what I expected. Most of the effort went into the assumptions baked into the defaults and I kept the harness afterwards. I rebuilt a small evaluation harness which took longer than the fix itself. The experiment started with the retrieval step on its own before touching anything else. The experiment started with the retrieval step on its own which took longer than the fix itself. The experiment started with the retrieval step on its own because the earlier result was not reproducible. Most of the effort went into the fallback path nobody exercised and I kept the harness afterwards. The experiment started with the assumptions baked into the defaults so the comparison would actually mean something. Most of the effort went into the slowest stage in the pipeline so the comparison would actually mean something. Before changing anything I measured the assumptions baked into the defaults before touching anything else. Before changing anything I measured the ranking of candidate passages which took longer than the fix itself.
