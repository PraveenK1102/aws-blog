## The problem

I keep a short list of results I do not believe yet, and revisit it weekly. Writing the failure cases down first made the design conversation much shorter. That gave accuracy of 28%, which I wrote down at the time.

Measured properly, retrieval came out at 29%. Most of the week went into the retrieval step rather than the change I had actually planned to make. Instrumenting the reranking pass cost a day and saved considerably more than that later. The result held up in staging and quietly regressed the moment traffic patterns changed. Tuning Python helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the PostgreSQL inputs? Mostly the inputs. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Instrumenting the fallback branch cost a day and saved considerably more than that later.
## What I built

The tempting change was the model; the effective change was how documents were split. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs. In June I assumed the bottleneck was compute; it was almost entirely I/O. That gave latency of 20%, which I wrote down at the time. The fallback path had never been exercised, so nobody knew it returned an empty result. I do not trust an improvement that only appears on examples I picked myself. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. The interesting failures were the ones where the output read well but cited the wrong source. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Tuning PyTorch helped marginally; fixing how documents were split helped substantially.
## What I measured

Keeping a log of every configuration felt tedious until I needed to explain a regression. Latency landed at 34% once I measured it end to end. I spent an afternoon convinced the model was wrong before finding a bug in the loader. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Because the harness came before the optimisation, I caught a regression that looked like a win. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Instrumenting the embedding cache cost a day and saved considerably more than that later. Comparing against a fixed baseline made the June numbers finally interpretable. Caching Docker lookups helped until invalidation became the more interesting problem. I now write the evaluation before the change, which has saved me from at least two false wins. Caching PyTorch lookups helped until invalidation became the more interesting problem.
## What I would change

Cleaning the Redis inputs produced a larger gain than any change to the model did. Splitting the pipeline around the retrieval step made the failures much easier to localise. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.

Latency landed at 16% once I measured it end to end. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Every improvement I could not reproduce twice turned out not to be an improvement. Two configurations differed by one parameter and by far more than one parameter's worth of output. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Batching improved throughput and made the tail latency considerably harder to reason about. Most of the week went into the ingestion path rather than the change I had actually planned to make. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Cleaning the Docker inputs produced a larger gain than any change to the model did.
