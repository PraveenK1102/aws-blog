## The problem

I spent an afternoon convinced the model was wrong before finding a bug in the loader. Because the harness came before the optimisation, I caught a regression that looked like a win. On the instrumented run, latency showed 6%. Every improvement I could not reproduce twice turned out not to be an improvement. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Writing the failure cases down first made the design conversation much shorter. The tempting change was the model; the effective change was how documents were split. Latency turned out to be dominated by the fallback branch, which I had assumed was cheap. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Splitting the pipeline around the scoring function made the failures much easier to localise. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially. Most of the week went into the chunking logic rather than the change I had actually planned to make. Splitting the pipeline around the fallback branch made the failures much easier to localise. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did. Instrumenting the reranking pass cost a day and saved considerably more than that later. Because the harness came before the optimisation, I caught a regression that looked like a win. The fallback path had never been exercised, so nobody knew it returned an empty result. Two configurations differed by one parameter and by far more than one parameter's worth of output. Comparing against a fixed baseline made the March numbers finally interpretable.
## What I built

Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Instrumenting the scoring function cost a day and saved considerably more than that later. My recorded figure for retrieval is 22%. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. The fallback path had never been exercised, so nobody knew it returned an empty result. Keeping a log of every configuration felt tedious until I needed to explain a regression. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Instrumenting the chunking logic cost a day and saved considerably more than that later. Tuning Docker helped marginally; fixing how documents were split helped substantially. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Cleaning the Redis inputs produced a larger gain than any change to the model did. Caching Redis lookups helped until invalidation became the more interesting problem. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Tuning Redis helped marginally; fixing how documents were split helped substantially. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Cleaning the Python inputs produced a larger gain than any change to the model did. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. In March I assumed the bottleneck was compute; it was almost entirely I/O. I do not trust an improvement that only appears on examples I picked myself. The tempting change was the model; the effective change was how documents were split.
## What I measured

The interesting failures were the ones where the output read well but cited the wrong source. Retrieval landed at 39% once I measured it end to end. I rebuilt the ingestion path so the before-and-after comparison would actually mean something.

Tuning Python helped marginally; fixing how documents were split helped substantially. I now write the evaluation before the change, which has saved me from at least two false wins. Batching improved throughput and made the tail latency considerably harder to reason about. The result held up in staging and quietly regressed the moment traffic patterns changed. Most of the week went into the ingestion path rather than the change I had actually planned to make. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Instrumenting the fallback branch cost a day and saved considerably more than that later. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Instrumenting the reranking pass cost a day and saved considerably more than that later. Writing the failure cases down first made the design conversation much shorter. I keep a short list of results I do not believe yet, and revisit it weekly. I spent an afternoon convinced the model was wrong before finding a bug in the loader.
## What I would change

I keep a short list of results I do not believe yet, and revisit it weekly. That gave accuracy of 7%, which I wrote down at the time. Two configurations differed by one parameter and by far more than one parameter's worth of output. Splitting the pipeline around the ingestion path made the failures much easier to localise. I do not trust an improvement that only appears on examples I picked myself. Comparing against a fixed baseline made the March numbers finally interpretable. In March I assumed the bottleneck was compute; it was almost entirely I/O. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. Caching PostgreSQL lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. Cleaning the Docker inputs produced a larger gain than any change to the model did. Caching PyTorch lookups helped until invalidation became the more interesting problem. Caching Docker lookups helped until invalidation became the more interesting problem. The result held up in staging and quietly regressed the moment traffic patterns changed. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Batching improved throughput and made the tail latency considerably harder to reason about.
