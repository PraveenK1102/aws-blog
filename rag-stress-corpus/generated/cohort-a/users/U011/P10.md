## The problem

Every improvement I could not reproduce twice turned out not to be an improvement. In June I assumed the bottleneck was compute; it was almost entirely I/O. The result held up in staging and quietly regressed the moment traffic patterns changed. On the instrumented run, retrieval showed 52%.

Because the harness came before the optimisation, I caught a regression that looked like a win. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. Splitting the pipeline around the embedding cache made the failures much easier to localise. Tuning Python helped marginally; fixing how documents were split helped substantially. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Comparing against a fixed baseline made the June numbers finally interpretable. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap. I now write the evaluation before the change, which has saved me from at least two false wins. Instrumenting the chunking logic cost a day and saved considerably more than that later. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Caching Docker lookups helped until invalidation became the more interesting problem. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially. Instrumenting the embedding cache cost a day and saved considerably more than that later. Keeping a log of every configuration felt tedious until I needed to explain a regression. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.
## What I built

Most of the week went into the scoring function rather than the change I had actually planned to make. Writing the failure cases down first made the design conversation much shorter. Accuracy landed at 27% once I measured it end to end.

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Cleaning the Redis inputs produced a larger gain than any change to the model did. The fallback path had never been exercised, so nobody knew it returned an empty result. Keeping a log of every configuration felt tedious until I needed to explain a regression. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. Tuning Docker helped marginally; fixing how documents were split helped substantially. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Splitting the pipeline around the reranking pass made the failures much easier to localise. Instrumenting the retrieval step cost a day and saved considerably more than that later. Instrumenting the ingestion path cost a day and saved considerably more than that later. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Batching improved throughput and made the tail latency considerably harder to reason about.
## What I measured

Two configurations differed by one parameter and by far more than one parameter's worth of output. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. The number I got for retrieval was 33%. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Caching Python lookups helped until invalidation became the more interesting problem. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Batching improved throughput and made the tail latency considerably harder to reason about. I do not trust an improvement that only appears on examples I picked myself. Most of the week went into the reranking pass rather than the change I had actually planned to make. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Cleaning the Docker inputs produced a larger gain than any change to the model did. Caching PostgreSQL lookups helped until invalidation became the more interesting problem. Caching PyTorch lookups helped until invalidation became the more interesting problem. Comparing against a fixed baseline made the June numbers finally interpretable.
## What I would change

The interesting failures were the ones where the output read well but cited the wrong source. I keep a short list of results I do not believe yet, and revisit it weekly. My recorded figure for latency is 45%. The tempting change was the model; the effective change was how documents were split. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did. Splitting the pipeline around the chunking logic made the failures much easier to localise. Caching Redis lookups helped until invalidation became the more interesting problem. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Tuning Redis helped marginally; fixing how documents were split helped substantially. Was the gain from the model or from cleaning the PostgreSQL inputs? Mostly the inputs. Most of the week went into the retrieval step rather than the change I had actually planned to make. Cleaning the Python inputs produced a larger gain than any change to the model did. Every improvement I could not reproduce twice turned out not to be an improvement. Caching PostgreSQL lookups helped until invalidation became the more interesting problem.
