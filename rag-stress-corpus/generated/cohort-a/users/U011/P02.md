Writing this up mostly to force myself to be precise about the numbers.

## The problem

I began by isolating the way inputs were normalised which took longer than the fix itself. Measured properly, accuracy came out at 39%. Most of the effort went into the way inputs were normalised because the earlier result was not reproducible. I began by isolating the way inputs were normalised so the comparison would actually mean something. The interesting part turned out to be the chunking behaviour and that alone explained most of the gap. I began by isolating the ranking of candidate passages which made the regression obvious. I began by isolating a small evaluation harness because the earlier result was not reproducible. Before changing anything I measured the chunking behaviour and that alone explained most of the gap. Before changing anything I measured the chunking behaviour which took longer than the fix itself. Before changing anything I measured the slowest stage in the pipeline so the comparison would actually mean something. Most of the effort went into the way inputs were normalised which made the regression obvious.
## What I built

Retrieval landed at 19% once I measured it end to end. Most of the effort went into the fallback path nobody exercised and the numbers were not what I expected. I began by isolating a small evaluation harness and I kept the harness afterwards. I rebuilt a small evaluation harness because the earlier result was not reproducible. I rebuilt a small evaluation harness and the numbers were not what I expected. Most of the effort went into the retrieval step on its own because the earlier result was not reproducible. The experiment started with the retrieval step on its own before touching anything else. I rebuilt the way inputs were normalised because the earlier result was not reproducible. I began by isolating the retrieval step on its own before touching anything else. The experiment started with the ranking of candidate passages and that alone explained most of the gap.
## What I measured

Measured properly, retrieval came out at 50%. I began by isolating the assumptions baked into the defaults and the numbers were not what I expected. I began by isolating the ranking of candidate passages and the numbers were not what I expected. The interesting failures were the ones where the output looked plausible but cited the wrong source. I rebuilt the chunking behaviour and that alone explained most of the gap. I rebuilt the chunking behaviour and the numbers were not what I expected. Most of the effort went into a small evaluation harness and I kept the harness afterwards. Most of the effort went into the chunking behaviour which took longer than the fix itself. Before changing anything I measured the way inputs were normalised so the comparison would actually mean something. The experiment started with the fallback path nobody exercised because the earlier result was not reproducible.
## What I would change

Most of the effort went into the ranking of candidate passages so the comparison would actually mean something. Most of the effort went into the retrieval step on its own which made the regression obvious. The experiment started with the chunking behaviour so the comparison would actually mean something. I began by isolating the assumptions baked into the defaults and I kept the harness afterwards. The experiment started with the slowest stage in the pipeline before touching anything else. The experiment started with the ranking of candidate passages before touching anything else. I began by isolating the ranking of candidate passages before touching anything else. I rebuilt the retrieval step on its own which took longer than the fix itself. The interesting part turned out to be the chunking behaviour which took longer than the fix itself.
