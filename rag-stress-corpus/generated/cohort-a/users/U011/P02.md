## The problem

Instrumenting the retrieval step cost a day and saved considerably more than that later. When I finally measured it, accuracy was 39%. I now write the evaluation before the change, which has saved me from at least two false wins. In March I assumed the bottleneck was compute; it was almost entirely I/O. Splitting the pipeline around the ingestion path made the failures much easier to localise. Every improvement I could not reproduce twice turned out not to be an improvement. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Cleaning the Redis inputs produced a larger gain than any change to the model did. I rebuilt the chunking logic so the before-and-after comparison would actually mean something.
## What I built

Running it end to end put retrieval at 19%. Two configurations differed by one parameter and by far more than one parameter's worth of output. Because the harness came before the optimisation, I caught a regression that looked like a win.

Writing the failure cases down first made the design conversation much shorter. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. The fallback path had never been exercised, so nobody knew it returned an empty result. The interesting failures were the ones where the output read well but cited the wrong source. Comparing against a fixed baseline made the March numbers finally interpretable. Caching PostgreSQL lookups helped until invalidation became the more interesting problem. Instrumenting the chunking logic cost a day and saved considerably more than that later. Caching Redis lookups helped until invalidation became the more interesting problem.
## What I measured

Caching PyTorch lookups helped until invalidation became the more interesting problem. I keep a short list of results I do not believe yet, and revisit it weekly. The number I got for retrieval was 50%. Latency turned out to be dominated by the scoring function, which I had assumed was cheap.

Batching improved throughput and made the tail latency considerably harder to reason about. I spent an afternoon convinced the model was wrong before finding a bug in the loader. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Splitting the pipeline around the fallback branch made the failures much easier to localise. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. The result held up in staging and quietly regressed the moment traffic patterns changed. Most of the week went into the chunking logic rather than the change I had actually planned to make. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs.
## What I would change

I do not trust an improvement that only appears on examples I picked myself. Instrumenting the fallback branch cost a day and saved considerably more than that later. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. The tempting change was the model; the effective change was how documents were split. Most of the week went into the embedding cache rather than the change I had actually planned to make. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning Docker helped marginally; fixing how documents were split helped substantially. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Tuning Python helped marginally; fixing how documents were split helped substantially.
