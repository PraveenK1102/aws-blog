## The problem

Keeping a log of every configuration felt tedious until I needed to explain a regression. Two configurations differed by one parameter and by far more than one parameter's worth of output. My recorded figure for latency is 52%. In July I assumed the bottleneck was compute; it was almost entirely I/O.

Most of the week went into the embedding cache rather than the change I had actually planned to make. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Every improvement I could not reproduce twice turned out not to be an improvement. The fallback path had never been exercised, so nobody knew it returned an empty result. I keep a short list of results I do not believe yet, and revisit it weekly. Because the harness came before the optimisation, I caught a regression that looked like a win. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Caching PostgreSQL lookups helped until invalidation became the more interesting problem.
## What I built

Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The tempting change was the model; the effective change was how documents were split. Batching improved throughput and made the tail latency considerably harder to reason about. Retrieval landed at 42% once I measured it end to end.

The interesting failures were the ones where the output read well but cited the wrong source. I rebuilt the ingestion path so the before-and-after comparison would actually mean something. Most of the week went into the reranking pass rather than the change I had actually planned to make. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Tuning Docker helped marginally; fixing how documents were split helped substantially. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Cleaning the Redis inputs produced a larger gain than any change to the model did.
## What I measured

The first version worked on the sample set and fell apart the moment real PostgreSQL inputs arrived. When I finally measured it, retrieval was 18%. I now write the evaluation before the change, which has saved me from at least two false wins. The result held up in staging and quietly regressed the moment traffic patterns changed. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in July. Writing the failure cases down first made the design conversation much shorter. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially.
## What I would change

Caching Redis lookups helped until invalidation became the more interesting problem. Instrumenting the scoring function cost a day and saved considerably more than that later. On the instrumented run, latency showed 38%. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did. Comparing against a fixed baseline made the July numbers finally interpretable. Cleaning the Docker inputs produced a larger gain than any change to the model did. Instrumenting the chunking logic cost a day and saved considerably more than that later. Splitting the pipeline around the embedding cache made the failures much easier to localise. I do not trust an improvement that only appears on examples I picked myself. Latency turned out to be dominated by the scoring function, which I had assumed was cheap.
