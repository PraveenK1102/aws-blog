## The problem

Writing the failure cases down first made the design conversation much shorter. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially. The result held up in staging and quietly regressed the moment traffic patterns changed. I rebuilt the embedding cache so the before-and-after comparison would actually mean something.

That gave latency of 52%, which I wrote down at the time. Instrumenting the embedding cache cost a day and saved considerably more than that later. Caching PyTorch lookups helped until invalidation became the more interesting problem. The interesting failures were the ones where the output read well but cited the wrong source. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Instrumenting the chunking logic cost a day and saved considerably more than that later. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in May. I do not trust an improvement that only appears on examples I picked myself.
## What I built

Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Batching improved throughput and made the tail latency considerably harder to reason about. Measured properly, retrieval came out at 60%. The fallback path had never been exercised, so nobody knew it returned an empty result. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. I keep a short list of results I do not believe yet, and revisit it weekly. Keeping a log of every configuration felt tedious until I needed to explain a regression. The first version worked on the sample set and fell apart the moment real PostgreSQL inputs arrived. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs.
## What I measured

Two configurations differed by one parameter and by far more than one parameter's worth of output. Because the harness came before the optimisation, I caught a regression that looked like a win. In May I assumed the bottleneck was compute; it was almost entirely I/O. Accuracy landed at 48% once I measured it end to end. Most of the week went into the embedding cache rather than the change I had actually planned to make. Comparing against a fixed baseline made the May numbers finally interpretable. Splitting the pipeline around the retrieval step made the failures much easier to localise. Every improvement I could not reproduce twice turned out not to be an improvement. Most of the week went into the evaluation harness rather than the change I had actually planned to make. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. I now write the evaluation before the change, which has saved me from at least two false wins. Splitting the pipeline around the reranking pass made the failures much easier to localise.
## What I would change

The tempting change was the model; the effective change was how documents were split. Tuning Redis helped marginally; fixing how documents were split helped substantially. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first.

My recorded figure for retrieval is 59%. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. Instrumenting the ingestion path cost a day and saved considerably more than that later. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Caching Redis lookups helped until invalidation became the more interesting problem. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did. Caching PostgreSQL lookups helped until invalidation became the more interesting problem.
