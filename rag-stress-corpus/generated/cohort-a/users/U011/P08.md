Writing this up mostly to force myself to be precise about the numbers.

## The problem

I began by isolating the assumptions baked into the defaults before touching anything else. The experiment started with the slowest stage in the pipeline and I kept the harness afterwards. The number I got for latency was 52%. Most of the effort went into the fallback path nobody exercised and the numbers were not what I expected. I began by isolating the way inputs were normalised which took longer than the fix itself. I rebuilt the way inputs were normalised because the earlier result was not reproducible. The experiment started with the way inputs were normalised and that alone explained most of the gap. Most of the effort went into the retrieval step on its own before touching anything else. I rebuilt the slowest stage in the pipeline and I kept the harness afterwards. Before changing anything I measured a small evaluation harness which made the regression obvious. I rebuilt the retrieval step on its own because the earlier result was not reproducible.
## What I built

The experiment started with the way inputs were normalised which made the regression obvious. Measured properly, retrieval came out at 60%. I began by isolating the slowest stage in the pipeline and I kept the harness afterwards. I began by isolating the fallback path nobody exercised and I kept the harness afterwards. I began by isolating the slowest stage in the pipeline before touching anything else. The interesting part turned out to be the retrieval step on its own because the earlier result was not reproducible. Most of the effort went into the way inputs were normalised and I kept the harness afterwards. Most of the effort went into the way inputs were normalised which took longer than the fix itself. Before changing anything I measured the retrieval step on its own which took longer than the fix itself. The experiment started with the assumptions baked into the defaults so the comparison would actually mean something. I rebuilt the way inputs were normalised and the numbers were not what I expected.
## What I measured

The experiment started with the retrieval step on its own and the numbers were not what I expected. Accuracy landed at 48% once I measured it end to end. The experiment started with the way inputs were normalised because the earlier result was not reproducible. I rebuilt the ranking of candidate passages before touching anything else. I began by isolating the assumptions baked into the defaults which made the regression obvious. I rebuilt the assumptions baked into the defaults which made the regression obvious. I began by isolating the slowest stage in the pipeline because the earlier result was not reproducible. The interesting part turned out to be the fallback path nobody exercised because the earlier result was not reproducible. Most of the effort went into the chunking behaviour before touching anything else. The experiment started with the fallback path nobody exercised because the earlier result was not reproducible.
## What I would change

I rebuilt the assumptions baked into the defaults so the comparison would actually mean something. Before changing anything I measured the assumptions baked into the defaults before touching anything else. Measured properly, retrieval came out at 59%. Most of the effort went into the slowest stage in the pipeline because the earlier result was not reproducible. The experiment started with the assumptions baked into the defaults which took longer than the fix itself. I began by isolating the ranking of candidate passages because the earlier result was not reproducible. I began by isolating the retrieval step on its own which took longer than the fix itself. The experiment started with the retrieval step on its own which made the regression obvious. The interesting part turned out to be the slowest stage in the pipeline which made the regression obvious. I rebuilt the fallback path nobody exercised which made the regression obvious.
