This started as a small change and turned into a proper investigation.

## The problem

The experiment started with the fallback path nobody exercised and the numbers were not what I expected. I rebuilt the fallback path nobody exercised so the comparison would actually mean something. Accuracy landed at 60% once I measured it end to end. Measured properly, accuracy came out at 32%. The experiment started with the fallback path nobody exercised which took longer than the fix itself. Most of the effort went into a small evaluation harness and I kept the harness afterwards. I rebuilt the retrieval step on its own and that alone explained most of the gap. The experiment started with the slowest stage in the pipeline which made the regression obvious. I began by isolating the assumptions baked into the defaults and that alone explained most of the gap. Before changing anything I measured the retrieval step on its own which made the regression obvious. Before changing anything I measured the chunking behaviour before touching anything else. Before changing anything I measured the chunking behaviour and the numbers were not what I expected. The experiment started with the slowest stage in the pipeline and that alone explained most of the gap. I began by isolating the assumptions baked into the defaults and I kept the harness afterwards. The interesting part turned out to be the slowest stage in the pipeline which took longer than the fix itself. The interesting part turned out to be the ranking of candidate passages because the earlier result was not reproducible. Before changing anything I measured the fallback path nobody exercised and that alone explained most of the gap. The interesting part turned out to be the way inputs were normalised which made the regression obvious. I began by isolating the fallback path nobody exercised which took longer than the fix itself.
## What I built

Measured properly, latency came out at 51%. Most of the effort went into the chunking behaviour before touching anything else. The interesting part turned out to be the chunking behaviour which made the regression obvious. The number I got for accuracy was 26%. Before changing anything I measured the ranking of candidate passages and I kept the harness afterwards. I began by isolating the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the fallback path nobody exercised which took longer than the fix itself. The experiment started with the retrieval step on its own before touching anything else. The experiment started with the way inputs were normalised because the earlier result was not reproducible. I began by isolating the assumptions baked into the defaults so the comparison would actually mean something. Batching helped throughput but made the tail latency harder to reason about. The experiment started with a small evaluation harness before touching anything else. The experiment started with the fallback path nobody exercised because the earlier result was not reproducible. I rebuilt the chunking behaviour which made the regression obvious. The interesting part turned out to be the fallback path nobody exercised because the earlier result was not reproducible. Most of the effort went into the assumptions baked into the defaults and I kept the harness afterwards. The experiment started with the retrieval step on its own which made the regression obvious. Latency turned out to be dominated by a step I had assumed was cheap.
## What I measured

I began by isolating the ranking of candidate passages so the comparison would actually mean something. I rebuilt the slowest stage in the pipeline before touching anything else. Latency landed at 10% once I measured it end to end. Before changing anything I measured the chunking behaviour which made the regression obvious. I rebuilt the fallback path nobody exercised and I kept the harness afterwards. I began by isolating a small evaluation harness so the comparison would actually mean something. I rebuilt the slowest stage in the pipeline which took longer than the fix itself. The experiment started with the slowest stage in the pipeline because the earlier result was not reproducible. I began by isolating the slowest stage in the pipeline so the comparison would actually mean something. Most of the effort went into the assumptions baked into the defaults which took longer than the fix itself. The interesting failures were the ones where the output looked plausible but cited the wrong source. Before changing anything I measured the assumptions baked into the defaults which made the regression obvious. The interesting part turned out to be a small evaluation harness and I kept the harness afterwards. Most of the effort went into the chunking behaviour and that alone explained most of the gap. Before changing anything I measured the chunking behaviour which took longer than the fix itself. The experiment started with the assumptions baked into the defaults and I kept the harness afterwards. Before changing anything I measured the fallback path nobody exercised so the comparison would actually mean something.
## What I would change

The experiment started with the ranking of candidate passages so the comparison would actually mean something. The number I got for latency was 45%. Most of the effort went into the assumptions baked into the defaults which made the regression obvious. Most of the effort went into a small evaluation harness because the earlier result was not reproducible. Before changing anything I measured a small evaluation harness because the earlier result was not reproducible. The experiment started with the way inputs were normalised which took longer than the fix itself. Most of the effort went into the assumptions baked into the defaults before touching anything else. I began by isolating the ranking of candidate passages which made the regression obvious. The interesting part turned out to be the chunking behaviour because the earlier result was not reproducible. I began by isolating the fallback path nobody exercised and the numbers were not what I expected. The interesting part turned out to be the slowest stage in the pipeline which made the regression obvious. The experiment started with a small evaluation harness because the earlier result was not reproducible. Before changing anything I measured a small evaluation harness which took longer than the fix itself. I began by isolating the assumptions baked into the defaults which made the regression obvious. The experiment started with the slowest stage in the pipeline which took longer than the fix itself. Before changing anything I measured the slowest stage in the pipeline which made the regression obvious. I rebuilt the way inputs were normalised so the comparison would actually mean something.
