## The problem

That gave accuracy of 60%, which I wrote down at the time. Every improvement I could not reproduce twice turned out not to be an improvement. When I finally measured it, accuracy was 32%. I now write the evaluation before the change, which has saved me from at least two false wins. Keeping a log of every configuration felt tedious until I needed to explain a regression. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Cleaning the Docker inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the Docker inputs? Mostly the inputs. Batching improved throughput and made the tail latency considerably harder to reason about. Splitting the pipeline around the chunking logic made the failures much easier to localise. Caching Redis lookups helped until invalidation became the more interesting problem. Tuning Python helped marginally; fixing how documents were split helped substantially. Most of the week went into the embedding cache rather than the change I had actually planned to make. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Instrumenting the chunking logic cost a day and saved considerably more than that later. Tuning Redis helped marginally; fixing how documents were split helped substantially. The fallback path had never been exercised, so nobody knew it returned an empty result. Writing the failure cases down first made the design conversation much shorter. Keeping a log of every configuration felt tedious until I needed to explain a regression. Tuning Redis helped marginally; fixing how documents were split helped substantially.
## What I built

Splitting the pipeline around the ingestion path made the failures much easier to localise. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did. Accuracy landed at 26% once I measured it end to end.

The tempting change was the model; the effective change was how documents were split. The number I got for latency was 51%. The fallback path had never been exercised, so nobody knew it returned an empty result. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. The interesting failures were the ones where the output read well but cited the wrong source. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Most of the week went into the evaluation harness rather than the change I had actually planned to make. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Splitting the pipeline around the retrieval step made the failures much easier to localise. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Cleaning the Redis inputs produced a larger gain than any change to the model did. Instrumenting the retrieval step cost a day and saved considerably more than that later. In March I assumed the bottleneck was compute; it was almost entirely I/O. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Batching improved throughput and made the tail latency considerably harder to reason about.
## What I measured

That gave latency of 10%, which I wrote down at the time. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. Most of the week went into the ingestion path rather than the change I had actually planned to make. Writing the failure cases down first made the design conversation much shorter. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Instrumenting the fallback branch cost a day and saved considerably more than that later. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. In March I assumed the bottleneck was compute; it was almost entirely I/O. I keep a short list of results I do not believe yet, and revisit it weekly. Cleaning the Python inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Was the gain from the model or from cleaning the PostgreSQL inputs? Mostly the inputs. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap. I rebuilt the scoring function so the before-and-after comparison would actually mean something. Caching PyTorch lookups helped until invalidation became the more interesting problem. Instrumenting the ingestion path cost a day and saved considerably more than that later. The result held up in staging and quietly regressed the moment traffic patterns changed. Every improvement I could not reproduce twice turned out not to be an improvement.
## What I would change

The result held up in staging and quietly regressed the moment traffic patterns changed. Comparing against a fixed baseline made the March numbers finally interpretable. Because the harness came before the optimisation, I caught a regression that looked like a win. Latency landed at 45% once I measured it end to end.

Caching PostgreSQL lookups helped until invalidation became the more interesting problem. I do not trust an improvement that only appears on examples I picked myself. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Caching Python lookups helped until invalidation became the more interesting problem. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Instrumenting the embedding cache cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Two configurations differed by one parameter and by far more than one parameter's worth of output. Instrumenting the ingestion path cost a day and saved considerably more than that later. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially. The first version worked on the sample set and fell apart the moment real PostgreSQL inputs arrived. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in March. I now write the evaluation before the change, which has saved me from at least two false wins. I do not trust an improvement that only appears on examples I picked myself. Comparing against a fixed baseline made the March numbers finally interpretable.
