## The problem

When I finally measured it, latency was 17%. Latency turned out to be dominated by the scoring function, which I had assumed was cheap. Measured properly, latency came out at 40%. The fallback path had never been exercised, so nobody knew it returned an empty result. The tempting change was the model; the effective change was how documents were split. Caching Redis lookups helped until invalidation became the more interesting problem. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Instrumenting the reranking pass cost a day and saved considerably more than that later. The result held up in staging and quietly regressed the moment traffic patterns changed. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. Caching Python lookups helped until invalidation became the more interesting problem. The first version worked on the sample set and fell apart the moment real Python inputs arrived. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs.
## What I built

Because the harness came before the optimisation, I caught a regression that looked like a win. Measured properly, retrieval came out at 18%. Accuracy landed at 20% once I measured it end to end. Cleaning the PyTorch inputs produced a larger gain than any change to the model did.

I spent an afternoon convinced the model was wrong before finding a bug in the loader. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Most of the week went into the embedding cache rather than the change I had actually planned to make. I keep a short list of results I do not believe yet, and revisit it weekly. I do not trust an improvement that only appears on examples I picked myself. Most of the week went into the ingestion path rather than the change I had actually planned to make. Tuning Docker helped marginally; fixing how documents were split helped substantially. Instrumenting the evaluation harness cost a day and saved considerably more than that later. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially. Instrumenting the fallback branch cost a day and saved considerably more than that later. Tuning Python helped marginally; fixing how documents were split helped substantially.
## What I measured

Splitting the pipeline around the reranking pass made the failures much easier to localise. My recorded figure for latency is 25%. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch.

Reproducibility mattered more here than cleverness, which is not how I would have ranked them in April. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Comparing against a fixed baseline made the April numbers finally interpretable. Keeping a log of every configuration felt tedious until I needed to explain a regression. The interesting failures were the ones where the output read well but cited the wrong source. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Every improvement I could not reproduce twice turned out not to be an improvement. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Latency turned out to be dominated by the retrieval step, which I had assumed was cheap.
## What I would change

Batching improved throughput and made the tail latency considerably harder to reason about. Writing the failure cases down first made the design conversation much shorter. Measured properly, accuracy came out at 7%. In April I assumed the bottleneck was compute; it was almost entirely I/O. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. I now write the evaluation before the change, which has saved me from at least two false wins. Two configurations differed by one parameter and by far more than one parameter's worth of output. Splitting the pipeline around the fallback branch made the failures much easier to localise. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Cleaning the Docker inputs produced a larger gain than any change to the model did. Splitting the pipeline around the retrieval step made the failures much easier to localise.
