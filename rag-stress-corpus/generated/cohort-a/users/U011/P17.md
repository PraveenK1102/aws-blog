## The problem

The number I got for accuracy was 51%. Comparing against a fixed baseline made the August numbers finally interpretable. Measured properly, accuracy came out at 44%. Writing the failure cases down first made the design conversation much shorter. The tempting change was the model; the effective change was how documents were split. Batching improved throughput and made the tail latency considerably harder to reason about. Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Most of the week went into the retrieval step rather than the change I had actually planned to make. Because the harness came before the optimisation, I caught a regression that looked like a win. Cleaning the PyTorch inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the Redis inputs? Mostly the inputs. Cleaning the Redis inputs produced a larger gain than any change to the model did. The first version worked on the sample set and fell apart the moment real PyTorch inputs arrived. Cleaning the Docker inputs produced a larger gain than any change to the model did. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Instrumenting the ingestion path cost a day and saved considerably more than that later. Cleaning the Python inputs produced a larger gain than any change to the model did. The tempting change was the model; the effective change was how documents were split.
## What I built

Keeping a log of every configuration felt tedious until I needed to explain a regression. The result held up in staging and quietly regressed the moment traffic patterns changed. I keep a short list of results I do not believe yet, and revisit it weekly.

When I finally measured it, latency was 14%. My recorded figure for retrieval is 45%. A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. Splitting the pipeline around the retrieval step made the failures much easier to localise. Instrumenting the retrieval step cost a day and saved considerably more than that later. In August I assumed the bottleneck was compute; it was almost entirely I/O. I now write the evaluation before the change, which has saved me from at least two false wins. Tuning PostgreSQL helped marginally; fixing how documents were split helped substantially. Instrumenting the reranking pass cost a day and saved considerably more than that later. Latency turned out to be dominated by the embedding cache, which I had assumed was cheap. Most of the week went into the scoring function rather than the change I had actually planned to make. I rebuilt the evaluation harness so the before-and-after comparison would actually mean something. I rebuilt the fallback branch so the before-and-after comparison would actually mean something. Tuning Redis helped marginally; fixing how documents were split helped substantially. I now write the evaluation before the change, which has saved me from at least two false wins.
## What I measured

Most of the week went into the chunking logic rather than the change I had actually planned to make. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in August. Tuning Docker helped marginally; fixing how documents were split helped substantially. Once I logged every configuration, explaining the regression to myself took minutes rather than a day.

On the instrumented run, retrieval showed 30%. Was the gain from the model or from cleaning the PostgreSQL inputs? Mostly the inputs. I do not trust an improvement that only appears on examples I picked myself. I rebuilt the chunking logic so the before-and-after comparison would actually mean something. Two configurations differed by one parameter and by far more than one parameter's worth of output. Caching PostgreSQL lookups helped until invalidation became the more interesting problem. The fallback path had never been exercised, so nobody knew it returned an empty result. Caching Python lookups helped until invalidation became the more interesting problem. Tuning Python helped marginally; fixing how documents were split helped substantially. Caching Redis lookups helped until invalidation became the more interesting problem. Tuning PyTorch helped marginally; fixing how documents were split helped substantially. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did. Comparing against a fixed baseline made the August numbers finally interpretable. Because the harness came before the optimisation, I caught a regression that looked like a win.
## What I would change

I spent an afternoon convinced the model was wrong before finding a bug in the loader. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Running it end to end put retrieval at 35%. Cleaning the Python inputs produced a larger gain than any change to the model did. Latency turned out to be dominated by the evaluation harness, which I had assumed was cheap. Every improvement I could not reproduce twice turned out not to be an improvement. The interesting failures were the ones where the output read well but cited the wrong source. Splitting the pipeline around the ingestion path made the failures much easier to localise. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. I rebuilt the embedding cache so the before-and-after comparison would actually mean something. Splitting the pipeline around the scoring function made the failures much easier to localise. Latency turned out to be dominated by the chunking logic, which I had assumed was cheap. Caching Docker lookups helped until invalidation became the more interesting problem. I do not trust an improvement that only appears on examples I picked myself. The interesting failures were the ones where the output read well but cited the wrong source.
