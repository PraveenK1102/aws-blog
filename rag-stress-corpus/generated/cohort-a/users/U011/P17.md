Spent most of this week on the retrieval side of the project.

## The problem

Most of the effort went into the retrieval step on its own which took longer than the fix itself. Accuracy landed at 44% once I measured it end to end. Measured properly, accuracy came out at 51%. I learned to distrust any improvement that only shows up on the examples I chose myself. The interesting part turned out to be the ranking of candidate passages which made the regression obvious. I began by isolating the chunking behaviour which made the regression obvious. Most of the effort went into the retrieval step on its own so the comparison would actually mean something. Before changing anything I measured the retrieval step on its own which made the regression obvious. Most of the effort went into the fallback path nobody exercised because the earlier result was not reproducible. The experiment started with the way inputs were normalised before touching anything else. I began by isolating the way inputs were normalised and the numbers were not what I expected. I rebuilt the assumptions baked into the defaults and I kept the harness afterwards. Most of the effort went into the assumptions baked into the defaults because the earlier result was not reproducible. I began by isolating the way inputs were normalised because the earlier result was not reproducible. Most of the effort went into the ranking of candidate passages so the comparison would actually mean something. The experiment started with the retrieval step on its own which took longer than the fix itself.
## What I built

I rebuilt the slowest stage in the pipeline and I kept the harness afterwards. Retrieval landed at 45% once I measured it end to end. Before changing anything I measured the retrieval step on its own so the comparison would actually mean something. The number I got for latency was 14%. I began by isolating the retrieval step on its own before touching anything else. Most of the effort went into the way inputs were normalised because the earlier result was not reproducible. The interesting part turned out to be the slowest stage in the pipeline which made the regression obvious. Most of the effort went into the way inputs were normalised which made the regression obvious. Before changing anything I measured a small evaluation harness before touching anything else. The experiment started with the ranking of candidate passages which made the regression obvious. I rebuilt the fallback path nobody exercised because the earlier result was not reproducible. I began by isolating the assumptions baked into the defaults so the comparison would actually mean something. The experiment started with the fallback path nobody exercised before touching anything else. I rebuilt a small evaluation harness and I kept the harness afterwards. The interesting part turned out to be the chunking behaviour and the numbers were not what I expected. I began by isolating the ranking of candidate passages and the numbers were not what I expected.
## What I measured

Retrieval landed at 30% once I measured it end to end. I rebuilt the way inputs were normalised which made the regression obvious. The interesting part turned out to be a small evaluation harness which made the regression obvious. The interesting failures were the ones where the output looked plausible but cited the wrong source. Before changing anything I measured the ranking of candidate passages which took longer than the fix itself. I rebuilt the retrieval step on its own which made the regression obvious. The interesting part turned out to be the way inputs were normalised and the numbers were not what I expected. I began by isolating the chunking behaviour which took longer than the fix itself. The interesting part turned out to be the way inputs were normalised and that alone explained most of the gap. Most of the effort went into a small evaluation harness before touching anything else. Most of the time went into building a way to measure the change rather than making the change. I rebuilt the ranking of candidate passages and the numbers were not what I expected. I rebuilt the chunking behaviour which made the regression obvious. I began by isolating the ranking of candidate passages which made the regression obvious. I rebuilt the ranking of candidate passages which made the regression obvious.
## What I would change

I rebuilt the assumptions baked into the defaults and the numbers were not what I expected. The number I got for retrieval was 35%. I began by isolating the retrieval step on its own and the numbers were not what I expected. The experiment started with a small evaluation harness so the comparison would actually mean something. Most of the effort went into the ranking of candidate passages because the earlier result was not reproducible. Before changing anything I measured the way inputs were normalised and the numbers were not what I expected. Most of the effort went into a small evaluation harness because the earlier result was not reproducible. Before changing anything I measured the way inputs were normalised which took longer than the fix itself. I rebuilt the fallback path nobody exercised and the numbers were not what I expected. I rebuilt the ranking of candidate passages and I kept the harness afterwards. Most of the effort went into the retrieval step on its own which made the regression obvious. The interesting part turned out to be a small evaluation harness which took longer than the fix itself. The interesting part turned out to be the fallback path nobody exercised because the earlier result was not reproducible. I began by isolating the chunking behaviour and the numbers were not what I expected. Splitting the pipeline into separately testable steps made the failures much easier to localise.
