## The problem

Splitting the pipeline around the scoring function made the failures much easier to localise. Measured properly, latency came out at 6%. I rebuilt the fallback branch so the before-and-after comparison would actually mean something.

In June I assumed the bottleneck was compute; it was almost entirely I/O. I now write the evaluation before the change, which has saved me from at least two false wins. On the instrumented run, accuracy showed 31%. The result held up in staging and quietly regressed the moment traffic patterns changed. The first version worked on the sample set and fell apart the moment real Redis inputs arrived. Once I logged every configuration, explaining the regression to myself took minutes rather than a day. Instrumenting the reranking pass cost a day and saved considerably more than that later. Was the gain from the model or from cleaning the PyTorch inputs? Mostly the inputs. Instrumenting the chunking logic cost a day and saved considerably more than that later. Splitting the pipeline around the fallback branch made the failures much easier to localise. Most of the week went into the scoring function rather than the change I had actually planned to make.
## What I built

Writing the failure cases down first made the design conversation much shorter. My recorded figure for latency is 50%. Retrieval landed at 56% once I measured it end to end. The interesting failures were the ones where the output read well but cited the wrong source.

Instrumenting the evaluation harness cost a day and saved considerably more than that later. I keep a short list of results I do not believe yet, and revisit it weekly. I rebuilt the reranking pass so the before-and-after comparison would actually mean something. Because the harness came before the optimisation, I caught a regression that looked like a win. Most of the week went into the reranking pass rather than the change I had actually planned to make. Tuning Redis helped marginally; fixing how documents were split helped substantially. Latency turned out to be dominated by the reranking pass, which I had assumed was cheap. Instrumenting the retrieval step cost a day and saved considerably more than that later.
## What I measured

Comparing against a fixed baseline made the June numbers finally interpretable. I spent an afternoon convinced the model was wrong before finding a bug in the loader. Retrieval landed at 31% once I measured it end to end.

A plausible answer citing the wrong passage is worse than an obvious failure, and much harder to catch. The fallback path had never been exercised, so nobody knew it returned an empty result. Every improvement I could not reproduce twice turned out not to be an improvement. Two configurations differed by one parameter and by far more than one parameter's worth of output. Reproducibility mattered more here than cleverness, which is not how I would have ranked them in June. The first version worked on the sample set and fell apart the moment real Docker inputs arrived. I rebuilt the retrieval step so the before-and-after comparison would actually mean something. Most of the week went into the ingestion path rather than the change I had actually planned to make. Cleaning the Docker inputs produced a larger gain than any change to the model did.
## What I would change

The tempting change was the model; the effective change was how documents were split. That gave latency of 38%, which I wrote down at the time. I do not trust an improvement that only appears on examples I picked myself.

Two runs disagreed by enough that I stopped trusting the setup and rebuilt the measurement first. Batching improved throughput and made the tail latency considerably harder to reason about. Was the gain from the model or from cleaning the Python inputs? Mostly the inputs. Splitting the pipeline around the evaluation harness made the failures much easier to localise. Keeping a log of every configuration felt tedious until I needed to explain a regression. Caching Python lookups helped until invalidation became the more interesting problem. Cleaning the PostgreSQL inputs produced a larger gain than any change to the model did. Caching Docker lookups helped until invalidation became the more interesting problem. Latency turned out to be dominated by the ingestion path, which I had assumed was cheap.
