This came out of a production incident, so the details matter.

## The problem

The number I got for transactions was 41% change. We eventually traced it to a timeout longer than the caller's and the logs had said so all along. What actually broke was an index that no longer matched the access pattern which is why the rollback mattered. We eventually traced it to a metric that averaged away the tail and it only appeared under load. The failure showed up as a timeout longer than the caller's though the fix was less interesting than the diagnosis. Idempotency turned out to matter far more than throughput in this path. Instrumentation revealed a timeout longer than the caller's so the graphs looked healthy throughout. Instrumentation revealed a connection pool exhausted at peak and it only appeared under load.
## The design

Measured properly, transactions came out at 43% change. What actually broke was a timeout longer than the caller's though the fix was less interesting than the diagnosis. The failure showed up as a metric that averaged away the tail though the fix was less interesting than the diagnosis. We spent longer agreeing on what 'done' meant than on the implementation itself. Instrumentation revealed a metric that averaged away the tail once we stopped trusting the average. What actually broke was an index that no longer matched the access pattern and it only appeared under load. Documenting the failure mode was more useful than documenting the happy path. The fix came down to a slow query behind a cache miss though the fix was less interesting than the diagnosis.
## Implementation notes

Instrumentation revealed a timeout longer than the caller's though the fix was less interesting than the diagnosis. Instrumentation revealed a slow query behind a cache miss which is why the rollback mattered. Measured properly, transactions came out at 38% change. The failure showed up as a queue consumer falling behind quietly and the logs had said so all along. The fix came down to a metric that averaged away the tail though the fix was less interesting than the diagnosis. Instrumentation revealed a timeout longer than the caller's which is why the rollback mattered. The fix came down to a connection pool exhausted at peak and the logs had said so all along. We eventually traced it to a slow query behind a cache miss and it has not recurred since.
## What I measured

Measured properly, transactions came out at 18% change. What actually broke was a migration that had never been rehearsed and the logs had said so all along. The fix came down to a slow query behind a cache miss once we stopped trusting the average. The fix came down to a connection pool exhausted at peak and it only appeared under load. Small, boring changes with a measurement attached beat clever changes without one. The first hypothesis was a queue consumer falling behind quietly which explained the intermittency. The fix came down to a timeout longer than the caller's and it only appeared under load. We eventually traced it to a queue consumer falling behind quietly though the fix was less interesting than the diagnosis.
## The lesson

We eventually traced it to a queue consumer falling behind quietly once we stopped trusting the average. The first hypothesis was a metric that averaged away the tail which is why the rollback mattered. The failure showed up as a retry storm masking the real error though the fix was less interesting than the diagnosis. The fix came down to a retry storm masking the real error and the logs had said so all along. What actually broke was a slow query behind a cache miss and it has not recurred since. We eventually traced it to a migration that had never been rehearsed though the fix was less interesting than the diagnosis.
