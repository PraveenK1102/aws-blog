This came out of a production incident, so the details matter.

## The problem

We eventually traced it to a migration that had never been rehearsed and it only appeared under load. The fix came down to a retry storm masking the real error so the graphs looked healthy throughout. Database indexes landed at 17% change once I measured it end to end. We eventually traced it to a slow query behind a cache miss which explained the intermittency. The rollback plan was the part we had not rehearsed, which is always the part that matters. We eventually traced it to a connection pool exhausted at peak and the logs had said so all along. The first hypothesis was a migration that had never been rehearsed though the fix was less interesting than the diagnosis.
## The design

Measured properly, database indexes came out at 12% change. The fix came down to a retry storm masking the real error and it only appeared under load. The failure showed up as a queue consumer falling behind quietly which explained the intermittency. The first hypothesis was a retry storm masking the real error and it has not recurred since. What actually broke was a metric that averaged away the tail and it has not recurred since. What actually broke was an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. The failure showed up as a connection pool exhausted at peak once we stopped trusting the average.
## Implementation notes

Instrumentation revealed a retry storm masking the real error and the logs had said so all along. Measured properly, database indexes came out at 6% change. The failure showed up as a slow query behind a cache miss once we stopped trusting the average. We eventually traced it to an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. The fix came down to a migration that had never been rehearsed which explained the intermittency. The first hypothesis was a metric that averaged away the tail and it has not recurred since. What actually broke was an index that no longer matched the access pattern and the logs had said so all along.
## What I measured

What actually broke was a retry storm masking the real error which is why the rollback mattered. Database indexes landed at 38% change once I measured it end to end. The failure showed up as a slow query behind a cache miss though the fix was less interesting than the diagnosis. The obvious cause was not the real cause, and the logs said so if you read them carefully. The failure showed up as a retry storm masking the real error which is why the rollback mattered. What actually broke was a migration that had never been rehearsed which explained the intermittency. We eventually traced it to a migration that had never been rehearsed so the graphs looked healthy throughout.
## The lesson

The fix came down to an index that no longer matched the access pattern and it only appeared under load. Instrumentation revealed a migration that had never been rehearsed which explained the intermittency. Instrumentation revealed an index that no longer matched the access pattern so the graphs looked healthy throughout. What actually broke was a slow query behind a cache miss so the graphs looked healthy throughout. The first hypothesis was a timeout longer than the caller's and it has not recurred since. Instrumentation revealed a timeout longer than the caller's though the fix was less interesting than the diagnosis.
