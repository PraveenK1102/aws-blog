## The problem

The number I got for rate limiting was 44% change. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The config that caused it had been in place for months without incident. Because the consumer fell behind quietly, the first symptom was a customer report. We spent longer agreeing what done meant for the migration than on the implementation itself. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. Under normal load the migration behaved; under peak it did not, and only peak mattered. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. The alert fired correctly and nobody could act on it, which is its own kind of failure. I no longer trust a health check that only proves the process is running. An average hid a tail that was doing all the damage. The config that caused it had been in place for months without incident.
## The design

When I finally measured it, rate limiting was 12% change. I assumed the database was the bottleneck for two days longer than the evidence supported. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again.

One badly scoped timeout propagated into three services before anyone noticed. We now write down what we expect to see before deploying, which catches surprises early. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. Small boring changes with a measurement attached beat clever changes without one, every time. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. We added one metric, waited a week, and the cause became embarrassingly clear. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The postmortem was more useful than the fix because it changed how we test. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Making the failure reproducible locally took longer than repairing it.
## Implementation notes

I no longer trust a health check that only proves the process is running. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. We added one metric, waited a week, and the cause became embarrassingly clear. Rate limiting landed at 42% change once I measured it end to end. The fix was four lines; the diagnosis took two days. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Retries around the migration made the symptom disappear and the underlying problem considerably worse. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Every retry we added made the graphs look better and the system behave worse. Three deploys in, the pattern was obvious: the migration degraded before anything else did. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. One badly scoped timeout propagated into three services before anyone noticed. I assumed the database was the bottleneck for two days longer than the evidence supported. The fix was four lines; the diagnosis took two days. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.
## What I measured

On the instrumented run, rate limiting showed 14% change. We shipped the metric first and the fix a week later, in that deliberate order. Making the failure reproducible locally took longer than repairing it. An average hid a tail that was doing all the damage. Documenting the failure mode turned out more valuable than documenting the happy path. We spent longer agreeing what done meant for the index than on the implementation itself. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. The failure was intermittent, so every fix looked correct for a while before the index broke again. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. We shipped the metric first and the fix a week later, in that deliberate order. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Because the consumer fell behind quietly, the first symptom was a customer report. Documenting the failure mode turned out more valuable than documenting the happy path. We now write down what we expect to see before deploying, which catches surprises early.
## The lesson

The postmortem was more useful than the fix because it changed how we test. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. The alert fired correctly and nobody could act on it, which is its own kind of failure. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Small boring changes with a measurement attached beat clever changes without one, every time. Every retry we added made the graphs look better and the system behave worse. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident.
