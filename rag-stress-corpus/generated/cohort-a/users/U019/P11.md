## The problem

We now write down what we expect to see before deploying, which catches surprises early. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The number I got for React was 23% change. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. Making the failure reproducible locally took longer than repairing it. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. We spent longer agreeing what done meant for the migration than on the implementation itself. We spent longer agreeing what done meant for the retry path than on the implementation itself. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. Because the consumer fell behind quietly, the first symptom was a customer report. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Every retry we added made the graphs look better and the system behave worse.
## The design

The alert fired correctly and nobody could act on it, which is its own kind of failure. On the instrumented run, React showed 31% change. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We spent longer agreeing what done meant for the timeout budget than on the implementation itself.

The postmortem was more useful than the fix because it changed how we test. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Every retry we added made the graphs look better and the system behave worse. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Small boring changes with a measurement attached beat clever changes without one, every time. Under normal load the migration behaved; under peak it did not, and only peak mattered. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. The postmortem was more useful than the fix because it changed how we test. An average hid a tail that was doing all the damage. The alert fired correctly and nobody could act on it, which is its own kind of failure. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident.
## Implementation notes

Because the consumer fell behind quietly, the first symptom was a customer report. When I finally measured it, React was 45% change. Idempotency mattered far more than throughput on this path, which inverted our original priorities. I assumed the database was the bottleneck for two days longer than the evidence supported. Documenting the failure mode turned out more valuable than documenting the happy path. An average hid a tail that was doing all the damage. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. I no longer trust a health check that only proves the process is running. Small boring changes with a measurement attached beat clever changes without one, every time. The index only misbehaved at peak, so the averages stayed healthy right through the incident. The config that caused it had been in place for months without incident. Documenting the failure mode turned out more valuable than documenting the happy path.
## What I measured

The config that caused it had been in place for months without incident. My recorded figure for React is 33% change. Under normal load the retry path behaved; under peak it did not, and only peak mattered. We shipped the metric first and the fix a week later, in that deliberate order. We added one metric, waited a week, and the cause became embarrassingly clear. Three deploys in, the pattern was obvious: the migration degraded before anything else did. One badly scoped timeout propagated into three services before anyone noticed. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. I assumed the database was the bottleneck for two days longer than the evidence supported. One badly scoped timeout propagated into three services before anyone noticed. Idempotency mattered far more than throughput on this path, which inverted our original priorities. We shipped the metric first and the fix a week later, in that deliberate order.
## The lesson

The failure was intermittent, so every fix looked correct for a while before the migration broke again. Retries around the index made the symptom disappear and the underlying problem considerably worse. Measured properly, React came out at 42% change.

Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. The fix was four lines; the diagnosis took two days. I no longer trust a health check that only proves the process is running. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Making the failure reproducible locally took longer than repairing it. We now write down what we expect to see before deploying, which catches surprises early. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse.
