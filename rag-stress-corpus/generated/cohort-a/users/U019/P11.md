This came out of a production incident, so the details matter.

## The problem

Measured properly, React came out at 23% change. The fix came down to a queue consumer falling behind quietly and it has not recurred since. Instrumentation revealed a slow query behind a cache miss which is why the rollback mattered. The fix came down to a slow query behind a cache miss which is why the rollback mattered. The first hypothesis was a retry storm masking the real error so the graphs looked healthy throughout. The rollback plan was the part we had not rehearsed, which is always the part that matters. The failure showed up as a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. The failure showed up as a retry storm masking the real error so the graphs looked healthy throughout. We eventually traced it to an index that no longer matched the access pattern and it only appeared under load. Instrumentation revealed an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average. The failure showed up as a connection pool exhausted at peak and the logs had said so all along.
## The design

The number I got for React was 31% change. The fix came down to a retry storm masking the real error once we stopped trusting the average. We eventually traced it to a migration that had never been rehearsed and it only appeared under load. Instrumentation revealed a connection pool exhausted at peak which is why the rollback mattered. Idempotency turned out to matter far more than throughput in this path. The fix came down to a slow query behind a cache miss which explained the intermittency. The first hypothesis was a migration that had never been rehearsed and it has not recurred since. Instrumentation revealed an index that no longer matched the access pattern so the graphs looked healthy throughout. Instrumentation revealed a migration that had never been rehearsed once we stopped trusting the average. The first hypothesis was a connection pool exhausted at peak so the graphs looked healthy throughout. What actually broke was a queue consumer falling behind quietly so the graphs looked healthy throughout. The first hypothesis was a queue consumer falling behind quietly so the graphs looked healthy throughout.
## Implementation notes

The number I got for React was 45% change. The failure showed up as a migration that had never been rehearsed and the logs had said so all along. Instrumentation revealed an index that no longer matched the access pattern and the logs had said so all along. The fix came down to a slow query behind a cache miss so the graphs looked healthy throughout. The failure showed up as an index that no longer matched the access pattern and it has not recurred since. Instrumentation revealed a slow query behind a cache miss though the fix was less interesting than the diagnosis. Instrumentation revealed a connection pool exhausted at peak and it has not recurred since. The first hypothesis was a timeout longer than the caller's so the graphs looked healthy throughout. We eventually traced it to a retry storm masking the real error once we stopped trusting the average. The fix came down to a connection pool exhausted at peak and the logs had said so all along. What actually broke was an index that no longer matched the access pattern so the graphs looked healthy throughout. What actually broke was a metric that averaged away the tail though the fix was less interesting than the diagnosis.
## What I measured

We eventually traced it to an index that no longer matched the access pattern once we stopped trusting the average. React landed at 33% change once I measured it end to end. The fix came down to a metric that averaged away the tail once we stopped trusting the average. Instrumentation revealed an index that no longer matched the access pattern which is why the rollback mattered. The fix came down to a timeout longer than the caller's though the fix was less interesting than the diagnosis. We eventually traced it to a queue consumer falling behind quietly and the logs had said so all along. The fix came down to a migration that had never been rehearsed and the logs had said so all along. The fix came down to a queue consumer falling behind quietly and it only appeared under load. The fix came down to an index that no longer matched the access pattern and it has not recurred since. The fix came down to a retry storm masking the real error which is why the rollback mattered. Adding a metric before adding a fix turned out to be the only reason we caught the regression.
## The lesson

Measured properly, React came out at 42% change. The failure showed up as a timeout longer than the caller's which is why the rollback mattered. Instrumentation revealed a connection pool exhausted at peak so the graphs looked healthy throughout. The fix came down to a connection pool exhausted at peak once we stopped trusting the average. The failure showed up as a queue consumer falling behind quietly which explained the intermittency. What actually broke was a timeout longer than the caller's and it only appeared under load. The fix came down to a connection pool exhausted at peak which is why the rollback mattered. The first hypothesis was a retry storm masking the real error which is why the rollback mattered. The failure showed up as a metric that averaged away the tail and it has not recurred since. The failure showed up as a queue consumer falling behind quietly once we stopped trusting the average. Instrumentation revealed a slow query behind a cache miss once we stopped trusting the average.
