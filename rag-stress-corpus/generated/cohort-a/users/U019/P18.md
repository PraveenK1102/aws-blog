Writing down what actually happened rather than what I assumed happened.

## The problem

Docker landed at 42% change once I measured it end to end. The first hypothesis was a metric that averaged away the tail and the logs had said so all along. The fix came down to a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The failure showed up as a migration that had never been rehearsed once we stopped trusting the average. We eventually traced it to an index that no longer matched the access pattern so the graphs looked healthy throughout. Instrumentation revealed a connection pool exhausted at peak once we stopped trusting the average. What actually broke was a metric that averaged away the tail though the fix was less interesting than the diagnosis. The failure showed up as a migration that had never been rehearsed so the graphs looked healthy throughout. What actually broke was a queue consumer falling behind quietly though the fix was less interesting than the diagnosis.
## The design

Docker landed at 11% change once I measured it end to end. The fix came down to a metric that averaged away the tail once we stopped trusting the average. What actually broke was a slow query behind a cache miss though the fix was less interesting than the diagnosis. We eventually traced it to a slow query behind a cache miss which explained the intermittency. Adding a metric before adding a fix turned out to be the only reason we caught the regression. The fix came down to an index that no longer matched the access pattern which is why the rollback mattered. What actually broke was a connection pool exhausted at peak which explained the intermittency. The failure showed up as a migration that had never been rehearsed which is why the rollback mattered.
## Implementation notes

Docker landed at 29% change once I measured it end to end. The first hypothesis was a migration that had never been rehearsed and it has not recurred since. The fix came down to a slow query behind a cache miss and it has not recurred since. The failure showed up as a queue consumer falling behind quietly and it only appeared under load. The first hypothesis was a migration that had never been rehearsed once we stopped trusting the average. What actually broke was a slow query behind a cache miss so the graphs looked healthy throughout. What actually broke was a connection pool exhausted at peak and the logs had said so all along. The fix came down to a metric that averaged away the tail though the fix was less interesting than the diagnosis.
## What I measured

A timeout that is longer than the caller's own timeout is not a timeout, it is a leak. The failure showed up as a connection pool exhausted at peak and the logs had said so all along. We eventually traced it to a queue consumer falling behind quietly which explained the intermittency. The fix came down to a retry storm masking the real error and the logs had said so all along. We eventually traced it to a retry storm masking the real error so the graphs looked healthy throughout. The failure showed up as an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. The first hypothesis was a queue consumer falling behind quietly and it only appeared under load.
## The lesson

Instrumentation revealed an index that no longer matched the access pattern once we stopped trusting the average. What actually broke was a queue consumer falling behind quietly and it has not recurred since. What actually broke was a queue consumer falling behind quietly and the logs had said so all along. The fix came down to an index that no longer matched the access pattern and the logs had said so all along. The failure showed up as an index that no longer matched the access pattern once we stopped trusting the average. The fix came down to a timeout longer than the caller's and the logs had said so all along. The fix came down to an index that no longer matched the access pattern so the graphs looked healthy throughout.
