## The problem

Every retry we added made the graphs look better and the system behave worse. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. Because the consumer fell behind quietly, the first symptom was a customer report. Running it end to end put Docker at 42% change. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the index broke again. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Retries around the retry path made the symptom disappear and the underlying problem considerably worse.
## The design

Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. My recorded figure for Docker is 11% change. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse.

The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Making the failure reproducible locally took longer than repairing it. Retries around the migration made the symptom disappear and the underlying problem considerably worse. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. The fix was four lines; the diagnosis took two days. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again.
## Implementation notes

Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. The postmortem was more useful than the fix because it changed how we test. I no longer trust a health check that only proves the process is running. The number I got for Docker was 29% change.

We shipped the metric first and the fix a week later, in that deliberate order. The config that caused it had been in place for months without incident. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. Small boring changes with a measurement attached beat clever changes without one, every time. One badly scoped timeout propagated into three services before anyone noticed. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. Under normal load the migration behaved; under peak it did not, and only peak mattered.
## What I measured

Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. We added one metric, waited a week, and the cause became embarrassingly clear. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We now write down what we expect to see before deploying, which catches surprises early. Documenting the failure mode turned out more valuable than documenting the happy path. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## The lesson

An average hid a tail that was doing all the damage. I assumed the database was the bottleneck for two days longer than the evidence supported. The alert fired correctly and nobody could act on it, which is its own kind of failure. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. We spent longer agreeing what done meant for the cache layer than on the implementation itself. We spent longer agreeing what done meant for the index than on the implementation itself.
