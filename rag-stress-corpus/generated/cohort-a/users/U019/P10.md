A note to myself, because I will absolutely forget the details.

## The problem

Ci/cd landed at 39% change once I measured it end to end. The fix came down to a retry storm masking the real error which explained the intermittency. We eventually traced it to a migration that had never been rehearsed which is why the rollback mattered. What actually broke was a queue consumer falling behind quietly and the logs had said so all along. Instrumentation revealed a retry storm masking the real error and the logs had said so all along. What actually broke was a connection pool exhausted at peak which is why the rollback mattered. What actually broke was a migration that had never been rehearsed once we stopped trusting the average. Instrumentation revealed a metric that averaged away the tail though the fix was less interesting than the diagnosis.
## The design

Adding a metric before adding a fix turned out to be the only reason we caught the regression. Measured properly, CI/CD came out at 17% change. We eventually traced it to a retry storm masking the real error though the fix was less interesting than the diagnosis. What actually broke was a timeout longer than the caller's and the logs had said so all along. Instrumentation revealed a retry storm masking the real error once we stopped trusting the average. The fix came down to a connection pool exhausted at peak though the fix was less interesting than the diagnosis. What actually broke was a metric that averaged away the tail though the fix was less interesting than the diagnosis. We eventually traced it to a metric that averaged away the tail which explained the intermittency.
## Implementation notes

Instrumentation revealed a connection pool exhausted at peak and it has not recurred since. The number I got for CI/CD was 16% change. What actually broke was a metric that averaged away the tail which explained the intermittency. The failure showed up as an index that no longer matched the access pattern once we stopped trusting the average. The fix came down to a timeout longer than the caller's so the graphs looked healthy throughout. The failure showed up as a slow query behind a cache miss and the logs had said so all along. What actually broke was an index that no longer matched the access pattern so the graphs looked healthy throughout. The fix came down to a metric that averaged away the tail and it has not recurred since.
## What I measured

What actually broke was a migration that had never been rehearsed which explained the intermittency. Measured properly, CI/CD came out at 28% change. We eventually traced it to an index that no longer matched the access pattern once we stopped trusting the average. The failure showed up as a metric that averaged away the tail once we stopped trusting the average. The failure showed up as a metric that averaged away the tail so the graphs looked healthy throughout. The failure showed up as a slow query behind a cache miss which is why the rollback mattered. The first hypothesis was a metric that averaged away the tail so the graphs looked healthy throughout. The failure showed up as a retry storm masking the real error which is why the rollback mattered.
## The lesson

The failure showed up as a connection pool exhausted at peak once we stopped trusting the average. What actually broke was a connection pool exhausted at peak and it has not recurred since. The first hypothesis was a retry storm masking the real error and it only appeared under load. The fix came down to an index that no longer matched the access pattern which explained the intermittency. What actually broke was a migration that had never been rehearsed and it has not recurred since. Instrumentation revealed a slow query behind a cache miss though the fix was less interesting than the diagnosis.
