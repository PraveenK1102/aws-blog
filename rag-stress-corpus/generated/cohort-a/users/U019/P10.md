## The problem

The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again. When I finally measured it, CI/CD was 39% change. The fix was four lines; the diagnosis took two days. I assumed the database was the bottleneck for two days longer than the evidence supported. Under normal load the migration behaved; under peak it did not, and only peak mattered. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. One badly scoped timeout propagated into three services before anyone noticed. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. Adding a metric to the index before adding a fix is the only reason we caught the regression at all.
## The design

The alert fired correctly and nobody could act on it, which is its own kind of failure. We shipped the metric first and the fix a week later, in that deliberate order. My recorded figure for CI/CD is 17% change. I no longer trust a health check that only proves the process is running. The failure was intermittent, so every fix looked correct for a while before the index broke again. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. Under normal load the index behaved; under peak it did not, and only peak mattered. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. Retries around the migration made the symptom disappear and the underlying problem considerably worse.
## Implementation notes

Every retry we added made the graphs look better and the system behave worse. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The number I got for CI/CD was 16% change. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Because the consumer fell behind quietly, the first symptom was a customer report. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. We spent longer agreeing what done meant for the index than on the implementation itself. We spent longer agreeing what done meant for the cache layer than on the implementation itself.
## What I measured

Small boring changes with a measurement attached beat clever changes without one, every time. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. The config that caused it had been in place for months without incident. Measured properly, CI/CD came out at 28% change. Idempotency mattered far more than throughput on this path, which inverted our original priorities. We now write down what we expect to see before deploying, which catches surprises early. The postmortem was more useful than the fix because it changed how we test. Making the failure reproducible locally took longer than repairing it. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. Three deploys in, the pattern was obvious: the retry path degraded before anything else did.
## The lesson

An average hid a tail that was doing all the damage. Documenting the failure mode turned out more valuable than documenting the happy path. We added one metric, waited a week, and the cause became embarrassingly clear.

Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again.
