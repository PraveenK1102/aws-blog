## The problem

The number I got for transactions was 19% change. I assumed the database was the bottleneck for two days longer than the evidence supported. Because the consumer fell behind quietly, the first symptom was a customer report. One badly scoped timeout propagated into three services before anyone noticed. I no longer trust a health check that only proves the process is running. The fix was four lines; the diagnosis took two days. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. An average hid a tail that was doing all the damage. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. We now write down what we expect to see before deploying, which catches surprises early. Small boring changes with a measurement attached beat clever changes without one, every time. One badly scoped timeout propagated into three services before anyone noticed. Because the consumer fell behind quietly, the first symptom was a customer report. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse.
## The design

We shipped the metric first and the fix a week later, in that deliberate order. On the instrumented run, transactions showed 20% change. Documenting the failure mode turned out more valuable than documenting the happy path.

Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. We spent longer agreeing what done meant for the index than on the implementation itself. Retries around the index made the symptom disappear and the underlying problem considerably worse. The postmortem was more useful than the fix because it changed how we test. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the migration broke again. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. The config that caused it had been in place for months without incident. We shipped the metric first and the fix a week later, in that deliberate order. I assumed the database was the bottleneck for two days longer than the evidence supported. Documenting the failure mode turned out more valuable than documenting the happy path. The postmortem was more useful than the fix because it changed how we test. Making the failure reproducible locally took longer than repairing it.
## Implementation notes

Running it end to end put transactions at 7% change. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. We now write down what we expect to see before deploying, which catches surprises early. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Making the failure reproducible locally took longer than repairing it. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. Under normal load the index behaved; under peak it did not, and only peak mattered. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Idempotency mattered far more than throughput on this path, which inverted our original priorities. An average hid a tail that was doing all the damage. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. I no longer trust a health check that only proves the process is running. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Small boring changes with a measurement attached beat clever changes without one, every time.
## What I measured

The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. When I finally measured it, transactions was 31% change. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The config that caused it had been in place for months without incident. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. We spent longer agreeing what done meant for the cache layer than on the implementation itself. The failure was intermittent, so every fix looked correct for a while before the index broke again. Making the failure reproducible locally took longer than repairing it. The fix was four lines; the diagnosis took two days. The alert fired correctly and nobody could act on it, which is its own kind of failure. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Retries around the index made the symptom disappear and the underlying problem considerably worse. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.
## The lesson

Small boring changes with a measurement attached beat clever changes without one, every time. We added one metric, waited a week, and the cause became embarrassingly clear. The alert fired correctly and nobody could act on it, which is its own kind of failure. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Every retry we added made the graphs look better and the system behave worse. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. We added one metric, waited a week, and the cause became embarrassingly clear. Every retry we added made the graphs look better and the system behave worse. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. The postmortem was more useful than the fix because it changed how we test.
