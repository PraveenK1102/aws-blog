## The problem

Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Measured properly, Redis came out at 36% change. The postmortem was more useful than the fix because it changed how we test. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all.

We spent longer agreeing what done meant for the connection pool than on the implementation itself. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. We shipped the metric first and the fix a week later, in that deliberate order. I no longer trust a health check that only proves the process is running. The alert fired correctly and nobody could act on it, which is its own kind of failure. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again.
## The design

We added one metric, waited a week, and the cause became embarrassingly clear. Redis landed at 32% change once I measured it end to end. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. The config that caused it had been in place for months without incident. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Making the failure reproducible locally took longer than repairing it.
## Implementation notes

An average hid a tail that was doing all the damage. One badly scoped timeout propagated into three services before anyone noticed. My recorded figure for Redis is 8% change. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. We now write down what we expect to see before deploying, which catches surprises early. Every retry we added made the graphs look better and the system behave worse. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse.
## What I measured

Running it end to end put Redis at 24% change. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. Documenting the failure mode turned out more valuable than documenting the happy path. The fix was four lines; the diagnosis took two days. Because the consumer fell behind quietly, the first symptom was a customer report. Under normal load the connection pool behaved; under peak it did not, and only peak mattered.
## The lesson

When I finally measured it, Redis was 40% change. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. We spent longer agreeing what done meant for the migration than on the implementation itself. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. I assumed the database was the bottleneck for two days longer than the evidence supported. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident.
