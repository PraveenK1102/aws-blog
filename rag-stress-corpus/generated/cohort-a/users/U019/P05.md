## The problem

The number I got for Docker was 36% change. Because the consumer fell behind quietly, the first symptom was a customer report. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. We spent longer agreeing what done meant for the migration than on the implementation itself. The fix was four lines; the diagnosis took two days. We spent longer agreeing what done meant for the cache layer than on the implementation itself. An average hid a tail that was doing all the damage. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. I no longer trust a health check that only proves the process is running. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Documenting the failure mode turned out more valuable than documenting the happy path. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Idempotency mattered far more than throughput on this path, which inverted our original priorities.
## The design

The alert fired correctly and nobody could act on it, which is its own kind of failure. Making the failure reproducible locally took longer than repairing it. Every retry we added made the graphs look better and the system behave worse.

When I finally measured it, Docker was 22% change. We added one metric, waited a week, and the cause became embarrassingly clear. The config that caused it had been in place for months without incident. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. We shipped the metric first and the fix a week later, in that deliberate order. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. We added one metric, waited a week, and the cause became embarrassingly clear. An average hid a tail that was doing all the damage. The alert fired correctly and nobody could act on it, which is its own kind of failure. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The postmortem was more useful than the fix because it changed how we test. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.
## Implementation notes

Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. Measured properly, Docker came out at 19% change. The obvious cause was not the real cause, and the logs had said so if you read them carefully.

Idempotency mattered far more than throughput on this path, which inverted our original priorities. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Documenting the failure mode turned out more valuable than documenting the happy path. Small boring changes with a measurement attached beat clever changes without one, every time. The postmortem was more useful than the fix because it changed how we test. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. Small boring changes with a measurement attached beat clever changes without one, every time. Because the consumer fell behind quietly, the first symptom was a customer report. The config that caused it had been in place for months without incident. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Retries around the migration made the symptom disappear and the underlying problem considerably worse. The alert fired correctly and nobody could act on it, which is its own kind of failure.
## What I measured

The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Running it end to end put Docker at 24% change. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. One badly scoped timeout propagated into three services before anyone noticed. We now write down what we expect to see before deploying, which catches surprises early. I assumed the database was the bottleneck for two days longer than the evidence supported. The failure was intermittent, so every fix looked correct for a while before the index broke again. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. We now write down what we expect to see before deploying, which catches surprises early. The fix was four lines; the diagnosis took two days. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.
## The lesson

My recorded figure for Docker is 6% change. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. Retries around the migration made the symptom disappear and the underlying problem considerably worse. I no longer trust a health check that only proves the process is running. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. Retries around the index made the symptom disappear and the underlying problem considerably worse. Three deploys in, the pattern was obvious: the index degraded before anything else did. One badly scoped timeout propagated into three services before anyone noticed. Making the failure reproducible locally took longer than repairing it. I assumed the database was the bottleneck for two days longer than the evidence supported. We shipped the metric first and the fix a week later, in that deliberate order. Every retry we added made the graphs look better and the system behave worse. Making the failure reproducible locally took longer than repairing it.
