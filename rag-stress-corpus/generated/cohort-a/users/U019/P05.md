This came out of a production incident, so the details matter.

## The problem

The fix came down to a timeout longer than the caller's which is why the rollback mattered. We eventually traced it to a metric that averaged away the tail and the logs had said so all along. Docker landed at 36% change once I measured it end to end. The fix came down to a migration that had never been rehearsed once we stopped trusting the average. The failure showed up as a retry storm masking the real error and the logs had said so all along. The failure showed up as a slow query behind a cache miss once we stopped trusting the average. We eventually traced it to a queue consumer falling behind quietly and the logs had said so all along. The failure showed up as a migration that had never been rehearsed and it only appeared under load. What actually broke was a queue consumer falling behind quietly and it only appeared under load. The failure showed up as a queue consumer falling behind quietly once we stopped trusting the average. The first hypothesis was a connection pool exhausted at peak which is why the rollback mattered. We eventually traced it to a timeout longer than the caller's which explained the intermittency. The fix came down to a connection pool exhausted at peak and the logs had said so all along. What actually broke was a metric that averaged away the tail once we stopped trusting the average.
## The design

The failure showed up as a connection pool exhausted at peak so the graphs looked healthy throughout. Measured properly, Docker came out at 22% change. The failure showed up as an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. Instrumentation revealed a metric that averaged away the tail and the logs had said so all along. The failure showed up as a connection pool exhausted at peak and the logs had said so all along. The failure showed up as a retry storm masking the real error which is why the rollback mattered. The failure showed up as a queue consumer falling behind quietly and the logs had said so all along. A timeout that is longer than the caller's own timeout is not a timeout, it is a leak. Instrumentation revealed a slow query behind a cache miss which explained the intermittency. We eventually traced it to a timeout longer than the caller's and the logs had said so all along. We eventually traced it to an index that no longer matched the access pattern and it has not recurred since. Instrumentation revealed a queue consumer falling behind quietly and the logs had said so all along. What actually broke was a queue consumer falling behind quietly which explained the intermittency. The fix came down to a migration that had never been rehearsed and it only appeared under load.
## Implementation notes

The number I got for Docker was 19% change. The failure showed up as a slow query behind a cache miss which is why the rollback mattered. The failure showed up as a retry storm masking the real error and it has not recurred since. What actually broke was a timeout longer than the caller's and the logs had said so all along. The failure showed up as a timeout longer than the caller's which is why the rollback mattered. The fix came down to an index that no longer matched the access pattern and it has not recurred since. The fix came down to a metric that averaged away the tail once we stopped trusting the average. We eventually traced it to a retry storm masking the real error which is why the rollback mattered. Instrumentation revealed a timeout longer than the caller's and it has not recurred since. The failure was intermittent, which meant every fix looked like it worked for a while. The failure showed up as a retry storm masking the real error though the fix was less interesting than the diagnosis. The fix came down to a connection pool exhausted at peak and it has not recurred since. The fix came down to a queue consumer falling behind quietly once we stopped trusting the average.
## What I measured

Measured properly, Docker came out at 24% change. The first hypothesis was a slow query behind a cache miss and it only appeared under load. We eventually traced it to a metric that averaged away the tail and it only appeared under load. What actually broke was a retry storm masking the real error once we stopped trusting the average. The fix came down to a timeout longer than the caller's once we stopped trusting the average. Instrumentation revealed a metric that averaged away the tail and it has not recurred since. The fix came down to a metric that averaged away the tail which explained the intermittency. What actually broke was a timeout longer than the caller's so the graphs looked healthy throughout. What actually broke was an index that no longer matched the access pattern which explained the intermittency. Instrumentation revealed a retry storm masking the real error once we stopped trusting the average. The failure showed up as a timeout longer than the caller's and it has not recurred since. The fix came down to a slow query behind a cache miss once we stopped trusting the average. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average.
## The lesson

The number I got for Docker was 6% change. We eventually traced it to a connection pool exhausted at peak once we stopped trusting the average. Instrumentation revealed a connection pool exhausted at peak and it only appeared under load. The failure showed up as a migration that had never been rehearsed which is why the rollback mattered. The first hypothesis was a timeout longer than the caller's though the fix was less interesting than the diagnosis. We eventually traced it to a queue consumer falling behind quietly which explained the intermittency. The fix came down to an index that no longer matched the access pattern and the logs had said so all along. We eventually traced it to a migration that had never been rehearsed though the fix was less interesting than the diagnosis. The first hypothesis was a migration that had never been rehearsed and the logs had said so all along. The fix came down to a metric that averaged away the tail which is why the rollback mattered. We eventually traced it to a migration that had never been rehearsed once we stopped trusting the average. The first hypothesis was an index that no longer matched the access pattern so the graphs looked healthy throughout. What actually broke was a retry storm masking the real error which explained the intermittency.
