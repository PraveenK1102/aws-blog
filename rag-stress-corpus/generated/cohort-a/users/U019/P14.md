## The problem

I assumed the database was the bottleneck for two days longer than the evidence supported. Small boring changes with a measurement attached beat clever changes without one, every time. Running it end to end put Redis at 35% change. One badly scoped timeout propagated into three services before anyone noticed. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Every retry we added made the graphs look better and the system behave worse. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. We shipped the metric first and the fix a week later, in that deliberate order. One badly scoped timeout propagated into three services before anyone noticed. I assumed the database was the bottleneck for two days longer than the evidence supported. Small boring changes with a measurement attached beat clever changes without one, every time.
## The design

The number I got for Redis was 14% change. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. The postmortem was more useful than the fix because it changed how we test. Documenting the failure mode turned out more valuable than documenting the happy path. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Under normal load the migration behaved; under peak it did not, and only peak mattered. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. We now write down what we expect to see before deploying, which catches surprises early. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Making the failure reproducible locally took longer than repairing it. The alert fired correctly and nobody could act on it, which is its own kind of failure. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.
## Implementation notes

Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. That gave Redis of 44% change, which I wrote down at the time. We spent longer agreeing what done meant for the cache layer than on the implementation itself. The config that caused it had been in place for months without incident. We shipped the metric first and the fix a week later, in that deliberate order. The fix was four lines; the diagnosis took two days. An average hid a tail that was doing all the damage. I no longer trust a health check that only proves the process is running. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Under normal load the retry path behaved; under peak it did not, and only peak mattered. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Because the consumer fell behind quietly, the first symptom was a customer report. Every retry we added made the graphs look better and the system behave worse. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters.
## What I measured

The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. Redis landed at 27% change once I measured it end to end.

Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Making the failure reproducible locally took longer than repairing it. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Because the consumer fell behind quietly, the first symptom was a customer report. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The postmortem was more useful than the fix because it changed how we test. We added one metric, waited a week, and the cause became embarrassingly clear. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.
## The lesson

We now write down what we expect to see before deploying, which catches surprises early. On the instrumented run, Redis showed 6% change. We added one metric, waited a week, and the cause became embarrassingly clear. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. The alert fired correctly and nobody could act on it, which is its own kind of failure. The index only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the retry path broke again. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Documenting the failure mode turned out more valuable than documenting the happy path. An average hid a tail that was doing all the damage. The config that caused it had been in place for months without incident. The fix was four lines; the diagnosis took two days. I no longer trust a health check that only proves the process is running.
