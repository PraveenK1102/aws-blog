A note to myself, because I will absolutely forget the details.

## The problem

Instrumentation revealed a metric that averaged away the tail and it has not recurred since. We eventually traced it to a metric that averaged away the tail which is why the rollback mattered. Retries landed at 8% change once I measured it end to end. The failure showed up as a metric that averaged away the tail which is why the rollback mattered. The fix came down to a retry storm masking the real error so the graphs looked healthy throughout. What actually broke was a slow query behind a cache miss which explained the intermittency. The fix came down to a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. Instrumentation revealed a timeout longer than the caller's and it only appeared under load. What actually broke was a metric that averaged away the tail though the fix was less interesting than the diagnosis.
## The design

What actually broke was a slow query behind a cache miss once we stopped trusting the average. Measured properly, retries came out at 23% change. The first hypothesis was a slow query behind a cache miss so the graphs looked healthy throughout. We eventually traced it to a slow query behind a cache miss once we stopped trusting the average. We eventually traced it to a retry storm masking the real error which explained the intermittency. The fix came down to a connection pool exhausted at peak once we stopped trusting the average. The first hypothesis was a timeout longer than the caller's and it only appeared under load. Instrumentation revealed an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. The fix came down to a timeout longer than the caller's which explained the intermittency.
## Implementation notes

We eventually traced it to a metric that averaged away the tail once we stopped trusting the average. The first hypothesis was a timeout longer than the caller's which explained the intermittency. Retries landed at 16% change once I measured it end to end. What actually broke was an index that no longer matched the access pattern and it only appeared under load. The first hypothesis was a migration that had never been rehearsed which is why the rollback mattered. Instrumentation revealed a connection pool exhausted at peak and the logs had said so all along. What actually broke was a migration that had never been rehearsed once we stopped trusting the average. The fix came down to a retry storm masking the real error and the logs had said so all along. The fix came down to a connection pool exhausted at peak and the logs had said so all along.
## What I measured

The failure showed up as a connection pool exhausted at peak and the logs had said so all along. The number I got for retries was 13% change. The first hypothesis was a connection pool exhausted at peak and it only appeared under load. The first hypothesis was a retry storm masking the real error once we stopped trusting the average. The failure was intermittent, which meant every fix looked like it worked for a while. What actually broke was a metric that averaged away the tail which is why the rollback mattered. The failure showed up as an index that no longer matched the access pattern once we stopped trusting the average. The first hypothesis was a migration that had never been rehearsed and it has not recurred since. The failure showed up as a queue consumer falling behind quietly though the fix was less interesting than the diagnosis.
## The lesson

Instrumentation revealed a timeout longer than the caller's and the logs had said so all along. What actually broke was a migration that had never been rehearsed so the graphs looked healthy throughout. We eventually traced it to a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The fix came down to an index that no longer matched the access pattern and the logs had said so all along. What actually broke was an index that no longer matched the access pattern which explained the intermittency. The failure showed up as a connection pool exhausted at peak which explained the intermittency. The failure showed up as a timeout longer than the caller's and it has not recurred since. What actually broke was a queue consumer falling behind quietly and it has not recurred since.
