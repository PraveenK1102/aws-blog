## The problem

Retries around the migration made the symptom disappear and the underlying problem considerably worse. The number I got for retries was 8% change. Making the failure reproducible locally took longer than repairing it. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. An average hid a tail that was doing all the damage. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. We spent longer agreeing what done meant for the cache layer than on the implementation itself. I assumed the database was the bottleneck for two days longer than the evidence supported. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. The failure was intermittent, so every fix looked correct for a while before the index broke again. I no longer trust a health check that only proves the process is running.
## The design

The alert fired correctly and nobody could act on it, which is its own kind of failure. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. We now write down what we expect to see before deploying, which catches surprises early. That gave retries of 23% change, which I wrote down at the time. We spent longer agreeing what done meant for the retry path than on the implementation itself. We shipped the metric first and the fix a week later, in that deliberate order. The config that caused it had been in place for months without incident. Three deploys in, the pattern was obvious: the index degraded before anything else did. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. The postmortem was more useful than the fix because it changed how we test.
## Implementation notes

Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. My recorded figure for retries is 16% change. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. The postmortem was more useful than the fix because it changed how we test. Every retry we added made the graphs look better and the system behave worse. Documenting the failure mode turned out more valuable than documenting the happy path. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident.
## What I measured

One badly scoped timeout propagated into three services before anyone noticed. Measured properly, retries came out at 13% change. Small boring changes with a measurement attached beat clever changes without one, every time. I no longer trust a health check that only proves the process is running. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Because the consumer fell behind quietly, the first symptom was a customer report. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse.
## The lesson

The fix was four lines; the diagnosis took two days. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. We added one metric, waited a week, and the cause became embarrassingly clear. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. We added one metric, waited a week, and the cause became embarrassingly clear.
