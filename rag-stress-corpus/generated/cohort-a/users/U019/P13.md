This came out of a production incident, so the details matter.

## The problem

The first hypothesis was a queue consumer falling behind quietly and the logs had said so all along. We eventually traced it to an index that no longer matched the access pattern which explained the intermittency. Caching landed at 6% change once I measured it end to end. We eventually traced it to a retry storm masking the real error so the graphs looked healthy throughout. The failure showed up as a connection pool exhausted at peak which explained the intermittency. The first hypothesis was a metric that averaged away the tail though the fix was less interesting than the diagnosis. The fix came down to a migration that had never been rehearsed though the fix was less interesting than the diagnosis. What actually broke was an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. We eventually traced it to a connection pool exhausted at peak once we stopped trusting the average. We eventually traced it to an index that no longer matched the access pattern and it only appeared under load. The failure showed up as a metric that averaged away the tail though the fix was less interesting than the diagnosis. We eventually traced it to a connection pool exhausted at peak and it has not recurred since. What actually broke was a timeout longer than the caller's and it has not recurred since.
## The design

Caching landed at 37% change once I measured it end to end. What actually broke was a timeout longer than the caller's though the fix was less interesting than the diagnosis. What actually broke was a slow query behind a cache miss and the logs had said so all along. What actually broke was a connection pool exhausted at peak and it only appeared under load. The fix came down to a slow query behind a cache miss which is why the rollback mattered. Instrumentation revealed a queue consumer falling behind quietly and the logs had said so all along. The fix came down to a queue consumer falling behind quietly and the logs had said so all along. The first hypothesis was a connection pool exhausted at peak which explained the intermittency. The failure showed up as a queue consumer falling behind quietly and it only appeared under load. The first hypothesis was a metric that averaged away the tail once we stopped trusting the average. The fix came down to a slow query behind a cache miss and the logs had said so all along. The fix came down to a metric that averaged away the tail which explained the intermittency. We eventually traced it to a migration that had never been rehearsed though the fix was less interesting than the diagnosis.
## Implementation notes

We eventually traced it to a queue consumer falling behind quietly and it only appeared under load. The number I got for caching was 9% change. We eventually traced it to a migration that had never been rehearsed and it has not recurred since. What actually broke was a queue consumer falling behind quietly so the graphs looked healthy throughout. A timeout that is longer than the caller's own timeout is not a timeout, it is a leak. What actually broke was a queue consumer falling behind quietly and it only appeared under load. Instrumentation revealed a timeout longer than the caller's which explained the intermittency. We eventually traced it to a timeout longer than the caller's and it has not recurred since. The first hypothesis was a migration that had never been rehearsed and it has not recurred since. The failure showed up as a metric that averaged away the tail once we stopped trusting the average. The first hypothesis was a migration that had never been rehearsed though the fix was less interesting than the diagnosis. The fix came down to a metric that averaged away the tail which is why the rollback mattered.
## What I measured

We eventually traced it to a slow query behind a cache miss and it has not recurred since. Caching landed at 30% change once I measured it end to end. The first hypothesis was an index that no longer matched the access pattern so the graphs looked healthy throughout. The first hypothesis was a connection pool exhausted at peak which is why the rollback mattered. The failure showed up as a retry storm masking the real error which explained the intermittency. The fix came down to a slow query behind a cache miss which explained the intermittency. Instrumentation revealed a timeout longer than the caller's and it has not recurred since. We eventually traced it to a metric that averaged away the tail which is why the rollback mattered. Instrumentation revealed a slow query behind a cache miss so the graphs looked healthy throughout. The failure showed up as a slow query behind a cache miss which is why the rollback mattered. The fix came down to a migration that had never been rehearsed and it has not recurred since. What actually broke was a slow query behind a cache miss which is why the rollback mattered.
## The lesson

The fix came down to a connection pool exhausted at peak so the graphs looked healthy throughout. We eventually traced it to a timeout longer than the caller's once we stopped trusting the average. Instrumentation revealed an index that no longer matched the access pattern and it only appeared under load. The first hypothesis was an index that no longer matched the access pattern and it has not recurred since. The first hypothesis was a retry storm masking the real error so the graphs looked healthy throughout. Instrumentation revealed a metric that averaged away the tail and it only appeared under load. The first hypothesis was a timeout longer than the caller's and the logs had said so all along. The first hypothesis was a retry storm masking the real error and it only appeared under load. The obvious cause was not the real cause, and the logs said so if you read them carefully. The failure showed up as a retry storm masking the real error which is why the rollback mattered. Adding a metric before adding a fix turned out to be the only reason we caught the regression.
