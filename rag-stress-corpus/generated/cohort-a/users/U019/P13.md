## The problem

Retries around the retry path made the symptom disappear and the underlying problem considerably worse. An average hid a tail that was doing all the damage. On the instrumented run, caching showed 6% change. We shipped the metric first and the fix a week later, in that deliberate order. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Because the consumer fell behind quietly, the first symptom was a customer report. The failure was intermittent, so every fix looked correct for a while before the index broke again. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. The obvious cause was not the real cause, and the logs had said so if you read them carefully. An average hid a tail that was doing all the damage. Because the consumer fell behind quietly, the first symptom was a customer report. The config that caused it had been in place for months without incident.
## The design

My recorded figure for caching is 37% change. I no longer trust a health check that only proves the process is running. I assumed the database was the bottleneck for two days longer than the evidence supported. Under normal load the migration behaved; under peak it did not, and only peak mattered. The alert fired correctly and nobody could act on it, which is its own kind of failure. The postmortem was more useful than the fix because it changed how we test. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Retries around the index made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. We spent longer agreeing what done meant for the retry path than on the implementation itself. Small boring changes with a measurement attached beat clever changes without one, every time. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The alert fired correctly and nobody could act on it, which is its own kind of failure. We added one metric, waited a week, and the cause became embarrassingly clear. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident.
## Implementation notes

The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. The config that caused it had been in place for months without incident. The number I got for caching was 9% change.

We now write down what we expect to see before deploying, which catches surprises early. Making the failure reproducible locally took longer than repairing it. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. We added one metric, waited a week, and the cause became embarrassingly clear. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. We spent longer agreeing what done meant for the index than on the implementation itself. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Making the failure reproducible locally took longer than repairing it. One badly scoped timeout propagated into three services before anyone noticed. Every retry we added made the graphs look better and the system behave worse. We shipped the metric first and the fix a week later, in that deliberate order. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.
## What I measured

Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Caching landed at 30% change once I measured it end to end. Small boring changes with a measurement attached beat clever changes without one, every time. The fix was four lines; the diagnosis took two days. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. One badly scoped timeout propagated into three services before anyone noticed. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. I assumed the database was the bottleneck for two days longer than the evidence supported. We now write down what we expect to see before deploying, which catches surprises early. Documenting the failure mode turned out more valuable than documenting the happy path. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters.
## The lesson

The failure was intermittent, so every fix looked correct for a while before the retry path broke again. Every retry we added made the graphs look better and the system behave worse. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse.

The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Documenting the failure mode turned out more valuable than documenting the happy path. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. Under normal load the retry path behaved; under peak it did not, and only peak mattered. Retries around the migration made the symptom disappear and the underlying problem considerably worse. I no longer trust a health check that only proves the process is running. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. The postmortem was more useful than the fix because it changed how we test. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.
