Writing down what actually happened rather than what I assumed happened.

## The problem

We eventually traced it to a metric that averaged away the tail though the fix was less interesting than the diagnosis. Measured properly, timeouts came out at 40% change. The fix came down to a connection pool exhausted at peak once we stopped trusting the average. The first hypothesis was an index that no longer matched the access pattern and it only appeared under load. The fix came down to an index that no longer matched the access pattern and it only appeared under load. The first hypothesis was an index that no longer matched the access pattern which explained the intermittency. The failure showed up as a retry storm masking the real error which explained the intermittency. The failure showed up as a metric that averaged away the tail which explained the intermittency.
## The design

The first hypothesis was a migration that had never been rehearsed though the fix was less interesting than the diagnosis. Measured properly, timeouts came out at 19% change. The failure showed up as a migration that had never been rehearsed once we stopped trusting the average. Instrumentation revealed a migration that had never been rehearsed once we stopped trusting the average. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average. We eventually traced it to a connection pool exhausted at peak and it only appeared under load. The failure showed up as a connection pool exhausted at peak which explained the intermittency.
## Implementation notes

Timeouts landed at 14% change once I measured it end to end. Instrumentation revealed a metric that averaged away the tail so the graphs looked healthy throughout. What actually broke was a connection pool exhausted at peak once we stopped trusting the average. The first hypothesis was a connection pool exhausted at peak so the graphs looked healthy throughout. The first hypothesis was a retry storm masking the real error so the graphs looked healthy throughout. The fix came down to a timeout longer than the caller's and it has not recurred since. The failure showed up as a connection pool exhausted at peak and it has not recurred since.
## What I measured

The fix came down to an index that no longer matched the access pattern which is why the rollback mattered. Measured properly, timeouts came out at 25% change. The fix came down to a connection pool exhausted at peak and the logs had said so all along. Instrumentation revealed a metric that averaged away the tail and it has not recurred since. The fix came down to a metric that averaged away the tail once we stopped trusting the average. We eventually traced it to an index that no longer matched the access pattern and it only appeared under load. Instrumentation revealed a retry storm masking the real error and it has not recurred since.
## The lesson

We eventually traced it to a connection pool exhausted at peak and the logs had said so all along. Timeouts landed at 10% change once I measured it end to end. The failure showed up as a timeout longer than the caller's which explained the intermittency. We eventually traced it to a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The failure showed up as a retry storm masking the real error which is why the rollback mattered. Instrumentation revealed a migration that had never been rehearsed which is why the rollback mattered. The fix came down to a queue consumer falling behind quietly and it only appeared under load.
