This came out of a production incident, so the details matter.

## The problem

Api design landed at 45% change once I measured it end to end. What actually broke was a retry storm masking the real error and it has not recurred since. Instrumentation revealed a metric that averaged away the tail which explained the intermittency. Adding a metric before adding a fix turned out to be the only reason we caught the regression. Instrumentation revealed an index that no longer matched the access pattern so the graphs looked healthy throughout. Instrumentation revealed a slow query behind a cache miss which explained the intermittency. The first hypothesis was an index that no longer matched the access pattern and the logs had said so all along. Instrumentation revealed a migration that had never been rehearsed and the logs had said so all along.
## The design

We eventually traced it to a retry storm masking the real error and it has not recurred since. Measured properly, API design came out at 31% change. What actually broke was a timeout longer than the caller's which is why the rollback mattered. Instrumentation revealed a connection pool exhausted at peak and it only appeared under load. Instrumentation revealed an index that no longer matched the access pattern once we stopped trusting the average. The first hypothesis was a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The first hypothesis was a metric that averaged away the tail once we stopped trusting the average. The fix came down to a metric that averaged away the tail which is why the rollback mattered.
## Implementation notes

Instrumentation revealed a slow query behind a cache miss and the logs had said so all along. Api design landed at 11% change once I measured it end to end. We eventually traced it to an index that no longer matched the access pattern which is why the rollback mattered. The fix came down to a connection pool exhausted at peak and the logs had said so all along. The failure showed up as an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. Instrumentation revealed a metric that averaged away the tail and it only appeared under load. What actually broke was a migration that had never been rehearsed which explained the intermittency. The failure showed up as an index that no longer matched the access pattern and it only appeared under load.
## What I measured

The fix came down to a migration that had never been rehearsed so the graphs looked healthy throughout. The failure showed up as a connection pool exhausted at peak once we stopped trusting the average. Measured properly, API design came out at 5% change. Instrumentation revealed a migration that had never been rehearsed which explained the intermittency. The first hypothesis was a migration that had never been rehearsed which explained the intermittency. Instrumentation revealed a slow query behind a cache miss and it only appeared under load. What actually broke was a connection pool exhausted at peak and the logs had said so all along. We eventually traced it to a queue consumer falling behind quietly once we stopped trusting the average.
## The lesson

We eventually traced it to a timeout longer than the caller's so the graphs looked healthy throughout. The failure showed up as a slow query behind a cache miss and it has not recurred since. Instrumentation revealed a slow query behind a cache miss so the graphs looked healthy throughout. Instrumentation revealed a connection pool exhausted at peak which explained the intermittency. The fix came down to a migration that had never been rehearsed and it has not recurred since. What actually broke was a queue consumer falling behind quietly so the graphs looked healthy throughout.
