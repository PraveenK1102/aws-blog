## The problem

Small boring changes with a measurement attached beat clever changes without one, every time. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. When I finally measured it, API design was 45% change.

One badly scoped timeout propagated into three services before anyone noticed. An average hid a tail that was doing all the damage. The alert fired correctly and nobody could act on it, which is its own kind of failure. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. Making the failure reproducible locally took longer than repairing it. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the migration broke again.
## The design

The obvious cause was not the real cause, and the logs had said so if you read them carefully. Running it end to end put API design at 31% change. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters.

We shipped the metric first and the fix a week later, in that deliberate order. The fix was four lines; the diagnosis took two days. Every retry we added made the graphs look better and the system behave worse. Idempotency mattered far more than throughput on this path, which inverted our original priorities. I no longer trust a health check that only proves the process is running. The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## Implementation notes

The number I got for API design was 11% change. The config that caused it had been in place for months without incident. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. The postmortem was more useful than the fix because it changed how we test. We spent longer agreeing what done meant for the index than on the implementation itself. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did.
## What I measured

The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. Because the consumer fell behind quietly, the first symptom was a customer report.

Api design landed at 5% change once I measured it end to end. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. We added one metric, waited a week, and the cause became embarrassingly clear. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all.
## The lesson

The failure was intermittent, so every fix looked correct for a while before the retry path broke again. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. I assumed the database was the bottleneck for two days longer than the evidence supported. Documenting the failure mode turned out more valuable than documenting the happy path. We now write down what we expect to see before deploying, which catches surprises early. We spent longer agreeing what done meant for the retry path than on the implementation itself.
