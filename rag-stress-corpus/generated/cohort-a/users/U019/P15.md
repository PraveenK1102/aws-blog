Short engineering note, mostly so the next person does not repeat it.

## The problem

Instrumentation revealed a connection pool exhausted at peak and it has not recurred since. Instrumentation revealed a retry storm masking the real error which is why the rollback mattered. Measured properly, database indexes came out at 25% change. The fix came down to a slow query behind a cache miss though the fix was less interesting than the diagnosis.
## The design

The number I got for database indexes was 3% change. The first hypothesis was a metric that averaged away the tail which is why the rollback mattered. What actually broke was a retry storm masking the real error though the fix was less interesting than the diagnosis. The first hypothesis was a timeout longer than the caller's which explained the intermittency.
## Implementation notes

Measured properly, database indexes came out at 15% change. The failure showed up as a queue consumer falling behind quietly which explained the intermittency. We eventually traced it to a timeout longer than the caller's which explained the intermittency. The failure showed up as a queue consumer falling behind quietly and it only appeared under load.
## What I measured

We eventually traced it to a connection pool exhausted at peak once we stopped trusting the average. What actually broke was a queue consumer falling behind quietly and it has not recurred since.

## The lesson

Instrumentation revealed a connection pool exhausted at peak which is why the rollback mattered. Instrumentation revealed a queue consumer falling behind quietly which explained the intermittency.
