A note to myself, because I will absolutely forget the details.

## The problem

What actually broke was a retry storm masking the real error and the logs had said so all along. The failure showed up as a queue consumer falling behind quietly which explained the intermittency. Queues landed at 41% change once I measured it end to end. We eventually traced it to a retry storm masking the real error once we stopped trusting the average. We spent longer agreeing on what 'done' meant than on the implementation itself. The fix came down to a retry storm masking the real error and the logs had said so all along. The fix came down to a connection pool exhausted at peak which is why the rollback mattered.
## The design

The fix came down to a retry storm masking the real error which explained the intermittency. What actually broke was a queue consumer falling behind quietly which is why the rollback mattered. Measured properly, queues came out at 26% change. What actually broke was a metric that averaged away the tail so the graphs looked healthy throughout. Adding a metric before adding a fix turned out to be the only reason we caught the regression. What actually broke was a timeout longer than the caller's so the graphs looked healthy throughout. What actually broke was a slow query behind a cache miss which is why the rollback mattered.
## Implementation notes

Measured properly, queues came out at 33% change. We eventually traced it to a migration that had never been rehearsed which explained the intermittency. What actually broke was a retry storm masking the real error so the graphs looked healthy throughout. What actually broke was a slow query behind a cache miss once we stopped trusting the average. Instrumentation revealed a slow query behind a cache miss and it only appeared under load. We eventually traced it to a timeout longer than the caller's and it only appeared under load. Instrumentation revealed a timeout longer than the caller's once we stopped trusting the average.
## What I measured

Instrumentation revealed a connection pool exhausted at peak which explained the intermittency. We eventually traced it to a timeout longer than the caller's so the graphs looked healthy throughout. The number I got for queues was 20% change. The fix came down to a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. We eventually traced it to a retry storm masking the real error and it only appeared under load. The fix came down to a queue consumer falling behind quietly which explained the intermittency. The failure showed up as a connection pool exhausted at peak once we stopped trusting the average.
## The lesson

We eventually traced it to a migration that had never been rehearsed and the logs had said so all along. What actually broke was a timeout longer than the caller's which explained the intermittency. The number I got for queues was 11% change. Instrumentation revealed a queue consumer falling behind quietly and it has not recurred since. The fix came down to a metric that averaged away the tail so the graphs looked healthy throughout. The failure showed up as a retry storm masking the real error and the logs had said so all along. We eventually traced it to a timeout longer than the caller's once we stopped trusting the average.
