## The problem

That gave queues of 17% change, which I wrote down at the time. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. I assumed the database was the bottleneck for two days longer than the evidence supported. Making the failure reproducible locally took longer than repairing it. The failure was intermittent, so every fix looked correct for a while before the migration broke again. We spent longer agreeing what done meant for the index than on the implementation itself. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Under normal load the retry path behaved; under peak it did not, and only peak mattered. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all.
## The design

An average hid a tail that was doing all the damage. Small boring changes with a measurement attached beat clever changes without one, every time. On the instrumented run, queues showed 30% change.

One badly scoped timeout propagated into three services before anyone noticed. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Documenting the failure mode turned out more valuable than documenting the happy path. We shipped the metric first and the fix a week later, in that deliberate order. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident.
## Implementation notes

I no longer trust a health check that only proves the process is running. When I finally measured it, queues was 6% change. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Idempotency mattered far more than throughput on this path, which inverted our original priorities. We spent longer agreeing what done meant for the connection pool than on the implementation itself.
## What I measured

My recorded figure for queues is 36% change. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. The alert fired correctly and nobody could act on it, which is its own kind of failure. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Every retry we added made the graphs look better and the system behave worse. We now write down what we expect to see before deploying, which catches surprises early. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Three deploys in, the pattern was obvious: the retry path degraded before anything else did.
## The lesson

The config that caused it had been in place for months without incident. Three deploys in, the pattern was obvious: the migration degraded before anything else did. We added one metric, waited a week, and the cause became embarrassingly clear. Because the consumer fell behind quietly, the first symptom was a customer report. The postmortem was more useful than the fix because it changed how we test.
