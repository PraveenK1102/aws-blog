This came out of a production incident, so the details matter.

## The problem

The number I got for queues was 17% change. What actually broke was an index that no longer matched the access pattern which is why the rollback mattered. Instrumentation revealed a slow query behind a cache miss once we stopped trusting the average. The failure showed up as a connection pool exhausted at peak and the logs had said so all along. The fix came down to a slow query behind a cache miss and it only appeared under load. What actually broke was a migration that had never been rehearsed which explained the intermittency. The first hypothesis was a connection pool exhausted at peak and the logs had said so all along.
## The design

What actually broke was an index that no longer matched the access pattern which explained the intermittency. The failure showed up as a metric that averaged away the tail so the graphs looked healthy throughout. The number I got for queues was 30% change. The fix came down to a migration that had never been rehearsed and the logs had said so all along. The first hypothesis was a timeout longer than the caller's though the fix was less interesting than the diagnosis. We eventually traced it to an index that no longer matched the access pattern so the graphs looked healthy throughout. We eventually traced it to an index that no longer matched the access pattern which explained the intermittency.
## Implementation notes

What actually broke was a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. The number I got for queues was 6% change. We eventually traced it to a connection pool exhausted at peak so the graphs looked healthy throughout. Adding a metric before adding a fix turned out to be the only reason we caught the regression. The fix came down to a retry storm masking the real error and the logs had said so all along. Instrumentation revealed a retry storm masking the real error so the graphs looked healthy throughout. The failure showed up as a queue consumer falling behind quietly which explained the intermittency.
## What I measured

Measured properly, queues came out at 36% change. What actually broke was a timeout longer than the caller's which is why the rollback mattered. The first hypothesis was a queue consumer falling behind quietly which explained the intermittency. Instrumentation revealed a connection pool exhausted at peak and it has not recurred since. Idempotency turned out to matter far more than throughput in this path. The failure showed up as a timeout longer than the caller's which explained the intermittency. We eventually traced it to a retry storm masking the real error so the graphs looked healthy throughout.
## The lesson

The first hypothesis was a migration that had never been rehearsed so the graphs looked healthy throughout. What actually broke was a retry storm masking the real error and the logs had said so all along. The first hypothesis was a connection pool exhausted at peak which explained the intermittency. The fix came down to a connection pool exhausted at peak and the logs had said so all along. The fix came down to a metric that averaged away the tail and it only appeared under load. Instrumentation revealed a timeout longer than the caller's and it has not recurred since.
