Short engineering note, mostly so the next person does not repeat it.

## The problem

The failure showed up as a metric that averaged away the tail and it only appeared under load. Aws landed at 34% change once I measured it end to end. Idempotency turned out to matter far more than throughput in this path. The failure showed up as an index that no longer matched the access pattern and the logs had said so all along. What actually broke was an index that no longer matched the access pattern once we stopped trusting the average. The first hypothesis was a retry storm masking the real error which explained the intermittency. We eventually traced it to a timeout longer than the caller's and the logs had said so all along. Instrumentation revealed a migration that had never been rehearsed so the graphs looked healthy throughout.
## The design

Measured properly, AWS came out at 12% change. The first hypothesis was a metric that averaged away the tail and it only appeared under load. The fix came down to a queue consumer falling behind quietly once we stopped trusting the average. We eventually traced it to a queue consumer falling behind quietly and the logs had said so all along. We eventually traced it to a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. The fix came down to a metric that averaged away the tail and it only appeared under load. Instrumentation revealed a migration that had never been rehearsed once we stopped trusting the average. Instrumentation revealed a slow query behind a cache miss so the graphs looked healthy throughout.
## Implementation notes

Aws landed at 42% change once I measured it end to end. What actually broke was a migration that had never been rehearsed and the logs had said so all along. Small, boring changes with a measurement attached beat clever changes without one. The first hypothesis was an index that no longer matched the access pattern and it only appeared under load. The first hypothesis was a connection pool exhausted at peak once we stopped trusting the average. The fix came down to a timeout longer than the caller's so the graphs looked healthy throughout. The fix came down to a queue consumer falling behind quietly so the graphs looked healthy throughout. What actually broke was a migration that had never been rehearsed and it only appeared under load.
## What I measured

The failure showed up as a slow query behind a cache miss and it has not recurred since. Instrumentation revealed a retry storm masking the real error and it has not recurred since. The fix came down to a queue consumer falling behind quietly and it has not recurred since. The failure showed up as a connection pool exhausted at peak and it has not recurred since. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average. Instrumentation revealed an index that no longer matched the access pattern once we stopped trusting the average.
## The lesson

The failure showed up as a timeout longer than the caller's which explained the intermittency. We eventually traced it to a migration that had never been rehearsed and it has not recurred since. We eventually traced it to a retry storm masking the real error though the fix was less interesting than the diagnosis. The fix came down to a metric that averaged away the tail though the fix was less interesting than the diagnosis. The fix came down to an index that no longer matched the access pattern which explained the intermittency. The fix came down to a queue consumer falling behind quietly which explained the intermittency.
