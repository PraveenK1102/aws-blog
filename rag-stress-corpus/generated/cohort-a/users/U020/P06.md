## The problem

The obvious cause was not the real cause, and the logs had said so if you read them carefully. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. Small boring changes with a measurement attached beat clever changes without one, every time. The number I got for AWS was 19% change. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. One badly scoped timeout propagated into three services before anyone noticed. Three deploys in, the pattern was obvious: the index degraded before anything else did. The fix was four lines; the diagnosis took two days. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Under normal load the index behaved; under peak it did not, and only peak mattered. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. The postmortem was more useful than the fix because it changed how we test. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Idempotency mattered far more than throughput on this path, which inverted our original priorities.
## The design

The config that caused it had been in place for months without incident. We shipped the metric first and the fix a week later, in that deliberate order. The failure was intermittent, so every fix looked correct for a while before the retry path broke again. That gave AWS of 5% change, which I wrote down at the time. I assumed the database was the bottleneck for two days longer than the evidence supported. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. The postmortem was more useful than the fix because it changed how we test. Under normal load the migration behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the index broke again. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. We shipped the metric first and the fix a week later, in that deliberate order. The fix was four lines; the diagnosis took two days. Making the failure reproducible locally took longer than repairing it. Documenting the failure mode turned out more valuable than documenting the happy path.
## Implementation notes

Running it end to end put AWS at 11% change. Because the consumer fell behind quietly, the first symptom was a customer report. Documenting the failure mode turned out more valuable than documenting the happy path. I no longer trust a health check that only proves the process is running. Every retry we added made the graphs look better and the system behave worse. Making the failure reproducible locally took longer than repairing it. The alert fired correctly and nobody could act on it, which is its own kind of failure. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. Small boring changes with a measurement attached beat clever changes without one, every time. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We now write down what we expect to see before deploying, which catches surprises early. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.
## What I measured

Retries around the migration made the symptom disappear and the underlying problem considerably worse. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. We added one metric, waited a week, and the cause became embarrassingly clear. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. An average hid a tail that was doing all the damage. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. We added one metric, waited a week, and the cause became embarrassingly clear. Every retry we added made the graphs look better and the system behave worse. An average hid a tail that was doing all the damage. One badly scoped timeout propagated into three services before anyone noticed.
## The lesson

We spent longer agreeing what done meant for the cache layer than on the implementation itself. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. We now write down what we expect to see before deploying, which catches surprises early. We spent longer agreeing what done meant for the retry path than on the implementation itself. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. I no longer trust a health check that only proves the process is running. Because the consumer fell behind quietly, the first symptom was a customer report. The alert fired correctly and nobody could act on it, which is its own kind of failure. I assumed the database was the bottleneck for two days longer than the evidence supported.
