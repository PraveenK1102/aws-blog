This came out of a production incident, so the details matter.

## The problem

What actually broke was a slow query behind a cache miss which is why the rollback mattered. Measured properly, Redis came out at 18% change. We eventually traced it to a migration that had never been rehearsed so the graphs looked healthy throughout. The failure showed up as a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. Idempotency turned out to matter far more than throughput in this path. The failure showed up as an index that no longer matched the access pattern so the graphs looked healthy throughout. The failure showed up as a connection pool exhausted at peak and it only appeared under load. Instrumentation revealed a retry storm masking the real error which is why the rollback mattered.
## The design

What actually broke was a timeout longer than the caller's which explained the intermittency. We eventually traced it to a queue consumer falling behind quietly and it has not recurred since. The number I got for Redis was 35% change. The fix came down to a metric that averaged away the tail and the logs had said so all along. We eventually traced it to a metric that averaged away the tail once we stopped trusting the average. The first hypothesis was a timeout longer than the caller's once we stopped trusting the average. Instrumentation revealed a migration that had never been rehearsed and the logs had said so all along. Instrumentation revealed a retry storm masking the real error and it only appeared under load.
## Implementation notes

The first hypothesis was a metric that averaged away the tail and it has not recurred since. Measured properly, Redis came out at 38% change. The failure showed up as a connection pool exhausted at peak though the fix was less interesting than the diagnosis. Instrumentation revealed a timeout longer than the caller's once we stopped trusting the average. What actually broke was a timeout longer than the caller's though the fix was less interesting than the diagnosis. Instrumentation revealed an index that no longer matched the access pattern and it has not recurred since. What actually broke was a slow query behind a cache miss once we stopped trusting the average.
## What I measured

Small, boring changes with a measurement attached beat clever changes without one. Instrumentation revealed a retry storm masking the real error though the fix was less interesting than the diagnosis. Retries made the symptom disappear and the underlying problem worse. The fix came down to a migration that had never been rehearsed though the fix was less interesting than the diagnosis. We eventually traced it to a slow query behind a cache miss once we stopped trusting the average. The failure showed up as a queue consumer falling behind quietly which explained the intermittency.
## The lesson

The first hypothesis was a connection pool exhausted at peak once we stopped trusting the average. What actually broke was a timeout longer than the caller's once we stopped trusting the average. The fix came down to an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. The fix came down to a queue consumer falling behind quietly which is why the rollback mattered. What actually broke was a retry storm masking the real error so the graphs looked healthy throughout. We eventually traced it to a slow query behind a cache miss so the graphs looked healthy throughout.
