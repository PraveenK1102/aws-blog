## The problem

Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The postmortem was more useful than the fix because it changed how we test. The failure was intermittent, so every fix looked correct for a while before the migration broke again.

Measured properly, Redis came out at 18% change. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Small boring changes with a measurement attached beat clever changes without one, every time. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. The index only misbehaved at peak, so the averages stayed healthy right through the incident. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

I no longer trust a health check that only proves the process is running. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. My recorded figure for Redis is 35% change. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Documenting the failure mode turned out more valuable than documenting the happy path. Three deploys in, the pattern was obvious: the index degraded before anything else did.
## Implementation notes

We added one metric, waited a week, and the cause became embarrassingly clear. An average hid a tail that was doing all the damage. The number I got for Redis was 38% change. The config that caused it had been in place for months without incident. Under normal load the retry path behaved; under peak it did not, and only peak mattered. We spent longer agreeing what done meant for the retry path than on the implementation itself. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the retry path broke again.
## What I measured

The alert fired correctly and nobody could act on it, which is its own kind of failure. One badly scoped timeout propagated into three services before anyone noticed. The fix was four lines; the diagnosis took two days. Because the consumer fell behind quietly, the first symptom was a customer report. I assumed the database was the bottleneck for two days longer than the evidence supported. We spent longer agreeing what done meant for the connection pool than on the implementation itself.
## The lesson

We now write down what we expect to see before deploying, which catches surprises early. We shipped the metric first and the fix a week later, in that deliberate order. Making the failure reproducible locally took longer than repairing it.

Every retry we added made the graphs look better and the system behave worse. The failure was intermittent, so every fix looked correct for a while before the index broke again. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all.
