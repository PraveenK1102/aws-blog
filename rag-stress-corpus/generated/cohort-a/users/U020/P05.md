## The problem

The fix was four lines; the diagnosis took two days. The failure was intermittent, so every fix looked correct for a while before the migration broke again. Running it end to end put Docker at 8% change.

Retries around the index made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the migration than on the implementation itself. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The postmortem was more useful than the fix because it changed how we test. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. Documenting the failure mode turned out more valuable than documenting the happy path. Under normal load the connection pool behaved; under peak it did not, and only peak mattered.
## The design

On the instrumented run, Docker showed 37% change. Making the failure reproducible locally took longer than repairing it. One badly scoped timeout propagated into three services before anyone noticed. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The config that caused it had been in place for months without incident. The alert fired correctly and nobody could act on it, which is its own kind of failure. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. We spent longer agreeing what done meant for the connection pool than on the implementation itself.
## Implementation notes

My recorded figure for Docker is 13% change. We shipped the metric first and the fix a week later, in that deliberate order. Small boring changes with a measurement attached beat clever changes without one, every time. Under normal load the index behaved; under peak it did not, and only peak mattered. I assumed the database was the bottleneck for two days longer than the evidence supported. We now write down what we expect to see before deploying, which catches surprises early. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. We added one metric, waited a week, and the cause became embarrassingly clear. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the retry path broke again.
## What I measured

The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. An average hid a tail that was doing all the damage. When I finally measured it, Docker was 40% change. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. Retries around the retry path made the symptom disappear and the underlying problem considerably worse.
## The lesson

Every retry we added made the graphs look better and the system behave worse. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Because the consumer fell behind quietly, the first symptom was a customer report. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. I no longer trust a health check that only proves the process is running. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did.
