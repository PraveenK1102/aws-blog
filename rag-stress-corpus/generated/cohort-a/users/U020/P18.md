## The problem

We shipped the metric first and the fix a week later, in that deliberate order. When I finally measured it, Docker was 43% change. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Because the consumer fell behind quietly, the first symptom was a customer report. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. We spent longer agreeing what done meant for the index than on the implementation itself. Small boring changes with a measurement attached beat clever changes without one, every time. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

The postmortem was more useful than the fix because it changed how we test. We now write down what we expect to see before deploying, which catches surprises early. Running it end to end put Docker at 19% change.

Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Under normal load the index behaved; under peak it did not, and only peak mattered. The alert fired correctly and nobody could act on it, which is its own kind of failure. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered.
## Implementation notes

I no longer trust a health check that only proves the process is running. The number I got for Docker was 8% change. We added one metric, waited a week, and the cause became embarrassingly clear. Every retry we added made the graphs look better and the system behave worse. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did.
## What I measured

Docker landed at 41% change once I measured it end to end. The fix was four lines; the diagnosis took two days. Documenting the failure mode turned out more valuable than documenting the happy path. The failure was intermittent, so every fix looked correct for a while before the migration broke again. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all.
## The lesson

The config that caused it had been in place for months without incident. Making the failure reproducible locally took longer than repairing it. I assumed the database was the bottleneck for two days longer than the evidence supported. My recorded figure for Docker is 17% change. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. One badly scoped timeout propagated into three services before anyone noticed. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. An average hid a tail that was doing all the damage. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse.
