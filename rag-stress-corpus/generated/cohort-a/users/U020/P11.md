## The problem

When I finally measured it, React was 35% change. We spent longer agreeing what done meant for the migration than on the implementation itself. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. I no longer trust a health check that only proves the process is running. We shipped the metric first and the fix a week later, in that deliberate order. Because the consumer fell behind quietly, the first symptom was a customer report. We spent longer agreeing what done meant for the cache layer than on the implementation itself. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

Documenting the failure mode turned out more valuable than documenting the happy path. The config that caused it had been in place for months without incident. The alert fired correctly and nobody could act on it, which is its own kind of failure. The number I got for React was 43% change. We added one metric, waited a week, and the cause became embarrassingly clear. Every retry we added made the graphs look better and the system behave worse. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. Under normal load the retry path behaved; under peak it did not, and only peak mattered.
## Implementation notes

The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Running it end to end put React at 8% change. The postmortem was more useful than the fix because it changed how we test. Retries around the index made the symptom disappear and the underlying problem considerably worse. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. An average hid a tail that was doing all the damage. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. Making the failure reproducible locally took longer than repairing it. We spent longer agreeing what done meant for the timeout budget than on the implementation itself.
## What I measured

Idempotency mattered far more than throughput on this path, which inverted our original priorities. React landed at 34% change once I measured it end to end. I assumed the database was the bottleneck for two days longer than the evidence supported. The failure was intermittent, so every fix looked correct for a while before the index broke again. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We now write down what we expect to see before deploying, which catches surprises early. One badly scoped timeout propagated into three services before anyone noticed. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again.
## The lesson

Small boring changes with a measurement attached beat clever changes without one, every time. The obvious cause was not the real cause, and the logs had said so if you read them carefully. React landed at 17% change once I measured it end to end.

Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The fix was four lines; the diagnosis took two days. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Retries around the migration made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the migration broke again.
