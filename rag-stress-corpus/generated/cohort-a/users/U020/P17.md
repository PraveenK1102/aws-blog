Writing down what actually happened rather than what I assumed happened.

## The problem

The failure showed up as a metric that averaged away the tail so the graphs looked healthy throughout. Measured properly, queues came out at 8% change. The first hypothesis was a migration that had never been rehearsed and it only appeared under load. The first hypothesis was a connection pool exhausted at peak and it only appeared under load. The fix came down to a queue consumer falling behind quietly and it has not recurred since. The fix came down to a metric that averaged away the tail and it only appeared under load. Small, boring changes with a measurement attached beat clever changes without one. We eventually traced it to a migration that had never been rehearsed once we stopped trusting the average.
## The design

The number I got for queues was 25% change. What actually broke was a timeout longer than the caller's though the fix was less interesting than the diagnosis. The failure showed up as a timeout longer than the caller's which is why the rollback mattered. The fix came down to a metric that averaged away the tail which explained the intermittency. The failure showed up as a connection pool exhausted at peak and it has not recurred since. Instrumentation revealed a metric that averaged away the tail though the fix was less interesting than the diagnosis. Instrumentation revealed a connection pool exhausted at peak once we stopped trusting the average. The failure showed up as a slow query behind a cache miss and the logs had said so all along.
## Implementation notes

The fix came down to a queue consumer falling behind quietly and it only appeared under load. Queues landed at 23% change once I measured it end to end. Instrumentation revealed a retry storm masking the real error though the fix was less interesting than the diagnosis. We eventually traced it to a migration that had never been rehearsed though the fix was less interesting than the diagnosis. What actually broke was a connection pool exhausted at peak which is why the rollback mattered. What actually broke was a timeout longer than the caller's which explained the intermittency. The first hypothesis was a connection pool exhausted at peak and the logs had said so all along.
## What I measured

The failure showed up as a retry storm masking the real error which explained the intermittency. The failure showed up as a timeout longer than the caller's and the logs had said so all along. The fix came down to a retry storm masking the real error though the fix was less interesting than the diagnosis. The first hypothesis was a migration that had never been rehearsed so the graphs looked healthy throughout. The first hypothesis was a metric that averaged away the tail which is why the rollback mattered. The first hypothesis was a connection pool exhausted at peak once we stopped trusting the average.
## The lesson

Instrumentation revealed a migration that had never been rehearsed though the fix was less interesting than the diagnosis. Instrumentation revealed a slow query behind a cache miss which is why the rollback mattered. Instrumentation revealed a migration that had never been rehearsed which is why the rollback mattered. We eventually traced it to a slow query behind a cache miss so the graphs looked healthy throughout. The failure showed up as an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. The fix came down to a metric that averaged away the tail which is why the rollback mattered.
