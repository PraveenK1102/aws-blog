## The problem

Making the failure reproducible locally took longer than repairing it. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Documenting the failure mode turned out more valuable than documenting the happy path. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. Queues landed at 8% change once I measured it end to end. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. An average hid a tail that was doing all the damage. I assumed the database was the bottleneck for two days longer than the evidence supported. Because the consumer fell behind quietly, the first symptom was a customer report. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered.
## The design

Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. We added one metric, waited a week, and the cause became embarrassingly clear. That gave queues of 25% change, which I wrote down at the time. Every retry we added made the graphs look better and the system behave worse. The alert fired correctly and nobody could act on it, which is its own kind of failure. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The index only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again.
## Implementation notes

Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. My recorded figure for queues is 23% change. We spent longer agreeing what done meant for the index than on the implementation itself. Small boring changes with a measurement attached beat clever changes without one, every time. Three deploys in, the pattern was obvious: the index degraded before anything else did. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.
## What I measured

The fix was four lines; the diagnosis took two days. Retries around the index made the symptom disappear and the underlying problem considerably worse. The postmortem was more useful than the fix because it changed how we test. We shipped the metric first and the fix a week later, in that deliberate order. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Retries around the migration made the symptom disappear and the underlying problem considerably worse.
## The lesson

One badly scoped timeout propagated into three services before anyone noticed. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Under normal load the index behaved; under peak it did not, and only peak mattered. We now write down what we expect to see before deploying, which catches surprises early. The config that caused it had been in place for months without incident. I no longer trust a health check that only proves the process is running. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident.
