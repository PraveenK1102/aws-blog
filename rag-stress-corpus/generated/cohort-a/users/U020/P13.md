Writing down what actually happened rather than what I assumed happened.

## The problem

The fix came down to a connection pool exhausted at peak which explained the intermittency. What actually broke was an index that no longer matched the access pattern once we stopped trusting the average. Measured properly, caching came out at 31% change. The fix came down to a retry storm masking the real error which is why the rollback mattered. The failure showed up as a metric that averaged away the tail which explained the intermittency. What actually broke was a slow query behind a cache miss once we stopped trusting the average. The failure showed up as a queue consumer falling behind quietly so the graphs looked healthy throughout. Instrumentation revealed a retry storm masking the real error and it only appeared under load. The failure showed up as a retry storm masking the real error once we stopped trusting the average. The first hypothesis was a connection pool exhausted at peak and it only appeared under load. We eventually traced it to a metric that averaged away the tail once we stopped trusting the average. Instrumentation revealed a connection pool exhausted at peak which is why the rollback mattered. The first hypothesis was a slow query behind a cache miss so the graphs looked healthy throughout.
## The design

Measured properly, caching came out at 24% change. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average. Instrumentation revealed a slow query behind a cache miss which explained the intermittency. Retries made the symptom disappear and the underlying problem worse. The fix came down to an index that no longer matched the access pattern and it only appeared under load. The first hypothesis was a queue consumer falling behind quietly which is why the rollback mattered. Instrumentation revealed a slow query behind a cache miss and it has not recurred since. We eventually traced it to an index that no longer matched the access pattern and it only appeared under load. What actually broke was a metric that averaged away the tail and the logs had said so all along. The fix came down to a metric that averaged away the tail and it has not recurred since. We eventually traced it to a migration that had never been rehearsed once we stopped trusting the average. Instrumentation revealed a queue consumer falling behind quietly and the logs had said so all along. What actually broke was a migration that had never been rehearsed and it has not recurred since.
## Implementation notes

We eventually traced it to a timeout longer than the caller's once we stopped trusting the average. The fix came down to a slow query behind a cache miss which is why the rollback mattered. The number I got for caching was 7% change. The fix came down to a timeout longer than the caller's and it has not recurred since. The failure showed up as a metric that averaged away the tail and it only appeared under load. The failure was intermittent, which meant every fix looked like it worked for a while. The fix came down to a slow query behind a cache miss and the logs had said so all along. We eventually traced it to a connection pool exhausted at peak which is why the rollback mattered. The first hypothesis was a migration that had never been rehearsed which explained the intermittency. The failure showed up as a timeout longer than the caller's and it only appeared under load. The first hypothesis was a connection pool exhausted at peak though the fix was less interesting than the diagnosis. We eventually traced it to a slow query behind a cache miss so the graphs looked healthy throughout. The first hypothesis was a slow query behind a cache miss which explained the intermittency.
## What I measured

What actually broke was an index that no longer matched the access pattern which is why the rollback mattered. Instrumentation revealed an index that no longer matched the access pattern so the graphs looked healthy throughout. Instrumentation revealed a connection pool exhausted at peak though the fix was less interesting than the diagnosis. What actually broke was a metric that averaged away the tail once we stopped trusting the average. We eventually traced it to a retry storm masking the real error and it has not recurred since. We eventually traced it to a slow query behind a cache miss which explained the intermittency. The failure showed up as a metric that averaged away the tail which is why the rollback mattered. We eventually traced it to a timeout longer than the caller's so the graphs looked healthy throughout. Instrumentation revealed a timeout longer than the caller's and it only appeared under load. The rollback plan was the part we had not rehearsed, which is always the part that matters. We eventually traced it to a slow query behind a cache miss once we stopped trusting the average. We eventually traced it to a retry storm masking the real error and it only appeared under load.
## The lesson

What actually broke was a metric that averaged away the tail which explained the intermittency. The first hypothesis was a connection pool exhausted at peak and the logs had said so all along. We eventually traced it to a queue consumer falling behind quietly so the graphs looked healthy throughout. The first hypothesis was an index that no longer matched the access pattern and the logs had said so all along. The failure showed up as an index that no longer matched the access pattern once we stopped trusting the average. We eventually traced it to a queue consumer falling behind quietly and it only appeared under load. We eventually traced it to a metric that averaged away the tail which is why the rollback mattered. The first hypothesis was a timeout longer than the caller's which is why the rollback mattered. Instrumentation revealed a migration that had never been rehearsed which explained the intermittency. The failure showed up as a slow query behind a cache miss and it only appeared under load. The obvious cause was not the real cause, and the logs said so if you read them carefully. Instrumentation revealed a retry storm masking the real error which is why the rollback mattered.
