## The problem

I assumed the database was the bottleneck for two days longer than the evidence supported. We shipped the metric first and the fix a week later, in that deliberate order. One badly scoped timeout propagated into three services before anyone noticed. The number I got for caching was 31% change. Small boring changes with a measurement attached beat clever changes without one, every time. I no longer trust a health check that only proves the process is running. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. The fix was four lines; the diagnosis took two days. We now write down what we expect to see before deploying, which catches surprises early. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. The config that caused it had been in place for months without incident. I no longer trust a health check that only proves the process is running. The fix was four lines; the diagnosis took two days. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The postmortem was more useful than the fix because it changed how we test.
## The design

The postmortem was more useful than the fix because it changed how we test. Measured properly, caching came out at 24% change. Making the failure reproducible locally took longer than repairing it. We spent longer agreeing what done meant for the cache layer than on the implementation itself. We added one metric, waited a week, and the cause became embarrassingly clear. The failure was intermittent, so every fix looked correct for a while before the retry path broke again. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. An average hid a tail that was doing all the damage. We now write down what we expect to see before deploying, which catches surprises early. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. The index only misbehaved at peak, so the averages stayed healthy right through the incident.
## Implementation notes

Documenting the failure mode turned out more valuable than documenting the happy path. My recorded figure for caching is 7% change. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. Under normal load the retry path behaved; under peak it did not, and only peak mattered. We spent longer agreeing what done meant for the index than on the implementation itself. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. We added one metric, waited a week, and the cause became embarrassingly clear. Every retry we added made the graphs look better and the system behave worse. The obvious cause was not the real cause, and the logs had said so if you read them carefully. One badly scoped timeout propagated into three services before anyone noticed. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse.
## What I measured

Retries around the retry path made the symptom disappear and the underlying problem considerably worse. The alert fired correctly and nobody could act on it, which is its own kind of failure. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Every retry we added made the graphs look better and the system behave worse. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Because the consumer fell behind quietly, the first symptom was a customer report. The alert fired correctly and nobody could act on it, which is its own kind of failure. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Documenting the failure mode turned out more valuable than documenting the happy path. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident.
## The lesson

The config that caused it had been in place for months without incident. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. Because the consumer fell behind quietly, the first symptom was a customer report. We spent longer agreeing what done meant for the migration than on the implementation itself. Retries around the migration made the symptom disappear and the underlying problem considerably worse. An average hid a tail that was doing all the damage. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. I assumed the database was the bottleneck for two days longer than the evidence supported. We shipped the metric first and the fix a week later, in that deliberate order. Making the failure reproducible locally took longer than repairing it. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Small boring changes with a measurement attached beat clever changes without one, every time. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.
