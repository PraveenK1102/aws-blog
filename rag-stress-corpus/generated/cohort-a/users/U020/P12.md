## The problem

Making the failure reproducible locally took longer than repairing it. Small boring changes with a measurement attached beat clever changes without one, every time. An average hid a tail that was doing all the damage.

The alert fired correctly and nobody could act on it, which is its own kind of failure. Api design landed at 3% change once I measured it end to end. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. We added one metric, waited a week, and the cause became embarrassingly clear. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

The obvious cause was not the real cause, and the logs had said so if you read them carefully. Measured properly, API design came out at 24% change. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. The failure was intermittent, so every fix looked correct for a while before the migration broke again. Every retry we added made the graphs look better and the system behave worse.
## Implementation notes

We now write down what we expect to see before deploying, which catches surprises early. The postmortem was more useful than the fix because it changed how we test. I no longer trust a health check that only proves the process is running.

Running it end to end put API design at 43% change. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. Retries around the index made the symptom disappear and the underlying problem considerably worse.
## What I measured

Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. On the instrumented run, API design showed 17% change. One badly scoped timeout propagated into three services before anyone noticed. The config that caused it had been in place for months without incident. Under normal load the index behaved; under peak it did not, and only peak mattered. We shipped the metric first and the fix a week later, in that deliberate order. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. Documenting the failure mode turned out more valuable than documenting the happy path.
## The lesson

I assumed the database was the bottleneck for two days longer than the evidence supported. The fix was four lines; the diagnosis took two days. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Because the consumer fell behind quietly, the first symptom was a customer report. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We spent longer agreeing what done meant for the cache layer than on the implementation itself. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again.
