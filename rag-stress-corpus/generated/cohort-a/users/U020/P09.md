Short engineering note, mostly so the next person does not repeat it.

## The problem

We eventually traced it to a connection pool exhausted at peak and it only appeared under load. The number I got for retries was 8% change. The fix came down to a metric that averaged away the tail though the fix was less interesting than the diagnosis. We eventually traced it to a connection pool exhausted at peak so the graphs looked healthy throughout. The fix came down to a slow query behind a cache miss and the logs had said so all along. We eventually traced it to a timeout longer than the caller's and it only appeared under load. The fix came down to a retry storm masking the real error which explained the intermittency. What actually broke was a timeout longer than the caller's and it only appeared under load. We eventually traced it to a slow query behind a cache miss and the logs had said so all along. We eventually traced it to a timeout longer than the caller's once we stopped trusting the average. Instrumentation revealed a timeout longer than the caller's which explained the intermittency. We eventually traced it to a timeout longer than the caller's and the logs had said so all along. We eventually traced it to a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. Instrumentation revealed a metric that averaged away the tail and it has not recurred since.
## The design

The failure showed up as a connection pool exhausted at peak and it only appeared under load. The number I got for retries was 19% change. We eventually traced it to a queue consumer falling behind quietly so the graphs looked healthy throughout. The first hypothesis was a metric that averaged away the tail which is why the rollback mattered. The failure showed up as a timeout longer than the caller's which explained the intermittency. The failure showed up as a connection pool exhausted at peak and it has not recurred since. The failure showed up as a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. The failure showed up as a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The fix came down to a retry storm masking the real error once we stopped trusting the average. The failure showed up as an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. The first hypothesis was an index that no longer matched the access pattern once we stopped trusting the average. The first hypothesis was a connection pool exhausted at peak and the logs had said so all along. We eventually traced it to a metric that averaged away the tail and the logs had said so all along. The fix came down to a queue consumer falling behind quietly so the graphs looked healthy throughout.
## Implementation notes

Retries landed at 23% change once I measured it end to end. We eventually traced it to a slow query behind a cache miss which is why the rollback mattered. Instrumentation revealed a retry storm masking the real error though the fix was less interesting than the diagnosis. Instrumentation revealed a metric that averaged away the tail and it only appeared under load. The fix came down to an index that no longer matched the access pattern and it has not recurred since. The failure showed up as a migration that had never been rehearsed though the fix was less interesting than the diagnosis. The first hypothesis was a connection pool exhausted at peak which is why the rollback mattered. Instrumentation revealed a retry storm masking the real error which explained the intermittency. The first hypothesis was an index that no longer matched the access pattern and the logs had said so all along. What actually broke was a slow query behind a cache miss which explained the intermittency. Instrumentation revealed a migration that had never been rehearsed so the graphs looked healthy throughout. The failure showed up as a migration that had never been rehearsed which explained the intermittency. We eventually traced it to an index that no longer matched the access pattern and it has not recurred since.
## What I measured

What actually broke was a connection pool exhausted at peak and it has not recurred since. The fix came down to a timeout longer than the caller's which is why the rollback mattered. The failure showed up as a timeout longer than the caller's though the fix was less interesting than the diagnosis. What actually broke was a queue consumer falling behind quietly and it only appeared under load. What actually broke was a metric that averaged away the tail and it has not recurred since. Instrumentation revealed a slow query behind a cache miss and the logs had said so all along. Instrumentation revealed a retry storm masking the real error and it has not recurred since. The fix came down to a retry storm masking the real error which is why the rollback mattered. Instrumentation revealed a slow query behind a cache miss which is why the rollback mattered. The failure showed up as a connection pool exhausted at peak once we stopped trusting the average. Instrumentation revealed a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The failure showed up as a retry storm masking the real error and it only appeared under load.
## The lesson

What actually broke was a migration that had never been rehearsed which is why the rollback mattered. The first hypothesis was a migration that had never been rehearsed which explained the intermittency. The fix came down to a metric that averaged away the tail once we stopped trusting the average. The first hypothesis was a slow query behind a cache miss which explained the intermittency. The failure showed up as a retry storm masking the real error so the graphs looked healthy throughout. The fix came down to a connection pool exhausted at peak and the logs had said so all along. The first hypothesis was a retry storm masking the real error which is why the rollback mattered. The failure showed up as a metric that averaged away the tail so the graphs looked healthy throughout. The first hypothesis was a metric that averaged away the tail though the fix was less interesting than the diagnosis. The failure showed up as a timeout longer than the caller's so the graphs looked healthy throughout. The failure showed up as a slow query behind a cache miss once we stopped trusting the average. What actually broke was a slow query behind a cache miss though the fix was less interesting than the diagnosis.
