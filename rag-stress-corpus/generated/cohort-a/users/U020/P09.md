## The problem

Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. My recorded figure for retries is 8% change. The fix was four lines; the diagnosis took two days. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Because the consumer fell behind quietly, the first symptom was a customer report. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. We now write down what we expect to see before deploying, which catches surprises early. Small boring changes with a measurement attached beat clever changes without one, every time. I no longer trust a health check that only proves the process is running. The config that caused it had been in place for months without incident. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.
## The design

The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. An average hid a tail that was doing all the damage. When I finally measured it, retries was 19% change.

Small boring changes with a measurement attached beat clever changes without one, every time. One badly scoped timeout propagated into three services before anyone noticed. Under normal load the migration behaved; under peak it did not, and only peak mattered. We added one metric, waited a week, and the cause became embarrassingly clear. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. Under normal load the index behaved; under peak it did not, and only peak mattered. We spent longer agreeing what done meant for the connection pool than on the implementation itself. The fix was four lines; the diagnosis took two days. Making the failure reproducible locally took longer than repairing it. The postmortem was more useful than the fix because it changed how we test. Because the consumer fell behind quietly, the first symptom was a customer report. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Because the consumer fell behind quietly, the first symptom was a customer report.
## Implementation notes

Every retry we added made the graphs look better and the system behave worse. We now write down what we expect to see before deploying, which catches surprises early. That gave retries of 23% change, which I wrote down at the time. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did.

I no longer trust a health check that only proves the process is running. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The config that caused it had been in place for months without incident. The index only misbehaved at peak, so the averages stayed healthy right through the incident. We spent longer agreeing what done meant for the retry path than on the implementation itself. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. We added one metric, waited a week, and the cause became embarrassingly clear. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Documenting the failure mode turned out more valuable than documenting the happy path. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. The fix was four lines; the diagnosis took two days.
## What I measured

The migration only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We shipped the metric first and the fix a week later, in that deliberate order. The postmortem was more useful than the fix because it changed how we test. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. I assumed the database was the bottleneck for two days longer than the evidence supported. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. I assumed the database was the bottleneck for two days longer than the evidence supported. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Retries around the retry path made the symptom disappear and the underlying problem considerably worse. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The obvious cause was not the real cause, and the logs had said so if you read them carefully.
## The lesson

The alert fired correctly and nobody could act on it, which is its own kind of failure. Documenting the failure mode turned out more valuable than documenting the happy path. Making the failure reproducible locally took longer than repairing it. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. Retries around the index made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the migration broke again. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. Every retry we added made the graphs look better and the system behave worse. One badly scoped timeout propagated into three services before anyone noticed. An average hid a tail that was doing all the damage. We shipped the metric first and the fix a week later, in that deliberate order. The alert fired correctly and nobody could act on it, which is its own kind of failure. Small boring changes with a measurement attached beat clever changes without one, every time.
