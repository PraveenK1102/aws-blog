Short engineering note, mostly so the next person does not repeat it.

## The problem

The number I got for timeouts was 14% change. The first hypothesis was a timeout longer than the caller's though the fix was less interesting than the diagnosis. The failure showed up as a retry storm masking the real error and it has not recurred since. Instrumentation revealed a metric that averaged away the tail and it only appeared under load. Instrumentation revealed a connection pool exhausted at peak and it has not recurred since.
## The design

The first hypothesis was a migration that had never been rehearsed and it has not recurred since. Measured properly, timeouts came out at 15% change. The fix came down to a timeout longer than the caller's though the fix was less interesting than the diagnosis. Instrumentation revealed a metric that averaged away the tail which is why the rollback mattered.
## Implementation notes

The first hypothesis was a metric that averaged away the tail once we stopped trusting the average. We eventually traced it to a timeout longer than the caller's which is why the rollback mattered. The first hypothesis was a queue consumer falling behind quietly though the fix was less interesting than the diagnosis.
## What I measured

What actually broke was a slow query behind a cache miss and the logs had said so all along. What actually broke was a timeout longer than the caller's and the logs had said so all along. The first hypothesis was a slow query behind a cache miss though the fix was less interesting than the diagnosis.
## The lesson

The fix came down to a timeout longer than the caller's once we stopped trusting the average. The fix came down to a retry storm masking the real error so the graphs looked healthy throughout. Instrumentation revealed a queue consumer falling behind quietly which explained the intermittency.
