## The problem

Measured properly, transactions came out at 29% change. Because the consumer fell behind quietly, the first symptom was a customer report. The postmortem was more useful than the fix because it changed how we test. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We shipped the metric first and the fix a week later, in that deliberate order. We spent longer agreeing what done meant for the index than on the implementation itself. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. Three deploys in, the pattern was obvious: the index degraded before anything else did. Because the consumer fell behind quietly, the first symptom was a customer report. The obvious cause was not the real cause, and the logs had said so if you read them carefully. I assumed the database was the bottleneck for two days longer than the evidence supported. We now write down what we expect to see before deploying, which catches surprises early. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.
## The design

Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Transactions landed at 17% change once I measured it end to end. Under normal load the index behaved; under peak it did not, and only peak mattered. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. The alert fired correctly and nobody could act on it, which is its own kind of failure. The index only misbehaved at peak, so the averages stayed healthy right through the incident. We now write down what we expect to see before deploying, which catches surprises early. We spent longer agreeing what done meant for the migration than on the implementation itself. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. The postmortem was more useful than the fix because it changed how we test. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.
## Implementation notes

Running it end to end put transactions at 9% change. Documenting the failure mode turned out more valuable than documenting the happy path. The config that caused it had been in place for months without incident. One badly scoped timeout propagated into three services before anyone noticed. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The failure was intermittent, so every fix looked correct for a while before the migration broke again. I assumed the database was the bottleneck for two days longer than the evidence supported. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. Adding a metric to the index before adding a fix is the only reason we caught the regression at all. The config that caused it had been in place for months without incident. Small boring changes with a measurement attached beat clever changes without one, every time. We shipped the metric first and the fix a week later, in that deliberate order. Idempotency mattered far more than throughput on this path, which inverted our original priorities. An average hid a tail that was doing all the damage. Every retry we added made the graphs look better and the system behave worse.
## What I measured

Every retry we added made the graphs look better and the system behave worse. The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. Small boring changes with a measurement attached beat clever changes without one, every time. An average hid a tail that was doing all the damage. Under normal load the retry path behaved; under peak it did not, and only peak mattered. I no longer trust a health check that only proves the process is running. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. Every retry we added made the graphs look better and the system behave worse. One badly scoped timeout propagated into three services before anyone noticed. Documenting the failure mode turned out more valuable than documenting the happy path. I no longer trust a health check that only proves the process is running. Retries around the index made the symptom disappear and the underlying problem considerably worse.
## The lesson

The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Making the failure reproducible locally took longer than repairing it. We added one metric, waited a week, and the cause became embarrassingly clear. The fix was four lines; the diagnosis took two days. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Retries around the index made the symptom disappear and the underlying problem considerably worse. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Making the failure reproducible locally took longer than repairing it. We added one metric, waited a week, and the cause became embarrassingly clear. The fix was four lines; the diagnosis took two days. The alert fired correctly and nobody could act on it, which is its own kind of failure. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident.
