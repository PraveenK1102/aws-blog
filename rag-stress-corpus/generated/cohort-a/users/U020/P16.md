Short engineering note, mostly so the next person does not repeat it.

## The problem

Measured properly, transactions came out at 29% change. The fix came down to an index that no longer matched the access pattern and the logs had said so all along. What actually broke was a timeout longer than the caller's which is why the rollback mattered. What actually broke was a migration that had never been rehearsed and the logs had said so all along. The fix came down to a slow query behind a cache miss though the fix was less interesting than the diagnosis. We eventually traced it to a migration that had never been rehearsed though the fix was less interesting than the diagnosis. The fix came down to a queue consumer falling behind quietly which is why the rollback mattered. The failure showed up as a slow query behind a cache miss once we stopped trusting the average. The failure was intermittent, which meant every fix looked like it worked for a while. Small, boring changes with a measurement attached beat clever changes without one. We eventually traced it to a retry storm masking the real error though the fix was less interesting than the diagnosis. What actually broke was a metric that averaged away the tail and it only appeared under load. We eventually traced it to a connection pool exhausted at peak and it has not recurred since.
## The design

The fix came down to a connection pool exhausted at peak which is why the rollback mattered. Measured properly, transactions came out at 17% change. The fix came down to a metric that averaged away the tail which is why the rollback mattered. The first hypothesis was a queue consumer falling behind quietly and it only appeared under load. What actually broke was a slow query behind a cache miss and it has not recurred since. The fix came down to a metric that averaged away the tail which explained the intermittency. Instrumentation revealed a retry storm masking the real error and it has not recurred since. What actually broke was a migration that had never been rehearsed which is why the rollback mattered. The first hypothesis was a timeout longer than the caller's and the logs had said so all along. We eventually traced it to an index that no longer matched the access pattern and it only appeared under load. We eventually traced it to an index that no longer matched the access pattern which is why the rollback mattered. What actually broke was a slow query behind a cache miss so the graphs looked healthy throughout. We eventually traced it to a connection pool exhausted at peak and the logs had said so all along.
## Implementation notes

The first hypothesis was a connection pool exhausted at peak which is why the rollback mattered. What actually broke was a retry storm masking the real error and the logs had said so all along. Transactions landed at 9% change once I measured it end to end. Instrumentation revealed a retry storm masking the real error once we stopped trusting the average. The first hypothesis was a connection pool exhausted at peak which explained the intermittency. The first hypothesis was a retry storm masking the real error which is why the rollback mattered. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average. The first hypothesis was a retry storm masking the real error and it has not recurred since. The failure showed up as a slow query behind a cache miss which is why the rollback mattered. The first hypothesis was a slow query behind a cache miss and the logs had said so all along. We eventually traced it to a retry storm masking the real error so the graphs looked healthy throughout. We eventually traced it to a migration that had never been rehearsed once we stopped trusting the average. We eventually traced it to a migration that had never been rehearsed so the graphs looked healthy throughout.
## What I measured

The fix came down to a retry storm masking the real error once we stopped trusting the average. Instrumentation revealed a queue consumer falling behind quietly which explained the intermittency. The failure showed up as a queue consumer falling behind quietly once we stopped trusting the average. We eventually traced it to a metric that averaged away the tail and it only appeared under load. The fix came down to a retry storm masking the real error and the logs had said so all along. We eventually traced it to a timeout longer than the caller's once we stopped trusting the average. The first hypothesis was a migration that had never been rehearsed which explained the intermittency. The fix came down to a retry storm masking the real error which is why the rollback mattered. The failure showed up as a metric that averaged away the tail which is why the rollback mattered. The obvious cause was not the real cause, and the logs said so if you read them carefully. What actually broke was a migration that had never been rehearsed once we stopped trusting the average. The failure showed up as a migration that had never been rehearsed which is why the rollback mattered.
## The lesson

The failure showed up as a retry storm masking the real error and it only appeared under load. We eventually traced it to a slow query behind a cache miss once we stopped trusting the average. Instrumentation revealed a metric that averaged away the tail so the graphs looked healthy throughout. The first hypothesis was a slow query behind a cache miss so the graphs looked healthy throughout. Instrumentation revealed a connection pool exhausted at peak once we stopped trusting the average. What actually broke was a timeout longer than the caller's and it only appeared under load. The failure showed up as a connection pool exhausted at peak which explained the intermittency. The failure showed up as an index that no longer matched the access pattern and it has not recurred since. What actually broke was a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. Documenting the failure mode was more useful than documenting the happy path. What actually broke was a connection pool exhausted at peak and it has not recurred since.
