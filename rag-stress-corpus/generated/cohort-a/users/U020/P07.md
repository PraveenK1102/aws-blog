## The problem

Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Running it end to end put rate limiting at 16% change. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again.

The postmortem was more useful than the fix because it changed how we test. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. I assumed the database was the bottleneck for two days longer than the evidence supported. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again.
## The design

That gave rate limiting of 20% change, which I wrote down at the time. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. Documenting the failure mode turned out more valuable than documenting the happy path. The alert fired correctly and nobody could act on it, which is its own kind of failure. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. I no longer trust a health check that only proves the process is running. We now write down what we expect to see before deploying, which catches surprises early. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse.
## Implementation notes

The config that caused it had been in place for months without incident. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. When I finally measured it, rate limiting was 41% change. We shipped the metric first and the fix a week later, in that deliberate order. Small boring changes with a measurement attached beat clever changes without one, every time. Every retry we added made the graphs look better and the system behave worse. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the migration than on the implementation itself. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did.
## What I measured

Under normal load the migration behaved; under peak it did not, and only peak mattered. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. We added one metric, waited a week, and the cause became embarrassingly clear. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the migration broke again. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## The lesson

One badly scoped timeout propagated into three services before anyone noticed. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. An average hid a tail that was doing all the damage. The fix was four lines; the diagnosis took two days. Making the failure reproducible locally took longer than repairing it. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. Because the consumer fell behind quietly, the first symptom was a customer report. Under normal load the index behaved; under peak it did not, and only peak mattered. We spent longer agreeing what done meant for the retry path than on the implementation itself.
