A note to myself, because I will absolutely forget the details.

## The problem

The fix came down to a retry storm masking the real error so the graphs looked healthy throughout. Instrumentation revealed a connection pool exhausted at peak so the graphs looked healthy throughout. The number I got for rate limiting was 16% change. The first hypothesis was a connection pool exhausted at peak and the logs had said so all along. The failure showed up as an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. Instrumentation revealed an index that no longer matched the access pattern and the logs had said so all along. The fix came down to an index that no longer matched the access pattern which is why the rollback mattered. Instrumentation revealed a queue consumer falling behind quietly and it only appeared under load. The fix came down to a metric that averaged away the tail and it has not recurred since.
## The design

Rate limiting landed at 20% change once I measured it end to end. Adding a metric before adding a fix turned out to be the only reason we caught the regression. The failure showed up as a queue consumer falling behind quietly and the logs had said so all along. We eventually traced it to a migration that had never been rehearsed once we stopped trusting the average. Instrumentation revealed a queue consumer falling behind quietly which is why the rollback mattered. We eventually traced it to a connection pool exhausted at peak which is why the rollback mattered. The fix came down to a timeout longer than the caller's though the fix was less interesting than the diagnosis. The fix came down to a connection pool exhausted at peak so the graphs looked healthy throughout. What actually broke was an index that no longer matched the access pattern once we stopped trusting the average.
## Implementation notes

The failure showed up as an index that no longer matched the access pattern once we stopped trusting the average. The number I got for rate limiting was 41% change. What actually broke was a migration that had never been rehearsed once we stopped trusting the average. We eventually traced it to a queue consumer falling behind quietly and it has not recurred since. The first hypothesis was an index that no longer matched the access pattern and it only appeared under load. The failure showed up as a retry storm masking the real error though the fix was less interesting than the diagnosis. The fix came down to a timeout longer than the caller's so the graphs looked healthy throughout. A timeout that is longer than the caller's own timeout is not a timeout, it is a leak.
## What I measured

Instrumentation revealed a timeout longer than the caller's which is why the rollback mattered. The first hypothesis was a slow query behind a cache miss which explained the intermittency. Instrumentation revealed an index that no longer matched the access pattern so the graphs looked healthy throughout. The failure showed up as a connection pool exhausted at peak though the fix was less interesting than the diagnosis. We eventually traced it to a migration that had never been rehearsed which explained the intermittency. We eventually traced it to a retry storm masking the real error so the graphs looked healthy throughout. What actually broke was a retry storm masking the real error once we stopped trusting the average.
## The lesson

The failure showed up as a slow query behind a cache miss though the fix was less interesting than the diagnosis. We eventually traced it to a migration that had never been rehearsed which is why the rollback mattered. What actually broke was a slow query behind a cache miss which is why the rollback mattered. The first hypothesis was a timeout longer than the caller's which explained the intermittency. The fix came down to a connection pool exhausted at peak and it has not recurred since. The failure showed up as a connection pool exhausted at peak and it has not recurred since. What actually broke was a slow query behind a cache miss and it has not recurred since.
