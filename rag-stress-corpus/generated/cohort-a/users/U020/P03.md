This came out of a production incident, so the details matter.

## The problem

Instrumentation revealed a retry storm masking the real error which is why the rollback mattered. Instrumentation revealed a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. Transactions landed at 6% change once I measured it end to end. The first hypothesis was a retry storm masking the real error once we stopped trusting the average. The failure showed up as an index that no longer matched the access pattern and it only appeared under load. The fix came down to a connection pool exhausted at peak so the graphs looked healthy throughout. What actually broke was a slow query behind a cache miss and it has not recurred since. The failure showed up as an index that no longer matched the access pattern once we stopped trusting the average.
## The design

The fix came down to a timeout longer than the caller's once we stopped trusting the average. The number I got for transactions was 32% change. The first hypothesis was a retry storm masking the real error which is why the rollback mattered. We eventually traced it to a queue consumer falling behind quietly and it only appeared under load. We eventually traced it to a queue consumer falling behind quietly which is why the rollback mattered. We eventually traced it to a metric that averaged away the tail which explained the intermittency. What actually broke was an index that no longer matched the access pattern and it has not recurred since. The fix came down to a slow query behind a cache miss though the fix was less interesting than the diagnosis.
## Implementation notes

Instrumentation revealed a queue consumer falling behind quietly which is why the rollback mattered. Instrumentation revealed a retry storm masking the real error though the fix was less interesting than the diagnosis. The number I got for transactions was 4% change. We eventually traced it to a retry storm masking the real error so the graphs looked healthy throughout. Instrumentation revealed an index that no longer matched the access pattern and it has not recurred since. A timeout that is longer than the caller's own timeout is not a timeout, it is a leak. The fix came down to an index that no longer matched the access pattern and the logs had said so all along.
## What I measured

We eventually traced it to an index that no longer matched the access pattern and it has not recurred since. Measured properly, transactions came out at 10% change. The failure showed up as a slow query behind a cache miss once we stopped trusting the average. The failure showed up as a queue consumer falling behind quietly once we stopped trusting the average. What actually broke was a queue consumer falling behind quietly which explained the intermittency. What actually broke was a timeout longer than the caller's and the logs had said so all along. The first hypothesis was a retry storm masking the real error though the fix was less interesting than the diagnosis.
## The lesson

The first hypothesis was a connection pool exhausted at peak which is why the rollback mattered. We eventually traced it to a slow query behind a cache miss and it only appeared under load. The fix came down to a connection pool exhausted at peak once we stopped trusting the average. The first hypothesis was a connection pool exhausted at peak so the graphs looked healthy throughout. We eventually traced it to a retry storm masking the real error which is why the rollback mattered. The failure showed up as a connection pool exhausted at peak though the fix was less interesting than the diagnosis.
