## The problem

We shipped the metric first and the fix a week later, in that deliberate order. We spent longer agreeing what done meant for the index than on the implementation itself. On the instrumented run, transactions showed 6% change.

We added one metric, waited a week, and the cause became embarrassingly clear. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The postmortem was more useful than the fix because it changed how we test. One badly scoped timeout propagated into three services before anyone noticed. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. That gave transactions of 32% change, which I wrote down at the time. I assumed the database was the bottleneck for two days longer than the evidence supported. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. Because the consumer fell behind quietly, the first symptom was a customer report. Making the failure reproducible locally took longer than repairing it. The fix was four lines; the diagnosis took two days. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again.
## Implementation notes

Transactions landed at 4% change once I measured it end to end. The config that caused it had been in place for months without incident. An average hid a tail that was doing all the damage. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Documenting the failure mode turned out more valuable than documenting the happy path. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered.
## What I measured

The migration only misbehaved at peak, so the averages stayed healthy right through the incident. Every retry we added made the graphs look better and the system behave worse. The number I got for transactions was 10% change.

The alert fired correctly and nobody could act on it, which is its own kind of failure. Retries around the migration made the symptom disappear and the underlying problem considerably worse. Retries around the index made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. Under normal load the retry path behaved; under peak it did not, and only peak mattered. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all.
## The lesson

We spent longer agreeing what done meant for the timeout budget than on the implementation itself. We now write down what we expect to see before deploying, which catches surprises early. Small boring changes with a measurement attached beat clever changes without one, every time. I no longer trust a health check that only proves the process is running. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.
