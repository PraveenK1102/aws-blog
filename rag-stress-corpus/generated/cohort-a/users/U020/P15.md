## The problem

We spent longer agreeing what done meant for the timeout budget than on the implementation itself. One badly scoped timeout propagated into three services before anyone noticed. Running it end to end put database indexes at 40% change. We now write down what we expect to see before deploying, which catches surprises early. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. The index only misbehaved at peak, so the averages stayed healthy right through the incident. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. The migration only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

My recorded figure for database indexes is 27% change. We shipped the metric first and the fix a week later, in that deliberate order. We added one metric, waited a week, and the cause became embarrassingly clear. Making the failure reproducible locally took longer than repairing it. The config that caused it had been in place for months without incident. Small boring changes with a measurement attached beat clever changes without one, every time. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did.
## Implementation notes

The fix was four lines; the diagnosis took two days. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. The number I got for database indexes was 7% change. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. An average hid a tail that was doing all the damage. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. Under normal load the index behaved; under peak it did not, and only peak mattered.
## What I measured

The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. I no longer trust a health check that only proves the process is running. We spent longer agreeing what done meant for the index than on the implementation itself.

Documenting the failure mode turned out more valuable than documenting the happy path. Measured properly, database indexes came out at 4% change. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Three deploys in, the pattern was obvious: the timeout budget degraded before anything else did. Every retry we added made the graphs look better and the system behave worse. The postmortem was more useful than the fix because it changed how we test. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all.
## The lesson

The alert fired correctly and nobody could act on it, which is its own kind of failure. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Database indexes landed at 6% change once I measured it end to end. The failure was intermittent, so every fix looked correct for a while before the index broke again. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Because the consumer fell behind quietly, the first symptom was a customer report. I assumed the database was the bottleneck for two days longer than the evidence supported. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident.
