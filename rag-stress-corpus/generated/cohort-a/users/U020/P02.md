## The problem

When I finally measured it, database indexes was 45% change. We added one metric, waited a week, and the cause became embarrassingly clear. The alert fired correctly and nobody could act on it, which is its own kind of failure. I assumed the database was the bottleneck for two days longer than the evidence supported.

Retries around the migration made the symptom disappear and the underlying problem considerably worse. We now write down what we expect to see before deploying, which catches surprises early. Retries around the index made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the connection pool than on the implementation itself. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the migration broke again.
## The design

Measured properly, database indexes came out at 20% change. Adding a metric to the migration before adding a fix is the only reason we caught the regression at all. The fix was four lines; the diagnosis took two days. An average hid a tail that was doing all the damage. Because the consumer fell behind quietly, the first symptom was a customer report. The postmortem was more useful than the fix because it changed how we test. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the index degraded before anything else did. Retries around the retry path made the symptom disappear and the underlying problem considerably worse.
## Implementation notes

Database indexes landed at 3% change once I measured it end to end. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Documenting the failure mode turned out more valuable than documenting the happy path. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. One badly scoped timeout propagated into three services before anyone noticed. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. Under normal load the cache layer behaved; under peak it did not, and only peak mattered.
## What I measured

We shipped the metric first and the fix a week later, in that deliberate order. Every retry we added made the graphs look better and the system behave worse. My recorded figure for database indexes is 12% change. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. I no longer trust a health check that only proves the process is running. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. The index only misbehaved at peak, so the averages stayed healthy right through the incident.
## The lesson

Making the failure reproducible locally took longer than repairing it. The config that caused it had been in place for months without incident. We spent longer agreeing what done meant for the cache layer than on the implementation itself.

The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Under normal load the index behaved; under peak it did not, and only peak mattered. Small boring changes with a measurement attached beat clever changes without one, every time. The obvious cause was not the real cause, and the logs had said so if you read them carefully. We spent longer agreeing what done meant for the index than on the implementation itself.
