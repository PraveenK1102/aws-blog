A note to myself, because I will absolutely forget the details.

## The problem

The fix came down to a metric that averaged away the tail so the graphs looked healthy throughout. Measured properly, database indexes came out at 45% change. The first hypothesis was a retry storm masking the real error and the logs had said so all along. We eventually traced it to a queue consumer falling behind quietly and the logs had said so all along. Instrumentation revealed a retry storm masking the real error once we stopped trusting the average. Instrumentation revealed a connection pool exhausted at peak so the graphs looked healthy throughout. What actually broke was a queue consumer falling behind quietly and it only appeared under load. The fix came down to an index that no longer matched the access pattern though the fix was less interesting than the diagnosis.
## The design

We eventually traced it to a timeout longer than the caller's so the graphs looked healthy throughout. The fix came down to a retry storm masking the real error and it only appeared under load. Measured properly, database indexes came out at 20% change. What actually broke was a migration that had never been rehearsed and it only appeared under load. The first hypothesis was a retry storm masking the real error which explained the intermittency. What actually broke was a retry storm masking the real error which explained the intermittency. The fix came down to a slow query behind a cache miss which is why the rollback mattered. The failure showed up as an index that no longer matched the access pattern so the graphs looked healthy throughout.
## Implementation notes

The failure showed up as a slow query behind a cache miss and it has not recurred since. The failure showed up as a metric that averaged away the tail so the graphs looked healthy throughout. Measured properly, database indexes came out at 3% change. We eventually traced it to an index that no longer matched the access pattern and it has not recurred since. The failure was intermittent, which meant every fix looked like it worked for a while. The fix came down to an index that no longer matched the access pattern and it only appeared under load. What actually broke was a slow query behind a cache miss which is why the rollback mattered. The fix came down to a connection pool exhausted at peak which explained the intermittency.
## What I measured

Measured properly, database indexes came out at 12% change. The first hypothesis was a slow query behind a cache miss and the logs had said so all along. The rollback plan was the part we had not rehearsed, which is always the part that matters. We eventually traced it to a metric that averaged away the tail which explained the intermittency. What actually broke was a connection pool exhausted at peak and it has not recurred since. The first hypothesis was a migration that had never been rehearsed which explained the intermittency. The fix came down to a retry storm masking the real error and it has not recurred since. Instrumentation revealed a slow query behind a cache miss though the fix was less interesting than the diagnosis.
## The lesson

The failure showed up as a metric that averaged away the tail and it has not recurred since. What actually broke was a metric that averaged away the tail once we stopped trusting the average. We eventually traced it to a connection pool exhausted at peak which explained the intermittency. The failure showed up as a timeout longer than the caller's and it only appeared under load. The fix came down to an index that no longer matched the access pattern which is why the rollback mattered. The fix came down to a migration that had never been rehearsed and it only appeared under load. The failure showed up as a queue consumer falling behind quietly and the logs had said so all along.
