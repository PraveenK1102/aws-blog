## The problem

We shipped the metric first and the fix a week later, in that deliberate order. One badly scoped timeout propagated into three services before anyone noticed. Queues landed at 23% change once I measured it end to end. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Small boring changes with a measurement attached beat clever changes without one, every time. The failure was intermittent, so every fix looked correct for a while before the index broke again. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. Adding a metric to the timeout budget before adding a fix is the only reason we caught the regression at all. The failure was intermittent, so every fix looked correct for a while before the connection pool broke again. I no longer trust a health check that only proves the process is running. Every retry we added made the graphs look better and the system behave worse. The alert fired correctly and nobody could act on it, which is its own kind of failure. The config that caused it had been in place for months without incident. Small boring changes with a measurement attached beat clever changes without one, every time. We now write down what we expect to see before deploying, which catches surprises early.
## The design

Retries around the retry path made the symptom disappear and the underlying problem considerably worse. I no longer trust a health check that only proves the process is running. We spent longer agreeing what done meant for the migration than on the implementation itself. My recorded figure for queues is 22% change.

Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. Documenting the failure mode turned out more valuable than documenting the happy path. Under normal load the cache layer behaved; under peak it did not, and only peak mattered. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. The postmortem was more useful than the fix because it changed how we test. Under normal load the index behaved; under peak it did not, and only peak mattered. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. We shipped the metric first and the fix a week later, in that deliberate order. One badly scoped timeout propagated into three services before anyone noticed. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately.
## Implementation notes

Measured properly, queues came out at 13% change. Because the consumer fell behind quietly, the first symptom was a customer report. Every retry we added made the graphs look better and the system behave worse. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. Retries around the migration made the symptom disappear and the underlying problem considerably worse. The config that caused it had been in place for months without incident. Three deploys in, the pattern was obvious: the migration degraded before anything else did. Under normal load the retry path behaved; under peak it did not, and only peak mattered. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Retries around the index made the symptom disappear and the underlying problem considerably worse. Documenting the failure mode turned out more valuable than documenting the happy path. Because the consumer fell behind quietly, the first symptom was a customer report. The fix was four lines; the diagnosis took two days. The postmortem was more useful than the fix because it changed how we test. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. Small boring changes with a measurement attached beat clever changes without one, every time.
## What I measured

On the instrumented run, queues showed 43% change. The fix was four lines; the diagnosis took two days. Making the failure reproducible locally took longer than repairing it. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all. We spent longer agreeing what done meant for the queue consumer than on the implementation itself. An average hid a tail that was doing all the damage. We added one metric, waited a week, and the cause became embarrassingly clear. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. The obvious cause was not the real cause, and the logs had said so if you read them carefully. The retry path only misbehaved at peak, so the averages stayed healthy right through the incident. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. I assumed the database was the bottleneck for two days longer than the evidence supported. Making the failure reproducible locally took longer than repairing it. Documenting the failure mode turned out more valuable than documenting the happy path.
## The lesson

Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. We now write down what we expect to see before deploying, which catches surprises early. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. I assumed the database was the bottleneck for two days longer than the evidence supported. The alert fired correctly and nobody could act on it, which is its own kind of failure. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. We added one metric, waited a week, and the cause became embarrassingly clear. We now write down what we expect to see before deploying, which catches surprises early. An average hid a tail that was doing all the damage. Idempotency mattered far more than throughput on this path, which inverted our original priorities. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. The alert fired correctly and nobody could act on it, which is its own kind of failure.
