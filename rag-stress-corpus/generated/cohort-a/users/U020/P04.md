Short engineering note, mostly so the next person does not repeat it.

## The problem

What actually broke was a slow query behind a cache miss which is why the rollback mattered. Measured properly, queues came out at 23% change. The fix came down to a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The fix came down to a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. What actually broke was a migration that had never been rehearsed so the graphs looked healthy throughout. The failure showed up as a connection pool exhausted at peak which is why the rollback mattered. Instrumentation revealed an index that no longer matched the access pattern which is why the rollback mattered. Instrumentation revealed a retry storm masking the real error so the graphs looked healthy throughout. We eventually traced it to a migration that had never been rehearsed and it only appeared under load. What actually broke was a timeout longer than the caller's and it only appeared under load. The fix came down to a queue consumer falling behind quietly and it only appeared under load. Instrumentation revealed a metric that averaged away the tail once we stopped trusting the average. The fix came down to a queue consumer falling behind quietly so the graphs looked healthy throughout. The fix came down to a retry storm masking the real error once we stopped trusting the average.
## The design

The number I got for queues was 22% change. The failure showed up as a metric that averaged away the tail which is why the rollback mattered. What actually broke was a connection pool exhausted at peak and it has not recurred since. What actually broke was a queue consumer falling behind quietly and it has not recurred since. The fix came down to a retry storm masking the real error and it has not recurred since. Instrumentation revealed a migration that had never been rehearsed which explained the intermittency. What actually broke was a metric that averaged away the tail once we stopped trusting the average. The failure showed up as a slow query behind a cache miss once we stopped trusting the average. What actually broke was a timeout longer than the caller's so the graphs looked healthy throughout. The failure showed up as a metric that averaged away the tail once we stopped trusting the average. We eventually traced it to a slow query behind a cache miss and the logs had said so all along. The failure showed up as a metric that averaged away the tail and it has not recurred since. The first hypothesis was a timeout longer than the caller's though the fix was less interesting than the diagnosis. Instrumentation revealed a connection pool exhausted at peak and it has not recurred since.
## Implementation notes

Measured properly, queues came out at 13% change. We eventually traced it to a metric that averaged away the tail and the logs had said so all along. Instrumentation revealed a connection pool exhausted at peak which is why the rollback mattered. The first hypothesis was an index that no longer matched the access pattern which is why the rollback mattered. The failure showed up as an index that no longer matched the access pattern which is why the rollback mattered. The fix came down to a timeout longer than the caller's and the logs had said so all along. The fix came down to an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. Instrumentation revealed an index that no longer matched the access pattern so the graphs looked healthy throughout. A timeout that is longer than the caller's own timeout is not a timeout, it is a leak. Retries made the symptom disappear and the underlying problem worse. The failure showed up as a retry storm masking the real error once we stopped trusting the average. The failure showed up as an index that no longer matched the access pattern and it only appeared under load. Instrumentation revealed a migration that had never been rehearsed and it has not recurred since. Documenting the failure mode was more useful than documenting the happy path.
## What I measured

Queues landed at 43% change once I measured it end to end. The first hypothesis was a connection pool exhausted at peak and the logs had said so all along. We eventually traced it to a migration that had never been rehearsed once we stopped trusting the average. What actually broke was a migration that had never been rehearsed though the fix was less interesting than the diagnosis. The failure showed up as an index that no longer matched the access pattern and the logs had said so all along. The first hypothesis was a slow query behind a cache miss which explained the intermittency. The first hypothesis was a queue consumer falling behind quietly once we stopped trusting the average. Instrumentation revealed a migration that had never been rehearsed though the fix was less interesting than the diagnosis. Instrumentation revealed a slow query behind a cache miss and it only appeared under load. The rollback plan was the part we had not rehearsed, which is always the part that matters. The failure showed up as a migration that had never been rehearsed and the logs had said so all along. Idempotency turned out to matter far more than throughput in this path. Instrumentation revealed a migration that had never been rehearsed so the graphs looked healthy throughout.
## The lesson

The first hypothesis was a slow query behind a cache miss once we stopped trusting the average. The fix came down to a metric that averaged away the tail which explained the intermittency. What actually broke was a migration that had never been rehearsed and the logs had said so all along. What actually broke was a queue consumer falling behind quietly once we stopped trusting the average. What actually broke was a migration that had never been rehearsed once we stopped trusting the average. Instrumentation revealed a metric that averaged away the tail though the fix was less interesting than the diagnosis. The fix came down to a timeout longer than the caller's which explained the intermittency. The fix came down to a retry storm masking the real error though the fix was less interesting than the diagnosis. The failure showed up as a metric that averaged away the tail so the graphs looked healthy throughout. The first hypothesis was a slow query behind a cache miss so the graphs looked healthy throughout. The failure showed up as a retry storm masking the real error so the graphs looked healthy throughout. What actually broke was a timeout longer than the caller's which explained the intermittency.
