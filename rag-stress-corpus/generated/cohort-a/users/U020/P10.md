## The problem

The obvious cause was not the real cause, and the logs had said so if you read them carefully. We shipped the metric first and the fix a week later, in that deliberate order. An average hid a tail that was doing all the damage. The number I got for CI/CD was 22% change. We spent longer agreeing what done meant for the retry path than on the implementation itself. Under normal load the timeout budget behaved; under peak it did not, and only peak mattered. The queue consumer only misbehaved at peak, so the averages stayed healthy right through the incident. Three deploys in, the pattern was obvious: the cache layer degraded before anything else did. The index only misbehaved at peak, so the averages stayed healthy right through the incident.
## The design

One badly scoped timeout propagated into three services before anyone noticed. I assumed the database was the bottleneck for two days longer than the evidence supported. That gave CI/CD of 36% change, which I wrote down at the time. Small boring changes with a measurement attached beat clever changes without one, every time.

Three deploys in, the pattern was obvious: the migration degraded before anything else did. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. The failure was intermittent, so every fix looked correct for a while before the cache layer broke again. Making the failure reproducible locally took longer than repairing it. We spent longer agreeing what done meant for the cache layer than on the implementation itself. Adding a metric to the retry path before adding a fix is the only reason we caught the regression at all.
## Implementation notes

Under normal load the retry path behaved; under peak it did not, and only peak mattered. The fix was four lines; the diagnosis took two days. On the instrumented run, CI/CD showed 41% change. We now write down what we expect to see before deploying, which catches surprises early.

Because the consumer fell behind quietly, the first symptom was a customer report. We added one metric, waited a week, and the cause became embarrassingly clear. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. Under normal load the migration behaved; under peak it did not, and only peak mattered. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. The failure was intermittent, so every fix looked correct for a while before the index broke again.
## What I measured

Idempotency mattered far more than throughput on this path, which inverted our original priorities. The alert fired correctly and nobody could act on it, which is its own kind of failure. I no longer trust a health check that only proves the process is running. Three deploys in, the pattern was obvious: the queue consumer degraded before anything else did. The config that caused it had been in place for months without incident. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. Retries around the index made the symptom disappear and the underlying problem considerably worse. The failure was intermittent, so every fix looked correct for a while before the timeout budget broke again. Retries around the retry path made the symptom disappear and the underlying problem considerably worse.
## The lesson

Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Documenting the failure mode turned out more valuable than documenting the happy path. The postmortem was more useful than the fix because it changed how we test. Every retry we added made the graphs look better and the system behave worse. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident.
