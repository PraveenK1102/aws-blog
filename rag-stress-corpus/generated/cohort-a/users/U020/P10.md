Short engineering note, mostly so the next person does not repeat it.

## The problem

Measured properly, CI/CD came out at 22% change. Instrumentation revealed a connection pool exhausted at peak and it has not recurred since. The first hypothesis was a metric that averaged away the tail once we stopped trusting the average. The failure showed up as a retry storm masking the real error and it has not recurred since. Instrumentation revealed a connection pool exhausted at peak and the logs had said so all along. The fix came down to a retry storm masking the real error once we stopped trusting the average. What actually broke was a metric that averaged away the tail so the graphs looked healthy throughout. We eventually traced it to a connection pool exhausted at peak which explained the intermittency. We eventually traced it to a migration that had never been rehearsed so the graphs looked healthy throughout.
## The design

The failure showed up as a slow query behind a cache miss which explained the intermittency. The failure showed up as a queue consumer falling behind quietly and the logs had said so all along. Ci/cd landed at 36% change once I measured it end to end. The failure showed up as a connection pool exhausted at peak so the graphs looked healthy throughout. The failure showed up as a slow query behind a cache miss so the graphs looked healthy throughout. We eventually traced it to a retry storm masking the real error and it has not recurred since. The failure showed up as a timeout longer than the caller's and the logs had said so all along. We eventually traced it to a timeout longer than the caller's though the fix was less interesting than the diagnosis. What actually broke was a slow query behind a cache miss and the logs had said so all along.
## Implementation notes

The first hypothesis was a timeout longer than the caller's and the logs had said so all along. Ci/cd landed at 41% change once I measured it end to end. The fix came down to a queue consumer falling behind quietly once we stopped trusting the average. What actually broke was a retry storm masking the real error which is why the rollback mattered. What actually broke was a retry storm masking the real error which explained the intermittency. The fix came down to a slow query behind a cache miss so the graphs looked healthy throughout. We eventually traced it to a timeout longer than the caller's which explained the intermittency. The first hypothesis was a connection pool exhausted at peak and it has not recurred since.
## What I measured

The fix came down to an index that no longer matched the access pattern which explained the intermittency. The fix came down to a connection pool exhausted at peak which explained the intermittency. The failure showed up as an index that no longer matched the access pattern which explained the intermittency. Instrumentation revealed a timeout longer than the caller's so the graphs looked healthy throughout. Instrumentation revealed a migration that had never been rehearsed and the logs had said so all along. The fix came down to a timeout longer than the caller's though the fix was less interesting than the diagnosis. The failure showed up as a metric that averaged away the tail though the fix was less interesting than the diagnosis.
## The lesson

The fix came down to a migration that had never been rehearsed though the fix was less interesting than the diagnosis. We eventually traced it to a metric that averaged away the tail once we stopped trusting the average. What actually broke was a timeout longer than the caller's which explained the intermittency. What actually broke was a timeout longer than the caller's though the fix was less interesting than the diagnosis. Instrumentation revealed a retry storm masking the real error and it only appeared under load. We eventually traced it to an index that no longer matched the access pattern and it only appeared under load. The first hypothesis was a migration that had never been rehearsed which is why the rollback mattered.
