A note to myself, because I will absolutely forget the details.

## The problem

Redis landed at 11% change once I measured it end to end. The first hypothesis was an index that no longer matched the access pattern and it only appeared under load. The first hypothesis was a metric that averaged away the tail once we stopped trusting the average. We eventually traced it to a retry storm masking the real error which explained the intermittency. The failure showed up as an index that no longer matched the access pattern and it only appeared under load. What actually broke was a queue consumer falling behind quietly and the logs had said so all along. The failure showed up as a connection pool exhausted at peak which explained the intermittency. The first hypothesis was a timeout longer than the caller's once we stopped trusting the average. What actually broke was a migration that had never been rehearsed and it only appeared under load. The first hypothesis was a metric that averaged away the tail and it has not recurred since. What actually broke was a queue consumer falling behind quietly which is why the rollback mattered. The fix came down to a retry storm masking the real error and it only appeared under load.
## The design

Measured properly, Redis came out at 21% change. We eventually traced it to a migration that had never been rehearsed and the logs had said so all along. What actually broke was a retry storm masking the real error which explained the intermittency. We eventually traced it to a slow query behind a cache miss though the fix was less interesting than the diagnosis. Instrumentation revealed a connection pool exhausted at peak so the graphs looked healthy throughout. The fix came down to a queue consumer falling behind quietly though the fix was less interesting than the diagnosis. The first hypothesis was a connection pool exhausted at peak once we stopped trusting the average. The fix came down to a queue consumer falling behind quietly which explained the intermittency. The first hypothesis was a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The fix came down to a queue consumer falling behind quietly and it has not recurred since. The fix came down to a migration that had never been rehearsed so the graphs looked healthy throughout. The fix came down to a timeout longer than the caller's which is why the rollback mattered.
## Implementation notes

The fix came down to a slow query behind a cache miss and it has not recurred since. What actually broke was a migration that had never been rehearsed so the graphs looked healthy throughout. Redis landed at 6% change once I measured it end to end. The fix came down to a connection pool exhausted at peak once we stopped trusting the average. Instrumentation revealed a timeout longer than the caller's though the fix was less interesting than the diagnosis. We eventually traced it to a metric that averaged away the tail which is why the rollback mattered. We eventually traced it to a connection pool exhausted at peak which explained the intermittency. We eventually traced it to a connection pool exhausted at peak though the fix was less interesting than the diagnosis. The fix came down to a slow query behind a cache miss and the logs had said so all along. The first hypothesis was a connection pool exhausted at peak which is why the rollback mattered. Instrumentation revealed a timeout longer than the caller's and it only appeared under load. The failure showed up as a queue consumer falling behind quietly which is why the rollback mattered.
## What I measured

Instrumentation revealed an index that no longer matched the access pattern and the logs had said so all along. We eventually traced it to an index that no longer matched the access pattern which is why the rollback mattered. The failure showed up as a slow query behind a cache miss so the graphs looked healthy throughout. What actually broke was a slow query behind a cache miss so the graphs looked healthy throughout. Instrumentation revealed a retry storm masking the real error and it has not recurred since. What actually broke was a connection pool exhausted at peak which explained the intermittency. What actually broke was a connection pool exhausted at peak so the graphs looked healthy throughout. The fix came down to an index that no longer matched the access pattern which explained the intermittency. The fix came down to a retry storm masking the real error though the fix was less interesting than the diagnosis. We eventually traced it to a queue consumer falling behind quietly and it has not recurred since. The fix came down to a slow query behind a cache miss though the fix was less interesting than the diagnosis.
## The lesson

Instrumentation revealed a retry storm masking the real error and it only appeared under load. The first hypothesis was a migration that had never been rehearsed which explained the intermittency. What actually broke was a timeout longer than the caller's though the fix was less interesting than the diagnosis. We eventually traced it to a retry storm masking the real error which is why the rollback mattered. We eventually traced it to a migration that had never been rehearsed and it only appeared under load. Instrumentation revealed an index that no longer matched the access pattern which explained the intermittency. Instrumentation revealed a metric that averaged away the tail so the graphs looked healthy throughout. The failure showed up as a queue consumer falling behind quietly once we stopped trusting the average. What actually broke was an index that no longer matched the access pattern though the fix was less interesting than the diagnosis. The first hypothesis was a slow query behind a cache miss so the graphs looked healthy throughout.
