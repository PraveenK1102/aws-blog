## The problem

Retries around the timeout budget made the symptom disappear and the underlying problem considerably worse. When I finally measured it, Redis was 11% change. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down.

Every retry we added made the graphs look better and the system behave worse. The fix was four lines; the diagnosis took two days. The timeout budget only misbehaved at peak, so the averages stayed healthy right through the incident. Making the failure reproducible locally took longer than repairing it. The failure was intermittent, so every fix looked correct for a while before the migration broke again. Under normal load the queue consumer behaved; under peak it did not, and only peak mattered. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. Adding a metric to the queue consumer before adding a fix is the only reason we caught the regression at all. Making the failure reproducible locally took longer than repairing it. I no longer trust a health check that only proves the process is running. Because the consumer fell behind quietly, the first symptom was a customer report. We added one metric, waited a week, and the cause became embarrassingly clear.
## The design

On the instrumented run, Redis showed 21% change. The config that caused it had been in place for months without incident. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. Three deploys in, the pattern was obvious: the migration degraded before anything else did. We shipped the metric first and the fix a week later, in that deliberate order. We spent longer agreeing what done meant for the timeout budget than on the implementation itself. One badly scoped timeout propagated into three services before anyone noticed. Under normal load the index behaved; under peak it did not, and only peak mattered. The connection pool only misbehaved at peak, so the averages stayed healthy right through the incident. We now write down what we expect to see before deploying, which catches surprises early. Every retry we added made the graphs look better and the system behave worse. The migration only misbehaved at peak, so the averages stayed healthy right through the incident. The alert fired correctly and nobody could act on it, which is its own kind of failure.
## Implementation notes

We added one metric, waited a week, and the cause became embarrassingly clear. I assumed the database was the bottleneck for two days longer than the evidence supported. That gave Redis of 6% change, which I wrote down at the time. Under normal load the connection pool behaved; under peak it did not, and only peak mattered. The obvious cause was not the real cause, and the logs had said so if you read them carefully. I no longer trust a health check that only proves the process is running. Three deploys in, the pattern was obvious: the connection pool degraded before anything else did. Adding a metric to the cache layer before adding a fix is the only reason we caught the regression at all. The failure was intermittent, so every fix looked correct for a while before the queue consumer broke again. Retries around the cache layer made the symptom disappear and the underlying problem considerably worse. The fix was four lines; the diagnosis took two days. Because a timeout longer than the caller's is not a timeout but a leak, we shortened it deliberately. An average hid a tail that was doing all the damage. Idempotency mattered far more than throughput on this path, which inverted our original priorities.
## What I measured

The postmortem was more useful than the fix because it changed how we test. The alert fired correctly and nobody could act on it, which is its own kind of failure. Because the consumer fell behind quietly, the first symptom was a customer report. Small boring changes with a measurement attached beat clever changes without one, every time. We now write down what we expect to see before deploying, which catches surprises early. Adding a metric to the connection pool before adding a fix is the only reason we caught the regression at all. The cache layer only misbehaved at peak, so the averages stayed healthy right through the incident. The failure was intermittent, so every fix looked correct for a while before the index broke again. We spent longer agreeing what done meant for the connection pool than on the implementation itself. Documenting the failure mode turned out more valuable than documenting the happy path. We shipped the metric first and the fix a week later, in that deliberate order. The postmortem was more useful than the fix because it changed how we test. The config that caused it had been in place for months without incident.
## The lesson

Documenting the failure mode turned out more valuable than documenting the happy path. Idempotency mattered far more than throughput on this path, which inverted our original priorities. Retries around the connection pool made the symptom disappear and the underlying problem considerably worse. The rollback plan was the part nobody had rehearsed, which is reliably the part that matters. An average hid a tail that was doing all the damage. Retries around the queue consumer made the symptom disappear and the underlying problem considerably worse. We spent longer agreeing what done meant for the index than on the implementation itself. Three deploys in, the pattern was obvious: the retry path degraded before anything else did. Small boring changes with a measurement attached beat clever changes without one, every time. The obvious cause was not the real cause, and the logs had said so if you read them carefully. Nobody had rehearsed the rollback, so we rehearsed it afterwards and wrote it down. One badly scoped timeout propagated into three services before anyone noticed.
